// Generated by @flighthq/tool-compiler from @flighthq/scene3d/sceneDocumentLights.ts#SceneDocumentLights. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_scene3d {

inline Scene3DLights create_scene3_dlights_from_document(Scene3DDocument document) {
  std::optional<AmbientLight> ambient = nullptr;
  std::optional<DirectionalLight> directional = nullptr;
  flight::Array<HemisphereLight> hemisphere = flight::Array<HemisphereLight>{};
  flight::Array<PointLight> point = flight::Array<PointLight>{};
  flight::Array<SpotLight> spot = flight::Array<SpotLight>{};
  {
    double i = 0.0;
    while ((i < document.lights.length)) {
      {
        auto source = document.lights[static_cast<size_t>(i)];
        auto descriptor = source.descriptor;
        if ((descriptor.kind == ambient_light_kind)) {
          if (!ambient.value().has_value()) {
            ambient.value() = clone_ambient_light(static_cast<AmbientLight>(descriptor));
          }
          {
            i += 1.0;
            continue;
          }
        }
        if ((descriptor.kind == directional_light_kind)) {
          if (!directional.value().has_value()) {
            directional.value() = clone_directional_light(static_cast<DirectionalLight>(descriptor));
            rotate_vector3_by_quaternion(directional.value().direction, directional.value().direction, source.transform.rotation);
          }
          {
            i += 1.0;
            continue;
          }
        }
        if ((descriptor.kind == hemisphere_light_kind)) {
          hemisphere.push(clone_hemisphere_light(static_cast<HemisphereLight>(descriptor)));
          {
            i += 1.0;
            continue;
          }
        }
        if ((descriptor.kind == point_light_kind)) {
          auto resolved = clone_point_light(static_cast<PointLight>(descriptor));
          transform_document_light_position(resolved.position, source.transform);
          point.push(resolved);
          {
            i += 1.0;
            continue;
          }
        }
        if ((descriptor.kind == spot_light_kind)) {
          auto resolved_2 = clone_spot_light(static_cast<SpotLight>(descriptor));
          transform_document_light_position(resolved_2.position, source.transform);
          rotate_vector3_by_quaternion(resolved_2.direction, resolved_2.direction, source.transform.rotation);
          spot.push(resolved_2);
        }
      }
      i += 1.0;
    }
  }
  return create_scene3_dlights({.ambient = ambient, .directional = directional, .hemisphere = hemisphere, .point = point, .spot = spot});
}

inline void transform_document_light_position(Vector3Like out, Transform3DLike transform) {
  out.x *= transform.scale.x;
  out.y *= transform.scale.y;
  out.z *= transform.scale.z;
  rotate_vector3_by_quaternion(out, out, transform.rotation);
  out.x += transform.position.x;
  out.y += transform.position.y;
  out.z += transform.position.z;
}

} // namespace flighthq_scene3d
