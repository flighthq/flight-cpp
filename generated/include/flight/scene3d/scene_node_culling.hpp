// Generated by @flighthq/tool-compiler from @flighthq/scene3d/sceneNodeCulling.ts#SceneNodeCulling. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "mesh.hpp"

namespace flighthq_scene3d {

inline void build_scene3_dfrustum(FrustumLike out, Matrix4Like view_projection) {
  set_frustum_from_matrix4(out, view_projection);
}

inline flight::Array<Node3D> cull_node3_dby_frustum(flight::Array<Node3D> out, Node3D root, FrustumLike frustum) {
  cull_node(out, root, frustum);
  return out;
}

inline void cull_node(flight::Array<Node3D> out, Node3D node, FrustumLike frustum) {
  if (!node.enabled) {
    return;
  }
  if (is_mesh(node)) {
    auto geom = node.geometry;
    auto local_bounds = geom.bounds;
    if (!local_bounds.has_value()) {
      compute_mesh_geometry_bounds(scratch_local_aabb, geom);
      local_bounds = scratch_local_aabb;
    }
    if ((local_bounds.min.x <= local_bounds.max.x)) {
      ensure_node_world_matrix4(node);
      auto world_matrix = get_node_world_matrix4(node);
      transform_aabb_by_matrix4(scratch_world_aabb, local_bounds, world_matrix);
      if (is_frustum_intersecting_aabb(frustum, scratch_world_aabb)) {
        out.push(static_cast<Node3D>(node));
      }
    }
  }
  auto children = get_node_runtime(node).children;
  if (children.has_value()) {
    {
      double i = 0.0;
      while ((i < children.length)) {
        {
          cull_node(out, static_cast<Node3D>(children[static_cast<size_t>(i)]), frustum);
        }
        i += 1.0;
      }
    }
  }
}

inline auto scratch_local_aabb = create_aabb();

inline auto scratch_world_aabb = create_aabb();

} // namespace flighthq_scene3d
