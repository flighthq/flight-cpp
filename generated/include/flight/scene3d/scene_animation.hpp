// Generated by @flighthq/tool-compiler from @flighthq/scene3d/sceneAnimation.ts#SceneAnimation. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_scene3d {

inline void apply_animation_clip_to_scene3_d(AnimationClip clip, double time) {
  auto channels = clip.channels;
  {
    double i = 0.0;
    while ((i < channels.length)) {
      {
        auto channel = channels[static_cast<size_t>(i)];
        std::optional<Scene3DAnimationTarget> target = static_cast<std::optional<Scene3DAnimationTarget>>(channel.target_ref);
        if (((!target.value().has_value() || (typeidtarget.value() != flight::String("object"))) || (target.value().node == undefined))) {
          i += 1.0;
          continue;
        }
        if ((target.value().path == flight::String("Weights"))) {
          auto morph = static_cast<Mesh>(target.value().node).morph;
          if (!morph.has_value()) {
            i += 1.0;
            continue;
          }
          sample_animation_track(morph.weights, channel.track, time);
          {
            i += 1.0;
            continue;
          }
        }
        sample_animation_track(scratch, channel.track, time);
        auto node = target.value().node;
        if ((target.value().path == flight::String("Translation"))) {
          set_vector3(node.position, scratch.element(0.0), scratch.element(1.0), scratch.element(2.0));
        }
        else {
          if ((target.value().path == flight::String("Scale"))) {
            set_vector3(node.scale, scratch.element(0.0), scratch.element(1.0), scratch.element(2.0));
          }
          else {
            set_quaternion(node.rotation, scratch.element(0.0), scratch.element(1.0), scratch.element(2.0), scratch.element(3.0));
          }
        }
        invalidate_node_local_transform(node);
      }
      i += 1.0;
    }
  }
}

inline flight::Array<double> scratch = flight::Array<double>{0.0, 0.0, 0.0, 0.0};

} // namespace flighthq_scene3d
