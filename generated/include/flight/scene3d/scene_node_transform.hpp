// Generated by @flighthq/tool-compiler from @flighthq/scene3d/sceneNodeTransform.ts#SceneNodeTransform. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_scene3d {

inline void set_node3_dlook_at(Node3D node, Vector3Like eye, Vector3Like target, Vector3Like up) {
  auto eye_x = eye.x;
  auto eye_y = eye.y;
  auto eye_z = eye.z;
  double zx = (eye_x - target.x);
  double zy = (eye_y - target.y);
  double zz = (eye_z - target.z);
  double zl = std::sqrt((((zx * zx) + (zy * zy)) + (zz * zz)));
  if ((zl == 0.0)) {
    zz = 1.0;
    zl = 1.0;
  }
  zx /= zl;
  zy /= zl;
  zz /= zl;
  double xx = ((up.y * zz) - (up.z * zy));
  double xy = ((up.z * zx) - (up.x * zz));
  double xz = ((up.x * zy) - (up.y * zx));
  const double xl = std::sqrt((((xx * xx) + (xy * xy)) + (xz * xz)));
  if ((xl != 0.0)) {
    xx /= xl;
    xy /= xl;
    xz /= xl;
  }
  const double yx = ((zy * xz) - (zz * xy));
  const double yy = ((zz * xx) - (zx * xz));
  const double yz = ((zx * xy) - (zy * xx));
  auto m = scratch_matrix.m;
  m[static_cast<size_t>(0.0)] = xx;
  m[static_cast<size_t>(1.0)] = xy;
  m[static_cast<size_t>(2.0)] = xz;
  m[static_cast<size_t>(3.0)] = 0.0;
  m[static_cast<size_t>(4.0)] = yx;
  m[static_cast<size_t>(5.0)] = yy;
  m[static_cast<size_t>(6.0)] = yz;
  m[static_cast<size_t>(7.0)] = 0.0;
  m[static_cast<size_t>(8.0)] = zx;
  m[static_cast<size_t>(9.0)] = zy;
  m[static_cast<size_t>(10.0)] = zz;
  m[static_cast<size_t>(11.0)] = 0.0;
  m[static_cast<size_t>(12.0)] = eye_x;
  m[static_cast<size_t>(13.0)] = eye_y;
  m[static_cast<size_t>(14.0)] = eye_z;
  m[static_cast<size_t>(15.0)] = 1.0;
  set_node_local_matrix4(node, scratch_matrix);
}

inline auto scratch_matrix = create_matrix4();

} // namespace flighthq_scene3d
