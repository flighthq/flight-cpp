// Generated by @flighthq/tool-compiler from @flighthq/particles-formats/parseParticleConfig.ts#ParseParticleConfig. Do not edit.
#pragma once
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "detect.hpp"
#include "format_registry.hpp"

namespace flighthq_particles_formats {

inline ParticleEmitterConfig parse_particle_config(flight::String text, std::optional<ParseParticleConfigOptions> options = std::nullopt) {
  auto format = detect_particle_format(text);
  if (!format.has_value()) {
    return create_particle_emitter_config();
  }
  auto codec = get_particle_format_codec(format);
  if (!codec.has_value()) {
    return create_particle_emitter_config();
  }
  try {
    return codec.parse_to_config(text, options.value());
  }
  catch (...) {
    return create_particle_emitter_config();
  }
}

inline ParticleConfigParseResult parse_particle_config_document(flight::String text, std::optional<ParseParticleConfigOptions> options = std::nullopt) {
  auto format = detect_particle_format(text);
  if (!format.has_value()) {
    flight::Array<ImportDiagnostic> diagnostics = flight::Array<ImportDiagnostic>{};
    report_import_diagnostic(diagnostics, import_diagnostic_severity.reject, flight::String("particles.unknown-format"), flight::String("parseParticleConfigDocument"), {.reason = flight::String("no-format-detected")});
    return {.config = create_particle_emitter_config(), .diagnostics = diagnostics, .format = nullptr};
  }
  auto codec = get_particle_format_codec(format);
  if (!codec.has_value()) {
    flight::Array<ImportDiagnostic> diagnostics_2 = flight::Array<ImportDiagnostic>{};
    report_import_diagnostic(diagnostics_2, import_diagnostic_severity.reject, flight::String("particles.unknown-format"), flight::String("parseParticleConfigDocument"), {.reason = flight::String("no-registered-parser")});
    return {.config = create_particle_emitter_config(), .diagnostics = diagnostics_2, .format = format};
  }
  try {
    auto result = codec.parse_to_document(text, options.value());
    return {.config = result.config, .diagnostics = result.diagnostics, .format = format};
  }
  catch (const std::exception& err) {
    flight::Array<ImportDiagnostic> diagnostics_3 = flight::Array<ImportDiagnostic>{};
    report_import_diagnostic(diagnostics_3, import_diagnostic_severity.reject, flight::String("particles.parse-error"), flight::String("parseParticleConfigDocument"), {.message = static_cast<flight::Error>(err).message});
    return {.config = create_particle_emitter_config(), .diagnostics = diagnostics_3, .format = format};
  }
}

} // namespace flighthq_particles_formats
