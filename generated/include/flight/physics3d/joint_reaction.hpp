// Generated by @flighthq/tool-compiler from @flighthq/physics3d/jointReaction.ts#JointReaction. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_physics3d {

inline void accumulate_physics3_djoint_row_reaction(Physics3DJoint joint, flight::Array<double> state, double offset, double impulse, Physics3DJointReaction out) {
  if ((impulse == 0.0)) {
    return;
  }
  const std::optional<double> direction_x = state.get(offset);
  const std::optional<double> direction_y = state.get((offset + 1.0));
  const std::optional<double> direction_z = state.get((offset + 2.0));
  out.force_x += (direction_x * impulse);
  out.force_y += (direction_y * impulse);
  out.force_z += (direction_z * impulse);
  const double lever_x = ((joint.r_by * direction_z) - (joint.r_bz * direction_y));
  const double lever_y = ((joint.r_bz * direction_x) - (joint.r_bx * direction_z));
  const double lever_z = ((joint.r_bx * direction_y) - (joint.r_by * direction_x));
  out.torque_x += ((state.element((offset + 6.0)) - lever_x) * impulse);
  out.torque_y += ((state.element((offset + 7.0)) - lever_y) * impulse);
  out.torque_z += ((state.element((offset + 8.0)) - lever_z) * impulse);
}

inline void clear_physics3_djoint_reaction(Physics3DJointReaction out) {
  out.force_x = 0.0;
  out.force_y = 0.0;
  out.force_z = 0.0;
  out.torque_x = 0.0;
  out.torque_y = 0.0;
  out.torque_z = 0.0;
}

inline Physics3DJointReaction create_physics3_djoint_reaction() {
  auto out = allocate_entity<Physics3DJointReaction>();
  initialize_physics3_djoint_reaction(out);
  return finish_entity(out);
}

inline double get_physics3_djoint_reaction_force(Physics3DJointReaction reaction) {
  return std::sqrt((((reaction.force_x * reaction.force_x) + (reaction.force_y * reaction.force_y)) + (reaction.force_z * reaction.force_z)));
}

inline double get_physics3_djoint_reaction_torque(Physics3DJointReaction reaction) {
  return std::sqrt((((reaction.torque_x * reaction.torque_x) + (reaction.torque_y * reaction.torque_y)) + (reaction.torque_z * reaction.torque_z)));
}

inline void initialize_physics3_djoint_reaction(EntityConstruction<Physics3DJointReaction> out) {
  out.force_x = 0.0;
  out.force_y = 0.0;
  out.force_z = 0.0;
  out.torque_x = 0.0;
  out.torque_y = 0.0;
  out.torque_z = 0.0;
}

inline bool write_physics3_djoint_reaction(Physics3DWorld world, Physics3DJoint joint, double dt, Physics3DJointReaction out) {
  clear_physics3_djoint_reaction(out);
  if (!(dt > 0.0)) {
    return false;
  }
  auto solver = world.joint_solvers.get(joint.kind);
  if ((([&]() -> std::optional<auto> { auto optional_chain_receiver = solver; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().write_reaction; }()) == undefined)) {
    return false;
  }
  return solver.write_reaction(joint, (1.0 / dt), out);
}

} // namespace flighthq_physics3d
