// Generated by @flighthq/tool-compiler from @flighthq/physics3d/jointRegistry.ts#JointRegistry. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "joint_collision_suppression.hpp"
#include "ownership.hpp"
#include "world.hpp"

namespace flighthq_physics3d {

template <typename T>
inline T add_physics3_djoint(Physics3DWorld world, T joint) {
  assert_physics3_dworld_not_stepping(world);
  if ((physics3_djoint_owners.has(joint) || world.joints.includes(joint))) {
    throw flight::Error(flight::String("Cannot add a physics joint that already belongs to a physics world"));
  }
  const bool active = (get_physics3_djoint_solver(world, joint.kind) != nullptr);
  if (active) {
    canonicalize_joint_ends(world, joint);
  }
  world.joints.push(joint);
  physics3_djoint_owners.set(joint, world);
  rebuild_physics3_djoint_collision_suppressions(world);
  if (active) {
    wake_joint_bodies(world, joint);
  }
  return joint;
}

inline std::optional<Physics3DJointSolver> get_physics3_djoint_solver(Physics3DWorld world, Physics3DJointKind kind) {
  return world.joint_solvers.get(kind).value_or(nullptr);
}

inline bool invalidate_physics3_djoint(Physics3DWorld world, Physics3DJoint joint) {
  assert_physics3_dworld_not_stepping(world);
  if (((physics3_djoint_owners.get(joint) != world) || !world.joints.includes(joint))) {
    return false;
  }
  ([&]() -> std::optional<auto> { auto optional_chain_receiver = ([&]() -> std::optional<auto> { auto optional_chain_receiver = get_physics3_djoint_solver(world, joint.kind); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().clear_accumulated_impulses; }()); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()(joint); }());
  joint.impulse0 = 0.0;
  joint.impulse1 = 0.0;
  joint.impulse2 = 0.0;
  joint.impulse3 = 0.0;
  joint.impulse4 = 0.0;
  joint.impulse5 = 0.0;
  rebuild_physics3_djoint_collision_suppressions(world);
  wake_joint_bodies(world, joint);
  return true;
}

inline bool is_physics3_dpair_ordered(double a, double b) {
  return (a <= b);
}

inline void register_physics3_djoint_solver(Physics3DWorld world, Physics3DJointKind kind, Physics3DJointSolver solver) {
  assert_physics3_dworld_not_stepping(world);
  world.joint_solvers.set(kind, solver);
  for (auto joint : world.joints) {
    if ((joint.kind != kind)) {
      continue;
    }
    canonicalize_joint_ends(world, joint);
    wake_joint_bodies(world, joint);
  }
  rebuild_physics3_djoint_collision_suppressions(world);
}

inline bool remove_physics3_djoint(Physics3DWorld world, Physics3DJoint joint) {
  assert_physics3_dworld_not_stepping(world);
  auto at = world.joints.index_of(joint);
  if ((at < 0.0)) {
    return false;
  }
  if ((get_physics3_djoint_solver(world, joint.kind) != nullptr)) {
    wake_joint_bodies(world, joint);
  }
  world.joints.splice(at, 1.0);
  physics3_djoint_owners.delete_(joint);
  rebuild_physics3_djoint_collision_suppressions(world);
  return true;
}

inline void canonicalize_joint_ends(Physics3DWorld world, Physics3DJoint joint) {
  std::optional<Physics3DJointSolver> solver = get_physics3_djoint_solver(world, joint.kind);
  if ((([&]() -> std::optional<auto> { auto optional_chain_receiver = solver.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().uses_body_a; }()) == false)) {
    return;
  }
  if (is_physics3_dpair_ordered(joint.body_a, joint.body_b)) {
    return;
  }
  if (!([&]() -> std::optional<auto> { auto optional_chain_receiver = ([&]() -> std::optional<auto> { auto optional_chain_receiver = solver.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().swap_ends; }()); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()(joint); }()).value_or(true)) {
    return;
  }
  auto body_a = joint.body_a;
  joint.body_a = joint.body_b;
  joint.body_b = body_a;
  auto anchor_x = joint.local_anchor_ax;
  auto anchor_y = joint.local_anchor_ay;
  auto anchor_z = joint.local_anchor_az;
  joint.local_anchor_ax = joint.local_anchor_bx;
  joint.local_anchor_ay = joint.local_anchor_by;
  joint.local_anchor_az = joint.local_anchor_bz;
  joint.local_anchor_bx = anchor_x;
  joint.local_anchor_by = anchor_y;
  joint.local_anchor_bz = anchor_z;
}

inline void wake_joint_bodies(Physics3DWorld world, Physics3DJoint joint) {
  std::optional<Physics3DJointSolver> solver = get_physics3_djoint_solver(world, joint.kind);
  if (!solver.value().has_value()) {
    return;
  }
  auto body_a = ((solver.value().uses_body_a == false) ? nullptr : find_physics3_dbody(world, joint.body_a));
  auto body_b = find_physics3_dbody(world, joint.body_b);
  if (body_a.has_value()) {
    wake_physics3_dbody(body_a);
  }
  if (body_b.has_value()) {
    wake_physics3_dbody(body_b);
  }
}

} // namespace flighthq_physics3d
