// Generated by @flighthq/tool-compiler from @flighthq/net/enableNetGuards.ts#EnableNetGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "net.hpp"

namespace flighthq_net {

inline bool are_net_guards_enabled() {
  return enabled;
}

inline void disable_net_guards() {
  set_net_guard(nullptr);
  enabled = false;
}

inline void enable_net_guards() {
  set_net_guard(warn_on_net_misuse);
  enabled = true;
}

inline void warn_on_net_misuse(NetGuardNotice notice) {
  auto object_pattern_value = notice.request;
  auto method = object_pattern_value.method;
  auto timeout_ms = object_pattern_value.timeout_ms;
  auto url = object_pattern_value.url;
  flight::String message;
  if ((notice.reason == flight::String("body-on-get-or-head"))) {
    message = flight::String("sendNetRequest: ") + flight::to_string(method.to_upper_case()) + flight::String(" requests do not carry a body — remove request.body or use a body-bearing method");
  }
  else {
    if ((notice.reason == flight::String("negative-timeout"))) {
      message = flight::String("sendNetRequest: timeoutMs is negative, so the provider cannot schedule a timeout — pass zero or a positive duration");
    }
    else {
      message = flight::String("sendNetRequest: the provider completed successfully without emitting the supplied progress signal — use a provider that supports download progress");
    }
  }
  log_once(flight::String("net:") + flight::to_string(notice.operation) + flight::String(":") + flight::to_string(notice.reason) + flight::String(""), log_level.warn, {.message = message, .method = method, .operation = notice.operation, .reason = notice.reason, .timeout_ms = timeout_ms, .url = url}, flight::String("net"));
}

inline bool enabled = false;

} // namespace flighthq_net
