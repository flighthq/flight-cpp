// Generated by @flighthq/tool-compiler from @flighthq/skeleton2d/pointAttachment2D.ts#PointAttachment2D. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "skeleton2d_constants.hpp"

namespace flighthq_skeleton2d {

inline void compute_skeleton2_dpoint_attachment_position(Vector2Like out, PointAttachment2D attachment, Skeleton2D skeleton, double bone_index) {
  auto world = skeleton.world_matrices;
  if (((bone_index < 0.0) || ((bone_index * matrix_stride) >= world.length))) {
    return;
  }
  const double b = (bone_index * matrix_stride);
  auto x = attachment.x;
  auto y = attachment.y;
  out.x = (((world[static_cast<size_t>(b)] * x) + (world[static_cast<size_t>((b + 2.0))] * y)) + world[static_cast<size_t>((b + 4.0))]);
  out.y = (((world[static_cast<size_t>((b + 1.0))] * x) + (world[static_cast<size_t>((b + 3.0))] * y)) + world[static_cast<size_t>((b + 5.0))]);
}

inline double compute_skeleton2_dpoint_attachment_rotation(PointAttachment2D attachment, Skeleton2D skeleton, double bone_index) {
  auto world = skeleton.world_matrices;
  if (((bone_index < 0.0) || ((bone_index * matrix_stride) >= world.length))) {
    return attachment.rotation;
  }
  const double b = (bone_index * matrix_stride);
  const double radians = (attachment.rotation * deg_to_rad);
  const double cos = std::cos(radians);
  const double sin = std::sin(radians);
  const double x = ((world[static_cast<size_t>(b)] * cos) + (world[static_cast<size_t>((b + 2.0))] * sin));
  const double y = ((world[static_cast<size_t>((b + 1.0))] * cos) + (world[static_cast<size_t>((b + 3.0))] * sin));
  return (cmath.atan2(y, x) * rad_to_deg);
}

} // namespace flighthq_skeleton2d
