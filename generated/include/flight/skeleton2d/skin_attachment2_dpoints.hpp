// Generated by @flighthq/tool-compiler from @flighthq/skeleton2d/packages/skeleton2d/src/skinAttachment2DPoints.ts#SkinAttachment2DPoints at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <flight/structural_ref.hpp>
#include <optional>
#include <variant>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/attachment_skin2_d.hpp>
#include <flight/types/bone2_d.hpp>
#include <flight/types/skeleton2_d.hpp>
#include <flight/types/skin2_d.hpp>
#include <flight/types/slot2_d.hpp>
#include <flight/skeleton2d/skeleton2d_constants.hpp>
#include <flight/skeleton2d/skeleton2d_guards.hpp>

namespace flight::skeleton2d {

inline void skin_skeleton2_dattachment_points(std::variant<flight::Array<double>, flight::Float32Array> out, std::variant<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Skin2D>>>>, flight::Null, flight::Undefined> skin, std::variant<flight::Float32Array, flight::Null, flight::Undefined> vertices, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Skeleton2D>>>> skeleton, double bone_index, std::optional<flight::Float32Array> deform, flight::String subject) {
  flight::Float32Array world = flight::row_get<flight::RowKey<"worldMatrices">>(skeleton);
  if ((!(std::holds_alternative<flight::Null>(std::get<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Skin2D>>>>>(skin))) && !(std::holds_alternative<flight::Undefined>(std::get<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Skin2D>>>>>(skin))))) {
    flight::Uint16Array counts = flight::row_get<flight::RowKey<"influenceCounts">>(std::get<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Skin2D>>>>>(skin));
    flight::Float32Array inf = flight::row_get<flight::RowKey<"influences">>(std::get<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Skin2D>>>>>(skin));
    auto offsets = ((deform.value().has_value() && ((deform.value().length * 2.0) == static_cast<double>(inf.size()))) ? deform.value() : nullptr);
    if ((deform.value().has_value() && !offsets.has_value())) {
      flight::skeleton2d::report_skeleton2_ddeform_length_mismatch(subject, deform.value().length, (static_cast<double>(inf.size()) / 2.0));
    }
    double vi = 0.0;
    double di = 0.0;
    double oi = 0.0;
    {
      double v = 0.0;
      while ((v < static_cast<double>(counts.size()))) {
        {
          double wx = 0.0;
          double wy = 0.0;
          auto n = counts.element(v);
          {
            double k = 0.0;
            while ((k < n)) {
              {
                const double b = (inf.element(vi) * flight::skeleton2d::skeleton_2_d_matrix_stride);
                auto lx = (!offsets.has_value() ? inf.element((vi + 1.0)) : (inf.element((vi + 1.0)) + offsets.element(di)));
                auto ly = (!offsets.has_value() ? inf.element((vi + 2.0)) : (inf.element((vi + 2.0)) + offsets.element((di + 1.0))));
                auto weight = inf.element((vi + 3.0));
                wx += (weight * (((world.element(b) * lx) + (world.element((b + 2.0)) * ly)) + world.element((b + 4.0))));
                wy += (weight * (((world.element((b + 1.0)) * lx) + (world.element((b + 3.0)) * ly)) + world.element((b + 5.0))));
                vi += 4.0;
                di += 2.0;
              }
              k += 1.0;
            }
          }
          out.element(oi) = wx;
          out.element((oi + 1.0)) = wy;
          oi += 2.0;
        }
        v += 1.0;
      }
    }
    return;
  }
  if ((std::holds_alternative<flight::Null>(std::get<flight::Float32Array>(vertices)) || std::holds_alternative<flight::Undefined>(std::get<flight::Float32Array>(vertices)))) {
    return;
  }
  if (((bone_index < 0.0) || ((bone_index * flight::skeleton2d::skeleton_2_d_matrix_stride) >= static_cast<double>(world.size())))) {
    return;
  }
  auto offsets_2 = ((deform.value().has_value() && (deform.value().length == std::get<flight::Float32Array>(vertices).length)) ? deform.value() : nullptr);
  if ((deform.value().has_value() && !offsets_2.has_value())) {
    flight::skeleton2d::report_skeleton2_ddeform_length_mismatch(subject, deform.value().length, std::get<flight::Float32Array>(vertices).length);
  }
  const double b_2 = (bone_index * flight::skeleton2d::skeleton_2_d_matrix_stride);
  auto a = world.element(b_2);
  auto bb = world.element((b_2 + 1.0));
  auto c = world.element((b_2 + 2.0));
  auto d = world.element((b_2 + 3.0));
  auto tx = world.element((b_2 + 4.0));
  auto ty = world.element((b_2 + 5.0));
  {
    double i = 0.0;
    while ((i < std::get<flight::Float32Array>(vertices).length)) {
      {
        auto vx = (!offsets_2.has_value() ? std::get<flight::Float32Array>(vertices).element(i) : (std::get<flight::Float32Array>(vertices).element(i) + offsets_2.element(i)));
        auto vy = (!offsets_2.has_value() ? std::get<flight::Float32Array>(vertices).element((i + 1.0)) : (std::get<flight::Float32Array>(vertices).element((i + 1.0)) + offsets_2.element((i + 1.0))));
        out.element(i) = (((a * vx) + (c * vy)) + tx);
        out.element((i + 1.0)) = (((bb * vx) + (d * vy)) + ty);
      }
      i += 2.0;
    }
  }
}

} // namespace flight::skeleton2d
