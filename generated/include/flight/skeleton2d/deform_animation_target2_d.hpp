// Generated by @flighthq/tool-compiler from @flighthq/skeleton2d/deformAnimationTarget2D.ts#DeformAnimationTarget2D. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "skeleton2d_animation_target.hpp"
#include "slot_deform2_d.hpp"

namespace flighthq_skeleton2d {

inline void register_skeleton2_ddeform_animation_target() {
  register_skeleton2_danimation_target_binder(skeleton2_danimation_target_kind.deform, bind_skeleton2_ddeform_channel);
}

inline void bind_skeleton2_ddeform_channel(AnimationChannel channel, Skeleton2D setup, Skeleton2D pose, auto target, double time) {
  Skeleton2DDeformAnimationTarget deform_target = static_cast<Skeleton2DDeformAnimationTarget>(target);
  auto slots = pose.slots;
  if ((!slots.has_value() || !slots.has_value())) {
    return;
  }
  auto slot_index = deform_target.slot_index;
  if ((((typeidslot_index != flight::String("number")) || (slot_index < 0.0)) || (slot_index >= slots.length))) {
    return;
  }
  auto components = channel.track.components;
  if ((components <= 0.0)) {
    return;
  }
  if ((scratch.length < components)) {
    scratch = flight::Float32Array(components);
  }
  sample_animation_track(scratch, channel.track, time);
  set_skeleton2_dslot_deform(slots[static_cast<size_t>(slot_index)], deform_target.attachment.value_or(nullptr), scratch.subarray(0.0, components));
}

inline auto scratch = flight::Float32Array(0.0);

} // namespace flighthq_skeleton2d
