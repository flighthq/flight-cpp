// Generated by @flighthq/tool-compiler from @flighthq/skeleton2d/enableSkeleton2DGuards.ts#EnableSkeleton2DGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "skeleton2d_guards.hpp"

namespace flighthq_skeleton2d {

inline void disable_skeleton2_dguards() {
  set_skeleton2_dcoerced_interpolation_guard(nullptr);
  set_skeleton2_ddeform_length_guard(nullptr);
}

inline void enable_skeleton2_dguards() {
  set_skeleton2_dcoerced_interpolation_guard(warn_on_coerced_interpolation);
  set_skeleton2_ddeform_length_guard(warn_on_deform_length_mismatch);
}

inline void warn_on_coerced_interpolation(Skeleton2DCoercedInterpolation report) {
  log_once(flight::String("skeleton2d:coerced-interpolation:") + flight::to_string(report.subject) + flight::String(""), log_level.warn, {.message = flight::String("A ") + flight::to_string(report.subject) + flight::String(" channel states '") + flight::to_string(report.stated) + flight::String("' interpolation but is walked as '") + flight::to_string(report.applied) + flight::String("'. The value it carries cannot be blended — an attachment index between two table entries names art nobody authored, and a draw order between two orderings gives fractional sort keys — so the step is forced and the stated easing has no effect. Author the track as '") + flight::to_string(report.applied) + flight::String("' to say what actually happens, or drive a blendable property instead.")}, flight::String("skeleton2d"));
}

inline void warn_on_deform_length_mismatch(Skeleton2DDeformLengthMismatch report) {
  log_once(flight::String("skeleton2d:deform-length:") + flight::to_string(report.subject) + flight::String(""), log_level.warn, {.message = flight::String("Deform offsets on '") + flight::to_string(report.subject) + flight::String("' carry ") + flight::to_string(report.offsets) + flight::String(" values but the vertex stream they address needs ") + flight::to_string(report.addressed) + flight::String(". The offsets are ignored rather than partly applied: a stream of the wrong length has no correct prefix, so consuming what fits would deform some vertices and silently leave the rest in their setup pose. Re-export the attachment, or check that the deform timeline belongs to this attachment.")}, flight::String("skeleton2d"));
}

} // namespace flighthq_skeleton2d
