// Generated by @flighthq/tool-compiler from @flighthq/skeleton2d/applyAnimationClipToSkeleton2D.ts#ApplyAnimationClipToSkeleton2D. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "skeleton2d_animation_target.hpp"

namespace flighthq_skeleton2d {

inline void apply_animation_clip_to_skeleton2_d(AnimationClip clip, Skeleton2D setup, Skeleton2D pose, double time) {
  if ((setup == pose)) {
    throw flight::Error(flight::String("applyAnimationClipToSkeleton2D: setup and pose must be distinct skeletons — pass a cloneSkeleton2D(setup) as pose"));
  }
  auto channels = clip.channels;
  {
    double i = 0.0;
    while ((i < channels.length)) {
      {
        auto channel = channels[static_cast<size_t>(i)];
        std::optional<Skeleton2DAnimationTarget> target = static_cast<std::optional<Skeleton2DAnimationTarget>>(channel.target_ref);
        if ((!target.value().has_value() || (typeidtarget.value() != flight::String("object")))) {
          i += 1.0;
          continue;
        }
        auto bind = get_skeleton2_danimation_target_binder(target.value().kind);
        if (!bind.has_value()) {
          i += 1.0;
          continue;
        }
        bind(channel, setup, pose, target.value(), time);
      }
      i += 1.0;
    }
  }
}

} // namespace flighthq_skeleton2d
