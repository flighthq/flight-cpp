// Generated by @flighthq/tool-compiler from @flighthq/textshaper/textShaperCluster.ts#TextShaperCluster. Do not edit.
#pragma once
#include <optional>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_textshaper {

inline flight::Array<double> get_caret_positions_for_run(ShapedRun run) {
  auto glyphs = run.glyphs;
  auto count = run.glyph_count;
  flight::Array<double> positions = flight::Array<double>((count + 1.0));
  positions.element(0.0) = 0.0;
  double x = 0.0;
  {
    double i = 0.0;
    while ((i < count)) {
      {
        x += glyphs[static_cast<size_t>(i)].x_advance;
        positions.element((i + 1.0)) = x;
      }
      i += 1.0;
    }
  }
  return positions;
}

inline double get_cluster_for_index(ShapedRun run, double string_index) {
  if ((run.glyph_count == 0.0)) {
    return -1.0;
  }
  if ((string_index < 0.0)) {
    return -1.0;
  }
  auto glyphs = run.glyphs;
  double best = -1.0;
  {
    double i = 0.0;
    while ((i < glyphs.length)) {
      {
        if ((glyphs[static_cast<size_t>(i)].cluster <= string_index)) {
          best = glyphs[static_cast<size_t>(i)].cluster;
        }
      }
      i += 1.0;
    }
  }
  return best;
}

inline std::optional<std::tuple<double, double>> get_index_range_for_cluster(ShapedRun run, double cluster, std::optional<double> string_length = std::nullopt) {
  if ((run.glyph_count == 0.0)) {
    return nullptr;
  }
  auto glyphs = run.glyphs;
  {
    double i = 0.0;
    while ((i < glyphs.length)) {
      {
        if ((glyphs[static_cast<size_t>(i)].cluster == cluster)) {
          std::optional<double> end;
          {
            double j = (i + 1.0);
            while ((j < glyphs.length)) {
              {
                if ((glyphs[static_cast<size_t>(j)].cluster != cluster)) {
                  end = glyphs[static_cast<size_t>(j)].cluster;
                  break;
                }
              }
              j += 1.0;
            }
          }
          if (!end.has_value()) {
            end = (string_length.has_value() ? string_length.value() : (cluster + 1.0));
          }
          return flight::Array{cluster, end.value()};
        }
      }
      i += 1.0;
    }
  }
  return nullptr;
}

} // namespace flighthq_textshaper
