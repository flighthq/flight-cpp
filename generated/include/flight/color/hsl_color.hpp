// Generated by @flighthq/tool-compiler from @flighthq/color/packages/color/src/hslColor.ts#HslColor at 903f3289590358eacc20698525e6982da2ab0e9d. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/hsl_color.hpp>

namespace flight::color {

inline flight::types::HslColor allocate_hsl_color() {
  return flight::Array{0.0, 0.0, 0.0};
}

inline flight::types::HslColor rgb_to_hsl(flight::types::HslColor out, double color) {
  const double r = (flight::bitwise_and(flight::unsigned_right_shift(color, 24.0), 255.0) / 255.0);
  const double g = (flight::bitwise_and(flight::unsigned_right_shift(color, 16.0), 255.0) / 255.0);
  const double b = (flight::bitwise_and(flight::unsigned_right_shift(color, 8.0), 255.0) / 255.0);
  const double max = flight::maximum(r, g, b);
  const double min = flight::minimum(r, g, b);
  const double l = ((max + min) / 2.0);
  if ((max == min)) {
    (out.element(0.0) = 0.0);
    (out.element(1.0) = 0.0);
    (out.element(2.0) = l);
    return out;
  }
  const double d = (max - min);
  const double s = ((l > 0.5) ? (d / ((2.0 - max) - min)) : (d / (max + min)));
  double h;
  if ((max == r)) {
    (h = ((((g - b) / d) + ((g < b) ? 6.0 : 0.0)) / 6.0));
  }
  else {
    if ((max == g)) {
      (h = ((((b - r) / d) + 2.0) / 6.0));
    }
    else {
      (h = ((((r - g) / d) + 4.0) / 6.0));
    }
  }
  (out.element(0.0) = (h * 360.0));
  (out.element(1.0) = s);
  (out.element(2.0) = l);
  return out;
}

inline double hue_to_rgb_channel(double p, double q, double t) {
  const double tn = std::fmod((std::fmod(t, 1.0) + 1.0), 1.0);
  if ((tn < (1.0 / 6.0))) {
    return (p + (((q - p) * 6.0) * tn));
  }
  if ((tn < (1.0 / 2.0))) {
    return q;
  }
  if ((tn < (2.0 / 3.0))) {
    return (p + (((q - p) * ((2.0 / 3.0) - tn)) * 6.0));
  }
  return p;
}

inline void hsl_to_rgb(flight::Array<double> out, double h, double s, double l) {
  if ((s == 0.0)) {
    (out.element(0.0) = l);
    (out.element(1.0) = l);
    (out.element(2.0) = l);
    return;
  }
  const double q = ((l < 0.5) ? (l * (1.0 + s)) : ((l + s) - (l * s)));
  const double p = ((2.0 * l) - q);
  const double hn = (h / 360.0);
  (out.element(0.0) = hue_to_rgb_channel(p, q, (hn + (1.0 / 3.0))));
  (out.element(1.0) = hue_to_rgb_channel(p, q, hn));
  (out.element(2.0) = hue_to_rgb_channel(p, q, (hn - (1.0 / 3.0))));
}

} // namespace flight::color
