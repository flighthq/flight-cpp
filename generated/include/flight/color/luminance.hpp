// Generated by @flighthq/tool-compiler from @flighthq/color/packages/color/src/luminance.ts#Luminance at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/color/srgb_transfer.hpp>

namespace flight::color {

inline double get_color_luminance(double color) {
  const double r = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(color, 24.0), 255.0) / 255.0));
  const double g = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(color, 16.0), 255.0) / 255.0));
  const double b = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(color, 8.0), 255.0) / 255.0));
  return (((0.2126 * r) + (0.7152 * g)) + (0.0722 * b));
}

inline double get_color_contrast_ratio(double a, double b) {
  const double la = get_color_luminance(a);
  const double lb = get_color_luminance(b);
  auto lighter = flight::maximum(la, lb);
  auto darker = flight::minimum(la, lb);
  return ((lighter + 0.05) / (darker + 0.05));
}

inline void get_rec2020_luminance_weights(flight::Array<double> out) {
  out.element(0.0) = 0.2627;
  out.element(1.0) = 0.678;
  out.element(2.0) = 0.0593;
}

inline void get_rec709_luminance_weights(flight::Array<double> out) {
  out.element(0.0) = 0.2126;
  out.element(1.0) = 0.7152;
  out.element(2.0) = 0.0722;
}

} // namespace flight::color
