// Generated by @flighthq/tool-compiler from @flighthq/color/packages/color/src/hsvColor.ts#HsvColor at 903f3289590358eacc20698525e6982da2ab0e9d. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/hsv_color.hpp>

namespace flight::color {

inline flight::types::HsvColor allocate_hsv_color() {
  return flight::Array{0.0, 0.0, 0.0};
}

inline void hsv_to_rgb(flight::Array<double> out, double h, double s, double v) {
  if ((s == 0.0)) {
    (out.element(0.0) = v);
    (out.element(1.0) = v);
    (out.element(2.0) = v);
    return;
  }
  const double hn = std::fmod((std::fmod(h, 360.0) + 360.0), 360.0);
  const double i = std::fmod(std::floor((hn / 60.0)), 6.0);
  const double f = ((hn / 60.0) - std::floor((hn / 60.0)));
  const double p = (v * (1.0 - s));
  const double q = (v * (1.0 - (f * s)));
  const double t = (v * (1.0 - ((1.0 - f) * s)));
  {
    auto switch_value = i;
    if (switch_value == 0.0) {
      (out.element(0.0) = v);
      (out.element(1.0) = t);
      (out.element(2.0) = p);
    }
    else if (switch_value == 1.0) {
      (out.element(0.0) = q);
      (out.element(1.0) = v);
      (out.element(2.0) = p);
    }
    else if (switch_value == 2.0) {
      (out.element(0.0) = p);
      (out.element(1.0) = v);
      (out.element(2.0) = t);
    }
    else if (switch_value == 3.0) {
      (out.element(0.0) = p);
      (out.element(1.0) = q);
      (out.element(2.0) = v);
    }
    else if (switch_value == 4.0) {
      (out.element(0.0) = t);
      (out.element(1.0) = p);
      (out.element(2.0) = v);
    }
    else {
      (out.element(0.0) = v);
      (out.element(1.0) = p);
      (out.element(2.0) = q);
    }
  }
}

inline flight::types::HsvColor rgb_to_hsv(flight::types::HsvColor out, double color) {
  const double r = (flight::bitwise_and(flight::unsigned_right_shift(color, 24.0), 255.0) / 255.0);
  const double g = (flight::bitwise_and(flight::unsigned_right_shift(color, 16.0), 255.0) / 255.0);
  const double b = (flight::bitwise_and(flight::unsigned_right_shift(color, 8.0), 255.0) / 255.0);
  const double max = flight::maximum(r, g, b);
  const double min = flight::minimum(r, g, b);
  const double d = (max - min);
  const double v = max;
  const double s = ((max == 0.0) ? 0.0 : (d / max));
  double h;
  if ((d == 0.0)) {
    (h = 0.0);
  }
  else {
    if ((max == r)) {
      (h = ((((g - b) / d) + ((g < b) ? 6.0 : 0.0)) / 6.0));
    }
    else {
      if ((max == g)) {
        (h = ((((b - r) / d) + 2.0) / 6.0));
      }
      else {
        (h = ((((r - g) / d) + 4.0) / 6.0));
      }
    }
  }
  (out.element(0.0) = (h * 360.0));
  (out.element(1.0) = s);
  (out.element(2.0) = v);
  return out;
}

} // namespace flight::color
