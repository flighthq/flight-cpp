// Generated by @flighthq/tool-compiler from @flighthq/color/packages/color/src/lerpColor.ts#LerpColor at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/linear_color.hpp>
#include <flight/color/pack_color.hpp>
#include <flight/color/srgb_transfer.hpp>

namespace flight::color {

inline double lerp_color(double start, double end, double t) {
  auto tc = flight::minimum(1.0, flight::maximum(0.0, t));
  const double sr = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(start, 24.0), 255.0) / 255.0));
  const double sg = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(start, 16.0), 255.0) / 255.0));
  const double sb = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(start, 8.0), 255.0) / 255.0));
  const double sa = (flight::bitwise_and(start, 255.0) / 255.0);
  const double er = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(end, 24.0), 255.0) / 255.0));
  const double eg = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(end, 16.0), 255.0) / 255.0));
  const double eb = flight::color::srgb_channel_to_linear((flight::bitwise_and(flight::unsigned_right_shift(end, 8.0), 255.0) / 255.0));
  const double ea = (flight::bitwise_and(end, 255.0) / 255.0);
  const double r = (sr + ((er - sr) * tc));
  const double g = (sg + ((eg - sg) * tc));
  const double b = (sb + ((eb - sb) * tc));
  const double a = (sa + ((ea - sa) * tc));
  return flight::color::pack_linear_to_color(flight::Array{r, g, b, a});
}

inline flight::types::LinearColor lerp_linear_color(flight::types::LinearColor out, flight::types::LinearColor start, flight::types::LinearColor end, double t) {
  auto tc = flight::minimum(1.0, flight::maximum(0.0, t));
  const double r0 = start.element(0.0);
  const double g0 = start.element(1.0);
  const double b0 = start.element(2.0);
  const double a0 = start.element(3.0);
  const double r1 = end.element(0.0);
  const double g1 = end.element(1.0);
  const double b1 = end.element(2.0);
  const double a1 = end.element(3.0);
  out.element(0.0) = (r0 + ((r1 - r0) * tc));
  out.element(1.0) = (g0 + ((g1 - g0) * tc));
  out.element(2.0) = (b0 + ((b1 - b0) * tc));
  out.element(3.0) = (a0 + ((a1 - a0) * tc));
  return out;
}

} // namespace flight::color
