// Generated by @flighthq/tool-compiler from @flighthq/velocity/transformVelocity.ts#TransformVelocity. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "velocity_field.hpp"

namespace flighthq_velocity {

template <typename Traits>
inline void contribute_transform_velocity(VelocityField field, Transform2DNode<Traits> root) {
  visit_transform_velocity(field, root);
}

template <typename Traits>
inline void visit_transform_velocity(VelocityField field, Transform2DNode<Traits> node) {
  Transform2DNode<Traits> mutable_node = static_cast<Transform2DNode<Traits>>(node);
  ensure_node_world_matrix(mutable_node);
  auto world = get_node_world_matrix(mutable_node);
  auto sample = ensure_velocity_sample(field, node);
  if ((sample.explicit_frame_id != field.frame_id)) {
    if ((sample.previous_world_transform != nullptr)) {
      sample.velocity.x = (world.tx - sample.previous_world_transform.tx);
      sample.velocity.y = (world.ty - sample.previous_world_transform.ty);
    }
    else {
      sample.velocity.x = 0.0;
      sample.velocity.y = 0.0;
    }
    sample.last_frame_id = field.frame_id;
  }
  if ((sample.previous_world_transform == nullptr)) {
    sample.previous_world_transform = create_matrix();
  }
  copy_matrix(sample.previous_world_transform, world);
  auto count = get_node_child_count(mutable_node);
  {
    double i = 0.0;
    while ((i < count)) {
      {
        auto child = get_node_child_at(mutable_node, i);
        if (child.has_value()) {
          visit_transform_velocity(field, static_cast<Transform2DNode<Traits>>(static_cast<auto>(child)));
        }
      }
      i += 1.0;
    }
  }
}

} // namespace flighthq_velocity
