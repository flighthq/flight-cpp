// Generated by @flighthq/tool-compiler from @flighthq/velocity/packages/velocity/src/velocityField.ts#VelocityField at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <flight/weak_map.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/entity/entity.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/matrix.hpp>
#include <flight/types/velocity.hpp>

namespace flight::velocity {

inline flight::Ref<flight::types::Velocity2D> add_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> a, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> b) {
  const double ax = flight::row_get<flight::RowKey<"x">>(a);
  const double ay = flight::row_get<flight::RowKey<"y">>(a);
  out->x = (ax + flight::row_get<flight::RowKey<"x">>(b));
  out->y = (ay + flight::row_get<flight::RowKey<"y">>(b));
  return out;
}

inline void begin_velocity_frame(flight::Ref<flight::types::VelocityField> field) {
  field->frame_id++;
}

inline flight::Ref<flight::types::Velocity2D> clamp_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> velocity, double max_length) {
  const double vx = flight::row_get<flight::RowKey<"x">>(velocity);
  const double vy = flight::row_get<flight::RowKey<"y">>(velocity);
  const double len_sq = ((vx * vx) + (vy * vy));
  const double max_sq = (max_length * max_length);
  if (((len_sq > max_sq) && (len_sq > 0.0))) {
    const double scale = (max_length / std::sqrt(len_sq));
    out->x = (vx * scale);
    out->y = (vy * scale);
  }
  else {
    out->x = vx;
    out->y = vy;
  }
  return out;
}

inline flight::Ref<flight::types::Velocity2D> copy_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> source) {
  const double sx = flight::row_get<flight::RowKey<"x">>(source);
  const double sy = flight::row_get<flight::RowKey<"y">>(source);
  out->x = sx;
  out->y = sy;
  return out;
}

inline flight::Ref<flight::types::Velocity2D> damp_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> current, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> previous, double factor) {
  const double cx = flight::row_get<flight::RowKey<"x">>(current);
  const double cy = flight::row_get<flight::RowKey<"y">>(current);
  const double px = flight::row_get<flight::RowKey<"x">>(previous);
  const double py = flight::row_get<flight::RowKey<"y">>(previous);
  out->x = ((cx * factor) + (px * (1.0 - factor)));
  out->y = ((cy * factor) + (py * (1.0 - factor)));
  return out;
}

inline flight::Ref<flight::types::VelocitySample> ensure_velocity_sample(flight::Ref<flight::types::VelocityField> field, flight::Ref<void> source) {
  std::optional<flight::Ref<flight::types::VelocitySample>> sample = field->samples.get(source);
  if (!sample.has_value()) {
    sample = {.previous_world_transform = nullptr, .velocity = flight::make_ref<flight::types::Velocity2D>(flight::types::Velocity2D{.x = 0.0, .y = 0.0}), .last_frame_id = -1.0, .explicit_frame_id = -1.0};
    field->samples.set(source, sample.value());
  }
  return sample.value();
}

inline void contribute_velocity(flight::Ref<flight::types::VelocityField> field, flight::Ref<void> source, double x, double y) {
  flight::Ref<flight::types::VelocitySample> sample = ensure_velocity_sample(field, source);
  sample->velocity->x = x;
  sample->velocity->y = y;
  sample->last_frame_id = field->frame_id;
  sample->explicit_frame_id = field->frame_id;
}

inline flight::Ref<flight::types::VelocityExplanation> explain_velocity(flight::Ref<flight::types::VelocityField> field, flight::Ref<void> source) {
  auto sample = field->samples.get(source);
  const double current_frame_id = field->frame_id;
  if (!sample.has_value()) {
    return ([&]() { auto object_member_current_frame_id = current_frame_id; auto object_member_explicit = false; auto object_member_last_frame_id = -1.0; auto object_member_reason = flight::String("no-sample"); auto object_member_x = 0.0; auto object_member_y = 0.0; return flight::make_ref<flight::types::VelocityExplanation>(flight::types::VelocityExplanation{.reason = object_member_reason, .x = object_member_x, .y = object_member_y, .last_frame_id = object_member_last_frame_id, .current_frame_id = object_member_current_frame_id, .explicit_ = object_member_explicit}); }());
  }
  if ((sample.value()->last_frame_id != current_frame_id)) {
    return ([&]() { auto object_member_current_frame_id_2 = current_frame_id; auto object_member_explicit_2 = false; auto object_member_last_frame_id_2 = sample.value()->last_frame_id; auto object_member_reason_2 = flight::String("stale"); auto object_member_x_2 = 0.0; auto object_member_y_2 = 0.0; return flight::make_ref<flight::types::VelocityExplanation>(flight::types::VelocityExplanation{.reason = object_member_reason_2, .x = object_member_x_2, .y = object_member_y_2, .last_frame_id = object_member_last_frame_id_2, .current_frame_id = object_member_current_frame_id_2, .explicit_ = object_member_explicit_2}); }());
  }
  const bool explicit_ = (sample.value()->explicit_frame_id == current_frame_id);
  const double x = sample.value()->velocity->x;
  const double y = sample.value()->velocity->y;
  if (((x == 0.0) && (y == 0.0))) {
    const flight::String reason = (explicit_ ? flight::String("explicit-zero") : flight::String("derived-zero"));
    return ([&]() { auto object_member_current_frame_id_3 = current_frame_id; auto object_member_explicit_3 = explicit_; auto object_member_last_frame_id_3 = sample.value()->last_frame_id; auto object_member_reason_3 = reason; auto object_member_x_3 = 0.0; auto object_member_y_3 = 0.0; return flight::make_ref<flight::types::VelocityExplanation>(flight::types::VelocityExplanation{.reason = object_member_reason_3, .x = object_member_x_3, .y = object_member_y_3, .last_frame_id = object_member_last_frame_id_3, .current_frame_id = object_member_current_frame_id_3, .explicit_ = object_member_explicit_3}); }());
  }
  return ([&]() { auto object_member_current_frame_id_4 = current_frame_id; auto object_member_explicit_4 = explicit_; auto object_member_last_frame_id_4 = sample.value()->last_frame_id; auto object_member_reason_4 = flight::String("ok"); auto object_member_x_4 = x; auto object_member_y_4 = y; return flight::make_ref<flight::types::VelocityExplanation>(flight::types::VelocityExplanation{.reason = object_member_reason_4, .x = object_member_x_4, .y = object_member_y_4, .last_frame_id = object_member_last_frame_id_4, .current_frame_id = object_member_current_frame_id_4, .explicit_ = object_member_explicit_4}); }());
}

inline flight::Ref<flight::types::Velocity2D> get_velocity(flight::Ref<flight::types::VelocityField> field, flight::Ref<void> source, flight::Ref<flight::types::Velocity2D> out) {
  auto sample = field->samples.get(source);
  if ((!sample.has_value() || (sample.value()->last_frame_id != field->frame_id))) {
    out->x = 0.0;
    out->y = 0.0;
    return out;
  }
  out->x = sample.value()->velocity->x;
  out->y = sample.value()->velocity->y;
  return out;
}

inline bool has_velocity(flight::Ref<flight::types::VelocityField> field, flight::Ref<void> source) {
  auto sample = field->samples.get(source);
  return ((sample.has_value() && (sample.value()->last_frame_id == field->frame_id)) && ((sample.value()->velocity->x != 0.0) || (sample.value()->velocity->y != 0.0)));
}

inline void initialize_velocity_field(flight::types::EntityConstruction<flight::Ref<flight::types::VelocityField>> out) {
  flight::row_set<flight::RowKey<"frameId">>(out, 0.0);
  flight::row_set<flight::RowKey<"samples">>(out, flight::WeakMap<flight::Ref<void>, flight::Ref<flight::types::VelocitySample>>());
}

inline flight::Ref<flight::types::VelocityField> create_velocity_field() {
  flight::types::EntityConstruction<flight::Ref<flight::types::VelocityField>> out = flight::entity::allocate_entity<flight::Ref<flight::types::VelocityField>>();
  initialize_velocity_field(out);
  return flight::entity::finish_entity(out);
}

inline bool is_velocity_zero(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> velocity, std::optional<double> epsilon = std::nullopt) {
  const double e = epsilon;
  return ((std::abs(flight::row_get<flight::RowKey<"x">>(velocity)) <= e) && (std::abs(flight::row_get<flight::RowKey<"y">>(velocity)) <= e));
}

inline double length_of_velocity(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> velocity) {
  return std::sqrt(((flight::row_get<flight::RowKey<"x">>(velocity) * flight::row_get<flight::RowKey<"x">>(velocity)) + (flight::row_get<flight::RowKey<"y">>(velocity) * flight::row_get<flight::RowKey<"y">>(velocity))));
}

inline flight::Ref<flight::types::Velocity2D> lerp_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> a, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> b, double t) {
  const double ax = flight::row_get<flight::RowKey<"x">>(a);
  const double ay = flight::row_get<flight::RowKey<"y">>(a);
  out->x = (ax + ((flight::row_get<flight::RowKey<"x">>(b) - ax) * t));
  out->y = (ay + ((flight::row_get<flight::RowKey<"y">>(b) - ay) * t));
  return out;
}

inline flight::Ref<flight::types::Velocity2D> normalize_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> source) {
  const double sx = flight::row_get<flight::RowKey<"x">>(source);
  const double sy = flight::row_get<flight::RowKey<"y">>(source);
  auto len = std::sqrt(((sx * sx) + (sy * sy)));
  if ((len > 0.0)) {
    const double inv = (1.0 / len);
    out->x = (sx * inv);
    out->y = (sy * inv);
  }
  else {
    out->x = 0.0;
    out->y = 0.0;
  }
  return out;
}

inline flight::Ref<flight::types::Velocity2D> scale_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> velocity, double scale) {
  const double vx = flight::row_get<flight::RowKey<"x">>(velocity);
  const double vy = flight::row_get<flight::RowKey<"y">>(velocity);
  out->x = (vx * scale);
  out->y = (vy * scale);
  return out;
}

inline flight::Ref<flight::types::Velocity2D> subtract_velocity(flight::Ref<flight::types::Velocity2D> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> a, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Velocity2D>>>> b) {
  const double ax = flight::row_get<flight::RowKey<"x">>(a);
  const double ay = flight::row_get<flight::RowKey<"y">>(a);
  out->x = (ax - flight::row_get<flight::RowKey<"x">>(b));
  out->y = (ay - flight::row_get<flight::RowKey<"y">>(b));
  return out;
}

inline void suppress_velocity(flight::Ref<flight::types::VelocityField> field, flight::Ref<void> source) {
  contribute_velocity(field, source, 0.0, 0.0);
}

inline flight::Ref<flight::types::Velocity2D> zero_velocity(flight::Ref<flight::types::Velocity2D> out) {
  out->x = 0.0;
  out->y = 0.0;
  return out;
}

} // namespace flight::velocity
