// Generated by @flighthq/tool-compiler from @flighthq/geometry/matrix3Pool.ts#Matrix3Pool. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "geometry_pool_guards.hpp"
#include "matrix3.hpp"

namespace flighthq_geometry {

inline Matrix3 acquire_identity_matrix3() {
  Matrix3 m = acquire_matrix3();
  set_matrix3_identity(m);
  return m;
}

inline Matrix3 acquire_matrix3() {
  Matrix3 m;
  if ((pool.length > 0.0)) {
    m = static_cast<Matrix3>(pool.pop());
  }
  else {
    m = create_matrix3();
  }
  return m;
}

inline void clear_matrix3_pool() {
  pool.length = 0.0;
}

inline void release_matrix3(Matrix3 m) {
  if (!m) {
    return;
  }
  if (((geometry_pool_release_guard != nullptr) && pool.includes(m))) {
    geometry_pool_release_guard(flight::String("releaseMatrix3"));
  }
  m[static_cast<size_t>(entity_runtime_key)] = undefined;
  pool.push(m);
}

inline flight::Array<Matrix3> pool = flight::Array<Matrix3>{};

} // namespace flighthq_geometry
