// Generated by @flighthq/tool-compiler from @flighthq/geometry/matrixPool.ts#MatrixPool. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "geometry_pool_guards.hpp"
#include "matrix.hpp"

namespace flighthq_geometry {

inline Matrix acquire_identity_matrix() {
  Matrix m = acquire_matrix();
  set_matrix_identity(m);
  return m;
}

inline Matrix acquire_matrix() {
  Matrix m;
  if ((pool.length > 0.0)) {
    m = static_cast<Matrix>(pool.pop());
  }
  else {
    m = create_matrix();
  }
  return m;
}

inline void clear_matrix_pool() {
  pool.length = 0.0;
}

inline void release_matrix(Matrix m) {
  if (!m) {
    return;
  }
  if (((geometry_pool_release_guard != nullptr) && pool.includes(m))) {
    geometry_pool_release_guard(flight::String("releaseMatrix"));
  }
  m[static_cast<size_t>(entity_runtime_key)] = undefined;
  pool.push(m);
}

inline flight::Array<Matrix> pool = flight::Array<Matrix>{};

} // namespace flighthq_geometry
