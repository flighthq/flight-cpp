// Generated by @flighthq/tool-compiler from @flighthq/geometry/packages/geometry/src/ray3d.ts#Ray3d at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/number.hpp>
#include <flight/structural_ref.hpp>
#include <limits>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { struct BoundingSphere; }
namespace flight::types { struct Entity; }
namespace flight::types { struct Matrix4; }
namespace flight::types { struct Plane; }
namespace flight::types { struct Ray3D; }
namespace flight::types { struct Vector3; }

#include <flight/entity/entity.hpp>
#include <flight/types/aabb.hpp>
#include <flight/types/bounding_sphere.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/matrix4.hpp>
#include <flight/types/plane.hpp>
#include <flight/types/ray3_d.hpp>
#include <flight/types/vector3.hpp>
#include <flight/geometry/vector3.hpp>

namespace flight::geometry {

inline void get_closest_point_between_ray3_ds(flight::types::Vector3Like out_a, flight::types::Vector3Like out_b, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> a, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> b) {
  const double aox = flight::row_get<flight::RowKey<"origin">>(a)->x;
  const double aoy = flight::row_get<flight::RowKey<"origin">>(a)->y;
  const double aoz = flight::row_get<flight::RowKey<"origin">>(a)->z;
  const double adx = flight::row_get<flight::RowKey<"direction">>(a)->x;
  const double ady = flight::row_get<flight::RowKey<"direction">>(a)->y;
  const double adz = flight::row_get<flight::RowKey<"direction">>(a)->z;
  const double box = flight::row_get<flight::RowKey<"origin">>(b)->x;
  const double boy = flight::row_get<flight::RowKey<"origin">>(b)->y;
  const double boz = flight::row_get<flight::RowKey<"origin">>(b)->z;
  const double bdx = flight::row_get<flight::RowKey<"direction">>(b)->x;
  const double bdy = flight::row_get<flight::RowKey<"direction">>(b)->y;
  const double bdz = flight::row_get<flight::RowKey<"direction">>(b)->z;
  const double aa = (((adx * adx) + (ady * ady)) + (adz * adz));
  const double bb = (((bdx * bdx) + (bdy * bdy)) + (bdz * bdz));
  const double ab = (((adx * bdx) + (ady * bdy)) + (adz * bdz));
  const double rx = (aox - box);
  const double ry = (aoy - boy);
  const double rz = (aoz - boz);
  const double ar = (((adx * rx) + (ady * ry)) + (adz * rz));
  const double br = (((bdx * rx) + (bdy * ry)) + (bdz * rz));
  const double denom = ((aa * bb) - (ab * ab));
  double ta;
  double tb;
  if ((denom != 0.0)) {
    (ta = (((ab * br) - (bb * ar)) / denom));
  }
  else {
    (ta = 0.0);
  }
  if ((ta < 0.0)) {
    (ta = 0.0);
  }
  (tb = ((bb != 0.0) ? (((ab * ta) + br) / bb) : 0.0));
  if ((tb < 0.0)) {
    (tb = 0.0);
    (ta = ((aa != 0.0) ? (-ar / aa) : 0.0));
    if ((ta < 0.0)) {
      (ta = 0.0);
    }
  }
  (out_a->x = (aox + (adx * ta)));
  (out_a->y = (aoy + (ady * ta)));
  (out_a->z = (aoz + (adz * ta)));
  (out_b->x = (box + (bdx * tb)));
  (out_b->y = (boy + (bdy * tb)));
  (out_b->z = (boz + (bdz * tb)));
}

inline void get_closest_point_on_ray3_d(flight::types::Vector3Like out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Vector3Like>>> point) {
  const double ox = flight::row_get<flight::RowKey<"origin">>(ray)->x;
  const double oy = flight::row_get<flight::RowKey<"origin">>(ray)->y;
  const double oz = flight::row_get<flight::RowKey<"origin">>(ray)->z;
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  const double px = flight::row_get<flight::RowKey<"x">>(point);
  const double py = flight::row_get<flight::RowKey<"y">>(point);
  const double pz = flight::row_get<flight::RowKey<"z">>(point);
  const double len_sq = (((dx * dx) + (dy * dy)) + (dz * dz));
  double t = ((len_sq != 0.0) ? (((((px - ox) * dx) + ((py - oy) * dy)) + ((pz - oz) * dz)) / len_sq) : 0.0);
  if ((t < 0.0)) {
    (t = 0.0);
  }
  (out->x = (ox + (dx * t)));
  (out->y = (oy + (dy * t)));
  (out->z = (oz + (dz * t)));
}

inline void get_ray3_dpoint_at(flight::types::Vector3Like out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, double t) {
  const double ox = flight::row_get<flight::RowKey<"origin">>(ray)->x;
  const double oy = flight::row_get<flight::RowKey<"origin">>(ray)->y;
  const double oz = flight::row_get<flight::RowKey<"origin">>(ray)->z;
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  (out->x = (ox + (dx * t)));
  (out->y = (oy + (dy * t)));
  (out->z = (oz + (dz * t)));
}

inline void initialize_ray3_d(flight::types::EntityConstruction<flight::Ref<flight::types::Ray3D>> out, flight::Ref<flight::types::Vector3> origin, flight::Ref<flight::types::Vector3> direction) {
  flight::row_set<flight::RowKey<"origin">>(out, origin);
  flight::row_set<flight::RowKey<"direction">>(out, direction);
}

inline flight::Ref<flight::types::Ray3D> create_ray3_d(std::optional<double> origin_x = std::nullopt, std::optional<double> origin_y = std::nullopt, std::optional<double> origin_z = std::nullopt, std::optional<double> direction_x = std::nullopt, std::optional<double> direction_y = std::nullopt, std::optional<double> direction_z = std::nullopt) {
  flight::Ref<flight::types::Vector3> origin = flight::geometry::create_vector3(origin_x.value_or(0.0), origin_y.value_or(0.0), origin_z.value_or(0.0));
  flight::Ref<flight::types::Vector3> direction = flight::geometry::create_vector3(direction_x.value_or(0.0), direction_y.value_or(0.0), direction_z.value_or(1.0));
  flight::types::EntityConstruction<flight::Ref<flight::types::Ray3D>> out = flight::entity::allocate_entity<flight::Ref<flight::types::Ray3D>>();
  initialize_ray3_d(out, origin, direction);
  return flight::entity::finish_entity<flight::Ref<flight::types::Ray3D>>(out);
}

inline double intersect_ray3_daabb(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::AabbLike>>>> aabb) {
  if ((((flight::row_get<flight::RowKey<"min">>(aabb)->x > flight::row_get<flight::RowKey<"max">>(aabb)->x) || (flight::row_get<flight::RowKey<"min">>(aabb)->y > flight::row_get<flight::RowKey<"max">>(aabb)->y)) || (flight::row_get<flight::RowKey<"min">>(aabb)->z > flight::row_get<flight::RowKey<"max">>(aabb)->z))) {
    return -1.0;
  }
  const double ox = flight::row_get<flight::RowKey<"origin">>(ray)->x;
  const double oy = flight::row_get<flight::RowKey<"origin">>(ray)->y;
  const double oz = flight::row_get<flight::RowKey<"origin">>(ray)->z;
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  if (((((dx * dx) + (dy * dy)) + (dz * dz)) == 0.0)) {
    return -1.0;
  }
  double t_min = 0.0;
  double t_max = std::numeric_limits<double>::infinity();
  if ((dx != 0.0)) {
    const double inv_dx = (1.0 / dx);
    double t1 = ((flight::row_get<flight::RowKey<"min">>(aabb)->x - ox) * inv_dx);
    double t2 = ((flight::row_get<flight::RowKey<"max">>(aabb)->x - ox) * inv_dx);
    if ((t1 > t2)) {
      const double tmp = t1;
      (t1 = t2);
      (t2 = tmp);
    }
    (t_min = flight::maximum(t_min, t1));
    (t_max = flight::minimum(t_max, t2));
    if ((t_min > t_max)) {
      return -1.0;
    }
  }
  else {
    if (((ox < flight::row_get<flight::RowKey<"min">>(aabb)->x) || (ox > flight::row_get<flight::RowKey<"max">>(aabb)->x))) {
      return -1.0;
    }
  }
  if ((dy != 0.0)) {
    const double inv_dy = (1.0 / dy);
    double t1_2 = ((flight::row_get<flight::RowKey<"min">>(aabb)->y - oy) * inv_dy);
    double t2_2 = ((flight::row_get<flight::RowKey<"max">>(aabb)->y - oy) * inv_dy);
    if ((t1_2 > t2_2)) {
      const double tmp_2 = t1_2;
      (t1_2 = t2_2);
      (t2_2 = tmp_2);
    }
    (t_min = flight::maximum(t_min, t1_2));
    (t_max = flight::minimum(t_max, t2_2));
    if ((t_min > t_max)) {
      return -1.0;
    }
  }
  else {
    if (((oy < flight::row_get<flight::RowKey<"min">>(aabb)->y) || (oy > flight::row_get<flight::RowKey<"max">>(aabb)->y))) {
      return -1.0;
    }
  }
  if ((dz != 0.0)) {
    const double inv_dz = (1.0 / dz);
    double t1_3 = ((flight::row_get<flight::RowKey<"min">>(aabb)->z - oz) * inv_dz);
    double t2_3 = ((flight::row_get<flight::RowKey<"max">>(aabb)->z - oz) * inv_dz);
    if ((t1_3 > t2_3)) {
      const double tmp_3 = t1_3;
      (t1_3 = t2_3);
      (t2_3 = tmp_3);
    }
    (t_min = flight::maximum(t_min, t1_3));
    (t_max = flight::minimum(t_max, t2_3));
    if ((t_min > t_max)) {
      return -1.0;
    }
  }
  else {
    if (((oz < flight::row_get<flight::RowKey<"min">>(aabb)->z) || (oz > flight::row_get<flight::RowKey<"max">>(aabb)->z))) {
      return -1.0;
    }
  }
  return t_min;
}

inline double intersect_ray3_dplane(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::PlaneLike>>> plane) {
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  const double normal_length_sq = (((flight::row_get<flight::RowKey<"a">>(plane) * flight::row_get<flight::RowKey<"a">>(plane)) + (flight::row_get<flight::RowKey<"b">>(plane) * flight::row_get<flight::RowKey<"b">>(plane))) + (flight::row_get<flight::RowKey<"c">>(plane) * flight::row_get<flight::RowKey<"c">>(plane)));
  const double direction_length_sq = (((dx * dx) + (dy * dy)) + (dz * dz));
  if (((normal_length_sq == 0.0) || (direction_length_sq == 0.0))) {
    return -1.0;
  }
  const double denom = (((flight::row_get<flight::RowKey<"a">>(plane) * dx) + (flight::row_get<flight::RowKey<"b">>(plane) * dy)) + (flight::row_get<flight::RowKey<"c">>(plane) * dz));
  if ((std::abs(denom) <= ((1e-10 * std::sqrt(normal_length_sq)) * std::sqrt(direction_length_sq)))) {
    return -1.0;
  }
  const double t = (-((((flight::row_get<flight::RowKey<"a">>(plane) * flight::row_get<flight::RowKey<"origin">>(ray)->x) + (flight::row_get<flight::RowKey<"b">>(plane) * flight::row_get<flight::RowKey<"origin">>(ray)->y)) + (flight::row_get<flight::RowKey<"c">>(plane) * flight::row_get<flight::RowKey<"origin">>(ray)->z)) + flight::row_get<flight::RowKey<"d">>(plane)) / denom);
  return ((t >= 0.0) ? t : -1.0);
}

inline double intersect_ray3_dsphere(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::BoundingSphereLike>>> sphere) {
  if ((flight::row_get<flight::RowKey<"radius">>(sphere) < 0.0)) {
    return -1.0;
  }
  const double ox = (flight::row_get<flight::RowKey<"origin">>(ray)->x - flight::row_get<flight::RowKey<"center">>(sphere)->x);
  const double oy = (flight::row_get<flight::RowKey<"origin">>(ray)->y - flight::row_get<flight::RowKey<"center">>(sphere)->y);
  const double oz = (flight::row_get<flight::RowKey<"origin">>(ray)->z - flight::row_get<flight::RowKey<"center">>(sphere)->z);
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  const double a = (((dx * dx) + (dy * dy)) + (dz * dz));
  if ((a == 0.0)) {
    return -1.0;
  }
  const double b = (((ox * dx) + (oy * dy)) + (oz * dz));
  const double c = ((((ox * ox) + (oy * oy)) + (oz * oz)) - (flight::row_get<flight::RowKey<"radius">>(sphere) * flight::row_get<flight::RowKey<"radius">>(sphere)));
  const double disc = ((b * b) - (a * c));
  if ((disc < 0.0)) {
    return -1.0;
  }
  const double sqrt_disc = std::sqrt(disc);
  const double t = ((-b - sqrt_disc) / a);
  if ((t >= 0.0)) {
    return t;
  }
  const double t2 = ((-b + sqrt_disc) / a);
  return ((t2 >= 0.0) ? 0.0 : -1.0);
}

inline double intersect_ray3_dtriangle(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Vector3Like>>> a, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Vector3Like>>> b, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Vector3Like>>> c) {
  const double e1x = (flight::row_get<flight::RowKey<"x">>(b) - flight::row_get<flight::RowKey<"x">>(a));
  const double e1y = (flight::row_get<flight::RowKey<"y">>(b) - flight::row_get<flight::RowKey<"y">>(a));
  const double e1z = (flight::row_get<flight::RowKey<"z">>(b) - flight::row_get<flight::RowKey<"z">>(a));
  const double e2x = (flight::row_get<flight::RowKey<"x">>(c) - flight::row_get<flight::RowKey<"x">>(a));
  const double e2y = (flight::row_get<flight::RowKey<"y">>(c) - flight::row_get<flight::RowKey<"y">>(a));
  const double e2z = (flight::row_get<flight::RowKey<"z">>(c) - flight::row_get<flight::RowKey<"z">>(a));
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  const double nx = ((e1y * e2z) - (e1z * e2y));
  const double ny = ((e1z * e2x) - (e1x * e2z));
  const double nz = ((e1x * e2y) - (e1y * e2x));
  const double normal_length_sq = (((nx * nx) + (ny * ny)) + (nz * nz));
  const double direction_length_sq = (((dx * dx) + (dy * dy)) + (dz * dz));
  if (((normal_length_sq == 0.0) || (direction_length_sq == 0.0))) {
    return -1.0;
  }
  const double hx = ((dy * e2z) - (dz * e2y));
  const double hy = ((dz * e2x) - (dx * e2z));
  const double hz = ((dx * e2y) - (dy * e2x));
  const double det = (((e1x * hx) + (e1y * hy)) + (e1z * hz));
  if ((std::abs(det) <= ((1e-10 * std::sqrt(normal_length_sq)) * std::sqrt(direction_length_sq)))) {
    return -1.0;
  }
  const double inv_det = (1.0 / det);
  const double sx = (flight::row_get<flight::RowKey<"origin">>(ray)->x - flight::row_get<flight::RowKey<"x">>(a));
  const double sy = (flight::row_get<flight::RowKey<"origin">>(ray)->y - flight::row_get<flight::RowKey<"y">>(a));
  const double sz = (flight::row_get<flight::RowKey<"origin">>(ray)->z - flight::row_get<flight::RowKey<"z">>(a));
  const double u = ((((sx * hx) + (sy * hy)) + (sz * hz)) * inv_det);
  if (((u < 0.0) || (u > 1.0))) {
    return -1.0;
  }
  const double qx = ((sy * e1z) - (sz * e1y));
  const double qy = ((sz * e1x) - (sx * e1z));
  const double qz = ((sx * e1y) - (sy * e1x));
  const double v = ((((dx * qx) + (dy * qy)) + (dz * qz)) * inv_det);
  if (((v < 0.0) || ((u + v) > 1.0))) {
    return -1.0;
  }
  const double t = ((((e2x * qx) + (e2y * qy)) + (e2z * qz)) * inv_det);
  return ((t >= 0.0) ? t : -1.0);
}

inline void set_ray3_d(flight::types::Ray3DLike out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Vector3Like>>> origin, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Vector3Like>>> direction) {
  const double ox = flight::row_get<flight::RowKey<"x">>(origin);
  const double oy = flight::row_get<flight::RowKey<"y">>(origin);
  const double oz = flight::row_get<flight::RowKey<"z">>(origin);
  const double dx = flight::row_get<flight::RowKey<"x">>(direction);
  const double dy = flight::row_get<flight::RowKey<"y">>(direction);
  const double dz = flight::row_get<flight::RowKey<"z">>(direction);
  (out->origin->x = ox);
  (out->origin->y = oy);
  (out->origin->z = oz);
  (out->direction->x = dx);
  (out->direction->y = dy);
  (out->direction->z = dz);
}

inline void transform_ray3_dby_matrix4(flight::types::Ray3DLike out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Ray3DLike>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::types::Matrix4Like>>> m) {
  flight::Float32Array mm = flight::row_get<flight::RowKey<"m">>(m);
  const double ox = flight::row_get<flight::RowKey<"origin">>(ray)->x;
  const double oy = flight::row_get<flight::RowKey<"origin">>(ray)->y;
  const double oz = flight::row_get<flight::RowKey<"origin">>(ray)->z;
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  (out->origin->x = ((((mm.element(0.0) * ox) + (mm.element(4.0) * oy)) + (mm.element(8.0) * oz)) + mm.element(12.0)));
  (out->origin->y = ((((mm.element(1.0) * ox) + (mm.element(5.0) * oy)) + (mm.element(9.0) * oz)) + mm.element(13.0)));
  (out->origin->z = ((((mm.element(2.0) * ox) + (mm.element(6.0) * oy)) + (mm.element(10.0) * oz)) + mm.element(14.0)));
  const double ndx = (((mm.element(0.0) * dx) + (mm.element(4.0) * dy)) + (mm.element(8.0) * dz));
  const double ndy = (((mm.element(1.0) * dx) + (mm.element(5.0) * dy)) + (mm.element(9.0) * dz));
  const double ndz = (((mm.element(2.0) * dx) + (mm.element(6.0) * dy)) + (mm.element(10.0) * dz));
  const double len = std::sqrt((((ndx * ndx) + (ndy * ndy)) + (ndz * ndz)));
  if ((len > 0.0)) {
    const double inv = (1.0 / len);
    (out->direction->x = (ndx * inv));
    (out->direction->y = (ndy * inv));
    (out->direction->z = (ndz * inv));
  }
  else {
    (out->direction->x = 0.0);
    (out->direction->y = 0.0);
    (out->direction->z = 0.0);
  }
}

} // namespace flight::geometry
