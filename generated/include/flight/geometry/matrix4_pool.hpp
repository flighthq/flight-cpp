// Generated by @flighthq/tool-compiler from @flighthq/geometry/packages/geometry/src/matrix4Pool.ts#Matrix4Pool at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/entity.hpp>
#include <flight/types/matrix4.hpp>
#include <flight/geometry/geometry_pool_guards.hpp>
#include <flight/geometry/matrix4.hpp>

namespace flight::geometry {

inline flight::Array<flight::Ref<flight::types::Matrix4>> pool = flight::Array<flight::Ref<flight::types::Matrix4>>{};

inline flight::Ref<flight::types::Matrix4> acquire_matrix4() {
  flight::Ref<flight::types::Matrix4> m;
  if ((static_cast<double>(pool.size()) > 0.0)) {
    m = static_cast<flight::Ref<flight::types::Matrix4>>(pool.pop());
  }
  else {
    m = flight::geometry::create_matrix4();
  }
  return m;
}

inline flight::Ref<flight::types::Matrix4> acquire_identity_matrix4() {
  flight::Ref<flight::types::Matrix4> m = acquire_matrix4();
  flight::geometry::set_matrix4_identity(m);
  return m;
}

inline void clear_matrix4_pool() {
  ([&]() { auto&& assignment_receiver = pool; const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
}

inline void release_matrix4(flight::Ref<flight::types::Matrix4> m) {
  if (!m) {
    return;
  }
  if (((flight::geometry::geometry_pool_release_guard != nullptr) && pool.includes(m))) {
    flight::geometry::geometry_pool_release_guard(flight::String("releaseMatrix4"));
  }
  m->entity_runtime_key = std::nullopt;
  pool.push(m);
}

} // namespace flight::geometry
