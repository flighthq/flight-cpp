// Generated by @flighthq/tool-compiler from @flighthq/geometry/matrix4Pool.ts#Matrix4Pool. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "geometry_pool_guards.hpp"
#include "matrix4.hpp"

namespace flighthq_geometry {

inline Matrix4 acquire_identity_matrix4() {
  Matrix4 m = acquire_matrix4();
  set_matrix4_identity(m);
  return m;
}

inline Matrix4 acquire_matrix4() {
  Matrix4 m;
  if ((pool.length > 0.0)) {
    m = static_cast<Matrix4>(pool.pop());
  }
  else {
    m = create_matrix4();
  }
  return m;
}

inline void clear_matrix4_pool() {
  pool.length = 0.0;
}

inline void release_matrix4(Matrix4 m) {
  if (!m) {
    return;
  }
  if (((geometry_pool_release_guard != nullptr) && pool.includes(m))) {
    geometry_pool_release_guard(flight::String("releaseMatrix4"));
  }
  m[static_cast<size_t>(entity_runtime_key)] = undefined;
  pool.push(m);
}

inline flight::Array<Matrix4> pool = flight::Array<Matrix4>{};

} // namespace flighthq_geometry
