// Generated by @flighthq/tool-compiler from @flighthq/geometry/transform2d.ts#Transform2d. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_geometry {

inline Transform2D create_transform2_d(std::optional<double> x = std::nullopt, std::optional<double> y = std::nullopt, std::optional<double> rotation = std::nullopt, std::optional<double> scale_x = std::nullopt, std::optional<double> scale_y = std::nullopt, std::optional<double> skew_x = std::nullopt, std::optional<double> skew_y = std::nullopt, std::optional<double> pivot_x = std::nullopt, std::optional<double> pivot_y = std::nullopt) {
  auto out = allocate_entity<Transform2D>();
  initialize_transform2_d(out, x.value_or(0.0), y.value_or(0.0), rotation.value_or(0.0), scale_x.value_or(1.0), scale_y.value_or(1.0), skew_x.value_or(0.0), skew_y.value_or(0.0), pivot_x.value_or(0.0), pivot_y.value_or(0.0));
  return finish_entity(out);
}

inline void decompose_matrix_to_transform2_d(Transform2DLike out, MatrixLike source) {
  auto a = source.a;
  auto b = source.b;
  auto c = source.c;
  auto d = source.d;
  const double scale_x = std::sqrt(((a * a) + (b * b)));
  const bool reflected = (((a * d) - (b * c)) < 0.0);
  const double scale_y = (reflected ? -std::sqrt(((c * c) + (d * d))) : std::sqrt(((c * c) + (d * d))));
  const double skew_xdegrees = ((reflected ? cmath.atan2(c, -d) : cmath.atan2(-c, d)) * rad_to_deg);
  const double skew_ydegrees = (cmath.atan2(b, a) * rad_to_deg);
  if ((skew_xdegrees == skew_ydegrees)) {
    out.rotation = skew_ydegrees;
    out.skew_x = 0.0;
    out.skew_y = 0.0;
  }
  else {
    out.rotation = 0.0;
    out.skew_x = skew_xdegrees;
    out.skew_y = skew_ydegrees;
  }
  out.pivot_x = 0.0;
  out.pivot_y = 0.0;
  out.scale_x = scale_x;
  out.scale_y = scale_y;
  out.x = source.tx;
  out.y = source.ty;
}

inline void initialize_transform2_d(EntityConstruction<Transform2D> out, double x, double y, double rotation, double scale_x, double scale_y, double skew_x, double skew_y, double pivot_x, double pivot_y) {
  out.x = x;
  out.y = y;
  out.rotation = rotation;
  out.scale_x = scale_x;
  out.scale_y = scale_y;
  out.skew_x = skew_x;
  out.skew_y = skew_y;
  out.pivot_x = pivot_x;
  out.pivot_y = pivot_y;
}

} // namespace flighthq_geometry
