// Generated by @flighthq/tool-compiler from @flighthq/assets/explainAssetLoad.ts#ExplainAssetLoad. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_assets {

inline AssetLoadExplanation explain_asset_load(AssetLibrary library, flight::String id) {
  auto runtime = library.runtime;
  auto descriptor = runtime.descriptors.get(id);
  if (!descriptor.has_value()) {
    return {.id = id, .ref_count = 0.0, .status = flight::String("missing-descriptor"), .type = nullptr};
  }
  auto type = descriptor.type;
  if (!runtime.adapters.has(type)) {
    return {.id = id, .ref_count = 0.0, .status = flight::String("missing-loader"), .type = type};
  }
  auto entry = runtime.entries.get(id);
  if (entry.has_value()) {
    return {.id = id, .ref_count = entry.refcount, .status = (entry.resident ? flight::String("resident") : flight::String("loading")), .type = type};
  }
  return {.id = id, .ref_count = 0.0, .status = (runtime.freed_ids.has(id) ? flight::String("freed") : flight::String("never-acquired")), .type = type};
}

} // namespace flighthq_assets
