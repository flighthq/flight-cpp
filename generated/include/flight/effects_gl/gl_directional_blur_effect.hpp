// Generated by @flighthq/tool-compiler from @flighthq/effects-gl/glDirectionalBlurEffect.ts#GlDirectionalBlurEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_effect_program_cache.hpp"
#include "gl_render_effect_registry.hpp"

namespace flighthq_effects_gl {

inline void apply_directional_blur_effect_to_gl(GlRenderState state, GlRenderTarget source, GlRenderTarget dest, DirectionalBlurEffect effect) {
  const double angle = ((effect.angle.value_or(0.0) * flight::pi) / 180.0);
  auto length = effect.length.value_or(8.0);
  auto samples = effect.samples.value_or(16.0);
  auto program = get_gl_effect_program(state, flight::String("directionalBlur"), directional_blur_fragment_src);
  draw_gl_fullscreen_pass(state, program, flight::Array{source.texture}, dest, [=](auto gl, auto p) {
  gl.uniform1f(gl.get_uniform_location(p.program, flight::String("u_angle")), angle);
  gl.uniform1f(gl.get_uniform_location(p.program, flight::String("u_length")), length);
  gl.uniform1f(gl.get_uniform_location(p.program, flight::String("u_samples")), samples);
  gl.uniform2f(gl.get_uniform_location(p.program, flight::String("u_resolution")), source.width, source.height);
  throw std::logic_error("Flight function completed without a value");
});
}

inline GlRenderEffectRunner default_gl_directional_blur_effect_runner = [=](auto ctx, auto effect) {
  apply_directional_blur_effect_to_gl(ctx.state, ctx.source, ctx.dest, static_cast<DirectionalBlurEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_gl_directional_blur_effect(GlRenderState state) {
  register_gl_render_effect(state, flight::String("DirectionalBlurEffect"), default_gl_directional_blur_effect_runner);
}

inline const flight::String directional_blur_fragment_src = flight::String("#version 300 es\nprecision highp float;\nin vec2 v_texCoord;\nuniform sampler2D u_texture0;\nuniform float u_angle;\nuniform float u_length;\nuniform float u_samples;\nuniform vec2 u_resolution;\nout vec4 o_color;\nconst int SAMPLES = 16;\nvoid main() {\n  // Negate sin to convert screen-space-Y-down angle to GL UV-Y-up direction.\n  vec2 dir = vec2(cos(u_angle), -sin(u_angle)) * (u_length / u_resolution);\n  float count = min(u_samples, 16.0);\n  vec4 sum = vec4(0.0);\n  float taken = 0.0;\n  for (int i = 0; i < SAMPLES; i++) {\n    if (float(i) >= count) break;\n    float t = count > 1.0 ? (float(i) / (count - 1.0)) - 0.5 : 0.0;\n    vec2 uv = v_texCoord + dir * t;\n    sum += texture(u_texture0, uv);\n    taken += 1.0;\n  }\n  o_color = sum / max(taken, 1.0);\n}");

} // namespace flighthq_effects_gl
