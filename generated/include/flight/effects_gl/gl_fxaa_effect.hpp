// Generated by @flighthq/tool-compiler from @flighthq/effects-gl/glFxaaEffect.ts#GlFxaaEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_effect_program_cache.hpp"
#include "gl_render_effect_registry.hpp"

namespace flighthq_effects_gl {

inline void apply_fxaa_effect_to_gl(GlRenderState state, GlRenderTarget source, GlRenderTarget dest, FxaaEffect effect) {
  auto edge_threshold = effect.edge_threshold.value_or(0.0312);
  auto program = get_gl_effect_program(state, flight::String("antialiasing.fxaa"), fxaa_fragment_src);
  draw_gl_fullscreen_pass(state, program, flight::Array{source.texture}, dest, [=](auto gl, auto p) {
  gl.uniform2f(gl.get_uniform_location(p.program, flight::String("u_resolution")), source.width, source.height);
  gl.uniform1f(gl.get_uniform_location(p.program, flight::String("u_edgeThreshold")), edge_threshold);
  throw std::logic_error("Flight function completed without a value");
});
}

inline GlRenderEffectRunner default_gl_fxaa_effect_runner = [=](auto ctx, auto effect) {
  apply_fxaa_effect_to_gl(ctx.state, ctx.source, ctx.dest, static_cast<FxaaEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_gl_fxaa_effect(GlRenderState state) {
  register_gl_render_effect(state, flight::String("FxaaEffect"), default_gl_fxaa_effect_runner);
}

inline const flight::String fxaa_fragment_src = flight::String("#version 300 es\nprecision highp float;\nin vec2 v_texCoord;\nuniform sampler2D u_texture0;\nuniform vec2 u_resolution;\nuniform float u_edgeThreshold;\nout vec4 o_color;\nfloat luma(vec3 c) {\n  return dot(c, vec3(0.299, 0.587, 0.114));\n}\nvoid main() {\n  vec2 texel = 1.0 / u_resolution;\n  vec3 rgbM = texture(u_texture0, v_texCoord).rgb;\n  vec3 rgbNW = texture(u_texture0, v_texCoord + vec2(-1.0, -1.0) * texel).rgb;\n  vec3 rgbNE = texture(u_texture0, v_texCoord + vec2(1.0, -1.0) * texel).rgb;\n  vec3 rgbSW = texture(u_texture0, v_texCoord + vec2(-1.0, 1.0) * texel).rgb;\n  vec3 rgbSE = texture(u_texture0, v_texCoord + vec2(1.0, 1.0) * texel).rgb;\n  float lumaM = luma(rgbM);\n  float lumaNW = luma(rgbNW);\n  float lumaNE = luma(rgbNE);\n  float lumaSW = luma(rgbSW);\n  float lumaSE = luma(rgbSE);\n  float lumaMin = min(lumaM, min(min(lumaNW, lumaNE), min(lumaSW, lumaSE)));\n  float lumaMax = max(lumaM, max(max(lumaNW, lumaNE), max(lumaSW, lumaSE)));\n  float range = lumaMax - lumaMin;\n  if (range < max(u_edgeThreshold, lumaMax * 0.125)) {\n    o_color = vec4(rgbM, texture(u_texture0, v_texCoord).a);\n    return;\n  }\n  vec2 dir;\n  dir.x = -((lumaNW + lumaNE) - (lumaSW + lumaSE));\n  dir.y = ((lumaNW + lumaSW) - (lumaNE + lumaSE));\n  float dirReduce = max((lumaNW + lumaNE + lumaSW + lumaSE) * 0.03125, 0.0078125);\n  float rcpDirMin = 1.0 / (min(abs(dir.x), abs(dir.y)) + dirReduce);\n  dir = clamp(dir * rcpDirMin, vec2(-8.0), vec2(8.0)) * texel;\n  vec3 rgbA = 0.5 * (\n    texture(u_texture0, v_texCoord + dir * (1.0 / 3.0 - 0.5)).rgb +\n    texture(u_texture0, v_texCoord + dir * (2.0 / 3.0 - 0.5)).rgb);\n  vec3 rgbB = rgbA * 0.5 + 0.25 * (\n    texture(u_texture0, v_texCoord + dir * -0.5).rgb +\n    texture(u_texture0, v_texCoord + dir * 0.5).rgb);\n  float lumaB = luma(rgbB);\n  vec3 result = (lumaB < lumaMin || lumaB > lumaMax) ? rgbA : rgbB;\n  o_color = vec4(result, texture(u_texture0, v_texCoord).a);\n}");

} // namespace flighthq_effects_gl
