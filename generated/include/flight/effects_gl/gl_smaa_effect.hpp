// Generated by @flighthq/tool-compiler from @flighthq/effects-gl/glSmaaEffect.ts#GlSmaaEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_effect_program_cache.hpp"
#include "gl_render_effect_registry.hpp"

namespace flighthq_effects_gl {

inline void apply_smaa_effect_to_gl(GlRenderState state, GlRenderTarget source, GlRenderTarget dest, SmaaEffect effect) {
  auto threshold = effect.threshold.value_or(0.1);
  auto program = get_gl_effect_program(state, flight::String("antialiasing.smaa"), smaa_fragment_src);
  draw_gl_fullscreen_pass(state, program, flight::Array{source.texture}, dest, [=](auto gl, auto p) {
  gl.uniform2f(gl.get_uniform_location(p.program, flight::String("u_resolution")), source.width, source.height);
  gl.uniform1f(gl.get_uniform_location(p.program, flight::String("u_threshold")), threshold);
  throw std::logic_error("Flight function completed without a value");
});
}

inline GlRenderEffectRunner default_gl_smaa_effect_runner = [=](auto ctx, auto effect) {
  apply_smaa_effect_to_gl(ctx.state, ctx.source, ctx.dest, static_cast<SmaaEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_gl_smaa_effect(GlRenderState state) {
  register_gl_render_effect(state, flight::String("SmaaEffect"), default_gl_smaa_effect_runner);
}

inline const flight::String smaa_fragment_src = flight::String("#version 300 es\nprecision highp float;\nin vec2 v_texCoord;\nuniform sampler2D u_texture0;\nuniform vec2 u_resolution;\nuniform float u_threshold;\nout vec4 o_color;\nfloat luma(vec3 c) {\n  return dot(c, vec3(0.299, 0.587, 0.114));\n}\nvoid main() {\n  vec2 texel = 1.0 / u_resolution;\n  vec4 center = texture(u_texture0, v_texCoord);\n  float lumaC = luma(center.rgb);\n  float lumaL = luma(texture(u_texture0, v_texCoord + vec2(-1.0, 0.0) * texel).rgb);\n  float lumaR = luma(texture(u_texture0, v_texCoord + vec2(1.0, 0.0) * texel).rgb);\n  float lumaT = luma(texture(u_texture0, v_texCoord + vec2(0.0, -1.0) * texel).rgb);\n  float lumaB = luma(texture(u_texture0, v_texCoord + vec2(0.0, 1.0) * texel).rgb);\n  float edge = max(abs(lumaC - lumaL), max(abs(lumaC - lumaR), max(abs(lumaC - lumaT), abs(lumaC - lumaB))));\n  if (edge < u_threshold) {\n    o_color = center;\n    return;\n  }\n  vec3 blurred = (\n    texture(u_texture0, v_texCoord + vec2(-1.0, 0.0) * texel).rgb +\n    texture(u_texture0, v_texCoord + vec2(1.0, 0.0) * texel).rgb +\n    texture(u_texture0, v_texCoord + vec2(0.0, -1.0) * texel).rgb +\n    texture(u_texture0, v_texCoord + vec2(0.0, 1.0) * texel).rgb +\n    center.rgb) / 5.0;\n  o_color = vec4(blurred, center.a);\n}");

} // namespace flighthq_effects_gl
