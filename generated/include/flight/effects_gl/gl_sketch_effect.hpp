// Generated by @flighthq/tool-compiler from @flighthq/effects-gl/glSketchEffect.ts#GlSketchEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_effect_program_cache.hpp"
#include "gl_render_effect_registry.hpp"

namespace flighthq_effects_gl {

inline void apply_sketch_effect_to_gl(GlRenderState state, GlRenderTarget source, GlRenderTarget dest, SketchEffect effect) {
  auto strength = effect.strength.value_or(1.0);
  auto program = get_gl_effect_program(state, flight::String("stylization.sketch"), sketch_fragment_src);
  draw_gl_fullscreen_pass(state, program, flight::Array{source.texture}, dest, [=](auto gl, auto p) {
  gl.uniform1f(gl.get_uniform_location(p.program, flight::String("u_strength")), strength);
  gl.uniform2f(gl.get_uniform_location(p.program, flight::String("u_resolution")), source.width, source.height);
  throw std::logic_error("Flight function completed without a value");
});
}

inline GlRenderEffectRunner default_gl_sketch_effect_runner = [=](auto ctx, auto effect) {
  apply_sketch_effect_to_gl(ctx.state, ctx.source, ctx.dest, static_cast<SketchEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_gl_sketch_effect(GlRenderState state) {
  register_gl_render_effect(state, flight::String("SketchEffect"), default_gl_sketch_effect_runner);
}

inline const flight::String sketch_fragment_src = flight::String("#version 300 es\nprecision highp float;\nin vec2 v_texCoord;\nuniform sampler2D u_texture0;\nuniform float u_strength;\nuniform vec2 u_resolution;\nout vec4 o_color;\nfloat lum(vec2 uv) {\n  return dot(texture(u_texture0, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n}\nvoid main() {\n  vec2 texel = 1.0 / u_resolution;\n  float tl = lum(v_texCoord + texel * vec2(-1.0, -1.0));\n  float t = lum(v_texCoord + texel * vec2(0.0, -1.0));\n  float tr = lum(v_texCoord + texel * vec2(1.0, -1.0));\n  float l = lum(v_texCoord + texel * vec2(-1.0, 0.0));\n  float rr = lum(v_texCoord + texel * vec2(1.0, 0.0));\n  float bl = lum(v_texCoord + texel * vec2(-1.0, 1.0));\n  float b = lum(v_texCoord + texel * vec2(0.0, 1.0));\n  float br = lum(v_texCoord + texel * vec2(1.0, 1.0));\n  float gx = -tl - 2.0 * l - bl + tr + 2.0 * rr + br;\n  float gy = -tl - 2.0 * t - tr + bl + 2.0 * b + br;\n  float edge = sqrt(gx * gx + gy * gy);\n  float pencil = clamp(1.0 - edge * u_strength, 0.0, 1.0);\n  float a = texture(u_texture0, v_texCoord).a;\n  o_color = vec4(vec3(pencil), a);\n}");

} // namespace flighthq_effects_gl
