// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuSsaoEffect.ts#WgpuSsaoEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_effect_texel_scale.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_ssao_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, SsaoEffect effect) {
  auto radius = effect.radius.value_or(1.0);
  auto intensity = effect.intensity.value_or(1.0);
  auto resolution = get_wgpu_effect_logical_resolution(state, source);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("atmospheric.ssao"), ssao_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = radius;
  f32[static_cast<size_t>(1.0)] = intensity;
  f32[static_cast<size_t>(2.0)] = resolution.width;
  f32[static_cast<size_t>(3.0)] = resolution.height;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_ssao_effect_runner = [=](auto ctx, auto effect) {
  apply_ssao_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<SsaoEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_ssao_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("SsaoEffect"), default_wgpu_ssao_effect_runner);
}

inline const flight::String ssao_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_radius : f32,\n  u_intensity : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn luma(c : vec3f) -> f32 {\n  return dot(c, vec3f(0.299, 0.587, 0.114));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = (1.0 / uni.u_resolution) * max(uni.u_radius, 1.0);\n  let center = textureSampleLevel(tex, smp, uv, 0.0);\n  let lc = luma(center.rgb);\n  var variation = 0.0;\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(-1.0, 0.0) * texel, 0.0).rgb));\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(1.0, 0.0) * texel, 0.0).rgb));\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(0.0, -1.0) * texel, 0.0).rgb));\n  variation = variation + abs(lc - luma(textureSampleLevel(tex, smp, uv + vec2f(0.0, 1.0) * texel, 0.0).rgb));\n  variation = variation * 0.25;\n  let occlusion = clamp(variation * uni.u_intensity, 0.0, 1.0);\n  return vec4f(center.rgb * (1.0 - occlusion), center.a);\n}");

} // namespace flighthq_effects_wgpu
