// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuFilmGrainEffect.ts#WgpuFilmGrainEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_film_grain_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, FilmGrainEffect effect) {
  auto intensity = effect.intensity.value_or(0.1);
  auto size = effect.size.value_or(1.0);
  auto seed = effect.seed.value_or(0.0);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("stylization.filmGrain"), film_grain_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = intensity;
  f32[static_cast<size_t>(1.0)] = std::max(0.0001, size);
  f32[static_cast<size_t>(2.0)] = seed;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_film_grain_effect_runner = [=](auto ctx, auto effect) {
  apply_film_grain_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<FilmGrainEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_film_grain_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("FilmGrainEffect"), default_wgpu_film_grain_effect_runner);
}

inline const flight::String film_grain_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_intensity : f32,\n  u_size : f32,\n  u_seed : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn hash(pIn : vec2f) -> f32 {\n  let p = floor(pIn / uni.u_size);\n  return fract(sin(dot(p, vec2f(127.1, 311.7)) + uni.u_seed) * 43758.5453123);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let n = hash(uv * 1024.0) - 0.5;\n  return vec4f(c.rgb + n * uni.u_intensity, c.a);\n}");

} // namespace flighthq_effects_wgpu
