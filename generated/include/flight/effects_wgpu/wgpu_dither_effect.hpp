// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuDitherEffect.ts#WgpuDitherEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_effect_texel_scale.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_dither_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, DitherEffect effect) {
  auto levels = effect.levels.value_or(4.0);
  auto resolution = get_wgpu_effect_logical_resolution(state, source);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("stylization.dither"), dither_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = std::max(2.0, levels);
  f32[static_cast<size_t>(2.0)] = resolution.width;
  f32[static_cast<size_t>(3.0)] = resolution.height;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_dither_effect_runner = [=](auto ctx, auto effect) {
  apply_dither_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<DitherEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_dither_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("DitherEffect"), default_wgpu_dither_effect_runner);
}

inline const flight::String dither_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_levels : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn bayer(p : vec2i) -> f32 {\n  let x = p.x & 3;\n  let y = p.y & 3;\n  var m = array<i32, 16>(0, 8, 2, 10, 12, 4, 14, 6, 3, 11, 1, 9, 15, 7, 13, 5);\n  return f32(m[y * 4 + x]) / 16.0;\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let px = vec2i(uv * uni.u_resolution);\n  let t = bayer(px) - 0.5;\n  let steps = uni.u_levels - 1.0;\n  let q = floor(c.rgb * steps + 0.5 + t) / steps;\n  return vec4f(clamp(q, vec3f(0.0), vec3f(1.0)), c.a);\n}");

} // namespace flighthq_effects_wgpu
