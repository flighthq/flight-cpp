// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuChromaticAberrationEffect.ts#WgpuChromaticAberrationEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_chromatic_aberration_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, ChromaticAberrationEffect effect) {
  auto intensity = effect.intensity.value_or(0.005);
  auto radial = effect.radial.value_or(true);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("lens.chromaticAberration"), chromatic_aberration_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = intensity;
  f32[static_cast<size_t>(1.0)] = (radial ? 1.0 : 0.0);
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_chromatic_aberration_effect_runner = [=](auto ctx, auto effect) {
  apply_chromatic_aberration_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<ChromaticAberrationEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_chromatic_aberration_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("ChromaticAberrationEffect"), default_wgpu_chromatic_aberration_effect_runner);
}

inline const flight::String chromatic_aberration_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_intensity : f32,\n  u_radial : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let centered = uv - vec2f(0.5);\n  let scale = mix(1.0, length(centered) * 2.0, uni.u_radial);\n  let dir = mix(vec2f(1.0, 0.0), normalize(centered + vec2f(1e-5)), uni.u_radial);\n  let offset = dir * uni.u_intensity * scale;\n  let r = textureSampleLevel(tex, smp, uv + offset, 0.0).r;\n  let g = textureSampleLevel(tex, smp, uv, 0.0).g;\n  let b = textureSampleLevel(tex, smp, uv - offset, 0.0).b;\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  return vec4f(r, g, b, a);\n}");

} // namespace flighthq_effects_wgpu
