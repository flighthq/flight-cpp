// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuSharpenEffect.ts#WgpuSharpenEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_effect_texel_scale.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_sharpen_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, SharpenEffect effect) {
  auto amount = effect.amount.value_or(0.5);
  auto resolution = get_wgpu_effect_logical_resolution(state, source);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("stylization.sharpen"), sharpen_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = amount;
  f32[static_cast<size_t>(2.0)] = resolution.width;
  f32[static_cast<size_t>(3.0)] = resolution.height;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_sharpen_effect_runner = [=](auto ctx, auto effect) {
  apply_sharpen_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<SharpenEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_sharpen_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("SharpenEffect"), default_wgpu_sharpen_effect_runner);
}

inline const flight::String sharpen_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_amount : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let c = textureSampleLevel(tex, smp, uv, 0.0).rgb;\n  let n = textureSampleLevel(tex, smp, uv + vec2f(0.0, -texel.y), 0.0).rgb;\n  let s = textureSampleLevel(tex, smp, uv + vec2f(0.0, texel.y), 0.0).rgb;\n  let e = textureSampleLevel(tex, smp, uv + vec2f(texel.x, 0.0), 0.0).rgb;\n  let w = textureSampleLevel(tex, smp, uv + vec2f(-texel.x, 0.0), 0.0).rgb;\n  let high = c * 4.0 - n - s - e - w;\n  let a = textureSampleLevel(tex, smp, uv, 0.0).a;\n  return vec4f(clamp(c + high * uni.u_amount, vec3f(0.0), vec3f(1.0)), a);\n}");

} // namespace flighthq_effects_wgpu
