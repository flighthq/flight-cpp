// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuKuwaharaEffect.ts#WgpuKuwaharaEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_effect_texel_scale.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_kuwahara_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, KuwaharaEffect effect) {
  auto radius = effect.radius.value_or(3.0);
  auto resolution = get_wgpu_effect_logical_resolution(state, source);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("stylization.kuwahara"), kuwahara_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = std::max(1.0, radius);
  f32[static_cast<size_t>(2.0)] = resolution.width;
  f32[static_cast<size_t>(3.0)] = resolution.height;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_kuwahara_effect_runner = [=](auto ctx, auto effect) {
  apply_kuwahara_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<KuwaharaEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_kuwahara_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("KuwaharaEffect"), default_wgpu_kuwahara_effect_runner);
}

inline const flight::String kuwahara_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_radius : f32,\n  _pad0 : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nconst R : i32 = 4;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let r = i32(min(f32(R), uni.u_radius));\n  var means : array<vec3f, 4>;\n  var vars : array<f32, 4>;\n  for (var q = 0; q < 4; q++) {\n    let ox = select(-r, 0, (q & 1) != 0);\n    let oy = select(-r, 0, (q & 2) != 0);\n    var sum = vec3f(0.0);\n    var sumSq = vec3f(0.0);\n    var n = 0.0;\n    for (var y = 0; y <= R; y++) {\n      for (var x = 0; x <= R; x++) {\n        if (x > r || y > r) { continue; }\n        let off = vec2f(f32(ox + x), f32(oy + y)) * texel;\n        let col = textureSampleLevel(tex, smp, uv + off, 0.0).rgb;\n        sum += col;\n        sumSq += col * col;\n        n += 1.0;\n      }\n    }\n    let mean = sum / n;\n    means[q] = mean;\n    let v = sumSq / n - mean * mean;\n    vars[q] = v.r + v.g + v.b;\n  }\n  var minVar = vars[0];\n  var result = means[0];\n  var tiedCount = 1.0;\n  for (var q = 1; q < 4; q++) {\n    if (vars[q] < minVar) {\n      minVar = vars[q];\n      result = means[q];\n      tiedCount = 1.0;\n    } else if (vars[q] == minVar) {\n      result += means[q];\n      tiedCount += 1.0;\n    }\n  }\n  result /= tiedCount;\n  return vec4f(result, textureSampleLevel(tex, smp, uv, 0.0).a);\n}");

} // namespace flighthq_effects_wgpu
