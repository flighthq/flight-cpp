// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuConvolutionEffect.ts#WgpuConvolutionEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline const double max_convolution_effect_wgpu_kernel_size = 49.0;

inline void apply_convolution_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, ConvolutionEffect effect) {
  ConvolutionEffect object_pattern_value = effect;
  auto matrix = object_pattern_value.matrix;
  auto matrix_x = object_pattern_value.matrix_x;
  auto matrix_y = object_pattern_value.matrix_y;
  if (((matrix_x <= 0.0) || (matrix_y <= 0.0))) {
    throw flight::Error(flight::String("Convolution matrix dimensions must be positive"));
  }
  if (((matrix_x * matrix_y) > max_convolution_effect_wgpu_kernel_size)) {
    throw flight::Error(flight::String("Convolution kernel exceeds the WebGPU maximum of 7×7 (") + flight::to_string(matrix_x) + flight::String("×") + flight::to_string(matrix_y) + flight::String(" given)"));
  }
  if ((matrix.length < (matrix_x * matrix_y))) {
    throw flight::Error(flight::String("Convolution matrix does not match its declared dimensions"));
  }
  auto bias = effect.bias.value_or(0.0);
  auto clamp_edge = effect.clamp.value_or(true);
  auto preserve_alpha = effect.preserve_alpha.value_or(true);
  auto edge_color = effect.color.value_or(0.0);
  auto divisor = effect.divisor.value_or(get_auto_divisor(matrix, (matrix_x * matrix_y)));
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("stylization.convolution"), convolution_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32, auto i32) {
  f32[static_cast<size_t>(0.0)] = (1.0 / source.width);
  f32[static_cast<size_t>(1.0)] = (1.0 / source.height);
  i32[static_cast<size_t>(2.0)] = matrix_x;
  i32[static_cast<size_t>(3.0)] = matrix_y;
  f32[static_cast<size_t>(4.0)] = divisor;
  f32[static_cast<size_t>(5.0)] = bias;
  i32[static_cast<size_t>(6.0)] = (clamp_edge ? 1.0 : 0.0);
  i32[static_cast<size_t>(7.0)] = (preserve_alpha ? 1.0 : 0.0);
  unpack_color_rgba(scratch_edge_color, edge_color);
  f32[static_cast<size_t>(8.0)] = std::get<0>(scratch_edge_color);
  f32[static_cast<size_t>(9.0)] = std::get<1>(scratch_edge_color);
  f32[static_cast<size_t>(10.0)] = std::get<2>(scratch_edge_color);
  f32[static_cast<size_t>(11.0)] = std::get<3>(scratch_edge_color);
  {
    double i = 0.0;
    while ((i < (matrix_x * matrix_y))) {
      f32[static_cast<size_t>((12.0 + i))] = matrix[static_cast<size_t>(i)];
      i += 1.0;
    }
  }
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_convolution_effect_runner = [=](auto ctx, auto effect) {
  apply_convolution_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<ConvolutionEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_convolution_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("ConvolutionEffect"), default_wgpu_convolution_effect_runner);
}

inline double get_auto_divisor(flight::Array<double> matrix, double length) {
  double sum = 0.0;
  {
    double i = 0.0;
    while ((i < length)) {
      sum += matrix.element(i);
      i += 1.0;
    }
  }
  return ((sum == 0.0) ? 1.0 : sum);
}

inline const flight::String convolution_wgsl = flight::String("\nstruct Uniforms {\n  texelSize : vec2f,\n  matrixX : i32,\n  matrixY : i32,\n  divisor : f32,\n  bias : f32,\n  clampEdge : i32,\n  preserveAlpha : i32,\n  edgeColor : vec4f,\n  matrix : array<f32, ") + flight::to_string(max_convolution_effect_wgpu_kernel_size) + flight::String(">,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn sampleAt(uv : vec2f) -> vec4f {\n  if (uni.clampEdge != 0) {\n    return textureSampleLevel(tex, smp, clamp(uv, vec2f(0.0), vec2f(1.0)), 0.0);\n  }\n  if (uv.x < 0.0 || uv.x > 1.0 || uv.y < 0.0 || uv.y > 1.0) {\n    return uni.edgeColor;\n  }\n  return textureSampleLevel(tex, smp, uv, 0.0);\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let offsetX = uni.matrixX / 2;\n  let offsetY = uni.matrixY / 2;\n  var sum = vec4f(0.0);\n  for (var ky : i32 = 0; ky < uni.matrixY; ky++) {\n    for (var kx : i32 = 0; kx < uni.matrixX; kx++) {\n      let weight = uni.matrix[ky * uni.matrixX + kx];\n      let off = vec2f(f32(kx - offsetX), f32(ky - offsetY)) * uni.texelSize;\n      sum += sampleAt(uv + off) * weight;\n    }\n  }\n  sum = sum / uni.divisor;\n  sum += uni.bias / 255.0;\n  sum = clamp(sum, vec4f(0.0), vec4f(1.0));\n  if (uni.preserveAlpha != 0) {\n    let origAlpha = textureSampleLevel(tex, smp, uv, 0.0).a;\n    let straightRGB = select(vec3f(0.0), clamp(sum.rgb / sum.a, vec3f(0.0), vec3f(1.0)), sum.a > 0.0);\n    sum = vec4f(straightRGB * origAlpha, origAlpha);\n  }\n  return sum;\n}");

inline std::tuple<double, double, double, double> scratch_edge_color = std::make_tuple(0.0, 0.0, 0.0, 0.0);

} // namespace flighthq_effects_wgpu
