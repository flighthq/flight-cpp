// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuFxaaEffect.ts#WgpuFxaaEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_fxaa_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, FxaaEffect effect) {
  auto edge_threshold = effect.edge_threshold.value_or(0.0312);
  auto width = source.width;
  auto height = source.height;
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("antialiasing.fxaa"), fxaa_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = width;
  f32[static_cast<size_t>(1.0)] = height;
  f32[static_cast<size_t>(2.0)] = edge_threshold;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_fxaa_effect_runner = [=](auto ctx, auto effect) {
  apply_fxaa_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<FxaaEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_fxaa_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("FxaaEffect"), default_wgpu_fxaa_effect_runner);
}

inline const flight::String fxaa_fragment_wgsl = flight::String("\nstruct Uniforms { u_resolution : vec2f, u_edgeThreshold : f32, _pad0 : f32, }\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\nfn luma(c : vec3f) -> f32 {\n  return dot(c, vec3f(0.299, 0.587, 0.114));\n}\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let texel = 1.0 / uni.u_resolution;\n  let rgbM = textureSampleLevel(tex, smp, uv, 0.0).rgb;\n  let rgbNW = textureSampleLevel(tex, smp, uv + vec2f(-1.0, -1.0) * texel, 0.0).rgb;\n  let rgbNE = textureSampleLevel(tex, smp, uv + vec2f(1.0, -1.0) * texel, 0.0).rgb;\n  let rgbSW = textureSampleLevel(tex, smp, uv + vec2f(-1.0, 1.0) * texel, 0.0).rgb;\n  let rgbSE = textureSampleLevel(tex, smp, uv + vec2f(1.0, 1.0) * texel, 0.0).rgb;\n  let lumaM = luma(rgbM);\n  let lumaNW = luma(rgbNW);\n  let lumaNE = luma(rgbNE);\n  let lumaSW = luma(rgbSW);\n  let lumaSE = luma(rgbSE);\n  let lumaMin = min(lumaM, min(min(lumaNW, lumaNE), min(lumaSW, lumaSE)));\n  let lumaMax = max(lumaM, max(max(lumaNW, lumaNE), max(lumaSW, lumaSE)));\n  let range = lumaMax - lumaMin;\n  if (range < max(uni.u_edgeThreshold, lumaMax * 0.125)) {\n    return vec4f(rgbM, textureSampleLevel(tex, smp, uv, 0.0).a);\n  }\n  var dir : vec2f;\n  dir.x = -((lumaNW + lumaNE) - (lumaSW + lumaSE));\n  dir.y = ((lumaNW + lumaSW) - (lumaNE + lumaSE));\n  let dirReduce = max((lumaNW + lumaNE + lumaSW + lumaSE) * 0.03125, 0.0078125);\n  let rcpDirMin = 1.0 / (min(abs(dir.x), abs(dir.y)) + dirReduce);\n  dir = clamp(dir * rcpDirMin, vec2f(-8.0), vec2f(8.0)) * texel;\n  let rgbA = 0.5 * (\n    textureSampleLevel(tex, smp, uv + dir * (1.0 / 3.0 - 0.5), 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + dir * (2.0 / 3.0 - 0.5), 0.0).rgb);\n  let rgbB = rgbA * 0.5 + 0.25 * (\n    textureSampleLevel(tex, smp, uv + dir * -0.5, 0.0).rgb +\n    textureSampleLevel(tex, smp, uv + dir * 0.5, 0.0).rgb);\n  let lumaB = luma(rgbB);\n  let result = select(rgbB, rgbA, lumaB < lumaMin || lumaB > lumaMax);\n  return vec4f(result, textureSampleLevel(tex, smp, uv, 0.0).a);\n}");

} // namespace flighthq_effects_wgpu
