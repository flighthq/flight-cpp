// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuLensDistortionEffect.ts#WgpuLensDistortionEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_lens_distortion_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, LensDistortionEffect effect) {
  auto amount = effect.amount.value_or(0.2);
  auto scale = effect.scale.value_or(1.0);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("lens.lensDistortion"), lens_distortion_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = amount;
  f32[static_cast<size_t>(1.0)] = scale;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_lens_distortion_effect_runner = [=](auto ctx, auto effect) {
  apply_lens_distortion_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<LensDistortionEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_lens_distortion_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("LensDistortionEffect"), default_wgpu_lens_distortion_effect_runner);
}

inline const flight::String lens_distortion_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_amount : f32,\n  u_scale : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let centered = (uv - vec2f(0.5)) / uni.u_scale;\n  let r2 = dot(centered, centered);\n  let distorted = centered * (1.0 + uni.u_amount * r2) + vec2f(0.5);\n  if (distorted.x < 0.0 || distorted.x > 1.0 || distorted.y < 0.0 || distorted.y > 1.0) {\n    return vec4f(0.0, 0.0, 0.0, 1.0);\n  }\n  return textureSampleLevel(tex, smp, distorted, 0.0);\n}");

} // namespace flighthq_effects_wgpu
