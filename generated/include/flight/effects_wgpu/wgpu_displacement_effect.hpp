// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuDisplacementEffect.ts#WgpuDisplacementEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_effect_texel_scale.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_displacement_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, DisplacementEffect effect) {
  auto intensity = effect.intensity.value_or(8.0);
  auto frequency = effect.frequency.value_or(12.0);
  auto seed = effect.seed.value_or(0.0);
  auto resolution = get_wgpu_effect_logical_resolution(state, source);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("lens.displacement"), displacement_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = intensity;
  f32[static_cast<size_t>(1.0)] = frequency;
  f32[static_cast<size_t>(2.0)] = seed;
  f32[static_cast<size_t>(4.0)] = resolution.width;
  f32[static_cast<size_t>(5.0)] = resolution.height;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_displacement_effect_runner = [=](auto ctx, auto effect) {
  apply_displacement_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<DisplacementEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_displacement_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("DisplacementEffect"), default_wgpu_displacement_effect_runner);
}

inline const flight::String displacement_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_intensity : f32,\n  u_frequency : f32,\n  u_seed : f32,\n  u_resolution : vec2f,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let f = uni.u_frequency;\n  let warp = vec2f(\n    sin(uv.y * f + uni.u_seed) + sin(uv.y * f * 2.3 + uni.u_seed * 1.7) * 0.5,\n    cos(uv.x * f * 0.8 + uni.u_seed * 1.3)\n  );\n  let displaced = uv + warp * (uni.u_intensity / uni.u_resolution);\n  return textureSampleLevel(tex, smp, displaced, 0.0);\n}");

} // namespace flighthq_effects_wgpu
