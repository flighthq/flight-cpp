// Generated by @flighthq/tool-compiler from @flighthq/effects-wgpu/wgpuScanlinesEffect.ts#WgpuScanlinesEffect. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_effect_pass.hpp"
#include "wgpu_effect_program_cache.hpp"
#include "wgpu_render_effect_registry.hpp"

namespace flighthq_effects_wgpu {

inline void apply_scanlines_effect_to_wgpu(WgpuRenderState state, WgpuRenderTarget source, WgpuRenderTarget dest, ScanlinesEffect effect) {
  auto count = effect.count.value_or(240.0);
  auto intensity = effect.intensity.value_or(0.3);
  auto pipeline = get_wgpu_effect_pipeline(state, flight::String("stylization.scanlines"), scanlines_fragment_wgsl, flight::String("replace"));
  draw_wgpu_effect_pass(state, static_cast<WgpuRenderTarget>(source), static_cast<WgpuRenderTarget>(dest), pipeline, [=](auto f32) {
  f32[static_cast<size_t>(0.0)] = count;
  f32[static_cast<size_t>(1.0)] = intensity;
  throw std::logic_error("Flight function completed without a value");
});
}

inline WgpuRenderEffectRunner default_wgpu_scanlines_effect_runner = [=](auto ctx, auto effect) {
  apply_scanlines_effect_to_wgpu(ctx.state, ctx.source, ctx.dest, static_cast<ScanlinesEffect>(effect));
  throw std::logic_error("Flight function completed without a value");
};

inline void register_wgpu_scanlines_effect(WgpuRenderState state) {
  register_wgpu_render_effect(state, flight::String("ScanlinesEffect"), default_wgpu_scanlines_effect_runner);
}

inline const flight::String scanlines_fragment_wgsl = flight::String("\nstruct Uniforms {\n  u_count : f32,\n  u_intensity : f32,\n}\n@group(0) @binding(0) var<uniform> uni : Uniforms;\n@group(1) @binding(0) var tex : texture_2d<f32>;\n@group(1) @binding(1) var smp : sampler;\n\n@fragment\nfn fs_main(@location(0) uv : vec2f) -> @location(0) vec4f {\n  let c = textureSampleLevel(tex, smp, uv, 0.0);\n  let line = sin(uv.y * uni.u_count * 3.14159265) * 0.5 + 0.5;\n  return vec4f(c.rgb * (1.0 - uni.u_intensity * (1.0 - line)), c.a);\n}");

} // namespace flighthq_effects_wgpu
