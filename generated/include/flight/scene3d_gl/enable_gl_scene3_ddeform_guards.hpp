// Generated by @flighthq/tool-compiler from @flighthq/scene3d-gl/enableGlScene3DDeformGuards.ts#EnableGlScene3DDeformGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_scene3_druntime.hpp"

namespace flighthq_scene3d_gl {

inline bool are_gl_scene3_ddeform_guards_enabled(GlRenderState state) {
  return (get_gl_scene3_druntime(state).deform_guard != nullptr);
}

inline void enable_gl_scene3_ddeform_guards(GlRenderState state) {
  get_gl_scene3_druntime(state).deform_guard = warn_gl_scene3_dmesh_drawn_undeformed;
}

inline void warn_gl_scene3_dmesh_drawn_undeformed(Mesh mesh) {
  if (((mesh.morph != nullptr) && (get_mesh_geometry_morph_bind_pose(mesh.geometry) == nullptr))) {
    log_once(flight::String("scene-gl:morph-drawn-without-prepare"), log_level.warn, {.message = flight::String("drawGlScene3D: a morphed mesh reached the draw un-blended (it will draw the bind pose) — call prepareScene3DMorph(scene) before prepareScene3DRender.")}, flight::String("scene-gl"));
  }
  if (((mesh.skin != nullptr) && has_mesh_geometry_skin(mesh.geometry))) {
    MeshRuntime runtime = static_cast<MeshRuntime>(get_node_runtime(static_cast<NodeAny>(mesh)));
    if ((runtime.deformed_local_bounds == nullptr)) {
      log_once(flight::String("scene-gl:skin-drawn-without-prepare"), log_level.warn, {.message = flight::String("drawGlScene3D: a GPU-skinned mesh reached the draw un-posed (its joint palette is uncomputed, collapsing it to the origin) — call prepareScene3DSkinning(scene) before prepareScene3DRender.")}, flight::String("scene-gl"));
    }
  }
}

} // namespace flighthq_scene3d_gl
