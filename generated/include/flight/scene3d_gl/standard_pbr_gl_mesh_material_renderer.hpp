// Generated by @flighthq/tool-compiler from @flighthq/scene3d-gl/standardPbrGlMeshMaterialRenderer.ts#StandardPbrGlMeshMaterialRenderer. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_lit_program.hpp"
#include "gl_mesh_program.hpp"
#include "gl_pbr_program_cache.hpp"
#include "gl_pbr_standard_block.hpp"
#include "gl_scene3_druntime.hpp"

namespace flighthq_scene3d_gl {

inline GlMeshMaterialRenderer standard_pbr_gl_mesh_material_renderer = {.bind = [=](GlRenderState state, std::optional<Material> material, Scene3DLightBlock lights, Camera3D camera) {
  auto gl = state.gl;
  std::optional<StandardPbrMaterial> pbr = static_cast<std::optional<StandardPbrMaterial>>(material.value());
  auto program = ensure_gl_pbr_program(state, build_gl_pbr_standard_define_key(state, pbr.value(), pbr.value()));
  begin_gl_mesh_draw(state, program, (pbr.value().has_value() && pbr.value().double_sided));
  set_gl_mesh_view_projection(state, program.loc_view_projection, camera);
  set_gl_mesh_camera_position(gl, program.loc_camera_position, camera);
  bind_gl_mesh_light_block(state, program, lights);
  bind_gl_pbr_standard_block(state, program, pbr.value());
  gl.uniform1f(program.loc_alpha_cutoff, (pbr.value().has_value() ? pbr.value().alpha_cutoff : 0.5));
}, .draw = [=](GlRenderState state, Scene3DRenderProxy proxy, MeshGeometry geometry) {
  auto program = get_gl_scene3_druntime(state).active_mesh_program;
  if (!program.has_value()) {
    return;
  }
  draw_gl_mesh_subset(state, program, proxy, geometry);
}};

} // namespace flighthq_scene3d_gl
