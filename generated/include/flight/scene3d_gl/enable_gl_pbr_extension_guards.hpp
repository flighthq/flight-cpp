// Generated by @flighthq/tool-compiler from @flighthq/scene3d-gl/enableGlPbrExtensionGuards.ts#EnableGlPbrExtensionGuards. Do not edit.
#pragma once
#include <functional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_pbr_extension_registry.hpp"
#include "gl_scene3_druntime.hpp"

namespace flighthq_scene3d_gl {

inline bool are_gl_pbr_extension_guards_enabled(GlRenderState state) {
  return (get_gl_scene3_druntime(state).pbr_extension_guard != nullptr);
}

inline void enable_gl_pbr_extension_guards(GlRenderState state) {
  get_gl_scene3_druntime(state).pbr_extension_guard = [=](auto extensions) {
  warn_gl_pbr_extension_issues(state, extensions);
};
}

inline flight::String get_gl_pbr_extension_issue_message(GlPbrExtensionIssue issue) {
  {
    auto switch_value = issue.code;
    if (switch_value == flight::String("duplicate-kind")) {
      return flight::String("extendedPbrGlMeshMaterialRenderer: extension kind '") + flight::to_string(issue.kind) + flight::String("' appears more than once — call createExtendedPbrMaterial with each extension kind at most once");
    }
    else if (switch_value == flight::String("framebuffer-feedback")) {
      return flight::String("extendedPbrGlMeshMaterialRenderer: transmission scene color aliases the active draw attachment — call setGlPbrTransmissionSceneColor(state, a distinct resolved scene-color target)");
    }
    else if (switch_value == flight::String("missing-registration")) {
      return flight::String("extendedPbrGlMeshMaterialRenderer: extension kind '") + flight::to_string(issue.kind) + flight::String("' has no GL registration — call registerGlPbrExtension(state, kind, registration)");
    }
    else if (switch_value == flight::String("texture-unit-exhaustion")) {
      return flight::String("extendedPbrGlMeshMaterialRenderer: extension maps exceed the available fragment texture units — call createExtendedPbrMaterial with fewer mapped extensions");
    }
    else if (switch_value == flight::String("unsupported-extension")) {
      return flight::String("extendedPbrGlMeshMaterialRenderer: extension kind '") + flight::to_string(issue.kind) + flight::String("' is unsupported by its GL registration — call registerGlPbrExtension(state, kind, a supported registration)");
    }
  }
  throw std::logic_error("Flight function completed without a value");
}

inline void warn_gl_pbr_extension_issues(GlRenderState state, flight::Array<PbrExtension> extensions) {
  auto issues = explain_gl_pbr_extensions(state, extensions);
  {
    double i = 0.0;
    while ((i < issues.length)) {
      {
        auto issue = issues[static_cast<size_t>(i)];
        log_once(flight::String("scene-gl:pbr-extension:") + flight::to_string(issue.code) + flight::String(":") + flight::to_string(issue.kind) + flight::String(""), log_level.warn, {.code = issue.code, .kind = issue.kind, .message = get_gl_pbr_extension_issue_message(issue)}, flight::String("scene-gl"));
      }
      i += 1.0;
    }
  }
}

} // namespace flighthq_scene3d_gl
