// Generated by @flighthq/tool-compiler from @flighthq/scene3d-gl/explainGlMeshGeometryUvSets.ts#ExplainGlMeshGeometryUvSets. Do not edit.
#pragma once
#include <functional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_scene3d_gl {

inline MeshGeometryUvSetExplanation explain_gl_mesh_geometry_uv_sets(MeshGeometry geometry, flight::Array<PbrUvSet> requested_uv_sets) {
  flight::Array<PbrUvSet> available_uv_sets = flight::Array<PbrUvSet>{};
  for (auto attribute : geometry.layout.attributes) {
    if (((attribute.semantic == flight::String("uv0")) && !available_uv_sets.includes(0.0))) {
      available_uv_sets.push(0.0);
    }
    if (((attribute.semantic == flight::String("uv1")) && !available_uv_sets.includes(1.0))) {
      available_uv_sets.push(1.0);
    }
  }
  flight::Array<PbrUvSet> requested = flight::Array<PbrUvSet>{};
  for (auto uv_set : requested_uv_sets) {
    if (!requested.includes(uv_set)) {
      requested.push(uv_set);
    }
  }
  requested.sort();
  auto unserved_uv_sets = requested.filter([=](auto uv_set) { return !available_uv_sets.includes(uv_set); });
  return {.available_uv_sets = available_uv_sets.sort(), .requested_uv_sets = requested, .samples_at_origin = (unserved_uv_sets.length > 0.0), .unserved_uv_sets = unserved_uv_sets};
}

} // namespace flighthq_scene3d_gl
