// Generated by @flighthq/tool-compiler from @flighthq/textlayout/packages/textlayout/src/textLineBreaks.ts#TextLineBreaks at 903f3289590358eacc20698525e6982da2ab0e9d. Do not edit.
#pragma once
#include <cmath>
#include <cstddef>
#include <cstdint>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::textlayout {

inline double get_text_line_break_index(flight::Array<double> line_breaks, std::optional<double> start_index = std::nullopt) {
  start_index = start_index.value_or(0.0);
  if ((static_cast<double>(line_breaks.size()) == 0.0)) {
    return -1.0;
  }
  double lo = 0.0;
  double hi = (static_cast<double>(line_breaks.size()) - 1.0);
  double result = -1.0;
  while ((lo <= hi)) {
    const double mid = flight::unsigned_right_shift((lo + hi), 1.0);
    if ((line_breaks.element(mid) >= start_index.value())) {
      (result = line_breaks.element(mid));
      (hi = (mid - 1.0));
    }
    else {
      (lo = (mid + 1.0));
    }
  }
  return result;
}

inline void get_text_line_breaks(flight::Array<double> out, flight::String text) {
  ([&]() { auto&& assignment_receiver = out; const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
  double index = -1.0;
  while ((index < static_cast<double>(text.length()))) {
    auto lf = text.index_of(flight::String("\n"), (index + 1.0));
    auto cr = text.index_of(flight::String("\r"), (index + 1.0));
    if (((lf == -1.0) && (cr == -1.0))) {
      break;
    }
    (index = ((cr == -1.0) ? lf : ((lf == -1.0) ? cr : flight::minimum(cr, lf))));
    out.push(index);
  }
}

} // namespace flight::textlayout
