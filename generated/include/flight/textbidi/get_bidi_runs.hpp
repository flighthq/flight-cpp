// Generated by @flighthq/tool-compiler from @flighthq/textbidi/getBidiRuns.ts#GetBidiRuns. Do not edit.
#pragma once
#include <cmath>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "resolve_bidi_levels.hpp"

namespace flighthq_textbidi {

inline flight::Array<BidiRun> get_bidi_runs(flight::String text, BidiDirection base_direction, std::optional<BidiClassBackend> bidi_class_backend = std::nullopt) {
  auto levels = resolve_bidi_levels(text, base_direction, bidi_class_backend.value());
  flight::Array<BidiRun> runs = flight::Array<BidiRun>{};
  auto length = levels.length;
  double start = 0.0;
  {
    double i = 1.0;
    while ((i <= length)) {
      {
        if (((i == length) || (levels[static_cast<size_t>(i)] != levels[static_cast<size_t>(start)]))) {
          auto level = levels[static_cast<size_t>(start)];
          runs.push({.start = start, .end = i, .level = level, .direction = ((std::fmod(level, 2.0) == 0.0) ? flight::String("ltr") : flight::String("rtl"))});
          start = i;
        }
      }
      i += 1.0;
    }
  }
  return runs;
}

} // namespace flighthq_textbidi
