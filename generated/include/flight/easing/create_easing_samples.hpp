// Generated by @flighthq/tool-compiler from @flighthq/easing/packages/easing/src/createEasingSamples.ts#CreateEasingSamples at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/number.hpp>
#include <limits>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/easing_function.hpp>

namespace flight::easing {

inline flight::Float32Array create_easing_samples(flight::types::EasingFunction ease, double count, std::optional<flight::Float32Array> out = std::nullopt) {
  if ((!std::isfinite(count) || (count < 1.0))) {
    throw flight::Error(flight::String("createEasingSamples: count must be a finite integer >= 1"));
  }
  auto n = std::floor(count);
  flight::Float32Array result = out;
  if ((n == 1.0)) {
    result.element(0.0) = ease(0.5);
    return result;
  }
  const double step = (1.0 / (n - 1.0));
  {
    double i = 0.0;
    while ((i < n)) {
      {
        const double t = (i * step);
        result.element(i) = ease(((t < 0.0) ? 0.0 : ((t > 1.0) ? 1.0 : t)));
      }
      i += 1.0;
    }
  }
  result.element(0.0) = ease(0.0);
  result.element((n - 1.0)) = ease(1.0);
  return result;
}

} // namespace flight::easing
