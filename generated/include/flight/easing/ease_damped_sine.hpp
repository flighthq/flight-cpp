// Generated by @flighthq/tool-compiler from @flighthq/easing/easeDampedSine.ts#EaseDampedSine. Do not edit.
#pragma once
#include <functional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_easing {

inline EasingFunction ease_in_damped_sine(double amplitude, double period) {
  const double wavelength = to_damped_sine_wavelength(period);
  const double overshoot = to_damped_sine_overshoot(amplitude);
  const double phase = to_damped_sine_phase(overshoot, wavelength);
  return [=](auto t) {
  if (((t == 0.0) || (t == 1.0))) {
    return t;
  }
  const double time = (t - 1.0);
  return -((overshoot * std::pow(2.0, (10.0 * time))) * std::sin((((time - phase) * tau) / wavelength)));
};
}

inline EasingFunction ease_in_out_damped_sine(double amplitude, double period) {
  const double wavelength = to_damped_sine_wavelength(period);
  const double overshoot = to_damped_sine_overshoot(amplitude);
  const double phase = to_damped_sine_phase(overshoot, wavelength);
  return [=](auto t) {
  if (((t == 0.0) || (t == 1.0))) {
    return t;
  }
  const double time = ((t * 2.0) - 1.0);
  if ((time < 0.0)) {
    return (((-0.5 * overshoot) * std::pow(2.0, (10.0 * time))) * std::sin((((time - phase) * tau) / wavelength)));
  }
  return ((((0.5 * overshoot) * std::pow(2.0, (-10.0 * time))) * std::sin((((time - phase) * tau) / wavelength))) + 1.0);
};
}

inline EasingFunction ease_out_damped_sine(double amplitude, double period) {
  const double wavelength = to_damped_sine_wavelength(period);
  const double overshoot = to_damped_sine_overshoot(amplitude);
  const double phase = to_damped_sine_phase(overshoot, wavelength);
  return [=](auto t) {
  if (((t == 0.0) || (t == 1.0))) {
    return t;
  }
  return (((overshoot * std::pow(2.0, (-10.0 * t))) * std::sin((((t - phase) * tau) / wavelength))) + 1.0);
};
}

inline double to_damped_sine_phase(double overshoot, double wavelength) {
  return ((wavelength / tau) * cmath.asin((1.0 / overshoot)));
}

inline double to_damped_sine_overshoot(double amplitude) {
  return ((amplitude < 1.0) ? 1.0 : amplitude);
}

inline double to_damped_sine_wavelength(double period) {
  return ((period > 0.0) ? period : default_damped_sine_period);
}

inline const double tau = (2.0 * flight::pi);

inline const double default_damped_sine_period = 0.4;

} // namespace flighthq_easing
