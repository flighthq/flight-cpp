// Generated by @flighthq/tool-compiler from @flighthq/easing/easeSteps.ts#EaseSteps. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_easing {

inline EasingFunction ease_steps(double count, std::optional<StepPosition> position = std::nullopt) {
  position = position.value_or(flight::String("jumpEnd"));
  if (((position.value() == flight::String("jumpNone")) && (count < 2.0))) {
    ([&]() -> std::optional<auto> { auto optional_chain_receiver = steps_guard; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()(count, position.value()); }());
  }
  const double jumps = ((position.value() == flight::String("jumpNone")) ? (count - 1.0) : ((position.value() == flight::String("jumpBoth")) ? (count + 1.0) : count));
  auto start_offset = (((position.value() == flight::String("jumpStart")) || (position.value() == flight::String("jumpBoth"))) ? 1.0 : 0.0);
  return [=](auto t) {
  double step = (std::floor((t * count)) + start_offset);
  if (((t >= 0.0) && (step < 0.0))) {
    step = 0.0;
  }
  if (((t <= 1.0) && (step > jumps))) {
    step = jumps;
  }
  return (step / jumps);
};
}

inline void set_easing_steps_guard(std::optional<EasingStepsGuard> guard) {
  steps_guard = guard.value();
}

inline std::optional<EasingStepsGuard> steps_guard = nullptr;

} // namespace flighthq_easing
