// Generated by @flighthq/tool-compiler from @flighthq/easing/packages/easing/src/easeSteps.ts#EaseSteps at 903f3289590358eacc20698525e6982da2ab0e9d. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <functional>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/easing_function.hpp>
#include <flight/types/step_position.hpp>

namespace flight::easing {

inline std::optional<std::function<void(double, flight::types::StepPosition)>> steps_guard = std::nullopt;

inline flight::types::EasingFunction ease_steps(double count, std::optional<flight::types::StepPosition> position = std::nullopt) {
  position = position.value_or(flight::String("jumpEnd"));
  if (((position.value() == flight::String("jumpNone")) && (count < 2.0))) {
    ([&]() { auto optional_chain_receiver = steps_guard; if (!optional_chain_receiver.has_value()) return; optional_chain_receiver.value()(count, position.value()); }());
  }
  const double jumps = ((position.value() == flight::String("jumpNone")) ? (count - 1.0) : ((position.value() == flight::String("jumpBoth")) ? (count + 1.0) : count));
  const double start_offset = (((position.value() == flight::String("jumpStart")) || (position.value() == flight::String("jumpBoth"))) ? 1.0 : 0.0);
  return [=](double t) {
  double step = (std::floor((t * count)) + start_offset);
  if (((t >= 0.0) && (step < 0.0))) {
    (step = 0.0);
  }
  if (((t <= 1.0) && (step > jumps))) {
    (step = jumps);
  }
  return (step / jumps);
};
}

inline void set_easing_steps_guard(std::optional<std::function<void(double, flight::types::StepPosition)>> guard) {
  (steps_guard = guard);
}

} // namespace flight::easing
