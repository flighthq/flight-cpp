// Generated by @flighthq/tool-compiler from @flighthq/easing/enableEasingGuards.ts#EnableEasingGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "ease_steps.hpp"

namespace flighthq_easing {

inline void disable_easing_guards() {
  set_easing_steps_guard(nullptr);
}

inline void enable_easing_guards() {
  set_easing_steps_guard(warn_on_degenerate_steps);
}

inline void warn_on_degenerate_steps(double count, StepPosition position) {
  log_once(flight::String("easing:degenerate-steps"), log_level.warn, {.message = flight::String("easeSteps(") + flight::to_string(count) + flight::String(", '") + flight::to_string(position) + flight::String("'): '") + flight::to_string(position) + flight::String("' has count - 1 jumps, so it needs count >= 2. With ") + flight::to_string(count) + flight::String(" there are no jumps, the easing divides by zero, and every input returns NaN. Pass count >= 2, or use a position that jumps at an edge ('jumpEnd', 'jumpStart', 'jumpBoth') — those are defined for count >= 1.")}, flight::String("easing"));
}

} // namespace flighthq_easing
