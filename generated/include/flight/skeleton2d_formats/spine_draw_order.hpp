// Generated by @flighthq/tool-compiler from @flighthq/skeleton2d-formats/packages/skeleton2d-formats/src/spineDrawOrder.ts#SpineDrawOrder at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <flight/number.hpp>
#include <flight/structural_ref.hpp>
#include <limits>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::skeleton2d_formats {

inline const double unclaimed_position = -1.0;

struct offset_slot_index : public flight::ReferenceEnabled {
  double offset;
  double slot_index;
};

inline std::optional<flight::Array<double>> resolve_spine_draw_ordering(flight::Array<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<offset_slot_index>>>>> moves, double slot_count) {
  if ((slot_count <= 0.0)) {
    return std::nullopt;
  }
  flight::Array<double> occupants = flight::Array<double>(slot_count).fill(unclaimed_position);
  flight::Array<bool> moved = flight::Array<bool>(slot_count).fill(false);
  for (auto move : moves) {
    const double slot_index = flight::row_get<flight::RowKey<"slotIndex">>(move);
    if (((!flight::is_integer(slot_index) || (slot_index < 0.0)) || (slot_index >= slot_count))) {
      return std::nullopt;
    }
    const double destination = (slot_index + flight::row_get<flight::RowKey<"offset">>(move));
    if (((!flight::is_integer(destination) || (destination < 0.0)) || (destination >= slot_count))) {
      return std::nullopt;
    }
    if (((occupants.element(destination) != unclaimed_position) || moved.element(slot_index))) {
      return std::nullopt;
    }
    occupants.element(destination) = slot_index;
    moved.element(slot_index) = true;
  }
  double next = 0.0;
  {
    double position = 0.0;
    while ((position < slot_count)) {
      {
        if ((occupants.element(position) != unclaimed_position)) {
          position += 1.0;
          continue;
        }
        while (((next < slot_count) && moved.element(next))) {
          next++;
        }
        if ((next >= slot_count)) {
          return std::nullopt;
        }
        occupants.element(position) = next;
        next++;
      }
      position += 1.0;
    }
  }
  flight::Array<double> sort_keys = flight::Array<double>(slot_count).fill(0.0);
  {
    double position_2 = 0.0;
    while ((position_2 < slot_count)) {
      sort_keys.element(occupants.element(position_2)) = position_2;
      position_2 += 1.0;
    }
  }
  return std::optional<flight::Array<double>>{sort_keys};
}

} // namespace flight::skeleton2d_formats
