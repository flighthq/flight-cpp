// Generated by @flighthq/tool-compiler from @flighthq/animation/animationStateMachineAdvance.ts#AnimationStateMachineAdvance. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "animation_advance.hpp"

namespace flighthq_animation {

inline void advance_animation_state_machine_with_scratch(AnimationStateMachine machine, double dt, flight::Array<AnimationPlayer> advanced) {
  advance_animation_players(machine.states[static_cast<size_t>(machine.current_state_index)].blend_tree.players, dt, advanced);
  auto to_index = machine.transition_to_state_index;
  if (!to_index.has_value()) {
    return;
  }
  advance_animation_players(machine.states[static_cast<size_t>(to_index)].blend_tree.players, dt, advanced);
  machine.transition_elapsed += dt;
  machine.transition_weight = machine.transition_curve(get_linear_animation_state_machine_transition_weight(machine.transition_elapsed, machine.transition_duration));
  if (((machine.transition_duration <= 0.0) || (machine.transition_elapsed >= machine.transition_duration))) {
    machine.current_state_index = to_index;
    machine.transition_from_state_index = nullptr;
    machine.transition_to_state_index = nullptr;
  }
}

inline double get_linear_animation_state_machine_transition_weight(double elapsed, double duration) {
  if ((duration <= 0.0)) {
    return 1.0;
  }
  const double normalized = (elapsed / duration);
  return ((normalized < 0.0) ? 0.0 : ((normalized > 1.0) ? 1.0 : normalized));
}

} // namespace flighthq_animation
