// Generated by @flighthq/tool-compiler from @flighthq/math/interpolation.ts#Interpolation. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_math {

inline double inverse_lerp(double a, double b, double value) {
  const double range = (b - a);
  return ((range == 0.0) ? 0.0 : ((value - a) / range));
}

inline double lerp(double a, double b, double t) {
  return (a + ((b - a) * t));
}

inline double remap(double value, double in_min, double in_max, double out_min, double out_max) {
  const double in_range = (in_max - in_min);
  if ((in_range == 0.0)) {
    return out_min;
  }
  return (out_min + (((value - in_min) / in_range) * (out_max - out_min)));
}

inline double smooth_step(double edge0, double edge1, double x) {
  const double t = ((x - edge0) / (edge1 - edge0));
  const double s = ((t < 0.0) ? 0.0 : ((t > 1.0) ? 1.0 : t));
  return ((s * s) * (3.0 - (2.0 * s)));
}

inline double step(double edge, double x) {
  return ((x < edge) ? 0.0 : 1.0);
}

} // namespace flighthq_math
