// Generated by @flighthq/tool-compiler from @flighthq/math/packages/math/src/interpolationAdvanced.ts#InterpolationAdvanced at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::math {

inline double damp(double current, double target, double lambda, double delta_time) {
  if (((delta_time <= 0.0) || (lambda <= 0.0))) {
    return current;
  }
  return (target + ((current - target) * std::exp((-lambda * delta_time))));
}

inline double lerp_angle(double a, double b, double t) {
  const double tau = (flight::pi * 2.0);
  double diff = std::fmod((std::fmod((b - a), tau) + tau), tau);
  if ((diff > flight::pi)) {
    (diff -= tau);
  }
  return (a + (diff * t));
}

inline double move_towards(double current, double target, double max_delta) {
  const double delta = (target - current);
  if ((std::abs(delta) <= max_delta)) {
    return target;
  }
  return (current + (flight::sign(delta) * max_delta));
}

inline double ping_pong(double t, double length) {
  const double cycle = (2.0 * length);
  const double mod = std::fmod((std::fmod(t, cycle) + cycle), cycle);
  return ((mod <= length) ? mod : (cycle - mod));
}

inline double repeat(double t, double length) {
  return std::fmod((std::fmod(t, length) + length), length);
}

inline double smoother_step(double edge0, double edge1, double x) {
  const double t = ((x - edge0) / (edge1 - edge0));
  const double s = ((t < 0.0) ? 0.0 : ((t > 1.0) ? 1.0 : t));
  return (((s * s) * s) * ((s * ((s * 6.0) - 15.0)) + 10.0));
}

} // namespace flight::math
