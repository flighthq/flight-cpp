// Generated by @flighthq/tool-compiler from @flighthq/math/packages/math/src/numberTheory.ts#NumberTheory at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/boolean.hpp>
#include <flight/error.hpp>
#include <flight/number.hpp>
#include <limits>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::math {

inline double factorial(double n) {
  if ((!flight::to_boolean(flight::is_integer(n)) || (n < 0.0))) {
    throw flight::RangeError(flight::String("factorial: n must be a non-negative integer"));
  }
  if (((n == 0.0) || (n == 1.0))) {
    return 1.0;
  }
  double result = 1.0;
  {
    double i = 2.0;
    while ((i <= n)) {
      (result *= i);
      (i += 1.0);
    }
  }
  return result;
}

inline double gcd(double a, double b) {
  if ((!flight::to_boolean(std::isfinite(a)) || !flight::to_boolean(std::isfinite(b)))) {
    throw flight::RangeError(flight::String("gcd: arguments must be finite"));
  }
  (a = std::abs(std::trunc(a)));
  (b = std::abs(std::trunc(b)));
  if (((a == 0.0) && (b == 0.0))) {
    throw flight::RangeError(flight::String("gcd: both arguments must not be 0"));
  }
  while ((b != 0.0)) {
    const double t = b;
    (b = std::fmod(a, b));
    (a = t);
  }
  return a;
}

inline double hypot2(double x, double y) {
  return ((x * x) + (y * y));
}

inline bool is_even(double n) {
  return (flight::bitwise_and(n, 1.0) == 0.0);
}

inline bool is_odd(double n) {
  return (flight::bitwise_and(n, 1.0) == 1.0);
}

inline double lcm(double a, double b) {
  const double g = gcd(a, b);
  return ((std::abs(std::trunc(a)) / g) * std::abs(std::trunc(b)));
}

} // namespace flight::math
