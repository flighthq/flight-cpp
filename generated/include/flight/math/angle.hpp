// Generated by @flighthq/tool-compiler from @flighthq/math/angle.ts#Angle. Do not edit.
#pragma once
#include <cmath>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "constants.hpp"

namespace flighthq_math {

inline double deg_to_rad(double degrees) {
  return (degrees * deg_to_rad_2);
}

inline double delta_angle(double from, double to) {
  const double diff = std::fmod((std::fmod((to - from), tau) + tau), tau);
  return ((diff > flight::pi) ? (diff - tau) : diff);
}

inline double normalize_angle(double radians) {
  const double wrapped = std::fmod((std::fmod(radians, tau) + tau), tau);
  return ((wrapped >= flight::pi) ? (wrapped - tau) : wrapped);
}

inline double rad_to_deg(double radians) {
  return (radians * rad_to_deg_2);
}

} // namespace flighthq_math
