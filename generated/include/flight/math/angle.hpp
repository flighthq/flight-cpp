// Generated by @flighthq/tool-compiler from @flighthq/math/packages/math/src/angle.ts#Angle at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/math/constants.hpp>

namespace flight::math {

inline double deg_to_rad(double degrees) {
  return (degrees * flight::math::deg_to_rad);
}

inline double delta_angle(double from, double to) {
  const double diff = std::fmod((std::fmod((to - from), flight::math::tau) + flight::math::tau), flight::math::tau);
  return ((diff > flight::pi) ? (diff - flight::math::tau) : diff);
}

inline double normalize_angle(double radians) {
  const double wrapped = std::fmod((std::fmod(radians, flight::math::tau) + flight::math::tau), flight::math::tau);
  return ((wrapped >= flight::pi) ? (wrapped - flight::math::tau) : wrapped);
}

inline double rad_to_deg(double radians) {
  return (radians * flight::math::rad_to_deg);
}

} // namespace flight::math
