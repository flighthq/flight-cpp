// Generated by @flighthq/tool-compiler from @flighthq/math/packages/math/src/randomRange.ts#RandomRange at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/error.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/random_source.hpp>

namespace flight::math {

inline bool random_bool(flight::types::RandomSource random, std::optional<double> probability = std::nullopt) {
  probability = probability.value_or(0.5);
  return (random() < probability.value());
}

inline double random_int(flight::types::RandomSource random, double min, double max) {
  const double lo = std::floor(min);
  const double hi = std::floor(max);
  if ((lo > hi)) {
    throw flight::RangeError(flight::String("randomInt: min must be <= max"));
  }
  return (lo + std::floor((random() * ((hi - lo) + 1.0))));
}

inline double random_range(flight::types::RandomSource random, double min, double max) {
  return (min + (random() * (max - min)));
}

inline double random_sign(flight::types::RandomSource random) {
  return ((random() < 0.5) ? -1.0 : 1.0);
}

} // namespace flight::math
