// Generated by @flighthq/tool-compiler from @flighthq/scene2d-canvas/canvasMaterialRegistry.ts#CanvasMaterialRegistry. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "canvas_render_state.hpp"

namespace flighthq_scene2d_canvas {

inline bool apply_canvas_material(CanvasRenderState state, std::optional<Material> material) {
  if (!material.value().has_value()) {
    return false;
  }
  std::optional<CanvasMaterialRenderer> renderer = resolve_canvas_material_renderer(state, material.value());
  if (!renderer.value().has_value()) {
    return false;
  }
  auto draw_state = renderer.value().get_state(material.value());
  auto context = state.context;
  context.save();
  if ((draw_state.composite != undefined)) {
    context.global_composite_operation = draw_state.composite;
  }
  if ((draw_state.filter != undefined)) {
    context.filter = draw_state.filter;
  }
  return true;
}

inline std::optional<CanvasMaterialRenderer> get_canvas_material_renderer(CanvasRenderState state, Kind kind) {
  auto entry = ([&]() -> std::optional<auto> { auto optional_chain_receiver = get_canvas_render_state_runtime(state).registries.material_renderers; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().entries; }()).get(kind);
  return ((([&]() -> std::optional<auto> { auto optional_chain_receiver = entry; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().state; }()) == registry_entry_state.bound) ? entry.value : nullptr);
}

inline void register_canvas_material_renderer(CanvasRenderState state, Kind kind, CanvasMaterialRenderer renderer) {
  auto runtime = get_canvas_render_state_runtime(state);
  auto table = runtime.registries.material_renderers.value_or(create_keyed_table(flight::String("CanvasMaterialRenderer"), flight::String("StandardMaterial")));
  runtime.registries.material_renderers = with_registry_table_entry(table, kind, renderer);
}

inline std::optional<CanvasMaterialRenderer> resolve_canvas_material_renderer(CanvasRenderState state, std::optional<Material> material) {
  auto entries = ([&]() -> std::optional<auto> { auto optional_chain_receiver = get_canvas_render_state_runtime(state).registries.material_renderers; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().entries; }());
  if (!entries.has_value()) {
    return nullptr;
  }
  if (material.value().has_value()) {
    auto entry = entries.get(material.value().kind);
    if ((([&]() -> std::optional<auto> { auto optional_chain_receiver = entry; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().state; }()) == registry_entry_state.bound)) {
      return entry.value;
    }
  }
  auto fallback = entries.get(standard_material_kind);
  return ((([&]() -> std::optional<auto> { auto optional_chain_receiver = fallback; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().state; }()) == registry_entry_state.bound) ? fallback.value : nullptr);
}

} // namespace flighthq_scene2d_canvas
