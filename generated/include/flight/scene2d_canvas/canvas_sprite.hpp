// Generated by @flighthq/tool-compiler from @flighthq/scene2d-canvas/canvasSprite.ts#CanvasSprite. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "canvas_node2_d.hpp"
#include "canvas_render_state.hpp"
#include "canvas_texture_resolver.hpp"
#include "canvas_texture_view.hpp"
#include "canvas_transform.hpp"

namespace flighthq_scene2d_canvas {

struct x_y {
  double x;
  double y;
};

inline void draw_canvas_sprite(CanvasRenderState state, RenderProxy2D sprite) {
  draw_canvas_scene2_d(state, sprite);
  auto texture = static_cast<Sprite>(sprite.source).data.texture;
  if ((!texture.has_value() || (texture.dimension != flight::String("2d")))) {
    return;
  }
  auto drawable = resolve_canvas_texture(get_canvas_render_state_texture_resolvers(state), texture);
  if (!drawable.has_value()) {
    return;
  }
  get_texture_view_size(sprite_view_size, texture);
  const double view_width = sprite_view_size.x;
  const double view_height = sprite_view_size.y;
  if (((view_width <= 0.0) || (view_height <= 0.0))) {
    return;
  }
  auto context = state.context;
  ([&]() -> std::optional<auto> { auto optional_chain_receiver = state.apply_blend_mode; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()(state, sprite.blend_mode); }());
  set_canvas_global_alpha(state, sprite.alpha);
  set_canvas_transform(state, context, sprite.transform2_d);
  auto smoothing = resolve_canvas_texture_smoothing(state, texture.sampler.mag_filter);
  if (!smoothing) {
    set_canvas_image_smoothing(state, false);
  }
  draw_canvas_texture_view(context, drawable, texture, view_width, view_height);
  if (!smoothing) {
    set_canvas_image_smoothing(state, true);
  }
}

inline Scene2DRenderer default_canvas_sprite_renderer = {.create_data = create_sprite_renderer_data, .is_dirty = is_sprite_renderer_dirty, .submit = draw_canvas_sprite};

inline x_y sprite_view_size = {.x = 0.0, .y = 0.0};

} // namespace flighthq_scene2d_canvas
