// Generated by @flighthq/tool-compiler from @flighthq/scene2d-canvas/canvasClipRectangle.ts#CanvasClipRectangle. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "canvas_transform.hpp"

namespace flighthq_scene2d_canvas {

inline void pop_canvas_clip_rectangle(CanvasRenderState state) {
  state.context.restore();
}

inline void push_canvas_clip_contours(CanvasRenderState state, flight::Array<flight::Array<double>> contours, PathWinding winding, Matrix transform) {
  state.context.save();
  set_canvas_transform(state, state.context, transform);
  state.context.begin_path();
  {
    double c = 0.0;
    while ((c < static_cast<double>(contours.size()))) {
      {
        std::optional<flight::Array<double>> contour = contours.get(c);
        if ((static_cast<double>(contour.size()) < 2.0)) {
          c += 1.0;
          continue;
        }
        state.context.move_to(contour.element(0.0), contour.element(1.0));
        {
          double i = 2.0;
          while ((i < static_cast<double>(contour.size()))) {
            {
              state.context.line_to(contour.element(i), contour.element((i + 1.0)));
            }
            i += 2.0;
          }
        }
        state.context.close_path();
      }
      c += 1.0;
    }
  }
  state.context.clip(((winding == flight::String("evenOdd")) ? flight::String("evenodd") : flight::String("nonzero")));
}

inline void push_canvas_clip_rectangle(CanvasRenderState state, RectangleLike rect, Matrix transform) {
  state.context.save();
  set_canvas_transform(state, state.context, transform);
  state.context.begin_path();
  state.context.rect(rect.x, rect.y, rect.width, rect.height);
  state.context.clip();
}

} // namespace flighthq_scene2d_canvas
