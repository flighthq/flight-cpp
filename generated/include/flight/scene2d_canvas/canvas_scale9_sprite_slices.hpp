// Generated by @flighthq/tool-compiler from @flighthq/scene2d-canvas/packages/scene2d-canvas/src/canvasScale9SpriteSlices.ts#CanvasScale9SpriteSlices at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <flight/structural_ref.hpp>
#include <functional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/rectangle.hpp>

namespace flight::scene2d_canvas {

inline const double canvas_scale9_sprite_slice_stride = 8.0;

inline const double band_stride = 4.0;

inline flight::Array<double> columns = flight::Array<double>{};

inline flight::Array<double> rows = flight::Array<double>{};

inline double write_axis_bands(flight::Array<double> out, double source_size, double grid_start, double grid_size, double target_size) {
  const auto out_capture = flight::make_binding_cell(flight::Array<double>{out});
  ([&]() { auto&& assignment_receiver = out_capture.read_binding(); const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
  if ((((grid_size < 0.0) || (grid_start < 0.0)) || ((grid_start + grid_size) > source_size))) {
    return 0.0;
  }
  const double start_size = grid_start;
  const double end_size = ((source_size - grid_start) - grid_size);
  const double fixed = (start_size + end_size);
  const double centre = (target_size - fixed);
  double destination_start_size = start_size;
  double destination_end_size = end_size;
  double destination_centre_size = centre;
  if ((centre < 0.0)) {
    const double shrink = ((fixed > 0.0) ? (target_size / fixed) : 0.0);
    destination_start_size = (start_size * shrink);
    destination_end_size = (end_size * shrink);
    destination_centre_size = 0.0;
  }
  const auto destination_start_capture = flight::make_binding_cell(double{0.0});
  const auto count_capture = flight::make_binding_cell(double{0.0});
  std::function<void(double, double, double)> push = [=](double source_start, double size, double destination_size) {
  if ((size > 0.0)) {
    out_capture.read_binding().push(source_start, size, destination_start_capture.read_binding(), destination_size);
    count_capture.update_binding([&](auto& binding_value) { return binding_value++; });
  }
  destination_start_capture.update_binding([&](auto& binding_value) { binding_value += destination_size; return binding_value; });
};
  push(0.0, start_size, destination_start_size);
  push(grid_start, grid_size, destination_centre_size);
  push((grid_start + grid_size), end_size, destination_end_size);
  return count_capture.read_binding();
}

inline double write_canvas_scale9_sprite_slices(flight::Array<double> out, double source_width, double source_height, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::RectangleLike>>>> grid, double target_width, double target_height) {
  ([&]() { auto&& assignment_receiver = out; const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
  if (((((source_width <= 0.0) || (source_height <= 0.0)) || (target_width <= 0.0)) || (target_height <= 0.0))) {
    return 0.0;
  }
  const double column_count = write_axis_bands(columns, source_width, flight::row_get<flight::RowKey<"x">>(grid), flight::row_get<flight::RowKey<"width">>(grid), target_width);
  const double row_count = write_axis_bands(rows, source_height, flight::row_get<flight::RowKey<"y">>(grid), flight::row_get<flight::RowKey<"height">>(grid), target_height);
  if (((column_count == 0.0) || (row_count == 0.0))) {
    return 0.0;
  }
  {
    double row = 0.0;
    while ((row < row_count)) {
      {
        const double r = (row * band_stride);
        {
          double column = 0.0;
          while ((column < column_count)) {
            {
              const double c = (column * band_stride);
              out.push(columns.element(c), rows.element(r), columns.element((c + 1.0)), rows.element((r + 1.0)), columns.element((c + 2.0)), rows.element((r + 2.0)), columns.element((c + 3.0)), rows.element((r + 3.0)));
            }
            column += 1.0;
          }
        }
      }
      row += 1.0;
    }
  }
  return (column_count * row_count);
}

} // namespace flight::scene2d_canvas
