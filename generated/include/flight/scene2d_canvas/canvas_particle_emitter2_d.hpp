// Generated by @flighthq/tool-compiler from @flighthq/scene2d-canvas/canvasParticleEmitter2D.ts#CanvasParticleEmitter2D. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "canvas_atlas_region.hpp"
#include "canvas_render_state.hpp"
#include "canvas_texture_resolver.hpp"

namespace flighthq_scene2d_canvas {

inline void draw_canvas_particle_emitter2_d(CanvasRenderState state, RenderProxy2D render_proxy) {
  ParticleEmitter2D source = static_cast<ParticleEmitter2D>(render_proxy.source);
  auto object_pattern_value = source.data;
  auto atlas = object_pattern_value.atlas;
  auto alphas = object_pattern_value.alphas;
  auto ids = object_pattern_value.ids;
  auto particle_count = object_pattern_value.particle_count;
  auto transforms = object_pattern_value.transforms;
  if ((((atlas == nullptr) || (atlas.texture == nullptr)) || (particle_count == 0.0))) {
    return;
  }
  auto image_source = resolve_canvas_texture(get_canvas_render_state_texture_resolvers(state), atlas.texture);
  if (!image_source.has_value()) {
    return;
  }
  auto regions = atlas.regions;
  auto num_regions = regions.length;
  auto node_alpha = render_proxy.alpha;
  auto t = render_proxy.transform2_d;
  auto context = state.context;
  ([&]() -> std::optional<auto> { auto optional_chain_receiver = state.apply_blend_mode; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()(state, render_proxy.blend_mode); }());
  auto smoothing = resolve_canvas_texture_smoothing(state, atlas.texture.sampler.mag_filter);
  if (!smoothing) {
    set_canvas_image_smoothing(state, false);
  }
  {
    double i = 0.0;
    while ((i < particle_count)) {
      {
        auto id = ids[static_cast<size_t>(i)];
        if (((id < 0.0) || (id >= num_regions))) {
          i += 1.0;
          continue;
        }
        auto region = regions[static_cast<size_t>(id)];
        if (((region.width <= 0.0) || (region.height <= 0.0))) {
          i += 1.0;
          continue;
        }
        const double tt = (i * 4.0);
        auto scale = transforms[static_cast<size_t>((tt + 3.0))];
        if (((scale == 0.0) || (alphas[static_cast<size_t>(i)] <= 0.0))) {
          i += 1.0;
          continue;
        }
        auto px = transforms[static_cast<size_t>(tt)];
        auto py = transforms[static_cast<size_t>((tt + 1.0))];
        auto rotation = transforms[static_cast<size_t>((tt + 2.0))];
        const double cos_r = (std::cos(rotation) * scale);
        const double sin_r = (std::sin(rotation) * scale);
        double a;
        double b;
        double c;
        double d;
        double tx;
        double ty;
        if (source.data.world_space) {
          a = cos_r;
          b = sin_r;
          c = -sin_r;
          d = cos_r;
          tx = px;
          ty = py;
        }
        else {
          a = ((t.a * cos_r) + (t.c * sin_r));
          b = ((t.b * cos_r) + (t.d * sin_r));
          c = ((t.a * -sin_r) + (t.c * cos_r));
          d = ((t.b * -sin_r) + (t.d * cos_r));
          tx = (((t.a * px) + (t.c * py)) + t.tx);
          ty = (((t.b * px) + (t.d * py)) + t.ty);
        }
        context.global_alpha = (node_alpha * alphas[static_cast<size_t>(i)]);
        context.set_transform(a, b, c, d, tx, ty);
        draw_canvas_atlas_region(context, image_source, region, 0.0, 0.0, ((region.rotation != texture_atlas_rotation.none) ? region.height : region.width), ((region.rotation != texture_atlas_rotation.none) ? region.width : region.height));
      }
      i += 1.0;
    }
  }
  if (!smoothing) {
    set_canvas_image_smoothing(state, true);
  }
}

inline SpriteRenderer default_canvas_particle_emitter2_drenderer = {.create_data = noop_renderer_data, .submit = draw_canvas_particle_emitter2_d};

} // namespace flighthq_scene2d_canvas
