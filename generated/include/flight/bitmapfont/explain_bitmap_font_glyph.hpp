// Generated by @flighthq/tool-compiler from @flighthq/bitmapfont/explainBitmapFontGlyph.ts#ExplainBitmapFontGlyph. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_bitmapfont {

struct glyph_height_glyph_width_page_page_count {
  auto glyph_height;
  auto glyph_width;
  auto page;
  auto page_count;
};

inline BitmapFontGlyphExplanation explain_bitmap_font_glyph(BitmapFont font, double codepoint) {
  auto page_count = font.pages.length;
  auto glyph = font.glyphs.get(codepoint);
  if (!glyph.has_value()) {
    return {.glyph_height = 0.0, .glyph_width = 0.0, .page = -1.0, .page_count = page_count, .reason = flight::String("no-glyph"), .renderable = false};
  }
  glyph_height_glyph_width_page_page_count shared = {.glyph_height = glyph.height, .glyph_width = glyph.width, .page = glyph.page, .page_count = page_count};
  if ((page_count == 0.0)) {
    return {.reason = flight::String("no-pages"), .renderable = false};
  }
  if (((glyph.width <= 0.0) || (glyph.height <= 0.0))) {
    return {.reason = flight::String("empty-glyph"), .renderable = false};
  }
  return {.reason = flight::String("ok"), .renderable = true};
}

} // namespace flighthq_bitmapfont
