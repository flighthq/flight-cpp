// Generated by @flighthq/tool-compiler from @flighthq/bitmapfont/enableBitmapFontGuards.ts#EnableBitmapFontGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap_font.hpp"

namespace flighthq_bitmapfont {

inline void disable_bitmap_font_guards() {
  set_bitmap_font_guard(nullptr);
}

inline void enable_bitmap_font_guards() {
  set_bitmap_font_guard(warn_on_bitmap_font_repair);
}

inline void warn_on_bitmap_font_repair(flight::String reason, double codepoint, double page) {
  if ((reason != flight::String("page-out-of-range"))) {
    return;
  }
  const flight::String printable = flight::String("U+") + flight::to_string(flight::to_string(codepoint).to_upper().pad_start(4.0, flight::String("0"))) + flight::String("");
  log_once(flight::String("bitmapfont:page-out-of-range"), log_level.warn, {.message = flight::String("createBitmapFont: ") + flight::to_string(printable) + flight::String(" names page ") + flight::to_string(page) + flight::String(", which this font does not have, so it was ") + flight::String("placed on page 0 and will sample whatever occupies those coordinates there. The font data is ") + flight::String("wrong, not the atlas — check the page index the exporter wrote.")}, flight::String("bitmapfont"));
}

} // namespace flighthq_bitmapfont
