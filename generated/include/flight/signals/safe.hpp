// Generated by @flighthq/tool-compiler from @flighthq/signals/packages/signals/src/safe.ts#Safe at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cstddef>
#include <exception>
#include <flight/any.hpp>
#include <functional>
#include <optional>
#include <utility>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { template <typename T> struct Signal; }
namespace flight::types { template <typename T> struct SignalData; }

#include <flight/types/signal.hpp>
#include <flight/signals/_internal_internal.hpp>

namespace flight::signals {

template <typename T>
inline void tombstone_once_slot(flight::Ref<flight::types::SignalData<T>> data, T slot, double priority) {
  {
    double i = 0.0;
    while ((i < static_cast<double>(data->slots.size()))) {
      {
        if ((((data->slots.element(i) != slot) || data->repeat.element(i)) || (data->priorities.element(i) != priority))) {
          (i += 1.0);
          continue;
        }
        (data->slots.element(i) = std::nullopt);
        return;
      }
      (i += 1.0);
    }
  }
}

template <typename T>
inline void compact_signal_data_flight_value_function_compact_u000053_ignal_u000044_ata_flight_private_d3c18adc52f06ae0(flight::Ref<flight::types::Signal<T>> signal, flight::Ref<flight::types::SignalData<T>> data) {
  double write = 0.0;
  {
    double read = 0.0;
    while ((read < static_cast<double>(data->slots.size()))) {
      {
        if (!data->slots.get(read).has_value()) {
          (read += 1.0);
          continue;
        }
        if ((write != read)) {
          (data->slots.element(write) = data->slots.element(read));
          (data->priorities.element(write) = data->priorities.element(read));
          (data->repeat.element(write) = data->repeat.element(read));
        }
        write++;
      }
      (read += 1.0);
    }
  }
  if ((write == static_cast<double>(data->slots.size()))) {
    return;
  }
  ([&]() { auto&& assignment_receiver = data->slots; const auto assignment_value = write; assignment_receiver.resize(assignment_value); return assignment_value; }());
  ([&]() { auto&& assignment_receiver = data->priorities; const auto assignment_value = write; assignment_receiver.resize(assignment_value); return assignment_value; }());
  ([&]() { auto&& assignment_receiver = data->repeat; const auto assignment_value = write; assignment_receiver.resize(assignment_value); return assignment_value; }());
  if (((write == 0.0) && (signal->data == data))) {
    (signal->emit = flight::bind_callable_v1<T>(flight::signals::null_signal_emit));
    (signal->data = std::nullopt);
  }
}

template <typename T, typename... ArgsPack>
  requires flight::callable_signature_v1<T>::template accepts<ArgsPack...>
inline void emit_signal_safe(flight::Ref<flight::types::Signal<T>> signal, ArgsPack&&... args) {
  auto data = signal->data;
  if (!data.has_value()) {
    return;
  }
  flight::Array<std::optional<std::function<void(flight::Array<flight::Any>)>>> slots = data.value()->slots.slice();
  flight::Array<double> priorities = data.value()->priorities.slice();
  flight::Array<bool> repeat = data.value()->repeat.slice();
  (data.value()->cancelled = false);
  data.value()->depth++;
  std::exception_ptr finally_exception;
  try {
    {
      double i = 0.0;
      while ((i < static_cast<double>(slots.size()))) {
        {
          std::optional<std::optional<std::function<void(flight::Array<flight::Any>)>>> slot = slots.get(i);
          if (!slot.has_value()) {
            (i += 1.0);
            continue;
          }
          if (!repeat.element(i)) {
            tombstone_once_slot<T>(data.value(), slot.value(), priorities.element(i));
          }
          slot.value()(std::forward<ArgsPack>(args)...);
          if (data.value()->cancelled) {
            break;
          }
        }
        (i += 1.0);
      }
    }
  }
  catch (...) {
    finally_exception = std::current_exception();
  }
  data.value()->depth--;
  if ((data.value()->depth == 0.0)) {
    compact_signal_data_flight_value_function_compact_u000053_ignal_u000044_ata_flight_private_d3c18adc52f06ae0<T>(signal, data.value());
  }
  if (finally_exception) std::rethrow_exception(finally_exception);
}

} // namespace flight::signals
