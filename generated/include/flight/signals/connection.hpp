// Generated by @flighthq/tool-compiler from @flighthq/signals/packages/signals/src/connection.ts#Connection at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <flight/any.hpp>
#include <flight/structural_ref.hpp>
#include <functional>
#include <optional>
#include <utility>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { struct SignalConnectOptions; }
namespace flight::types { struct SignalScope; }
namespace flight::types { struct SignalTrackedConnectOptions; }
namespace flight::types { template <typename T> struct Signal; }
namespace flight::types { template <typename T> struct SignalConnection; }
namespace flight::types { template <typename T> struct SignalData; }

#include <flight/types/signal.hpp>
#include <flight/types/signal_connect_options.hpp>
#include <flight/types/signal_connection.hpp>
#include <flight/types/signal_scope.hpp>
#include <flight/types/signal_tracked_connect_options.hpp>
#include <flight/signals/slot.hpp>

namespace flight::signals {

template <typename T>
inline void disconnect_signal_connection(flight::Ref<flight::types::SignalConnection<T>> connection) {
  if (!connection->connected) {
    return;
  }
  (connection->connected = false);
  flight::signals::disconnect_signal<T>(connection->signal, connection->slot);
}

template <typename T>
inline flight::Ref<flight::types::SignalConnection<T>> connect_signal_tracked(flight::Ref<flight::types::Signal<T>> signal, T slot, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SignalTrackedConnectOptions>>>>> options = std::nullopt) {
  const auto connection_capture = flight::make_binding_cell(flight::Ref<flight::types::SignalConnection<T>>{([&]() { auto object_member_connected_2 = true; auto object_member_paused_2 = false; auto object_member_signal_2 = signal; auto object_member_slot_2 = slot; return flight::make_ref<flight::types::SignalConnection<T>>(flight::types::SignalConnection<T>{.signal = object_member_signal_2, .slot = object_member_slot_2, .connected = object_member_connected_2, .paused = object_member_paused_2}); }())});
  const bool once = ([&]() -> std::optional<bool> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return flight::row_get<flight::RowKey<"once">>(optional_chain_receiver.value()); }()).value_or(false);
  T tracked_slot = flight::bind_callable_v1<T>([=]<typename... ArgsPack>(ArgsPack&&... args) requires flight::callable_signature_v1<T>::template accepts<ArgsPack...> {
  if (connection_capture.read_binding()->paused) {
    return;
  }
  if (once) {
    disconnect_signal_connection<T>(connection_capture.read_binding());
  }
  slot(std::forward<ArgsPack>(args)...);
});
  (connection_capture.read_binding()->slot = tracked_slot);
  const std::optional<double> priority = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return flight::row_get<flight::RowKey<"priority">>(optional_chain_receiver.value()); }());
  flight::signals::connect_signal<T>(signal, tracked_slot, (!priority.has_value() ? std::nullopt : flight::make_structural_ref<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SignalConnectOptions>>>>(flight::row_field<flight::RowKey<"priority">>(priority))));
  ([&]() -> std::optional<flight::Array<flight::Ref<flight::types::SignalConnection<std::function<void(flight::Array<flight::Any>)>>>>> { auto optional_chain_receiver = ([&]() -> std::optional<flight::Ref<flight::types::SignalScope>> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return flight::row_get<flight::RowKey<"scope">>(optional_chain_receiver.value()); }()); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->connections; }()).push(static_cast<flight::Ref<flight::types::SignalConnection<std::function<void(flight::Array<flight::Any>)>>>>(static_cast<flight::Any>(connection_capture.read_binding())));
  return connection_capture.read_binding();
}

template <typename T>
inline void pause_signal_connection(flight::Ref<flight::types::SignalConnection<T>> connection) {
  if (connection->connected) {
    (connection->paused = true);
  }
}

template <typename T>
inline void resume_signal_connection(flight::Ref<flight::types::SignalConnection<T>> connection) {
  if (connection->connected) {
    (connection->paused = false);
  }
}

} // namespace flight::signals
