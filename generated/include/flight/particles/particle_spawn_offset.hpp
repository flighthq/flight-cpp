// Generated by @flighthq/tool-compiler from @flighthq/particles/particleSpawnOffset.ts#ParticleSpawnOffset. Do not edit.
#pragma once
#include <variant>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_particles {

inline const double two_pi = (flight::pi * 2.0);

inline void write_particle_spawn_offset(std::variant<flight::Array<double>, flight::Float32Array> out, double offset, ParticleEmitterConfig config, RandomSource random) {
  double x = 0.0;
  double y = 0.0;
  auto shape = config.emitter_shape;
  if (((shape == flight::String("circle")) && (config.emitter_radius > 0.0))) {
    const double radius = (std::sqrt(random()) * config.emitter_radius);
    const double angle_2 = (random() * two_pi);
    x = (std::cos(angle_2) * radius);
    y = (std::sin(angle_2) * radius);
  }
  else {
    if (((shape == flight::String("line")) && (config.emitter_width > 0.0))) {
      x = ((random() - 0.5) * config.emitter_width);
    }
    else {
      if (((shape == flight::String("rect")) && ((config.emitter_width > 0.0) || (config.emitter_height > 0.0)))) {
        auto x_sample = random();
        auto y_sample = random();
        x = ((x_sample - 0.5) * config.emitter_width);
        y = ((y_sample - 0.5) * config.emitter_height);
      }
      else {
        if (((shape == flight::String("ring")) && (config.emitter_radius > 0.0))) {
          const double angle = (random() * two_pi);
          x = (std::cos(angle) * config.emitter_radius);
          y = (std::sin(angle) * config.emitter_radius);
        }
      }
    }
  }
  out.element(offset) = x;
  out.element((offset + 1.0)) = y;
}

} // namespace flighthq_particles
