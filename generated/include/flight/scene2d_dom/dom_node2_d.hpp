// Generated by @flighthq/tool-compiler from @flighthq/scene2d-dom/domNode2D.ts#DomNode2D. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "dom_reconcile.hpp"
#include "dom_render_state.hpp"

namespace flighthq_scene2d_dom {

inline void draw_dom_scene2_d(DomRenderState state, RenderProxy2D render_proxy) {
}

inline Scene2DRenderer default_dom_scene2_drenderer = {.create_data = noop_renderer_data, .submit = draw_dom_scene2_d};

inline void render_dom_scene2_d(DomRenderState state, Node2D source) {
  auto runtime = get_dom_render_state_runtime(state);
  auto container = state.element;
  auto clip_hooks = state.display_object_clip_hooks;
  auto apply_clip = runtime.dom_clip_hooks;
  auto frame_id = runtime.current_frame_id;
  auto temp_stack = runtime.temp_stack;
  double stack_length = 1.0;
  temp_stack[static_cast<size_t>(0.0)] = source;
  double new_length = 0.0;
  bool needs_reconcile = false;
  while ((stack_length > 0.0)) {
    Node2D current = static_cast<Node2D>(temp_stack[static_cast<size_t>(--stack_length)]);
    if (!current.enabled) {
      continue;
    }
    auto data = get_render_proxy2_d(state, current);
    if (!data.has_value()) {
      continue;
    }
    ([&]() -> std::optional<auto> { auto optional_chain_receiver = clip_hooks; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().pop_clip; }())(state, data, current);
    if (!is_render_proxy_visible(data)) {
      continue;
    }
    ([&]() -> std::optional<auto> { auto optional_chain_receiver = clip_hooks; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().push_clip; }())(state, data, current);
    if ((data.renderer != nullptr)) {
      auto result = process_dom_node(runtime, data, frame_id, [=]() { return data.renderer.submit(state, data); }, new_length);
      new_length = result.new_length;
      if (result.needs_reconcile) {
        needs_reconcile = true;
      }
      ([&]() -> std::optional<auto> { auto optional_chain_receiver = apply_clip; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().apply; }())(state, data);
    }
    if (data.traverse_children) {
      auto children = get_node2_druntime(current).children;
      if (children.has_value()) {
        {
          double i = (children.length - 1.0);
          while ((i >= 0.0)) {
            {
              temp_stack[static_cast<size_t>(stack_length++)] = static_cast<Node2D>(children[static_cast<size_t>(i)]);
            }
            i -= 1.0;
          }
        }
      }
    }
  }
  ([&]() -> std::optional<auto> { auto optional_chain_receiver = clip_hooks; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().finalize; }())(state);
  if (has_dom_structure_changed(runtime, new_length, needs_reconcile)) {
    reconcile_dom_container(container, runtime, new_length);
  }
  swap_dom_order_lists(runtime, new_length);
}

} // namespace flighthq_scene2d_dom
