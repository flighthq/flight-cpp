// Generated by @flighthq/tool-compiler from @flighthq/camera/basis.ts#Basis. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_camera {

inline void get_camera3_dforward(Vector3Like out, Camera3D camera) {
  auto m = camera.view.m;
  out.x = -m[static_cast<size_t>(2.0)];
  out.y = -m[static_cast<size_t>(6.0)];
  out.z = -m[static_cast<size_t>(10.0)];
}

inline void get_camera3_dposition(Vector3Like out, Camera3D camera) {
  auto m = camera.view.m;
  auto m00 = m[static_cast<size_t>(0.0)];
  auto m01 = m[static_cast<size_t>(1.0)];
  auto m02 = m[static_cast<size_t>(2.0)];
  auto m10 = m[static_cast<size_t>(4.0)];
  auto m11 = m[static_cast<size_t>(5.0)];
  auto m12 = m[static_cast<size_t>(6.0)];
  auto m20 = m[static_cast<size_t>(8.0)];
  auto m21 = m[static_cast<size_t>(9.0)];
  auto m22 = m[static_cast<size_t>(10.0)];
  auto tx = m[static_cast<size_t>(12.0)];
  auto ty = m[static_cast<size_t>(13.0)];
  auto tz = m[static_cast<size_t>(14.0)];
  out.x = -(((m00 * tx) + (m01 * ty)) + (m02 * tz));
  out.y = -(((m10 * tx) + (m11 * ty)) + (m12 * tz));
  out.z = -(((m20 * tx) + (m21 * ty)) + (m22 * tz));
}

inline void get_camera3_dright(Vector3Like out, Camera3D camera) {
  auto m = camera.view.m;
  out.x = m[static_cast<size_t>(0.0)];
  out.y = m[static_cast<size_t>(4.0)];
  out.z = m[static_cast<size_t>(8.0)];
}

inline void get_camera3_dup(Vector3Like out, Camera3D camera) {
  auto m = camera.view.m;
  out.x = m[static_cast<size_t>(1.0)];
  out.y = m[static_cast<size_t>(5.0)];
  out.z = m[static_cast<size_t>(9.0)];
}

} // namespace flighthq_camera
