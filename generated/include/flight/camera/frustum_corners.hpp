// Generated by @flighthq/tool-compiler from @flighthq/camera/packages/camera/src/frustumCorners.ts#FrustumCorners at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <flight/structural_ref.hpp>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/geometry/matrix4.hpp>
#include <flight/types/camera3_d.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/matrix4.hpp>
#include <flight/types/vector3.hpp>
#include <flight/camera/camera.hpp>

namespace flight::camera {

inline flight::Ref<flight::types::Matrix4> scratch_view_projection = flight::geometry::create_matrix4();

inline flight::Ref<flight::types::Matrix4> scratch_inverse_vp = flight::geometry::create_matrix4();

inline bool get_camera3_dfrustum_corners(flight::Array<flight::Ref<flight::types::Vector3Like>> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Camera3D>>>> camera, double aspect) {
  flight::camera::get_camera3_dview_projection_matrix4(scratch_view_projection, camera, aspect);
  if (!flight::geometry::inverse_matrix4(scratch_inverse_vp, scratch_view_projection)) {
    return false;
  }
  flight::Float32Array m = scratch_inverse_vp->m;
  flight::Array<flight::Array<double>> ndc_corners = flight::Array<flight::Array<double>>{flight::Array<double>{-1.0, -1.0, -1.0}, flight::Array<double>{1.0, -1.0, -1.0}, flight::Array<double>{-1.0, 1.0, -1.0}, flight::Array<double>{1.0, 1.0, -1.0}, flight::Array<double>{-1.0, -1.0, 1.0}, flight::Array<double>{1.0, -1.0, 1.0}, flight::Array<double>{-1.0, 1.0, 1.0}, flight::Array<double>{1.0, 1.0, 1.0}};
  flight::Array<flight::Array<double>> results = flight::Array<flight::Array<double>>{};
  {
    double i = 0.0;
    while ((i < 8.0)) {
      {
        flight::Array<double> array_pattern_value = ndc_corners.element(i);
        const double nx = array_pattern_value.element(0.0);
        const double ny = array_pattern_value.element(1.0);
        const double nz = array_pattern_value.element(2.0);
        double wx = ((((m.element(0.0) * nx) + (m.element(4.0) * ny)) + (m.element(8.0) * nz)) + m.element(12.0));
        double wy = ((((m.element(1.0) * nx) + (m.element(5.0) * ny)) + (m.element(9.0) * nz)) + m.element(13.0));
        double wz = ((((m.element(2.0) * nx) + (m.element(6.0) * ny)) + (m.element(10.0) * nz)) + m.element(14.0));
        const double ww = ((((m.element(3.0) * nx) + (m.element(7.0) * ny)) + (m.element(11.0) * nz)) + m.element(15.0));
        if ((ww != 0.0)) {
          const double inv_w = (1.0 / ww);
          wx *= inv_w;
          wy *= inv_w;
          wz *= inv_w;
        }
        results.push(flight::Array{wx, wy, wz});
      }
      i += 1.0;
    }
  }
  {
    double i_2 = 0.0;
    while ((i_2 < 8.0)) {
      {
        out.element(i_2).x = results.element(i_2).element(0.0);
        out.element(i_2).y = results.element(i_2).element(1.0);
        out.element(i_2).z = results.element(i_2).element(2.0);
      }
      i_2 += 1.0;
    }
  }
  return true;
}

} // namespace flight::camera
