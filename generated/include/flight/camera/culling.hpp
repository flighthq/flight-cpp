// Generated by @flighthq/tool-compiler from @flighthq/camera/culling.ts#Culling. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "camera.hpp"

namespace flighthq_camera {

inline void get_camera3_dfrustum(FrustumLike out, Camera3D camera, double aspect) {
  get_camera3_dview_projection_matrix4(scratch_view_projection, camera, aspect);
  set_frustum_from_matrix4(out, scratch_view_projection);
}

inline bool is_box_in_camera3_dfrustum(Camera3D camera, AabbLike aabb, double aspect) {
  get_camera3_dfrustum(scratch_frustum, camera, aspect);
  return is_frustum_intersecting_aabb(scratch_frustum, aabb);
}

inline bool is_point_in_camera3_dfrustum(Camera3D camera, Vector3Like point, double aspect) {
  get_camera3_dfrustum(scratch_frustum, camera, aspect);
  return is_frustum_containing_point(scratch_frustum, point);
}

inline bool is_sphere_in_camera3_dfrustum(Camera3D camera, BoundingSphereLike sphere, double aspect) {
  get_camera3_dfrustum(scratch_frustum, camera, aspect);
  return is_frustum_intersecting_sphere(scratch_frustum, sphere);
}

inline auto scratch_view_projection = create_matrix4();

inline auto scratch_frustum = create_frustum();

} // namespace flighthq_camera
