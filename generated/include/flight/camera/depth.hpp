// Generated by @flighthq/tool-compiler from @flighthq/camera/depth.ts#Depth. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_camera {

inline double get_camera3_dlinear_depth(Camera3D camera, double ndc_z) {
  auto near = camera.near;
  auto far = camera.far;
  const double range = (far - near);
  if ((range == 0.0)) {
    return 0.0;
  }
  if ((camera.projection.kind == flight::String("orthographic"))) {
    return -(near + (((ndc_z + 1.0) * range) / 2.0));
  }
  const double denominator = ((ndc_z * range) - (far + near));
  if ((denominator == 0.0)) {
    return 0.0;
  }
  return (((2.0 * far) * near) / denominator);
}

inline double get_camera3_dview_space_z(Camera3D camera, double ndc_z) {
  return -get_camera3_dlinear_depth(camera, ndc_z);
}

} // namespace flighthq_camera
