// Generated by @flighthq/tool-compiler from @flighthq/camera/intersection.ts#Intersection. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "picking.hpp"

namespace flighthq_camera {

inline bool get_camera3_dray_through_bounding_sphere(Ray3DLike out, Camera3D camera, BoundingSphereLike sphere, double aspect) {
  if ((sphere.radius < 0.0)) {
    return false;
  }
  if (!get_camera3_dworld_to_screen(scratch_ndc, camera, sphere.center, aspect)) {
    return false;
  }
  return get_camera3_dscreen_to_world_ray(out, camera, scratch_ndc.x, scratch_ndc.y, aspect);
}

inline bool intersect_camera3_dray_with_plane(Vector3Like out, Ray3DLike ray, PlaneLike plane) {
  auto dx = ray.direction.x;
  auto dy = ray.direction.y;
  auto dz = ray.direction.z;
  auto ox = ray.origin.x;
  auto oy = ray.origin.y;
  auto oz = ray.origin.z;
  auto a = plane.a;
  auto b = plane.b;
  auto c = plane.c;
  auto d = plane.d;
  const double denom = (((a * dx) + (b * dy)) + (c * dz));
  if ((denom == 0.0)) {
    return false;
  }
  const double t = (-((((a * ox) + (b * oy)) + (c * oz)) + d) / denom);
  if ((t < 0.0)) {
    return false;
  }
  out.x = (ox + (t * dx));
  out.y = (oy + (t * dy));
  out.z = (oz + (t * dz));
  return true;
}

inline Vector3Like scratch_ndc = {.x = 0.0, .y = 0.0, .z = 0.0};

} // namespace flighthq_camera
