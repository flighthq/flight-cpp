// Generated by @flighthq/tool-compiler from @flighthq/camera/packages/camera/src/intersection.ts#Intersection at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <flight/structural_ref.hpp>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bounding_sphere.hpp>
#include <flight/types/camera3_d.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/plane.hpp>
#include <flight/types/ray3_d.hpp>
#include <flight/types/vector3.hpp>
#include <flight/camera/picking.hpp>

namespace flight::camera {

inline bool intersect_camera3_dray_with_plane(flight::Ref<flight::types::Vector3Like> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Ray3DLike>>>> ray, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::PlaneLike>>>> plane) {
  const double dx = flight::row_get<flight::RowKey<"direction">>(ray)->x;
  const double dy = flight::row_get<flight::RowKey<"direction">>(ray)->y;
  const double dz = flight::row_get<flight::RowKey<"direction">>(ray)->z;
  const double ox = flight::row_get<flight::RowKey<"origin">>(ray)->x;
  const double oy = flight::row_get<flight::RowKey<"origin">>(ray)->y;
  const double oz = flight::row_get<flight::RowKey<"origin">>(ray)->z;
  const double a = flight::row_get<flight::RowKey<"a">>(plane);
  const double b = flight::row_get<flight::RowKey<"b">>(plane);
  const double c = flight::row_get<flight::RowKey<"c">>(plane);
  const double d = flight::row_get<flight::RowKey<"d">>(plane);
  const double denom = (((a * dx) + (b * dy)) + (c * dz));
  if ((denom == 0.0)) {
    return false;
  }
  const double t = (-((((a * ox) + (b * oy)) + (c * oz)) + d) / denom);
  if ((t < 0.0)) {
    return false;
  }
  out->x = (ox + (t * dx));
  out->y = (oy + (t * dy));
  out->z = (oz + (t * dz));
  return true;
}

inline flight::Ref<flight::types::Vector3Like> scratch_ndc = flight::make_ref<flight::types::Vector3Like>(flight::types::Vector3Like{.x = 0.0, .y = 0.0, .z = 0.0});

inline bool get_camera3_dray_through_bounding_sphere(flight::Ref<flight::types::Ray3DLike> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Camera3D>>>> camera, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BoundingSphereLike>>>> sphere, double aspect) {
  if ((flight::row_get<flight::RowKey<"radius">>(sphere) < 0.0)) {
    return false;
  }
  if (!flight::camera::get_camera3_dworld_to_screen(scratch_ndc, camera, flight::row_get<flight::RowKey<"center">>(sphere), aspect)) {
    return false;
  }
  return flight::camera::get_camera3_dscreen_to_world_ray(out, camera, scratch_ndc->x, scratch_ndc->y, aspect);
}

} // namespace flight::camera
