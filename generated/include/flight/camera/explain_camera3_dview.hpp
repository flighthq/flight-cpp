// Generated by @flighthq/tool-compiler from @flighthq/camera/packages/camera/src/explainCamera3DView.ts#ExplainCamera3DView at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/camera3_d.hpp>
#include <flight/types/camera3_dview_explanation.hpp>
#include <flight/types/matrix4.hpp>
#include <flight/types/vector2.hpp>

namespace flight::camera {

inline const double orthonormal_tolerance = 0.001;

struct x_y_z : public flight::ReferenceEnabled {
  double x;
  double y;
  double z;
};

inline flight::Ref<flight::types::Camera3DViewExplanation> explain_camera3_dview(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Camera3D>>>> camera) {
  flight::Float32Array m = flight::row_get<flight::RowKey<"view">>(camera)->m;
  flight::Ref<x_y_z> x = flight::make_ref<x_y_z>(x_y_z{.x = m.element(0.0), .y = m.element(1.0), .z = m.element(2.0)});
  flight::Ref<x_y_z> y = flight::make_ref<x_y_z>(x_y_z{.x = m.element(4.0), .y = m.element(5.0), .z = m.element(6.0)});
  flight::Ref<x_y_z> z = flight::make_ref<x_y_z>(x_y_z{.x = m.element(8.0), .y = m.element(9.0), .z = m.element(10.0)});
  const double length_x = std::hypot(x->x, x->y, x->z);
  const double length_y = std::hypot(y->x, y->y, y->z);
  const double length_z = std::hypot(z->x, z->y, z->z);
  const double determinant = (((m.element(0.0) * ((m.element(5.0) * m.element(10.0)) - (m.element(6.0) * m.element(9.0)))) - (m.element(4.0) * ((m.element(1.0) * m.element(10.0)) - (m.element(2.0) * m.element(9.0))))) + (m.element(8.0) * ((m.element(1.0) * m.element(6.0)) - (m.element(2.0) * m.element(5.0)))));
  auto scale_deviation = flight::maximum(std::abs((length_x - 1.0)), std::abs((length_y - 1.0)), std::abs((length_z - 1.0)));
  auto shear_deviation = flight::maximum(std::abs((((x->x * y->x) + (x->y * y->y)) + (x->z * y->z))), std::abs((((x->x * z->x) + (x->y * z->y)) + (x->z * z->z))), std::abs((((y->x * z->x) + (y->y * z->y)) + (y->z * z->z))));
  return flight::make_ref<flight::types::Camera3DViewExplanation>(flight::types::Camera3DViewExplanation{.determinant = determinant, .is_orthonormal = ((scale_deviation <= orthonormal_tolerance) && (shear_deviation <= orthonormal_tolerance)), .is_reflection = (determinant < 0.0), .scale_deviation = scale_deviation, .shear_deviation = shear_deviation});
}

} // namespace flight::camera
