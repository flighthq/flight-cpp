// Generated by @flighthq/tool-compiler from @flighthq/camera/explainCamera3DView.ts#ExplainCamera3DView. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_camera {

struct x_y_z {
  auto x;
  auto y;
  auto z;
};

inline Camera3DViewExplanation explain_camera3_dview(Camera3D camera) {
  auto m = camera.view.m;
  x_y_z x = {.x = m[static_cast<size_t>(0.0)], .y = m[static_cast<size_t>(1.0)], .z = m[static_cast<size_t>(2.0)]};
  x_y_z y = {.x = m[static_cast<size_t>(4.0)], .y = m[static_cast<size_t>(5.0)], .z = m[static_cast<size_t>(6.0)]};
  x_y_z z = {.x = m[static_cast<size_t>(8.0)], .y = m[static_cast<size_t>(9.0)], .z = m[static_cast<size_t>(10.0)]};
  auto length_x = cmath.hypot(x.x, x.y, x.z);
  auto length_y = cmath.hypot(y.x, y.y, y.z);
  auto length_z = cmath.hypot(z.x, z.y, z.z);
  const double determinant = (((m[static_cast<size_t>(0.0)] * ((m[static_cast<size_t>(5.0)] * m[static_cast<size_t>(10.0)]) - (m[static_cast<size_t>(6.0)] * m[static_cast<size_t>(9.0)]))) - (m[static_cast<size_t>(4.0)] * ((m[static_cast<size_t>(1.0)] * m[static_cast<size_t>(10.0)]) - (m[static_cast<size_t>(2.0)] * m[static_cast<size_t>(9.0)])))) + (m[static_cast<size_t>(8.0)] * ((m[static_cast<size_t>(1.0)] * m[static_cast<size_t>(6.0)]) - (m[static_cast<size_t>(2.0)] * m[static_cast<size_t>(5.0)]))));
  const double scale_deviation = std::max(std::abs((length_x - 1.0)), std::abs((length_y - 1.0)), std::abs((length_z - 1.0)));
  const double shear_deviation = std::max(std::abs((((x.x * y.x) + (x.y * y.y)) + (x.z * y.z))), std::abs((((x.x * z.x) + (x.y * z.y)) + (x.z * z.z))), std::abs((((y.x * z.x) + (y.y * z.y)) + (y.z * z.z))));
  return {.determinant = determinant, .is_orthonormal = ((scale_deviation <= orthonormal_tolerance) && (shear_deviation <= orthonormal_tolerance)), .is_reflection = (determinant < 0.0), .scale_deviation = scale_deviation, .shear_deviation = shear_deviation};
}

inline const double orthonormal_tolerance = 0.001;

} // namespace flighthq_camera
