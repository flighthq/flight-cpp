// Generated by @flighthq/tool-compiler from @flighthq/camera/packages/camera/src/picking.ts#Picking at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <flight/structural_ref.hpp>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/geometry/matrix4.hpp>
#include <flight/geometry/vector3.hpp>
#include <flight/types/camera3_d.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/matrix4.hpp>
#include <flight/types/ray3_d.hpp>
#include <flight/types/vector3.hpp>
#include <flight/camera/camera.hpp>

namespace flight::camera {

inline flight::Ref<flight::types::Matrix4> scratch_view_projection = flight::geometry::create_matrix4();

inline bool get_camera3_dworld_to_screen(flight::Ref<flight::types::Vector3Like> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Camera3D>>>> camera, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>> world_point, double aspect) {
  flight::camera::get_camera3_dview_projection_matrix4(scratch_view_projection, camera, aspect);
  flight::Float32Array m = scratch_view_projection->m;
  const double wx = flight::row_get<flight::RowKey<"x">>(world_point);
  const double wy = flight::row_get<flight::RowKey<"y">>(world_point);
  const double wz = flight::row_get<flight::RowKey<"z">>(world_point);
  const double clip_x = ((((m.element(0.0) * wx) + (m.element(4.0) * wy)) + (m.element(8.0) * wz)) + m.element(12.0));
  const double clip_y = ((((m.element(1.0) * wx) + (m.element(5.0) * wy)) + (m.element(9.0) * wz)) + m.element(13.0));
  const double clip_z = ((((m.element(2.0) * wx) + (m.element(6.0) * wy)) + (m.element(10.0) * wz)) + m.element(14.0));
  const double clip_w = ((((m.element(3.0) * wx) + (m.element(7.0) * wy)) + (m.element(11.0) * wz)) + m.element(15.0));
  if ((clip_w <= 0.0)) {
    return false;
  }
  const double inv_w = (1.0 / clip_w);
  out->x = (clip_x * inv_w);
  out->y = (clip_y * inv_w);
  out->z = (clip_z * inv_w);
  return true;
}

inline flight::Ref<flight::types::Matrix4> scratch_inverse_vp = flight::geometry::create_matrix4();

inline flight::Ref<flight::types::Vector3> scratch_near = flight::geometry::create_vector3();

inline flight::Ref<flight::types::Vector3> scratch_far = flight::geometry::create_vector3();

inline flight::Ref<flight::types::Vector3> scratch_dir = flight::geometry::create_vector3();

inline bool get_camera3_dscreen_to_world_ray(flight::Ref<flight::types::Ray3DLike> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Camera3D>>>> camera, double ndc_x, double ndc_y, double aspect) {
  flight::camera::get_camera3_dview_projection_matrix4(scratch_view_projection, camera, aspect);
  if (!flight::geometry::inverse_matrix4(scratch_inverse_vp, scratch_view_projection)) {
    return false;
  }
  flight::Float32Array m = scratch_inverse_vp->m;
  const double nx = ndc_x;
  const double ny = ndc_y;
  double near_x = ((((m.element(0.0) * nx) + (m.element(4.0) * ny)) + (m.element(8.0) * -1.0)) + m.element(12.0));
  double near_y = ((((m.element(1.0) * nx) + (m.element(5.0) * ny)) + (m.element(9.0) * -1.0)) + m.element(13.0));
  double near_z = ((((m.element(2.0) * nx) + (m.element(6.0) * ny)) + (m.element(10.0) * -1.0)) + m.element(14.0));
  const double near_w = ((((m.element(3.0) * nx) + (m.element(7.0) * ny)) + (m.element(11.0) * -1.0)) + m.element(15.0));
  if ((near_w != 0.0)) {
    const double inv_w = (1.0 / near_w);
    near_x *= inv_w;
    near_y *= inv_w;
    near_z *= inv_w;
  }
  double far_x = ((((m.element(0.0) * nx) + (m.element(4.0) * ny)) + m.element(8.0)) + m.element(12.0));
  double far_y = ((((m.element(1.0) * nx) + (m.element(5.0) * ny)) + m.element(9.0)) + m.element(13.0));
  double far_z = ((((m.element(2.0) * nx) + (m.element(6.0) * ny)) + m.element(10.0)) + m.element(14.0));
  const double far_w = ((((m.element(3.0) * nx) + (m.element(7.0) * ny)) + m.element(11.0)) + m.element(15.0));
  if ((far_w != 0.0)) {
    const double inv_w_2 = (1.0 / far_w);
    far_x *= inv_w_2;
    far_y *= inv_w_2;
    far_z *= inv_w_2;
  }
  scratch_near->x = near_x;
  scratch_near->y = near_y;
  scratch_near->z = near_z;
  scratch_far->x = far_x;
  scratch_far->y = far_y;
  scratch_far->z = far_z;
  flight::geometry::subtract_vector3(scratch_dir, scratch_far, scratch_near);
  flight::geometry::normalize_vector3(scratch_dir, scratch_dir);
  out->origin->x = near_x;
  out->origin->y = near_y;
  out->origin->z = near_z;
  out->direction->x = scratch_dir->x;
  out->direction->y = scratch_dir->y;
  out->direction->z = scratch_dir->z;
  return true;
}

} // namespace flight::camera
