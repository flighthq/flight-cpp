// Generated by @flighthq/tool-compiler from @flighthq/text-markup/enableTextMarkupGuards.ts#EnableTextMarkupGuards. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "text_markup_guards.hpp"

namespace flighthq_text_markup {

inline void disable_text_markup_guards() {
  set_text_markup_guard(nullptr);
}

inline void enable_text_markup_guards() {
  set_text_markup_guard(warn_on_text_markup_issue);
}

inline void warn_on_text_markup_issue(TextMarkupIssue issue) {
  {
    auto switch_value = issue.kind;
    if (switch_value == flight::String("unknown-tag")) {
      log_once(flight::String("text-markup:unknown-tag:") + flight::to_string(issue.tag) + flight::String(""), log_level.warn, {.message = flight::String("parseTextMarkup: <") + flight::to_string(issue.tag) + flight::String("> has no registered handler, so the tag was dropped while its text was kept. Register it with registerMarkupTag(registry, '") + flight::to_string(issue.tag) + flight::String("', handler), or remove it from the source markup.")}, flight::String("text-markup"));
    }
    else if (switch_value == flight::String("unresolved-font-color")) {
      log_once(flight::String("text-markup:unresolved-font-color:") + flight::to_string(issue.value.value_or(flight::String(""))) + flight::String(""), log_level.warn, {.message = flight::String("parseTextMarkup: <font color=\"") + flight::to_string(issue.value.value_or(flight::String(""))) + flight::String("\"> did not resolve, so that color was ignored. Register named colors with registerMarkupNamedColors(registry), assign registry.colorResolver, or use #rgb, #rrggbb, or 0xRRGGBB.")}, flight::String("text-markup"));
    }
    else if (switch_value == flight::String("unsafe-href")) {
      log_once(flight::String("text-markup:unsafe-href:") + flight::to_string(issue.value.value_or(flight::String(""))) + flight::String(""), log_level.warn, {.message = flight::String("parseTextMarkup: <a href=\"") + flight::to_string(issue.value.value_or(flight::String(""))) + flight::String("\"> uses an unsafe URL scheme, so the href was dropped. Use an http, https, mailto, tel, relative, or fragment URL instead.")}, flight::String("text-markup"));
    }
  }
}

} // namespace flighthq_text_markup
