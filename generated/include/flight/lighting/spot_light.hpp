// Generated by @flighthq/tool-compiler from @flighthq/lighting/spotLight.ts#SpotLight. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_lighting {

inline SpotLight clone_spot_light(SpotLight source) {
  auto out = allocate_entity<SpotLight>();
  out.casts_shadow = source.casts_shadow;
  out.color = source.color;
  out.decay = source.decay;
  out.direction = clone_vector3(source.direction);
  out.enabled = source.enabled;
  out.inner_cone_cos = source.inner_cone_cos;
  out.intensity = source.intensity;
  out.intensity_unit = source.intensity_unit;
  out.layer_mask = source.layer_mask;
  out.priority = source.priority;
  out.kind = spot_light_kind;
  out.normal_bias = source.normal_bias;
  out.outer_cone_cos = source.outer_cone_cos;
  out.pcf_radius = source.pcf_radius;
  out.position = clone_vector3(source.position);
  out.range = source.range;
  out.shadow_bias = source.shadow_bias;
  out.shadow_far = source.shadow_far;
  out.shadow_map_size = source.shadow_map_size;
  out.shadow_near = source.shadow_near;
  out.shadow_strength = source.shadow_strength;
  out.spot_blend = source.spot_blend;
  return finish_entity(out);
}

inline SpotLight create_spot_light(std::optional<SpotLightOptions> options = std::nullopt) {
  auto light = allocate_entity<SpotLight>();
  initialize_spot_light(light, options.value());
  return light;
}

inline void get_spot_light_cone_degrees(SpotLightConeAngles out, SpotLight source) {
  out.inner_degrees = ((cmath.acos(source.inner_cone_cos) * 180.0) / flight::pi);
  out.outer_degrees = ((cmath.acos(source.outer_cone_cos) * 180.0) / flight::pi);
}

inline void initialize_spot_light(EntityConstruction<SpotLight> light, std::optional<SpotLightOptions> options = std::nullopt) {
  auto position = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().position; }());
  auto direction = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().direction; }());
  light.casts_shadow = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().casts_shadow; }()).value_or(false);
  light.color = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().color; }()).value_or(4294967295.0);
  light.decay = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().decay; }()).value_or(2.0);
  light.direction = (direction ? clone_vector3(direction) : create_vector3(0.0, -1.0, 0.0));
  light.enabled = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().enabled; }()).value_or(true);
  light.inner_cone_cos = 1.0;
  light.intensity = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().intensity; }()).value_or(1.0);
  light.intensity_unit = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().intensity_unit; }()).value_or(unitless_light_unit);
  light.layer_mask = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().layer_mask; }()).value_or(all_light_layers);
  light.priority = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().priority; }()).value_or(0.0);
  light.kind = spot_light_kind;
  light.normal_bias = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().normal_bias; }()).value_or(0.0);
  light.outer_cone_cos = 1.0;
  light.pcf_radius = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().pcf_radius; }()).value_or(0.0);
  light.position = (position ? clone_vector3(position) : create_vector3(0.0, 0.0, 0.0));
  light.range = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().range; }()).value_or(-1.0);
  light.shadow_bias = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().shadow_bias; }()).value_or(0.0);
  light.shadow_far = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().shadow_far; }()).value_or(500.0);
  light.shadow_map_size = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().shadow_map_size; }()).value_or(1024.0);
  light.shadow_near = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().shadow_near; }()).value_or(0.5);
  light.shadow_strength = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().shadow_strength; }()).value_or(1.0);
  light.spot_blend = 0.0;
  set_spot_light_cone(light, ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().inner_cone_degrees; }()).value_or(0.0), ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().outer_cone_degrees; }()).value_or(45.0));
  set_spot_light_blend(light, ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().spot_blend; }()).value_or(0.0));
}

inline void set_spot_light_blend(SpotLight out, double blend) {
  out.spot_blend = std::max(0.0, std::min(1.0, blend));
}

inline void set_spot_light_cone(SpotLight out, double inner_degrees, double outer_degrees) {
  out.inner_cone_cos = std::cos(((inner_degrees * flight::pi) / 180.0));
  out.outer_cone_cos = std::cos(((outer_degrees * flight::pi) / 180.0));
}

inline void set_spot_light_direction(SpotLight out, double x, double y, double z) {
  const double lx = x;
  const double ly = y;
  const double lz = z;
  const double len = std::sqrt((((lx * lx) + (ly * ly)) + (lz * lz)));
  if ((len > 0.0)) {
    set_vector3(out.direction, (lx / len), (ly / len), (lz / len));
  }
}

inline void set_spot_light_target(SpotLight out, double target_x, double target_y, double target_z) {
  auto px = out.position.x;
  auto py = out.position.y;
  auto pz = out.position.z;
  const double dx = (target_x - px);
  const double dy = (target_y - py);
  const double dz = (target_z - pz);
  const double len = std::sqrt((((dx * dx) + (dy * dy)) + (dz * dz)));
  if ((len > 0.0)) {
    set_vector3(out.direction, (dx / len), (dy / len), (dz / len));
  }
}

} // namespace flighthq_lighting
