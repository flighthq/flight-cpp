// Generated by @flighthq/tool-compiler from @flighthq/clip/enableClipGuards.ts#EnableClipGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "clip_region.hpp"

namespace flighthq_clip {

inline void disable_clip_guards() {
  set_clip_region_contours_guard(nullptr);
  set_clip_region_release_guard(nullptr);
  set_clip_region_use_guard(nullptr);
}

inline void enable_clip_guards() {
  set_clip_region_contours_guard(warn_on_invalid_contours);
  set_clip_region_release_guard(warn_on_double_release);
  set_clip_region_use_guard(warn_on_use_after_release);
}

inline void warn_on_invalid_contours(ClipRegionContoursExplanation explanation) {
  log_once(flight::String("clip:invalid-contour:") + flight::to_string(explanation.reason) + flight::String(""), log_level.warn, {.message = flight::String("createClipRegionFromContours: contour ") + flight::to_string(explanation.contour_index) + flight::String(" has ") + flight::to_string(explanation.coordinate_count) + flight::String(" coordinates (") + flight::to_string(explanation.reason) + flight::String("). Contours require at least three complete x/y pairs.")}, flight::String("clip"));
}

inline void warn_on_double_release(ClipRegion clip) {
  log_once(flight::String("clip:double-release"), log_level.warn, {.message = flight::String("releaseClipRegion: this region is already in the pool, so it is being released twice. Two later acquireClipRegion calls will hand back the same object and the clips will alias each other. Every acquireClipRegion pairs with exactly one releaseClipRegion, and the region must not be used after release.")}, flight::String("clip"));
}

inline void warn_on_use_after_release(ClipRegion clip) {
  log_once(flight::String("clip:use-after-release"), log_level.warn, {.message = flight::String("A released ClipRegion was used while it was still in the pool. Do not retain or access a region after releaseClipRegion; acquire a new region before use.")}, flight::String("clip"));
}

} // namespace flighthq_clip
