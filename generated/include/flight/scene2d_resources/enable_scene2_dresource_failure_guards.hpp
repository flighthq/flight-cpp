// Generated by @flighthq/tool-compiler from @flighthq/scene2d-resources/enableScene2DResourceFailureGuards.ts#EnableScene2DResourceFailureGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "scene2_dresource_diagnostics.hpp"

namespace flighthq_scene2d_resources {

inline bool are_scene2_dresource_failure_guards_enabled() {
  return enabled;
}

inline void disable_scene2_dresource_failure_guards() {
  set_scene2_dresource_failure_guard(nullptr);
  enabled = false;
}

inline void enable_scene2_dresource_failure_guards() {
  set_scene2_dresource_failure_guard(warn_on_scene2_dresource_failure);
  enabled = true;
}

inline void warn_on_scene2_dresource_failure(Scene2DResourceFailureNotice notice) {
  flight::String message;
  if ((notice.reason == flight::String("audio-resources-unresolved"))) {
    message = flight::String("Scene2D audio resource loading left selected references unresolved — inspect result.unresolved and call explainAudioResourceReferenceResolution for each failure");
  }
  else {
    if ((notice.reason == flight::String("document-fetch-failed"))) {
      message = flight::String("Scene2D document acquisition returned null — verify the URL and the caller-supplied fetch seam");
    }
    else {
      if ((notice.reason == flight::String("document-import-failed"))) {
        message = flight::String("A matching Scene2D document importer rejected the acquired bytes — verify the payload and MIME hint");
      }
      else {
        if ((notice.reason == flight::String("document-importer-missing"))) {
          message = flight::String("No registered Scene2D document importer matched the bytes — register the required format importer");
        }
        else {
          if ((notice.reason == flight::String("image-resources-unresolved"))) {
            message = flight::String("Scene2D image resource loading left selected references unresolved — inspect result.unresolved and call explainImageResourceReferenceResolution for each failure");
          }
          else {
            message = flight::String("Scene2D slot resolution left required slots unresolved — inspect result.unresolved and supply content for each required reference");
          }
        }
      }
    }
  }
  log_once(flight::String("scene2d-resources:") + flight::to_string(notice.reason) + flight::String(""), log_level.warn, {.message = message}, flight::String("scene2d-resources"));
}

inline bool enabled = false;

} // namespace flighthq_scene2d_resources
