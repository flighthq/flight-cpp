// Generated by @flighthq/tool-compiler from @flighthq/scene2d-resources/scene2DResourceDiagnostics.ts#Scene2DResourceDiagnostics. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_scene2d_resources {

struct resolved_total {
  double resolved;
  double total;
};

struct state {
  flight::String state;
};

inline Scene2DResourceCoverageExplanation explain_scene2_dresource_coverage(Scene2DDocument document) {
  Scene2DResourceCoverageCount audio_resources = count_resolved(document.audio_resources);
  Scene2DResourceCoverageCount image_resources = count_resolved(document.image_resources);
  double required_slot_total = 0.0;
  double required_slot_resolved = 0.0;
  {
    double i = 0.0;
    while ((i < document.slots.length)) {
      {
        auto slot = document.slots[static_cast<size_t>(i)];
        if (!slot.required) {
          i += 1.0;
          continue;
        }
        required_slot_total++;
        if ((slot.content != nullptr)) {
          required_slot_resolved++;
        }
      }
      i += 1.0;
    }
  }
  resolved_total required_slots = {.resolved = required_slot_resolved, .total = required_slot_total};
  return {.audio_resources = audio_resources, .complete = (((audio_resources.resolved == audio_resources.total) && (image_resources.resolved == image_resources.total)) && (required_slots.resolved == required_slots.total)), .image_resources = image_resources, .required_slots = required_slots};
}

inline void report_scene2_dresource_failure(Scene2DResourceFailureNotice notice) {
  ([&]() -> std::optional<auto> { auto optional_chain_receiver = guard; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()(notice); }());
}

inline void set_scene2_dresource_failure_guard(std::optional<Scene2DResourceFailureGuard> guard) {
  guard = guard.value();
}

inline std::optional<Scene2DResourceFailureGuard> guard = nullptr;

inline Scene2DResourceCoverageCount count_resolved(flight::Array<state> references) {
  double resolved = 0.0;
  {
    double i = 0.0;
    while ((i < static_cast<double>(references.size()))) {
      {
        if ((references.element(i).state == resource_resolution_state.resolved)) {
          resolved++;
        }
      }
      i += 1.0;
    }
  }
  return {.resolved = resolved, .total = static_cast<double>(references.size())};
}

} // namespace flighthq_scene2d_resources
