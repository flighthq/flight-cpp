// Generated by @flighthq/tool-compiler from @flighthq/shading/getModifierDefineKey.ts#GetModifierDefineKey. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "modifier_registry.hpp"
#include "order_modifier_stack.hpp"

namespace flighthq_shading {

inline flight::String get_modifier_define_key(flight::Array<Modifier> stack, std::optional<ModifierRegistry> registry = std::nullopt) {
  auto ordered = order_modifier_stack(stack);
  flight::String key = flight::String("");
  for (auto modifier : ordered) {
    const flight::String signature = get_define_signature(modifier, registry.value());
    auto token = ((static_cast<double>(signature.length()) > 0.0) ? flight::String("") + flight::to_string(modifier.kind) + flight::String(":") + flight::to_string(signature) + flight::String("") : modifier.kind);
    key = ((static_cast<double>(key.length()) > 0.0) ? flight::String("") + flight::to_string(key) + flight::String("+") + flight::to_string(token) + flight::String("") : token);
  }
  return key;
}

inline flight::String get_define_signature(Modifier modifier, std::optional<ModifierRegistry> registry = std::nullopt) {
  if (!registry.value().has_value()) {
    return flight::String("");
  }
  auto definition = resolve_modifier(registry.value(), modifier.kind);
  if ((!definition.has_value() || (definition.get_define_signature == undefined))) {
    return flight::String("");
  }
  return definition.get_define_signature(modifier);
}

} // namespace flighthq_shading
