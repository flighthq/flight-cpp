// Generated by @flighthq/tool-compiler from @flighthq/adjustments/packages/adjustments/src/hueSaturationAdjustment.ts#HueSaturationAdjustment at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <functional>
#include <optional>
#include <random>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/entity/entity.hpp>
#include <flight/types/adjustment_kind.hpp>
#include <flight/types/color_transform_function.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/hue_saturation_adjustment.hpp>
#include <flight/adjustments/color_lut_adjustment.hpp>

namespace flight::adjustments {

inline double clamp01(double v) {
  return ((v < 0.0) ? 0.0 : ((v > 1.0) ? 1.0 : v));
}

inline double fract(double v) {
  return (v - std::floor(v));
}

inline double hue2rgb(double p, double q, double t_raw) {
  double t = t_raw;
  if ((t < 0.0)) {
    t += 1.0;
  }
  if ((t > 1.0)) {
    t -= 1.0;
  }
  if ((t < (1.0 / 6.0))) {
    return (p + (((q - p) * 6.0) * t));
  }
  if ((t < (1.0 / 2.0))) {
    return q;
  }
  if ((t < (2.0 / 3.0))) {
    return (p + (((q - p) * ((2.0 / 3.0) - t)) * 6.0));
  }
  return p;
}

inline void initialize_hue_saturation_adjustment(flight::types::EntityConstruction<flight::Ref<flight::types::HueSaturationAdjustment>> out, std::optional<flight::Ref<flight::types::HueSaturationAdjustment>> options = std::nullopt) {
  options = options.value_or(flight::make_ref<flight::types::HueSaturationAdjustment>(flight::types::HueSaturationAdjustment{}));
  const double hue = (options.value()->hue.value_or(0.0) / 360.0);
  auto saturation = options.value()->saturation.value_or(1.0);
  auto lightness = options.value()->lightness.value_or(0.0);
  flight::types::ColorTransformFunction transform = [=](flight::Array<double> out, double r, double g, double b) {
  auto mx = flight::maximum(r, g, b);
  auto mn = flight::minimum(r, g, b);
  double h = 0.0;
  double s = 0.0;
  const double l = ((mx + mn) * 0.5);
  const double d = (mx - mn);
  if ((d > 0.0001)) {
    s = ((l < 0.5) ? (d / (mx + mn)) : (d / ((2.0 - mx) - mn)));
    if ((mx == r)) {
      h = (((g - b) / d) + ((g < b) ? 6.0 : 0.0));
    }
    else {
      if ((mx == g)) {
        h = (((b - r) / d) + 2.0);
      }
      else {
        h = (((r - g) / d) + 4.0);
      }
    }
    h /= 6.0;
  }
  h = fract((h + hue));
  s = clamp01((s * saturation));
  const double ln = clamp01((l + lightness));
  if ((s <= 0.0)) {
    out.element(0.0) = ln;
    out.element(1.0) = ln;
    out.element(2.0) = ln;
    return;
  }
  const double q = ((ln < 0.5) ? (ln * (1.0 + s)) : ((ln + s) - (ln * s)));
  const double p = ((2.0 * ln) - q);
  out.element(0.0) = hue2rgb(p, q, (h + (1.0 / 3.0)));
  out.element(1.0) = hue2rgb(p, q, h);
  out.element(2.0) = hue2rgb(p, q, (h - (1.0 / 3.0)));
  throw std::logic_error("Flight function completed without a value");
};
  flight::adjustments::initialize_color_lut_adjustment(out, flight::String("HueSaturationAdjustment"), transform);
  flight::row_set<flight::RowKey<"hue">>(out, std::optional<double>{options.value()->hue.value_or(0.0)});
  flight::row_set<flight::RowKey<"saturation">>(out, std::optional<double>{options.value()->saturation.value_or(1.0)});
  flight::row_set<flight::RowKey<"lightness">>(out, std::optional<double>{options.value()->lightness.value_or(0.0)});
}

inline flight::Ref<flight::types::HueSaturationAdjustment> create_hue_saturation_adjustment(std::optional<flight::Ref<flight::types::HueSaturationAdjustment>> options = std::nullopt) {
  options = options.value_or(flight::make_ref<flight::types::HueSaturationAdjustment>(flight::types::HueSaturationAdjustment{}));
  flight::types::EntityConstruction<flight::Ref<flight::types::HueSaturationAdjustment>> out = flight::entity::allocate_entity<flight::Ref<flight::types::HueSaturationAdjustment>>();
  initialize_hue_saturation_adjustment(out, options.value());
  return flight::entity::finish_entity(out);
}

} // namespace flight::adjustments
