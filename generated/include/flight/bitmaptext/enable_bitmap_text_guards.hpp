// Generated by @flighthq/tool-compiler from @flighthq/bitmaptext/enableBitmapTextGuards.ts#EnableBitmapTextGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "update_bitmap_text.hpp"

namespace flighthq_bitmaptext {

inline void disable_bitmap_text_guards() {
  set_bitmap_text_layout_guard(nullptr);
  set_bitmap_text_missing_glyph_guard(nullptr);
}

inline void enable_bitmap_text_guards() {
  set_bitmap_text_layout_guard(warn_on_bitmap_text_layout_blocked);
  set_bitmap_text_missing_glyph_guard(warn_on_bitmap_text_missing_glyph);
}

inline void warn_on_bitmap_text_missing_glyph(double codepoint) {
  log_once(flight::String("bitmaptext:missing-glyph:") + flight::to_string(codepoint) + flight::String(""), log_level.warn, {.message = flight::String("updateBitmapText: no glyph for codepoint U+") + flight::to_string(flight::to_string(codepoint).to_upper().pad_start(4.0, flight::String("0"))) + flight::String(" ") + flight::String("(") + flight::to_string(flight::String.from_code_point(codepoint)) + flight::String(") — the glyph source does not contain this character, so it is ") + flight::String("skipped during layout. Use explainBitmapTextMissingGlyphs for a full inventory.")}, flight::String("bitmaptext"));
}

inline void warn_on_bitmap_text_layout_blocked(flight::String reason, double attempts) {
  log_once(flight::String("bitmaptext:layout-did-not-converge"), log_level.warn, {.message = flight::String("updateBitmapText: the glyph source repacked during all ") + flight::to_string(attempts) + flight::String(" layout passes, so this text ") + flight::String("never settled — its glyphs cannot all be resident in the atlas at once, and the quads drawn from ") + flight::String("the final pass sample stale rects. Enlarge the glyph atlas or reduce the font size; ") + flight::String("enableGlyphAtlasGuards reports the repacks from the atlas side.")}, flight::String("bitmaptext"));
}

} // namespace flighthq_bitmaptext
