// Generated by @flighthq/tool-compiler from @flighthq/scene3d-resources/sceneResourceRecovery.ts#SceneResourceRecovery. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "get_scene3_dresource_textures.hpp"
#include "resolve_scene3_dresources.hpp"

namespace flighthq_scene3d_resources {

inline double retry_failed_scene3_dresources(Scene3D scene, Scene3DResourceResolverWithRuntime resolver, std::optional<UpdateScene3DResourceStreamingOptions> options = std::nullopt) {
  flight::Array<Texture> textures = flight::Array<Texture>{};
  get_scene3_dresource_textures(textures, scene);
  auto reset = flight::Set<ImageResourceReference>();
  {
    double i = 0.0;
    while ((i < static_cast<double>(textures.size()))) {
      {
        std::optional<Texture> texture = textures.get(i);
        auto ref = get_scene3_dtexture_resource_reference(scene, texture);
        if ((!ref.has_value() || reset.has(ref))) {
          i += 1.0;
          continue;
        }
        if (((([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().select; }()) != undefined) && !options.value().select(texture, ref))) {
          i += 1.0;
          continue;
        }
        if (reset_failed_image_resource_reference(ref)) {
          reset.add(ref);
        }
      }
      i += 1.0;
    }
  }
  update_scene3_dresource_streaming(scene, resolver, options.value());
  return reset.size;
}

} // namespace flighthq_scene3d_resources
