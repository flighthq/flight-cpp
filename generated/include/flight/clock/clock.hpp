// Generated by @flighthq/tool-compiler from @flighthq/clock/packages/clock/src/clock.ts#Clock at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cstddef>
#include <flight/structural_ref.hpp>
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { struct Clock; }
namespace flight::types { struct ClockOptions; }
namespace flight::types { struct Entity; }
namespace flight::types { template <typename T> struct Signal; }

#include <flight/entity/entity.hpp>
#include <flight/signals/emitter.hpp>
#include <flight/signals/slot.hpp>
#include <flight/types/clock.hpp>
#include <flight/types/clock_options.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/signal.hpp>

namespace flight::clock {

inline void advance_clock(flight::Ref<flight::types::Clock> clock, double delta_time) {
  const double scaled_delta = (clock->paused ? 0.0 : (delta_time * clock->scale));
  (clock->delta_time = scaled_delta);
  (clock->elapsed += scaled_delta);
  if (clock->on_tick.has_value()) {
    flight::signals::emit_signal<std::function<void(double)>>(clock->on_tick.value(), scaled_delta);
  }
  flight::Array<flight::Ref<flight::types::Clock>> children = clock->children;
  {
    double i = 0.0;
    while ((i < static_cast<double>(children.size()))) {
      {
        advance_clock(children.element(i), scaled_delta);
      }
      (i += 1.0);
    }
  }
}

inline double get_clock_effective_scale(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>> clock) {
  double scale = flight::row_get<flight::RowKey<"scale">>(clock);
  std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>> current = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>> { auto contextual_union_source = flight::row_get<flight::RowKey<"parent">>(clock); if (!contextual_union_source.has_value()) return std::nullopt; return std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>{flight::structural_ref_cast<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>(flight::StructuralRef<flight::RowWritable<flight::RowOf<flight::Ref<flight::types::Clock>>>>(contextual_union_source.value()))}; }());
  while (current.has_value()) {
    (scale *= flight::row_get<flight::RowKey<"scale">>(current.value()));
    (current = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>> { auto contextual_union_source_2 = flight::row_get<flight::RowKey<"parent">>(current.value()); if (!contextual_union_source_2.has_value()) return std::nullopt; return std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>{flight::structural_ref_cast<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>(flight::StructuralRef<flight::RowWritable<flight::RowOf<flight::Ref<flight::types::Clock>>>>(contextual_union_source_2.value()))}; }()));
  }
  return scale;
}

inline std::optional<flight::Ref<flight::types::Clock>> get_clock_parent(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>> clock) {
  return flight::row_get<flight::RowKey<"parent">>(clock);
}

inline void initialize_clock(flight::types::EntityConstruction<flight::Ref<flight::types::Clock>> out, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::ClockOptions>>>>> options = std::nullopt) {
  flight::row_set<flight::RowKey<"scale">>(out, ([&]() -> double { auto nullish_coalesce_left = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return flight::row_get<flight::RowKey<"scale">>(optional_chain_receiver.value()); }()); if (nullish_coalesce_left.has_value()) return nullish_coalesce_left.value(); return 1.0; }()));
  flight::row_set<flight::RowKey<"paused">>(out, ([&]() -> bool { auto nullish_coalesce_left = ([&]() -> std::optional<bool> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return flight::row_get<flight::RowKey<"paused">>(optional_chain_receiver.value()); }()); if (nullish_coalesce_left.has_value()) return nullish_coalesce_left.value(); return false; }()));
  flight::row_set<flight::RowKey<"deltaTime">>(out, 0.0);
  flight::row_set<flight::RowKey<"elapsed">>(out, 0.0);
  flight::row_set<flight::RowKey<"parent">>(out, std::nullopt);
  flight::row_set<flight::RowKey<"children">>(out, flight::Array<flight::Ref<flight::types::Clock>>{});
  flight::row_set<flight::RowKey<"onTick">>(out, std::nullopt);
}

inline flight::Ref<flight::types::Clock> create_clock(std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::ClockOptions>>>>> options = std::nullopt) {
  flight::types::EntityConstruction<flight::Ref<flight::types::Clock>> out = flight::entity::allocate_entity<flight::Ref<flight::types::Clock>>();
  initialize_clock(out, options);
  return flight::entity::finish_entity<flight::Ref<flight::types::Clock>>(out);
}

inline bool is_clock_effectively_paused(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>> clock) {
  std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>> current = std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>{clock};
  while (current.has_value()) {
    if (flight::row_get<flight::RowKey<"paused">>(current.value())) {
      return true;
    }
    (current = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>> { auto contextual_union_source_3 = flight::row_get<flight::RowKey<"parent">>(current.value()); if (!contextual_union_source_3.has_value()) return std::nullopt; return std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>{flight::structural_ref_cast<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Clock>>>>>(flight::StructuralRef<flight::RowWritable<flight::RowOf<flight::Ref<flight::types::Clock>>>>(contextual_union_source_3.value()))}; }()));
  }
  return false;
}

inline void pause_clock(flight::Ref<flight::types::Clock> clock) {
  (clock->paused = true);
}

inline void remove_clock_child(flight::Ref<flight::types::Clock> parent, flight::Ref<flight::types::Clock> child) {
  const double index = parent->children.index_of(child);
  if ((index == -1.0)) {
    return;
  }
  parent->children.splice(index, 1.0);
  (child->parent = std::nullopt);
}

inline void add_clock_child(flight::Ref<flight::types::Clock> parent, flight::Ref<flight::types::Clock> child) {
  if ((child->parent == parent)) {
    return;
  }
  if (child->parent.has_value()) {
    remove_clock_child(child->parent.value(), child);
  }
  (child->parent = std::optional<flight::Ref<flight::types::Clock>>{parent});
  parent->children.push(child);
}

inline flight::Ref<flight::types::Clock> create_child_clock(flight::Ref<flight::types::Clock> parent, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::ClockOptions>>>>> options = std::nullopt) {
  auto child = create_clock(options);
  add_clock_child(parent, child);
  return child;
}

inline void dispose_clock(flight::Ref<flight::types::Clock> clock) {
  if (clock->parent.has_value()) {
    remove_clock_child(clock->parent.value(), clock);
  }
  flight::Array<flight::Ref<flight::types::Clock>> children = clock->children;
  {
    double i = 0.0;
    while ((i < static_cast<double>(children.size()))) {
      {
        (children.element(i)->parent = std::nullopt);
      }
      (i += 1.0);
    }
  }
  ([&]() { auto&& assignment_receiver = children; const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
  if (clock->on_tick.has_value()) {
    flight::signals::clear_signal<std::function<void(double)>>(clock->on_tick.value());
  }
}

inline void reset_clock(flight::Ref<flight::types::Clock> clock) {
  (clock->elapsed = 0.0);
  (clock->delta_time = 0.0);
}

inline void resume_clock(flight::Ref<flight::types::Clock> clock) {
  (clock->paused = false);
}

inline void set_clock_scale(flight::Ref<flight::types::Clock> clock, double scale) {
  (clock->scale = scale);
}

} // namespace flight::clock
