// Generated by @flighthq/tool-compiler from @flighthq/path/pathMorph.ts#PathMorph. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "path_morph_geometry.hpp"

namespace flighthq_path {

inline std::optional<PathMorph> create_path_morph(Path start, Path end) {
  auto morph = build_path_morph(start, end).morph;
  if (!morph.has_value()) {
    return nullptr;
  }
  auto out = allocate_entity<PathMorph>();
  initialize_path_morph(out, morph);
  return finish_entity(out);
}

inline void initialize_path_morph(EntityConstruction<PathMorph> out, PathMorph morph) {
  out.commands = morph.commands;
  out.end_data = morph.end_data;
  out.start_data = morph.start_data;
  out.winding = morph.winding;
}

inline void sample_path_morph(Path out, PathMorph morph, double progress) {
  auto commands = morph.commands;
  out.commands.length = commands.length;
  {
    double i = 0.0;
    while ((i < commands.length)) {
      out.commands[static_cast<size_t>(i)] = commands[static_cast<size_t>(i)];
      i += 1.0;
    }
  }
  auto start_data = morph.start_data;
  auto end_data = morph.end_data;
  out.data.length = start_data.length;
  if (((progress == 0.0) || (progress == 1.0))) {
    auto endpoint = ((progress == 0.0) ? start_data : end_data);
    {
      double i_2 = 0.0;
      while ((i_2 < endpoint.length)) {
        out.data[static_cast<size_t>(i_2)] = endpoint[static_cast<size_t>(i_2)];
        i_2 += 1.0;
      }
    }
    out.winding = morph.winding;
    return;
  }
  {
    double i_3 = 0.0;
    while ((i_3 < start_data.length)) {
      {
        auto start = start_data[static_cast<size_t>(i_3)];
        auto end = end_data[static_cast<size_t>(i_3)];
        out.data[static_cast<size_t>(i_3)] = (start + ((end - start) * progress));
      }
      i_3 += 1.0;
    }
  }
  out.winding = morph.winding;
}

} // namespace flighthq_path
