// Generated by @flighthq/tool-compiler from @flighthq/path/tessellatePathTyped.ts#TessellatePathTyped. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "tessellate_path.hpp"

namespace flighthq_path {

inline PathMeshTyped tessellate_path_typed(Path path, std::optional<double> tolerance = std::nullopt) {
  tolerance = tolerance.value_or(0.25);
  auto mesh = tessellate_path(path, tolerance.value());
  return {.vertices = flight::Float32Array(mesh.vertices), .indices = flight::Uint32Array(mesh.indices)};
}

inline void tessellate_path_typed_into(Path path, PathMeshTyped out, std::optional<double> tolerance = std::nullopt) {
  tolerance = tolerance.value_or(0.25);
  auto mesh = tessellate_path(path, tolerance.value());
  auto v_len = mesh.vertices.length;
  auto i_len = mesh.indices.length;
  if ((out.vertices.length < v_len)) {
    out.vertices = flight::Float32Array(v_len);
  }
  if ((out.indices.length < i_len)) {
    out.indices = flight::Uint32Array(i_len);
  }
  {
    double i = 0.0;
    while ((i < v_len)) {
      out.vertices[static_cast<size_t>(i)] = mesh.vertices[static_cast<size_t>(i)];
      i += 1.0;
    }
  }
  {
    double i_2 = 0.0;
    while ((i_2 < i_len)) {
      out.indices[static_cast<size_t>(i_2)] = mesh.indices[static_cast<size_t>(i_2)];
      i_2 += 1.0;
    }
  }
}

} // namespace flighthq_path
