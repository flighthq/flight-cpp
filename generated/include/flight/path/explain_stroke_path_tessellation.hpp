// Generated by @flighthq/tool-compiler from @flighthq/path/explainStrokePathTessellation.ts#ExplainStrokePathTessellation. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "stroke_path_geometry.hpp"

namespace flighthq_path {

inline StrokePathTessellationExplanation explain_stroke_path_tessellation(Path path, StrokeStyle style, std::optional<double> tolerance = std::nullopt) {
  tolerance = tolerance.value_or(0.25);
  auto geometry = build_stroke_path_geometry(path, style, tolerance.value());
  return {.reason = get_reason(geometry.issue), .subpath = geometry.issue_subpath, .supported = (geometry.issue == stroke_path_tessellation_issue_none)};
}

inline StrokePathTessellationReason get_reason(double issue) {
  if ((issue == stroke_path_tessellation_issue_invalid_style)) {
    return flight::String("invalid-style");
  }
  if ((issue == stroke_path_tessellation_issue_invalid_path)) {
    return flight::String("invalid-path");
  }
  if ((issue == stroke_path_tessellation_issue_self_intersecting_centerline)) {
    return flight::String("self-intersecting-centerline");
  }
  if ((issue == stroke_path_tessellation_issue_reversing_join)) {
    return flight::String("reversing-join");
  }
  if ((issue == stroke_path_tessellation_issue_self_intersecting_outline)) {
    return flight::String("self-intersecting-outline");
  }
  return flight::String("ok");
}

} // namespace flighthq_path
