// Generated by @flighthq/tool-compiler from @flighthq/path/copyPath.ts#CopyPath. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_path {

inline Path clone_path(Path source) {
  return copy_path(source);
}

inline Path copy_path(Path source, std::optional<Path> out = std::nullopt) {
  if (!out.value().has_value()) {
    auto out_2 = allocate_entity<Path>();
    assign_path_fields(out_2, source.commands.slice(), source.data.slice(), source.winding);
    return finish_entity(out_2);
  }
  if ((out.value() != source)) {
    out.value().commands.length = 0.0;
    {
      double i = 0.0;
      while ((i < source.commands.length)) {
        out.value().commands.push(source.commands[static_cast<size_t>(i)]);
        i += 1.0;
      }
    }
    out.value().data.length = 0.0;
    {
      double i_2 = 0.0;
      while ((i_2 < source.data.length)) {
        out.value().data.push(source.data[static_cast<size_t>(i_2)]);
        i_2 += 1.0;
      }
    }
    out.value().winding = source.winding;
  }
  return out.value();
}

inline void assign_path_fields(EntityConstruction<Path> out, flight::Array<double> commands, flight::Array<double> data, PathWinding winding) {
  out.commands = commands;
  out.data = data;
  out.winding = winding;
}

} // namespace flighthq_path
