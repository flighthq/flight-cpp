// Generated by @flighthq/tool-compiler from @flighthq/path/getPathContourLengths.ts#GetPathContourLengths. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "flatten_path.hpp"

namespace flighthq_path {

inline flight::Array<double> get_path_contour_lengths(Path path, std::optional<double> tolerance = std::nullopt) {
  tolerance = tolerance.value_or(0.25);
  auto contours = flatten_path(path, tolerance.value());
  flight::Array<double> lengths = flight::Array<double>{};
  {
    double ci = 0.0;
    while ((ci < contours.length)) {
      {
        lengths.push(contour_length(contours[static_cast<size_t>(ci)]));
      }
      ci += 1.0;
    }
  }
  return lengths;
}

inline double contour_length(flight::Array<double> contour) {
  double len = 0.0;
  {
    double i = 2.0;
    while ((i < contour.length)) {
      {
        const double dx = (contour[static_cast<size_t>(i)] - contour[static_cast<size_t>((i - 2.0))]);
        const double dy = (contour[static_cast<size_t>((i + 1.0))] - contour[static_cast<size_t>((i - 1.0))]);
        len += std::sqrt(((dx * dx) + (dy * dy)));
      }
      i += 2.0;
    }
  }
  return len;
}

} // namespace flighthq_path
