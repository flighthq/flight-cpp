// Generated by @flighthq/tool-compiler from @flighthq/scene2d-wgpu/wgpuClip.ts#WgpuClip. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "wgpu_clip_contours.hpp"
#include "wgpu_clip_rectangle.hpp"

namespace flighthq_scene2d_wgpu {

inline void enable_wgpu_clip_support(WgpuRenderState state) {
  state.display_object_clip_hooks = webgpu_clip_hooks;
}

inline void pop_one_wgpu_clip(WgpuRenderState state) {
  auto runtime = get_wgpu_render_state_runtime(state);
  auto form = runtime.clip_forms.pop();
  if ((form == flight::String("contour"))) {
    pop_wgpu_clip_contours(state);
  }
  else {
    pop_wgpu_clip_rectangle(state);
  }
}

inline Scene2DClipHooks webgpu_clip_hooks = {.finalize = [=](WgpuRenderState state) {
  auto runtime = get_wgpu_render_state_runtime(state);
  while ((runtime.clip_forms.length > 0.0)) {
    pop_one_wgpu_clip(state);
  }
}, .pop_clip = [=](WgpuRenderState state, RenderProxy2D data, Node2D source) {
  auto runtime = get_wgpu_render_state_runtime(state);
  const double target = (data.clip_depth - ((source.clip != nullptr) ? 1.0 : 0.0));
  while ((runtime.clip_forms.length > target)) {
    pop_one_wgpu_clip(state);
  }
}, .push_clip = [=](WgpuRenderState state, RenderProxy2D data, Node2D source) {
  auto runtime = get_wgpu_render_state_runtime(state);
  auto clip = source.clip;
  if (!clip.has_value()) {
    return;
  }
  if ((clip.contours == nullptr)) {
    push_wgpu_clip_rectangle(state, clip.rect, data.transform2_d);
    runtime.clip_forms.push(flight::String("rect"));
  }
  else {
    push_wgpu_clip_contours(state, clip.contours, clip.winding, data.transform2_d);
    runtime.clip_forms.push(flight::String("contour"));
  }
}};

} // namespace flighthq_scene2d_wgpu
