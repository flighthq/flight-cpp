// Generated by @flighthq/tool-compiler from @flighthq/glyphatlas/explainGlyphAtlasEntry.ts#ExplainGlyphAtlasEntry. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_glyphatlas {

inline GlyphAtlasEntryExplanation explain_glyph_atlas_entry(GlyphAtlas atlas, double codepoint) {
  auto runtime = atlas.runtime;
  auto padding = runtime.padding;
  const double usable_width = (runtime.bitmap.width - (2.0 * padding));
  const double usable_height = (runtime.bitmap.height - (2.0 * padding));
  if (runtime.entries.has(codepoint)) {
    auto entry = runtime.entries.get(codepoint);
    return {.renderable = true, .reason = flight::String("ok"), .glyph_width = entry.width, .glyph_height = entry.height, .usable_width = usable_width, .usable_height = usable_height};
  }
  auto bitmap = runtime.rasterizer_backend.rasterize(codepoint, runtime.rasterize_options);
  if (!bitmap.has_value()) {
    return {.renderable = false, .reason = flight::String("rasterizer-returned-null"), .glyph_width = 0.0, .glyph_height = 0.0, .usable_width = usable_width, .usable_height = usable_height};
  }
  const bool fits = ((bitmap.width <= usable_width) && (bitmap.height <= usable_height));
  return {.renderable = fits, .reason = (fits ? flight::String("ok") : flight::String("glyph-larger-than-atlas")), .glyph_width = bitmap.width, .glyph_height = bitmap.height, .usable_width = usable_width, .usable_height = usable_height};
}

} // namespace flighthq_glyphatlas
