// Generated by @flighthq/tool-compiler from @flighthq/materials/enableMaterialConversionGuards.ts#EnableMaterialConversionGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "pbr_materials.hpp"

namespace flighthq_materials {

inline void disable_material_conversion_guards() {
  set_material_conversion_guard(nullptr);
}

inline void enable_material_conversion_guards() {
  set_material_conversion_guard(warn_material_conversion_drop);
}

inline void warn_material_conversion_drop(MaterialConversionExplanation explanation, flight::String conversion) {
  auto maps = explanation.dropped_maps.join(flight::String(", "));
  log_once(flight::String("materials:conversion-dropped-map:") + flight::to_string(conversion) + flight::String(""), log_level.warn, {.message = ((explanation.reason == flight::String("incompatible-channel-semantics")) ? flight::String("") + flight::to_string(conversion) + flight::String(" discarded ") + flight::to_string(maps) + flight::String(": its channels pack different quantities than the target slot, so no assignment preserves meaning. Bake the texture into the target's own layout and pass it explicitly.") : flight::String("") + flight::to_string(conversion) + flight::String(" discarded ") + flight::to_string(maps) + flight::String(": the target material model has no slot for that quantity. Supply the nearest target map explicitly if the surface needs it."))}, flight::String("materials"));
}

} // namespace flighthq_materials
