// Generated by @flighthq/tool-compiler from @flighthq/materials/packages/materials/src/materialValidation.ts#MaterialValidation at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/number.hpp>
#include <limits>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/standard_pbr_material.hpp>
#include <flight/types/texture.hpp>

namespace flight::materials {

inline flight::Ref<flight::types::StandardPbrMaterialProperties> clamp_standard_pbr_material_properties(flight::Ref<flight::types::StandardPbrMaterialProperties> out) {
  out->metallic = flight::minimum(1.0, flight::maximum(0.0, out->metallic));
  out->roughness = flight::minimum(1.0, flight::maximum(0.0, out->roughness));
  out->occlusion_strength = flight::minimum(1.0, flight::maximum(0.0, out->occlusion_strength));
  out->emissive_strength = flight::maximum(0.0, out->emissive_strength);
  out->normal_scale = flight::maximum(0.0, out->normal_scale);
  return out;
}

inline bool is_valid_material_clearcoat(double value) {
  return ((std::isfinite(value) && (value >= 0.0)) && (value <= 1.0));
}

inline bool is_valid_material_iridescence_thickness(double value) {
  return (std::isfinite(value) && (value >= 0.0));
}

inline bool is_valid_material_weight(double value) {
  return ((std::isfinite(value) && (value >= 0.0)) && (value <= 1.0));
}

inline const double min_material_ior = 1.0;

inline const double max_material_ior = 5.0;

inline bool is_valid_material_ior(double value) {
  return ((std::isfinite(value) && (value >= min_material_ior)) && (value <= max_material_ior));
}

} // namespace flight::materials
