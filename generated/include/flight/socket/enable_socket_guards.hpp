// Generated by @flighthq/tool-compiler from @flighthq/socket/enableSocketGuards.ts#EnableSocketGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "socket.hpp"

namespace flighthq_socket {

inline bool are_socket_guards_enabled() {
  return enabled;
}

inline void disable_socket_guards() {
  set_socket_guard(nullptr);
  enabled = false;
}

inline void enable_socket_guards() {
  set_socket_guard(warn_on_socket_misuse);
  enabled = true;
}

inline void warn_on_socket_misuse(SocketGuardNotice notice) {
  auto url = notice.socket.url;
  const flight::String message = ((notice.reason == flight::String("no-connection")) ? flight::String("createSocket: the host carries no socket provider for this transport, or its provider returned no connection — pass a host whose net.socket supports it") : flight::String("") + flight::to_string(notice.operation) + flight::String(": socket is already disposed — call createSocket(...) to create a new socket"));
  log_once(flight::String("socket:") + flight::to_string(notice.operation) + flight::String(":") + flight::to_string(notice.reason) + flight::String(""), log_level.warn, {.message = message, .operation = notice.operation, .reason = notice.reason, .url = url}, flight::String("socket"));
}

inline bool enabled = false;

} // namespace flighthq_socket
