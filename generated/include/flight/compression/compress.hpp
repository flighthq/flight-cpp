// Generated by @flighthq/tool-compiler from @flighthq/compression/packages/compression/src/compress.ts#Compress at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <functional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/compression/deflate_format.hpp>

namespace flight::compression {

struct length_slice_subarray : public flight::ReferenceEnabled {
  auto length;
  std::function<auto(auto, auto)> slice;
  std::function<auto(auto, auto)> subarray;
};

struct DeflateBitWriter : public flight::ReferenceEnabled {
  double bit_count = 0.0;
  double bit_buffer = 0.0;
  flight::Uint8Array bytes = flight::Uint8Array(initial_output_bytes);
  double length = 0.0;
  void write_bits(double value, double count) {
    {
      double i = 0.0;
      while ((i < count)) {
        this->write_bit(flight::bitwise_and(flight::unsigned_right_shift(value, i), 1.0));
        i += 1.0;
      }
    }
  }
  void write_code(double code, double count) {
    {
      double i = (count - 1.0);
      while ((i >= 0.0)) {
        this->write_bit(flight::bitwise_and(flight::unsigned_right_shift(code, i), 1.0));
        i -= 1.0;
      }
    }
  }
  void align_to_byte() {
    if ((this->bit_count != 0.0)) {
      this->push(this->bit_buffer);
      this->bit_buffer = 0.0;
      this->bit_count = 0.0;
    }
  }
  void write_byte(double value) {
    this->push(value);
  }
  flight::Uint8Array finish() {
    this->align_to_byte();
    return this->bytes.slice(0.0, this->length);
  }
  double byte_length() {
    return (this->length + ((this->bit_count == 0.0) ? 0.0 : 1.0));
  }
  void write_bit(double bit) {
    ([&]() { auto&& assignment_target = this->bit_buffer; assignment_target = flight::bitwise_or(assignment_target, flight::left_shift(bit, this->bit_count)); return assignment_target; }());
    this->bit_count++;
    if ((this->bit_count == 8.0)) {
      this->push(this->bit_buffer);
      this->bit_buffer = 0.0;
      this->bit_count = 0.0;
    }
  }
  void push(double value) {
    if ((this->length == this->bytes.length)) {
      auto grown = flight::Uint8Array((this->bytes.length * 2.0));
      grown.set(this->bytes);
      this->bytes = grown;
    }
    this->bytes.element(this->length++) = value;
  }
};

inline flight::Uint8Array encode_fixed_huffman_block(flight::Uint8Array input) {
  flight::Ref<DeflateBitWriter> writer = flight::make_ref<DeflateBitWriter>();
  writer->write_bits(1.0, 1.0);
  writer->write_bits(fixed_huffman_block, 2.0);
  auto head = flight::Int32Array(hash_size).fill(-1.0);
  auto previous = flight::Int32Array(input.length).fill(-1.0);
  double position = 0.0;
  while ((position < input.length)) {
    double match_length = 0.0;
    double match_distance = 0.0;
    if (((position + min_match) <= input.length)) {
      const double key = hash_at(input, position);
      auto candidate = head[static_cast<size_t>(key)];
      double attempts = 0.0;
      while (((candidate >= 0.0) && (attempts < max_chain))) {
        const double distance = (position - candidate);
        if ((distance > max_distance)) {
          break;
        }
        const double length = match_run_length(input, candidate, position);
        if ((length > match_length)) {
          match_length = length;
          match_distance = distance;
          if ((length == max_match)) {
            break;
          }
        }
        candidate = previous[static_cast<size_t>(candidate)];
        attempts++;
      }
      previous[static_cast<size_t>(position)] = head[static_cast<size_t>(key)];
      head[static_cast<size_t>(key)] = position;
    }
    if ((match_length >= min_match)) {
      write_length_symbol(writer, match_length);
      write_distance_symbol(writer, match_distance);
      {
        double i = 1.0;
        while ((i < match_length)) {
          {
            const double inner = (position + i);
            if (((inner + min_match) <= input.length)) {
              const double key_2 = hash_at(input, inner);
              previous[static_cast<size_t>(inner)] = head[static_cast<size_t>(key_2)];
              head[static_cast<size_t>(key_2)] = inner;
            }
          }
          i += 1.0;
        }
      }
      position += match_length;
      continue;
    }
    write_literal_symbol(writer, input.element(position));
    position++;
  }
  write_literal_symbol(writer, end_of_block);
  return writer->finish();
}

inline flight::Uint8Array encode_stored_blocks(flight::Uint8Array input) {
  flight::Ref<DeflateBitWriter> writer = flight::make_ref<DeflateBitWriter>();
  double offset = 0.0;
  do {
    const double size = flight::minimum(stored_block_max, (input.length - offset));
    const double final = (((offset + size) >= input.length) ? 1.0 : 0.0);
    writer->write_bits(final, 1.0);
    writer->write_bits(stored_block, 2.0);
    writer->align_to_byte();
    writer->write_byte(flight::bitwise_and(size, 255.0));
    writer->write_byte(flight::bitwise_and(flight::unsigned_right_shift(size, 8.0), 255.0));
    writer->write_byte(flight::bitwise_and(flight::bitwise_not(size), 255.0));
    writer->write_byte(flight::bitwise_and(flight::unsigned_right_shift(flight::bitwise_not(size), 8.0), 255.0));
    {
      double i = 0.0;
      while ((i < size)) {
        writer->write_byte(input.element((offset + i)));
        i += 1.0;
      }
    }
    offset += size;
  } while ((offset < input.length));
  return writer->finish();
}

inline double stored_length(double input_length) {
  const double blocks = flight::maximum(1.0, std::ceil((input_length / stored_block_max)));
  return ((blocks * stored_block_overhead) + input_length);
}

inline void write_literal_symbol(flight::Ref<DeflateBitWriter> writer, double symbol) {
  if ((symbol <= 143.0)) {
    writer->write_code((48.0 + symbol), 8.0);
  }
  else {
    if ((symbol <= 255.0)) {
      writer->write_code(((400.0 + symbol) - 144.0), 9.0);
    }
    else {
      if ((symbol <= 279.0)) {
        writer->write_code((symbol - 256.0), 7.0);
      }
      else {
        writer->write_code(((192.0 + symbol) - 280.0), 8.0);
      }
    }
  }
}

inline void write_length_symbol(flight::Ref<DeflateBitWriter> writer, double length) {
  double index = (length_base.length - 1.0);
  while (((index > 0.0) && (length_base.element(index) > length))) {
    index--;
  }
  write_literal_symbol(writer, (first_length_symbol + index));
  writer->write_bits((length - length_base.element(index)), length_extra.element(index));
}

inline void write_distance_symbol(flight::Ref<DeflateBitWriter> writer, double distance) {
  double index = (distance_base.length - 1.0);
  while (((index > 0.0) && (distance_base.element(index) > distance))) {
    index--;
  }
  writer->write_code(index, 5.0);
  writer->write_bits((distance - distance_base.element(index)), distance_extra.element(index));
}

inline double match_run_length(flight::Uint8Array input, double candidate, double position) {
  const double limit = flight::minimum((input.length - position), max_match);
  double length = 0.0;
  while (((length < limit) && (input.element((candidate + length)) == input.element((position + length))))) {
    length++;
  }
  return length;
}

inline double hash_at(flight::Uint8Array input, double position) {
  return flight::bitwise_and(flight::bitwise_xor(flight::bitwise_xor(flight::left_shift(input.element(position), 10.0), flight::left_shift(input.element((position + 1.0)), 5.0)), input.element((position + 2.0))), hash_mask);
}

inline flight::Uint8Array compress_deflate(flight::Uint8Array bytes) {
  flight::Ref<length_slice_subarray> input = static_cast<flight::Uint8Array>(bytes);
  flight::Ref<length_slice_subarray> huffman = encode_fixed_huffman_block(input);
  return ((huffman->length <= stored_length(input->length)) ? huffman : encode_stored_blocks(input));
}

inline flight::Uint8Array compress_deflate_zlib(flight::Uint8Array bytes) {
  flight::Ref<length_slice_subarray> deflated = compress_deflate(bytes);
  auto out = flight::Uint8Array(((zlib_header_bytes + deflated->length) + zlib_trailer_bytes));
  out.element(0.0) = zlib_cmf;
  out.element(1.0) = zlib_flg;
  out.set(deflated, zlib_header_bytes);
  const double checksum = compute_adler32(bytes);
  const double trailer = (zlib_header_bytes + deflated->length);
  out.element(trailer) = flight::bitwise_and(flight::unsigned_right_shift(checksum, 24.0), 255.0);
  out.element((trailer + 1.0)) = flight::bitwise_and(flight::unsigned_right_shift(checksum, 16.0), 255.0);
  out.element((trailer + 2.0)) = flight::bitwise_and(flight::unsigned_right_shift(checksum, 8.0), 255.0);
  out.element((trailer + 3.0)) = flight::bitwise_and(checksum, 255.0);
  return out;
}

inline const double end_of_block = 256.0;

inline const double first_length_symbol = 257.0;

inline const double fixed_huffman_block = 1.0;

inline const double hash_bits = 15.0;

inline const double hash_mask = (flight::left_shift(1.0, hash_bits) - 1.0);

inline const double hash_size = flight::left_shift(1.0, hash_bits);

inline const double initial_output_bytes = 1024.0;

inline const double max_chain = 32.0;

inline const double max_distance = 32768.0;

inline const double max_match = 258.0;

inline const double min_match = 3.0;

inline const double stored_block = 0.0;

inline const double stored_block_max = 65535.0;

inline const double stored_block_overhead = 5.0;

inline const double zlib_cmf = 120.0;

inline const double zlib_flg = 1.0;

inline const double zlib_header_bytes = 2.0;

inline const double zlib_trailer_bytes = 4.0;

} // namespace flight::compression
