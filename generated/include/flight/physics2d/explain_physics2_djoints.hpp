// Generated by @flighthq/tool-compiler from @flighthq/physics2d/explainPhysics2DJoints.ts#ExplainPhysics2DJoints. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "world.hpp"

namespace flighthq_physics2d {

inline Physics2DJointResolutionExplanation explain_physics2_djoints(Physics2DWorld world) {
  flight::Array<Physics2DJointResolution> joints = flight::Array<Physics2DJointResolution>{};
  double ready_count = 0.0;
  {
    double joint_index = 0.0;
    while ((joint_index < world.joints.length)) {
      {
        auto joint = world.joints[static_cast<size_t>(joint_index)];
        auto solver = world.joint_solvers.get(joint.kind);
        const bool solver_registered = solver.has_value();
        const bool body_aused = (([&]() -> std::optional<auto> { auto optional_chain_receiver = solver; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().uses_body_a; }()) != false);
        const bool body_afound = (find_physics2_dbody(world, joint.body_a) != nullptr);
        const bool body_bfound = (find_physics2_dbody(world, joint.body_b) != nullptr);
        Physics2DJointResolutionStatus status = get_joint_resolution_status(solver_registered, body_aused, body_afound, body_bfound);
        if ((status == flight::String("ready"))) {
          ready_count++;
        }
        joints.push({.body_a = joint.body_a, .body_afound = body_afound, .body_aused = body_aused, .body_b = joint.body_b, .body_bfound = body_bfound, .joint_index = joint_index, .kind = joint.kind, .solver_registered = solver_registered, .status = status});
      }
      joint_index += 1.0;
    }
  }
  return {.joints = joints, .ready_count = ready_count, .status = ((ready_count == static_cast<double>(joints.size())) ? flight::String("complete") : flight::String("unresolved-joints"))};
}

inline Physics2DJointResolutionStatus get_joint_resolution_status(bool solver_registered, bool body_aused, bool body_afound, bool body_bfound) {
  if (!solver_registered) {
    return flight::String("solver-unregistered");
  }
  if (((body_aused && !body_afound) && !body_bfound)) {
    return flight::String("bodies-missing");
  }
  if ((body_aused && !body_afound)) {
    return flight::String("body-a-missing");
  }
  if (!body_bfound) {
    return flight::String("body-b-missing");
  }
  return flight::String("ready");
}

} // namespace flighthq_physics2d
