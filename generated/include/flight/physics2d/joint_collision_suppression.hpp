// Generated by @flighthq/tool-compiler from @flighthq/physics2d/packages/physics2d/src/jointCollisionSuppression.ts#JointCollisionSuppression at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { struct Physics2DContact; }
namespace flight::types { struct Physics2DContactEvents; }
namespace flight::types { struct Physics2DContactHooks; }
namespace flight::types { struct Physics2DJoint; }
namespace flight::types { struct Physics2DJointEvents; }
namespace flight::types { struct Physics2DJointReaction; }
namespace flight::types { struct Physics2DJointSolver; }
namespace flight::types { struct Physics2DSolverConfig; }
namespace flight::types { struct Physics2DWorld; }
namespace flight::types { struct RigidBody2D; }
namespace flight::types { struct SpatialIndexBackend2D; }

#include <flight/types/collision.hpp>
#include <flight/types/physics2_d.hpp>
#include <flight/types/spatial.hpp>

namespace flight::physics2d {

inline bool is_physics2_dpair_joint_suppressed(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Physics2DWorld>>>> world, double body_a, double body_b) {
  const double first = flight::minimum(body_a, body_b);
  const double second = flight::maximum(body_a, body_b);
  return (([&]() -> double { auto nullish_coalesce_left = ([&]() -> std::optional<double> { auto optional_chain_receiver = flight::row_get<flight::RowKey<"jointCollisionSuppressions">>(world).get(first); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().get(second); }()); if (nullish_coalesce_left.has_value()) return nullish_coalesce_left.value(); return 0.0; }()) > 0.0);
}

inline void rebuild_physics2_djoint_collision_suppressions(flight::Ref<flight::types::Physics2DWorld> world) {
  flight::Map<double, flight::Map<double, double>> suppressions = world->joint_collision_suppressions;
  suppressions.clear();
  for (auto joint : world->joints) {
    if (joint->collide_connected) {
      continue;
    }
    auto solver = world->joint_solvers.get(joint->kind);
    if ((!solver.has_value() || (solver.value()->uses_body_a == false))) {
      continue;
    }
    const double first = flight::minimum(joint->body_a, joint->body_b);
    const double second = flight::maximum(joint->body_a, joint->body_b);
    std::optional<flight::Map<double, double>> seconds = suppressions.get(first);
    if (!seconds.has_value()) {
      (seconds = std::optional<flight::Map<double, double>>{flight::Map<double, double>()});
      suppressions.set(first, seconds.value());
    }
    seconds.value().set(second, (([&]() -> double { auto nullish_coalesce_left = seconds.value().get(second); if (nullish_coalesce_left.has_value()) return nullish_coalesce_left.value(); return 0.0; }()) + 1.0));
  }
}

} // namespace flight::physics2d
