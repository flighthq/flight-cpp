// Generated by @flighthq/tool-compiler from @flighthq/skeleton3d/prepareScene3DSkinning.ts#PrepareScene3DSkinning. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "get_mesh_skin_bounds.hpp"
#include "skeleton3d.hpp"
#include "skin_mesh_geometry.hpp"

namespace flighthq_skeleton3d {

inline void prepare_mesh_skinning(Mesh mesh) {
  auto skin = mesh.skin;
  if (!skin.has_value()) {
    return;
  }
  compute_skeleton3_djoint_matrices(skin.skeleton);
  auto geometry = mesh.geometry;
  auto bind_pose = get_mesh_geometry_skin_bind_pose(geometry);
  if (!bind_pose.has_value()) {
    bind_pose = capture_mesh_skin_bind_pose(geometry);
    set_mesh_geometry_skin_bind_pose(geometry, bind_pose);
  }
  MeshRuntime runtime = static_cast<MeshRuntime>(get_node_runtime(static_cast<NodeAny>(mesh)));
  auto bounds = runtime.deformed_local_bounds;
  if (!bounds.has_value()) {
    bounds = create_aabb();
    runtime.deformed_local_bounds = bounds;
  }
  get_mesh_skin_conservative_bounds(bounds, bind_pose, skin.skeleton);
}

inline void prepare_scene3_dskinning(NodeAny scene) {
  if (!scene.enabled) {
    return;
  }
  Mesh mesh = static_cast<Mesh>(static_cast<auto>(scene));
  if ((mesh.geometry != nullptr)) {
    prepare_mesh_skinning(mesh);
  }
  auto children = get_node_runtime(scene).children;
  if (children.has_value()) {
    {
      double i = 0.0;
      while ((i < children.length)) {
        {
          prepare_scene3_dskinning(children[static_cast<size_t>(i)]);
        }
        i += 1.0;
      }
    }
  }
}

} // namespace flighthq_skeleton3d
