// Generated by @flighthq/tool-compiler from @flighthq/texture/cubeTexture.ts#CubeTexture. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "sampler.hpp"
#include "texture.hpp"

namespace flighthq_texture {

inline TextureSourceCubeFaces get_cube_sources(CubeTexture texture) {
  return texture.sources;
}

inline CubeTexture clone_cube_texture(CubeTexture source) {
  return static_cast<CubeTexture>(clone_texture(source));
}

inline void copy_cube_texture(CubeTexture out, CubeTexture source) {
  copy_texture(out, source);
}

inline CubeTexture create_cube_texture(std::optional<CreateCubeTextureOptions> opts = std::nullopt) {
  return static_cast<CubeTexture>(create_texture({.color_space = ([&]() -> std::optional<auto> { auto optional_chain_receiver = opts.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().color_space; }()).value_or(flight::String("srgb")), .sampler = (([&]() -> std::optional<auto> { auto optional_chain_receiver = opts.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().sampler; }()) ? clone_sampler(opts.value().sampler) : undefined), .dimension = flight::String("cube"), .sources = ([&]() -> std::optional<auto> { auto optional_chain_receiver = opts.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().sources; }()).value_or(flight::Array{nullptr, nullptr, nullptr, nullptr, nullptr, nullptr})}));
}

inline bool equals_cube_texture(std::optional<CubeTexture> a, std::optional<CubeTexture> b) {
  if ((!a.value() || !b.value())) {
    return false;
  }
  return equals_texture(a.value(), b.value());
}

inline double get_cube_texture_face_size(CubeTexture cube) {
  TextureSourceCubeFaces sources = get_cube_sources(cube);
  {
    double i = 0.0;
    while ((i < 6.0)) {
      {
        auto face = sources[static_cast<size_t>(i)];
        if (face.has_value()) {
          return face.width;
        }
      }
      i += 1.0;
    }
  }
  return -1.0;
}

inline bool is_cube_texture_complete(CubeTexture cube) {
  return get_cube_sources(cube).every([=](auto face) { return face.has_value(); });
}

inline void set_cube_texture_face(CubeTexture cube, double face_index, std::optional<TextureSource> source) {
  TextureSourceCubeFaces sources = get_cube_sources(cube);
  if ((sources[static_cast<size_t>(face_index)] == source.value())) {
    return;
  }
  static_cast<flight::Array<std::optional<TextureSource>>>(static_cast<auto>(sources)).element(face_index) = source.value();
  cube.version = ((cube.version + 1.0) >>> 0.0);
}

} // namespace flighthq_texture
