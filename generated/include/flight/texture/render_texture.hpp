// Generated by @flighthq/tool-compiler from @flighthq/texture/renderTexture.ts#RenderTexture. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "sampler.hpp"
#include "texture.hpp"

namespace flighthq_texture {

inline RenderTexture create_render_texture(CreateRenderTextureOptions options) {
  auto color_space = options.color_space.value_or(flight::String("linear"));
  RenderTexture texture = static_cast<RenderTexture>(create_texture({.color_space = options.color_space.value_or(flight::String("linear")), .flip_x = options.flip_x.value_or(false), .flip_y = options.flip_y.value_or(false), .dimension = flight::String("2d"), .source = ([=]() {
  auto out = allocate_entity<RenderTarget>();
  initialize_render_texture_target(out, options, color_space);
  return static_cast<RenderTarget>(finish_entity(out));
})(), .uv_rotation = options.uv_rotation}));
  if ((options.sampler != undefined)) {
    copy_sampler(texture.sampler, options.sampler);
  }
  if ((options.uv_offset != undefined)) {
    texture.uv_offset.x = options.uv_offset.x;
    texture.uv_offset.y = options.uv_offset.y;
  }
  if ((options.uv_scale != undefined)) {
    texture.uv_scale.x = options.uv_scale.x;
    texture.uv_scale.y = options.uv_scale.y;
  }
  return texture;
}

inline void initialize_render_texture_target(EntityConstruction<RenderTarget> out, CreateRenderTextureOptions options, RenderTargetColorSpace color_space) {
  out.color_attachments = options.color_attachments;
  out.color_formats = options.color_formats;
  out.color_space = color_space;
  out.clear_colors = options.clear_colors;
  out.clear_depth = options.clear_depth;
  out.depth = options.depth;
  out.format = options.format;
  out.height = options.height;
  out.kind = render_target_texture_source_kind;
  out.sample_count = options.sample_count;
  out.version = 0.0;
  out.width = options.width;
}

} // namespace flighthq_texture
