// Generated by @flighthq/tool-compiler from @flighthq/collision/convexVertices2D.ts#ConvexVertices2D. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_collision {

inline void write_aabb_vertices(CollisionAabb2D aabb, flight::Float64Array out) {
  auto min_x = aabb.min_x;
  auto min_y = aabb.min_y;
  auto max_x = aabb.max_x;
  auto max_y = aabb.max_y;
  out.element(0.0) = min_x;
  out.element(1.0) = min_y;
  out.element(2.0) = max_x;
  out.element(3.0) = min_y;
  out.element(4.0) = max_x;
  out.element(5.0) = max_y;
  out.element(6.0) = min_x;
  out.element(7.0) = max_y;
}

inline void write_obb_vertices(CollisionObb2D obb, flight::Float64Array out) {
  auto cx = obb.x;
  auto cy = obb.y;
  auto half_w = obb.half_w;
  auto half_h = obb.half_h;
  const double cos = std::cos(obb.rotation);
  const double sin = std::sin(obb.rotation);
  const double wx = (cos * half_w);
  const double wy = (sin * half_w);
  const double hx = (-sin * half_h);
  const double hy = (cos * half_h);
  out.element(0.0) = ((cx - wx) - hx);
  out.element(1.0) = ((cy - wy) - hy);
  out.element(2.0) = ((cx + wx) - hx);
  out.element(3.0) = ((cy + wy) - hy);
  out.element(4.0) = ((cx + wx) + hx);
  out.element(5.0) = ((cy + wy) + hy);
  out.element(6.0) = ((cx - wx) + hx);
  out.element(7.0) = ((cy - wy) + hy);
}

} // namespace flighthq_collision
