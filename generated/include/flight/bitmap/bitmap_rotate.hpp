// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapRotate.ts#BitmapRotate. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"
#include "bitmap_affine.hpp"

namespace flighthq_bitmap {

inline void rotate_bitmap(BitmapRegion dest, BitmapRegion source, double angle, std::optional<double> pivot_x = std::nullopt, std::optional<double> pivot_y = std::nullopt, std::optional<BitmapEdgeMode> edge_mode = std::nullopt, std::optional<BitmapResizeMode> sample_mode = std::nullopt) {
  pivot_x = pivot_x.value_or(((source.width - 1.0) / 2.0));
  pivot_y = pivot_y.value_or(((source.height - 1.0) / 2.0));
  edge_mode = edge_mode.value_or(flight::String("clamp"));
  sample_mode = sample_mode.value_or(flight::String("bilinear"));
  const double cos_a = std::cos(-angle);
  const double sin_a = std::sin(-angle);
  const double dest_pivot_x = ((dest.width - 1.0) / 2.0);
  const double dest_pivot_y = ((dest.height - 1.0) / 2.0);
  const double e = ((pivot_x.value() - (cos_a * dest_pivot_x)) + (sin_a * dest_pivot_y));
  const double f = ((pivot_y.value() - (sin_a * dest_pivot_x)) - (cos_a * dest_pivot_y));
  transform_bitmap(dest, source, flight::Array{cos_a, sin_a, -sin_a, cos_a, e, f}, edge_mode.value(), sample_mode.value());
}

inline void rotate_bitmap180(BitmapRegion dest, BitmapRegion source) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  if (is_same_region(dest, source)) {
    auto data = dest.bitmap.data;
    auto stride = dest.bitmap.width;
    const double total = (w * h);
    const double half = static_cast<double>(static_cast<int32_t>(total) >> static_cast<int32_t>(1.0));
    {
      double k = 0.0;
      while ((k < half)) {
        {
          auto ax = (dest.x + std::fmod(k, w));
          auto ay = (dest.y + std::floor((k / w)));
          auto bx = (dest.x + ((w - 1.0) - std::fmod(k, w)));
          auto by = (dest.y + ((h - 1.0) - std::floor((k / w))));
          if ((!in_bounds(ax, ay, stride, dest.bitmap.height) || !in_bounds(bx, by, stride, dest.bitmap.height))) {
            k += 1.0;
            continue;
          }
          swap_pixels(data, (((ay * stride) + ax) * 4.0), (((by * stride) + bx) * 4.0));
        }
        k += 1.0;
      }
    }
    return;
  }
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  auto s_stride = source.bitmap.width;
  auto d_stride = dest.bitmap.width;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + ((h - 1.0) - py));
        auto dy = (dest.y + py);
        if ((!in_bounds(0.0, sy, s_stride, source.bitmap.height) || !in_bounds(0.0, dy, d_stride, dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + ((w - 1.0) - px));
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= s_stride)) || (dx < 0.0)) || (dx >= d_stride))) {
                px += 1.0;
                continue;
              }
              copy_pixel(dd, (((dy * d_stride) + dx) * 4.0), sd, (((sy * s_stride) + sx) * 4.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void rotate_bitmap_clockwise(BitmapRegion dest, BitmapRegion source) {
  auto sw = source.width;
  auto sh = source.height;
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  auto s_stride = source.bitmap.width;
  auto d_stride = dest.bitmap.width;
  {
    double py = 0.0;
    while ((py < sh)) {
      {
        auto sy = (source.y + py);
        if (((sy < 0.0) || (sy >= source.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < sw)) {
            {
              auto sx = (source.x + px);
              if (((sx < 0.0) || (sx >= s_stride))) {
                px += 1.0;
                continue;
              }
              auto dx = (dest.x + ((sh - 1.0) - py));
              auto dy = (dest.y + px);
              if (((((dx < 0.0) || (dx >= d_stride)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
                px += 1.0;
                continue;
              }
              copy_pixel(dd, (((dy * d_stride) + dx) * 4.0), sd, (((sy * s_stride) + sx) * 4.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void rotate_bitmap_counter_clockwise(BitmapRegion dest, BitmapRegion source) {
  auto sw = source.width;
  auto sh = source.height;
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  auto s_stride = source.bitmap.width;
  auto d_stride = dest.bitmap.width;
  {
    double py = 0.0;
    while ((py < sh)) {
      {
        auto sy = (source.y + py);
        if (((sy < 0.0) || (sy >= source.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < sw)) {
            {
              auto sx = (source.x + px);
              if (((sx < 0.0) || (sx >= s_stride))) {
                px += 1.0;
                continue;
              }
              auto dx = (dest.x + py);
              auto dy = (dest.y + ((sw - 1.0) - px));
              if (((((dx < 0.0) || (dx >= d_stride)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
                px += 1.0;
                continue;
              }
              copy_pixel(dd, (((dy * d_stride) + dx) * 4.0), sd, (((sy * s_stride) + sx) * 4.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void copy_pixel(flight::Uint8ClampedArray dest, double di, flight::Uint8ClampedArray source, double si) {
  dest.element(di) = source[static_cast<size_t>(si)];
  dest.element((di + 1.0)) = source[static_cast<size_t>((si + 1.0))];
  dest.element((di + 2.0)) = source[static_cast<size_t>((si + 2.0))];
  dest.element((di + 3.0)) = source[static_cast<size_t>((si + 3.0))];
}

inline bool in_bounds(double x, double y, double width, double height) {
  return ((((x >= 0.0) && (x < width)) && (y >= 0.0)) && (y < height));
}

inline bool is_same_region(BitmapRegion a, BitmapRegion b) {
  return (((((a.bitmap == b.bitmap) && (a.x == b.x)) && (a.y == b.y)) && (a.width == b.width)) && (a.height == b.height));
}

inline void swap_pixels(flight::Uint8ClampedArray data, double a, double b) {
  {
    double c = 0.0;
    while ((c < 4.0)) {
      {
        std::optional<auto> t = data.get((a + c));
        data.element((a + c)) = data.element((b + c));
        data.element((b + c)) = t;
      }
      c += 1.0;
    }
  }
}

} // namespace flighthq_bitmap
