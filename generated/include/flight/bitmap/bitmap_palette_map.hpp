// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapPaletteMap.ts#BitmapPaletteMap. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline void apply_bitmap_palette_map(BitmapRegion dest, BitmapRegion source, std::optional<flight::Array<double>> red_map, std::optional<flight::Array<double>> green_map, std::optional<flight::Array<double>> blue_map, std::optional<flight::Array<double>> alpha_map) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              auto r = sd[static_cast<size_t>(si)];
              auto g = sd[static_cast<size_t>((si + 1.0))];
              auto b = sd[static_cast<size_t>((si + 2.0))];
              auto a = sd[static_cast<size_t>((si + 3.0))];
              dd[static_cast<size_t>(di)] = (red_map ? red_map.value().element(r) : r);
              dd[static_cast<size_t>((di + 1.0))] = (green_map ? green_map.value().element(g) : g);
              dd[static_cast<size_t>((di + 2.0))] = (blue_map ? blue_map.value().element(b) : b);
              dd[static_cast<size_t>((di + 3.0))] = (alpha_map ? alpha_map.value().element(a) : a);
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

} // namespace flighthq_bitmap
