// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapDissolve.ts#BitmapDissolve at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/bitmap/bitmap.hpp>

namespace flight::bitmap {

inline double permute_pixel_index(double cursor, double bits, double mask) {
  double v = flight::bitwise_and(cursor, mask);
  const double shift = ((bits > 1.0) ? flight::signed_right_shift(bits, 1.0) : 1.0);
  v = flight::bitwise_and(([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(v, 2654435761.0), mask);
  ([&]() { auto&& assignment_target = v; assignment_target = flight::bitwise_xor(assignment_target, flight::unsigned_right_shift(v, shift)); return assignment_target; }());
  v = flight::bitwise_and(([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(v, 2246822519.0), mask);
  ([&]() { auto&& assignment_target = v; assignment_target = flight::bitwise_xor(assignment_target, flight::bitwise_and(668265263.0, mask)); return assignment_target; }());
  return flight::bitwise_and(v, mask);
}

inline double dissolve_bitmap_pixels(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, double seed, double pixel_count, std::optional<double> fill_color = std::nullopt) {
  fill_color = fill_color.value_or(0.0);
  const double width = flight::row_get<flight::RowKey<"width">>(dest);
  const double height = flight::row_get<flight::RowKey<"height">>(dest);
  const double total = (width * height);
  if ((total <= 0.0)) {
    return seed;
  }
  double bits = 0.0;
  while ((flight::left_shift(1.0, bits) < total)) {
    bits++;
  }
  const double period = flight::left_shift(1.0, bits);
  const double mask = (period - 1.0);
  double cursor = ((seed < 0.0) ? 0.0 : ((seed > period) ? period : flight::bitwise_or(seed, 0.0)));
  if ((pixel_count <= 0.0)) {
    return cursor;
  }
  const bool to_fill = (((((flight::row_get<flight::RowKey<"bitmap">>(source) == flight::row_get<flight::RowKey<"bitmap">>(dest)) && (flight::row_get<flight::RowKey<"x">>(source) == flight::row_get<flight::RowKey<"x">>(dest))) && (flight::row_get<flight::RowKey<"y">>(source) == flight::row_get<flight::RowKey<"y">>(dest))) && (flight::row_get<flight::RowKey<"width">>(source) == flight::row_get<flight::RowKey<"width">>(dest))) && (flight::row_get<flight::RowKey<"height">>(source) == flight::row_get<flight::RowKey<"height">>(dest)));
  const double fill_r = flight::bitwise_and(flight::unsigned_right_shift(fill_color.value(), 24.0), 255.0);
  const double fill_g = flight::bitwise_and(flight::signed_right_shift(fill_color.value(), 16.0), 255.0);
  const double fill_b = flight::bitwise_and(flight::signed_right_shift(fill_color.value(), 8.0), 255.0);
  const double fill_a = flight::bitwise_and(fill_color.value(), 255.0);
  flight::Uint8ClampedArray<flight::ArrayBuffer> dest_data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto dest_stride = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  auto dest_bitmap_height = flight::row_get<flight::RowKey<"bitmap">>(dest)->height;
  flight::Uint8ClampedArray<flight::ArrayBuffer> source_data = flight::row_get<flight::RowKey<"bitmap">>(source)->data;
  auto source_stride = flight::row_get<flight::RowKey<"bitmap">>(source)->width;
  auto source_bitmap_height = flight::row_get<flight::RowKey<"bitmap">>(source)->height;
  double dissolved = 0.0;
  while (((dissolved < pixel_count) && (cursor < period))) {
    const double pixel_index = permute_pixel_index(cursor, bits, mask);
    cursor++;
    if ((pixel_index >= total)) {
      continue;
    }
    dissolved++;
    const double px = std::fmod(pixel_index, width);
    const double py = flight::bitwise_or((pixel_index / width), 0.0);
    auto dx = (flight::row_get<flight::RowKey<"x">>(dest) + px);
    auto dy = (flight::row_get<flight::RowKey<"y">>(dest) + py);
    if (((((dx < 0.0) || (dx >= dest_stride)) || (dy < 0.0)) || (dy >= dest_bitmap_height))) {
      continue;
    }
    const double di = (((dy * dest_stride) + dx) * 4.0);
    if (to_fill) {
      dest_data.element(di) = fill_r;
      dest_data.element((di + 1.0)) = fill_g;
      dest_data.element((di + 2.0)) = fill_b;
      dest_data.element((di + 3.0)) = fill_a;
      continue;
    }
    auto sx = (flight::row_get<flight::RowKey<"x">>(source) + px);
    auto sy = (flight::row_get<flight::RowKey<"y">>(source) + py);
    if (((((sx < 0.0) || (sx >= source_stride)) || (sy < 0.0)) || (sy >= source_bitmap_height))) {
      continue;
    }
    const double si = (((sy * source_stride) + sx) * 4.0);
    dest_data.element(di) = source_data.element(si);
    dest_data.element((di + 1.0)) = source_data.element((si + 1.0));
    dest_data.element((di + 2.0)) = source_data.element((si + 2.0));
    dest_data.element((di + 3.0)) = source_data.element((si + 3.0));
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
  return cursor;
}

} // namespace flight::bitmap
