// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapDissolve.ts#BitmapDissolve. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline double dissolve_bitmap_pixels(BitmapRegion dest, BitmapRegion source, double seed, double pixel_count, std::optional<double> fill_color = std::nullopt) {
  fill_color = fill_color.value_or(0.0);
  auto width = dest.width;
  auto height = dest.height;
  const double total = (width * height);
  if ((total <= 0.0)) {
    return seed;
  }
  double bits = 0.0;
  while ((static_cast<double>(static_cast<int32_t>(1.0) << static_cast<int32_t>(bits)) < total)) {
    bits++;
  }
  const double period = static_cast<double>(static_cast<int32_t>(1.0) << static_cast<int32_t>(bits));
  const double mask = (period - 1.0);
  double cursor = ((seed < 0.0) ? 0.0 : ((seed > period) ? period : static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0))));
  if ((pixel_count <= 0.0)) {
    return cursor;
  }
  const bool to_fill = (((((source.bitmap == dest.bitmap) && (source.x == dest.x)) && (source.y == dest.y)) && (source.width == dest.width)) && (source.height == dest.height));
  const double fill_r = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(fill_color.value())) >> static_cast<uint32_t>(24.0))) & static_cast<int32_t>(255.0));
  const double fill_g = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(fill_color.value()) >> static_cast<int32_t>(16.0))) & static_cast<int32_t>(255.0));
  const double fill_b = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(fill_color.value()) >> static_cast<int32_t>(8.0))) & static_cast<int32_t>(255.0));
  const double fill_a = static_cast<double>(static_cast<int32_t>(fill_color.value()) & static_cast<int32_t>(255.0));
  auto dest_data = dest.bitmap.data;
  auto dest_stride = dest.bitmap.width;
  auto dest_bitmap_height = dest.bitmap.height;
  auto source_data = source.bitmap.data;
  auto source_stride = source.bitmap.width;
  auto source_bitmap_height = source.bitmap.height;
  double dissolved = 0.0;
  while (((dissolved < pixel_count) && (cursor < period))) {
    const double pixel_index = permute_pixel_index(cursor, bits, mask);
    cursor++;
    if ((pixel_index >= total)) {
      continue;
    }
    dissolved++;
    const double px = std::fmod(pixel_index, width);
    const double py = static_cast<double>(static_cast<int32_t>((pixel_index / width)) | static_cast<int32_t>(0.0));
    auto dx = (dest.x + px);
    auto dy = (dest.y + py);
    if (((((dx < 0.0) || (dx >= dest_stride)) || (dy < 0.0)) || (dy >= dest_bitmap_height))) {
      continue;
    }
    const double di = (((dy * dest_stride) + dx) * 4.0);
    if (to_fill) {
      dest_data[static_cast<size_t>(di)] = fill_r;
      dest_data[static_cast<size_t>((di + 1.0))] = fill_g;
      dest_data[static_cast<size_t>((di + 2.0))] = fill_b;
      dest_data[static_cast<size_t>((di + 3.0))] = fill_a;
      continue;
    }
    auto sx = (source.x + px);
    auto sy = (source.y + py);
    if (((((sx < 0.0) || (sx >= source_stride)) || (sy < 0.0)) || (sy >= source_bitmap_height))) {
      continue;
    }
    const double si = (((sy * source_stride) + sx) * 4.0);
    dest_data[static_cast<size_t>(di)] = source_data[static_cast<size_t>(si)];
    dest_data[static_cast<size_t>((di + 1.0))] = source_data[static_cast<size_t>((si + 1.0))];
    dest_data[static_cast<size_t>((di + 2.0))] = source_data[static_cast<size_t>((si + 2.0))];
    dest_data[static_cast<size_t>((di + 3.0))] = source_data[static_cast<size_t>((si + 3.0))];
  }
  invalidate_bitmap(dest.bitmap);
  return cursor;
}

inline double permute_pixel_index(double cursor, double bits, double mask) {
  double v = static_cast<double>(static_cast<int32_t>(cursor) & static_cast<int32_t>(mask));
  const double shift = ((bits > 1.0) ? static_cast<double>(static_cast<int32_t>(bits) >> static_cast<int32_t>(1.0)) : 1.0);
  v = static_cast<double>(static_cast<int32_t>(cmath.imul(v, 2654435761.0)) & static_cast<int32_t>(mask));
  v ^= static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(v)) >> static_cast<uint32_t>(shift));
  v = static_cast<double>(static_cast<int32_t>(cmath.imul(v, 2246822519.0)) & static_cast<int32_t>(mask));
  v ^= static_cast<double>(static_cast<int32_t>(668265263.0) & static_cast<int32_t>(mask));
  return static_cast<double>(static_cast<int32_t>(v) & static_cast<int32_t>(mask));
}

} // namespace flighthq_bitmap
