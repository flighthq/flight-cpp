// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapTransform.ts#BitmapTransform. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline std::optional<flight::Uint8ClampedArray> scroll_scratch = nullptr;

inline void apply_bitmap_color_scale_bias(BitmapRegion dest, BitmapRegion source, ColorScaleBiasLike ct) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              auto r = source.bitmap.data[static_cast<size_t>(si)];
              auto g = source.bitmap.data[static_cast<size_t>((si + 1.0))];
              auto b = source.bitmap.data[static_cast<size_t>((si + 2.0))];
              auto a = source.bitmap.data[static_cast<size_t>((si + 3.0))];
              dest.bitmap.data[static_cast<size_t>(di)] = std::max(0.0, std::min(255.0, flight::round(((r * ct.red_scale) + (ct.red_bias * 255.0)))));
              dest.bitmap.data[static_cast<size_t>((di + 1.0))] = std::max(0.0, std::min(255.0, flight::round(((g * ct.green_scale) + (ct.green_bias * 255.0)))));
              dest.bitmap.data[static_cast<size_t>((di + 2.0))] = std::max(0.0, std::min(255.0, flight::round(((b * ct.blue_scale) + (ct.blue_bias * 255.0)))));
              dest.bitmap.data[static_cast<size_t>((di + 3.0))] = std::max(0.0, std::min(255.0, flight::round(((a * ct.alpha_scale) + (ct.alpha_bias * 255.0)))));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline double apply_bitmap_threshold(BitmapRegion dest, BitmapRegion source, ThresholdOperation operation, double threshold_value, std::optional<double> color = std::nullopt, std::optional<double> mask = std::nullopt, std::optional<bool> copy_source = std::nullopt) {
  color = color.value_or(0.0);
  mask = mask.value_or(4294967295.0);
  copy_source = copy_source.value_or(false);
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  double changed = 0.0;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              const double pixel = static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(sd[static_cast<size_t>(si)]) << static_cast<int32_t>(24.0))) | static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(sd[static_cast<size_t>((si + 1.0))]) << static_cast<int32_t>(16.0))))) | static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(sd[static_cast<size_t>((si + 2.0))]) << static_cast<int32_t>(8.0))))) | static_cast<int32_t>(sd[static_cast<size_t>((si + 3.0))]))) & static_cast<int32_t>(mask.value())))) >> static_cast<uint32_t>(0.0));
              const bool passes = compare(pixel, operation, static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(threshold_value)) >> static_cast<uint32_t>(0.0)));
              if (passes) {
                dd[static_cast<size_t>(di)] = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(color.value())) >> static_cast<uint32_t>(24.0))) & static_cast<int32_t>(255.0));
                dd[static_cast<size_t>((di + 1.0))] = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color.value()) >> static_cast<int32_t>(16.0))) & static_cast<int32_t>(255.0));
                dd[static_cast<size_t>((di + 2.0))] = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color.value()) >> static_cast<int32_t>(8.0))) & static_cast<int32_t>(255.0));
                dd[static_cast<size_t>((di + 3.0))] = static_cast<double>(static_cast<int32_t>(color.value()) & static_cast<int32_t>(255.0));
                changed++;
              }
              else {
                if (copy_source.value()) {
                  dd[static_cast<size_t>(di)] = sd[static_cast<size_t>(si)];
                  dd[static_cast<size_t>((di + 1.0))] = sd[static_cast<size_t>((si + 1.0))];
                  dd[static_cast<size_t>((di + 2.0))] = sd[static_cast<size_t>((si + 2.0))];
                  dd[static_cast<size_t>((di + 3.0))] = sd[static_cast<size_t>((si + 3.0))];
                }
              }
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
  return changed;
}

inline void merge_bitmap(BitmapRegion dest, BitmapRegion source, double red_scale, double green_scale, double blue_scale, double alpha_scale) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              dd[static_cast<size_t>(di)] = flight::round(((sd[static_cast<size_t>(si)] * red_scale) + (dd[static_cast<size_t>(di)] * (1.0 - red_scale))));
              dd[static_cast<size_t>((di + 1.0))] = flight::round(((sd[static_cast<size_t>((si + 1.0))] * green_scale) + (dd[static_cast<size_t>((di + 1.0))] * (1.0 - green_scale))));
              dd[static_cast<size_t>((di + 2.0))] = flight::round(((sd[static_cast<size_t>((si + 2.0))] * blue_scale) + (dd[static_cast<size_t>((di + 2.0))] * (1.0 - blue_scale))));
              dd[static_cast<size_t>((di + 3.0))] = flight::round(((sd[static_cast<size_t>((si + 3.0))] * alpha_scale) + (dd[static_cast<size_t>((di + 3.0))] * (1.0 - alpha_scale))));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void scroll_bitmap(Bitmap out, double dx, double dy) {
  auto needed = out.data.length;
  if ((!scroll_scratch.value().has_value() || (scroll_scratch.value().length < needed))) {
    scroll_scratch.value() = flight::Uint8ClampedArray(needed);
  }
  scroll_scratch.value().set(out.data, 0.0);
  {
    double py = 0.0;
    while ((py < out.height)) {
      {
        const double src_y = std::fmod((std::fmod((py - dy), out.height) + out.height), out.height);
        {
          double px = 0.0;
          while ((px < out.width)) {
            {
              const double src_x = std::fmod((std::fmod((px - dx), out.width) + out.width), out.width);
              const double si = (((src_y * out.width) + src_x) * 4.0);
              const double di = (((py * out.width) + px) * 4.0);
              out.data[static_cast<size_t>(di)] = scroll_scratch.value().element(si);
              out.data[static_cast<size_t>((di + 1.0))] = scroll_scratch.value().element((si + 1.0));
              out.data[static_cast<size_t>((di + 2.0))] = scroll_scratch.value().element((si + 2.0));
              out.data[static_cast<size_t>((di + 3.0))] = scroll_scratch.value().element((si + 3.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(out);
}

inline bool compare(double a, ThresholdOperation op, double b) {
  {
    auto switch_value = op;
    if (switch_value == flight::String("<")) {
      return (a < b);
    }
    else if (switch_value == flight::String("<=")) {
      return (a <= b);
    }
    else if (switch_value == flight::String(">")) {
      return (a > b);
    }
    else if (switch_value == flight::String(">=")) {
      return (a >= b);
    }
    else if (switch_value == flight::String("==")) {
      return (a == b);
    }
    else if (switch_value == flight::String("!=")) {
      return (a != b);
    }
  }
  throw std::logic_error("Flight function completed without a value");
}

} // namespace flighthq_bitmap
