// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapMedian.ts#BitmapMedian. Do not edit.
#pragma once
#include <cstdint>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_bitmap {

inline void median_bitmap(flight::Uint8ClampedArray out, BitmapRegion source, double radius) {
  const double r = std::max(0.0, flight::round(radius));
  auto w = source.width;
  auto h = source.height;
  auto bitmap_width = source.bitmap.width;
  auto bitmap_height = source.bitmap.height;
  auto data = source.bitmap.data;
  const double area = (((2.0 * r) + 1.0) * ((2.0 * r) + 1.0));
  if (((window_red == nullptr) || (window_red.length < area))) {
    window_red = flight::Uint8Array(area);
    window_green = flight::Uint8Array(area);
    window_blue = flight::Uint8Array(area);
    window_alpha = flight::Uint8Array(area);
  }
  std::optional<flight::Uint8Array> rs = window_red;
  flight::Uint8Array gs = static_cast<flight::Uint8Array>(window_green);
  flight::Uint8Array bs = static_cast<flight::Uint8Array>(window_blue);
  flight::Uint8Array as = static_cast<flight::Uint8Array>(window_alpha);
  {
    double py = 0.0;
    while ((py < h)) {
      {
        {
          double px = 0.0;
          while ((px < w)) {
            {
              double n = 0.0;
              {
                double ky = -r;
                while ((ky <= r)) {
                  {
                    const double sy = std::max(0.0, std::min((bitmap_height - 1.0), ((source.y + py) + ky)));
                    {
                      double kx = -r;
                      while ((kx <= r)) {
                        {
                          const double sx = std::max(0.0, std::min((bitmap_width - 1.0), ((source.x + px) + kx)));
                          const double si = (((sy * bitmap_width) + sx) * 4.0);
                          rs.value()[static_cast<size_t>(n)] = data[static_cast<size_t>(si)];
                          gs.element(n) = data[static_cast<size_t>((si + 1.0))];
                          bs.element(n) = data[static_cast<size_t>((si + 2.0))];
                          as.element(n) = data[static_cast<size_t>((si + 3.0))];
                          n++;
                        }
                        kx += 1.0;
                      }
                    }
                  }
                  ky += 1.0;
                }
              }
              const double mid = static_cast<double>(static_cast<int32_t>(n) >> static_cast<int32_t>(1.0));
              const double di = (((py * w) + px) * 4.0);
              out.element(di) = median_of(rs.value(), n, mid);
              out.element((di + 1.0)) = median_of(gs, n, mid);
              out.element((di + 2.0)) = median_of(bs, n, mid);
              out.element((di + 3.0)) = median_of(as, n, mid);
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
}

inline double median_of(flight::Uint8Array values, double n, double mid) {
  {
    double i = 1.0;
    while ((i < n)) {
      {
        std::optional<auto> v = values.get(i);
        double j = (i - 1.0);
        while (((j >= 0.0) && (values.element(j) > v))) {
          values.element((j + 1.0)) = values.element(j);
          j--;
        }
        values.element((j + 1.0)) = v;
      }
      i += 1.0;
    }
  }
  return values.element(mid);
}

inline std::optional<flight::Uint8Array> window_red = nullptr;

inline std::optional<flight::Uint8Array> window_green = nullptr;

inline std::optional<flight::Uint8Array> window_blue = nullptr;

inline std::optional<flight::Uint8Array> window_alpha = nullptr;

} // namespace flighthq_bitmap
