// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapPixelate.ts#BitmapPixelate. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_bitmap {

inline void pixelate_bitmap(flight::Uint8ClampedArray out, BitmapRegion source, double block_size) {
  const double block = std::max(1.0, flight::round(block_size));
  auto w = source.width;
  auto h = source.height;
  auto bitmap_width = source.bitmap.width;
  auto bitmap_height = source.bitmap.height;
  auto data = source.bitmap.data;
  {
    double by = 0.0;
    while ((by < h)) {
      {
        const double y_end = std::min((by + block), h);
        {
          double bx = 0.0;
          while ((bx < w)) {
            {
              const double x_end = std::min((bx + block), w);
              double r = 0.0;
              double g = 0.0;
              double b = 0.0;
              double a = 0.0;
              double count = 0.0;
              {
                double py = by;
                while ((py < y_end)) {
                  {
                    auto sy = (source.y + py);
                    if (((sy < 0.0) || (sy >= bitmap_height))) {
                      py += 1.0;
                      continue;
                    }
                    {
                      double px = bx;
                      while ((px < x_end)) {
                        {
                          auto sx = (source.x + px);
                          if (((sx < 0.0) || (sx >= bitmap_width))) {
                            px += 1.0;
                            continue;
                          }
                          const double si = (((sy * bitmap_width) + sx) * 4.0);
                          r += data[static_cast<size_t>(si)];
                          g += data[static_cast<size_t>((si + 1.0))];
                          b += data[static_cast<size_t>((si + 2.0))];
                          a += data[static_cast<size_t>((si + 3.0))];
                          count++;
                        }
                        px += 1.0;
                      }
                    }
                  }
                  py += 1.0;
                }
              }
              if ((count == 0.0)) {
                bx += block;
                continue;
              }
              const double ar = flight::round((r / count));
              const double ag = flight::round((g / count));
              const double ab = flight::round((b / count));
              const double aa = flight::round((a / count));
              {
                double py_2 = by;
                while ((py_2 < y_end)) {
                  {
                    {
                      double px_2 = bx;
                      while ((px_2 < x_end)) {
                        {
                          const double di = (((py_2 * w) + px_2) * 4.0);
                          out.element(di) = ar;
                          out.element((di + 1.0)) = ag;
                          out.element((di + 2.0)) = ab;
                          out.element((di + 3.0)) = aa;
                        }
                        px_2 += 1.0;
                      }
                    }
                  }
                  py_2 += 1.0;
                }
              }
            }
            bx += block;
          }
        }
      }
      by += block;
    }
  }
}

} // namespace flighthq_bitmap
