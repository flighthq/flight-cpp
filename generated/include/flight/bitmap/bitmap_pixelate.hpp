// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapPixelate.ts#BitmapPixelate at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>

namespace flight::bitmap {

inline void pixelate_bitmap(flight::Uint8ClampedArray out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, double block_size) {
  auto block = flight::maximum(1.0, flight::round(block_size));
  const double w = flight::row_get<flight::RowKey<"width">>(source);
  const double h = flight::row_get<flight::RowKey<"height">>(source);
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(source)->width;
  auto bitmap_height = flight::row_get<flight::RowKey<"bitmap">>(source)->height;
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(source)->data;
  {
    double by = 0.0;
    while ((by < h)) {
      {
        auto y_end = flight::minimum((by + block), h);
        {
          double bx = 0.0;
          while ((bx < w)) {
            {
              auto x_end = flight::minimum((bx + block), w);
              double r = 0.0;
              double g = 0.0;
              double b = 0.0;
              double a = 0.0;
              double count = 0.0;
              {
                double py = by;
                while ((py < y_end)) {
                  {
                    auto sy = (flight::row_get<flight::RowKey<"y">>(source) + py);
                    if (((sy < 0.0) || (sy >= bitmap_height))) {
                      py += 1.0;
                      continue;
                    }
                    {
                      double px = bx;
                      while ((px < x_end)) {
                        {
                          auto sx = (flight::row_get<flight::RowKey<"x">>(source) + px);
                          if (((sx < 0.0) || (sx >= bitmap_width))) {
                            px += 1.0;
                            continue;
                          }
                          const double si = (((sy * bitmap_width) + sx) * 4.0);
                          r += data.element(si);
                          g += data.element((si + 1.0));
                          b += data.element((si + 2.0));
                          a += data.element((si + 3.0));
                          count++;
                        }
                        px += 1.0;
                      }
                    }
                  }
                  py += 1.0;
                }
              }
              if ((count == 0.0)) {
                bx += block;
                continue;
              }
              auto ar = flight::round((r / count));
              auto ag = flight::round((g / count));
              auto ab = flight::round((b / count));
              auto aa = flight::round((a / count));
              {
                double py_2 = by;
                while ((py_2 < y_end)) {
                  {
                    {
                      double px_2 = bx;
                      while ((px_2 < x_end)) {
                        {
                          const double di = (((py_2 * w) + px_2) * 4.0);
                          out.element(di) = ar;
                          out.element((di + 1.0)) = ag;
                          out.element((di + 2.0)) = ab;
                          out.element((di + 3.0)) = aa;
                        }
                        px_2 += 1.0;
                      }
                    }
                  }
                  py_2 += 1.0;
                }
              }
            }
            bx += block;
          }
        }
      }
      by += block;
    }
  }
}

} // namespace flight::bitmap
