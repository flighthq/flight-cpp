// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapComposite.ts#BitmapComposite. Do not edit.
#pragma once
#include <cstdint>
#include <optional>
#include <stdexcept>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline void composite_bitmap_pixels(BitmapRegion dest, flight::Uint8ClampedArray pixels, std::optional<bitmap_composite_mode> mode = std::nullopt) {
  mode = mode.value_or(bitmap_composite_mode.normal);
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        if (((y < 0.0) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              auto x = (dest.x + px);
              if (((x < 0.0) || (x >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((py * dest.width) + px) * 4.0);
              composite_pixel_into(dest.bitmap.data, (((y * dest.bitmap.width) + x) * 4.0), pixels[static_cast<size_t>(si)], pixels[static_cast<size_t>((si + 1.0))], pixels[static_cast<size_t>((si + 2.0))], pixels[static_cast<size_t>((si + 3.0))], mode.value());
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void composite_bitmap_region(BitmapRegion dest, BitmapRegion source, std::optional<bitmap_composite_mode> mode = std::nullopt) {
  mode = mode.value_or(bitmap_composite_mode.normal);
  const double sw = std::min(dest.width, source.width);
  const double sh = std::min(dest.height, source.height);
  {
    double py = 0.0;
    while ((py < sh)) {
      {
        auto source_y = (source.y + py);
        auto y = (dest.y + py);
        if (((((source_y < 0.0) || (source_y >= source.bitmap.height)) || (y < 0.0)) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < sw)) {
            {
              auto source_x = (source.x + px);
              auto x = (dest.x + px);
              if (((((source_x < 0.0) || (source_x >= source.bitmap.width)) || (x < 0.0)) || (x >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((source_y * source.bitmap.width) + source_x) * 4.0);
              composite_pixel_into(dest.bitmap.data, (((y * dest.bitmap.width) + x) * 4.0), source.bitmap.data[static_cast<size_t>(si)], source.bitmap.data[static_cast<size_t>((si + 1.0))], source.bitmap.data[static_cast<size_t>((si + 2.0))], source.bitmap.data[static_cast<size_t>((si + 3.0))], mode.value());
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void extract_bitmap_pixels(flight::Uint8ClampedArray out, BitmapRegion source) {
  {
    double py = 0.0;
    while ((py < source.height)) {
      {
        auto source_y = (source.y + py);
        if (((source_y < 0.0) || (source_y >= source.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < source.width)) {
            {
              auto source_x = (source.x + px);
              if (((source_x < 0.0) || (source_x >= source.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((source_y * source.bitmap.width) + source_x) * 4.0);
              const double di = (((py * source.width) + px) * 4.0);
              out.element(di) = source.bitmap.data[static_cast<size_t>(si)];
              out.element((di + 1.0)) = source.bitmap.data[static_cast<size_t>((si + 1.0))];
              out.element((di + 2.0)) = source.bitmap.data[static_cast<size_t>((si + 2.0))];
              out.element((di + 3.0)) = source.bitmap.data[static_cast<size_t>((si + 3.0))];
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
}

inline void extract_bitmap_pixels32(flight::Uint32Array out, BitmapRegion source) {
  {
    double py = 0.0;
    while ((py < source.height)) {
      {
        auto source_y = (source.y + py);
        if (((source_y < 0.0) || (source_y >= source.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < source.width)) {
            {
              auto source_x = (source.x + px);
              if (((source_x < 0.0) || (source_x >= source.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((source_y * source.bitmap.width) + source_x) * 4.0);
              out.element(((py * source.width) + px)) = static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(source.bitmap.data[static_cast<size_t>(si)]) << static_cast<int32_t>(24.0))) | static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(source.bitmap.data[static_cast<size_t>((si + 1.0))]) << static_cast<int32_t>(16.0))))) | static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(source.bitmap.data[static_cast<size_t>((si + 2.0))]) << static_cast<int32_t>(8.0))))) | static_cast<int32_t>(source.bitmap.data[static_cast<size_t>((si + 3.0))])))) >> static_cast<uint32_t>(0.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
}

inline void write_bitmap_pixels(BitmapRegion dest, flight::Uint8ClampedArray pixels) {
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        if (((y < 0.0) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              auto x = (dest.x + px);
              if (((x < 0.0) || (x >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((py * dest.width) + px) * 4.0);
              const double di = (((y * dest.bitmap.width) + x) * 4.0);
              dest.bitmap.data[static_cast<size_t>(di)] = pixels[static_cast<size_t>(si)];
              dest.bitmap.data[static_cast<size_t>((di + 1.0))] = pixels[static_cast<size_t>((si + 1.0))];
              dest.bitmap.data[static_cast<size_t>((di + 2.0))] = pixels[static_cast<size_t>((si + 2.0))];
              dest.bitmap.data[static_cast<size_t>((di + 3.0))] = pixels[static_cast<size_t>((si + 3.0))];
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void write_bitmap_pixels32(BitmapRegion dest, flight::Uint32Array pixels) {
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        if (((y < 0.0) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              auto x = (dest.x + px);
              if (((x < 0.0) || (x >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              auto color = pixels[static_cast<size_t>(((py * dest.width) + px))];
              const double di = (((y * dest.bitmap.width) + x) * 4.0);
              dest.bitmap.data[static_cast<size_t>(di)] = static_cast<double>(static_cast<int32_t>((color >>> 24.0)) & static_cast<int32_t>(255.0));
              dest.bitmap.data[static_cast<size_t>((di + 1.0))] = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color) >> static_cast<int32_t>(16.0))) & static_cast<int32_t>(255.0));
              dest.bitmap.data[static_cast<size_t>((di + 2.0))] = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color) >> static_cast<int32_t>(8.0))) & static_cast<int32_t>(255.0));
              dest.bitmap.data[static_cast<size_t>((di + 3.0))] = static_cast<double>(static_cast<int32_t>(color) & static_cast<int32_t>(255.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline std::tuple<double, double> porter_duff_factors(bitmap_composite_mode mode, double src_a, double dst_a) {
  {
    auto switch_value = mode;
    if (switch_value == bitmap_composite_mode.destination_over) {
      return std::make_tuple((1.0 - dst_a), 1.0);
    }
    else if (switch_value == bitmap_composite_mode.source_in) {
      return std::make_tuple(dst_a, 0.0);
    }
    else if (switch_value == bitmap_composite_mode.destination_in) {
      return std::make_tuple(0.0, src_a);
    }
    else if (switch_value == bitmap_composite_mode.source_out) {
      return std::make_tuple((1.0 - dst_a), 0.0);
    }
    else if (switch_value == bitmap_composite_mode.destination_out) {
      return std::make_tuple(0.0, (1.0 - src_a));
    }
    else if (switch_value == bitmap_composite_mode.source_atop) {
      return std::make_tuple(dst_a, (1.0 - src_a));
    }
    else if (switch_value == bitmap_composite_mode.destination_atop) {
      return std::make_tuple((1.0 - dst_a), src_a);
    }
    else if (switch_value == bitmap_composite_mode.xor_) {
      return std::make_tuple((1.0 - dst_a), (1.0 - src_a));
    }
    else if (switch_value == bitmap_composite_mode.copy) {
      return std::make_tuple(1.0, 0.0);
    }
    else if (switch_value == bitmap_composite_mode.clear) {
      return std::make_tuple(0.0, 0.0);
    }
    else {
      return std::make_tuple(1.0, (1.0 - src_a));
    }
  }
  throw std::logic_error("Flight function completed without a value");
}

inline double blend_channel(bitmap_composite_mode mode, double cb, double cs) {
  {
    auto switch_value_2 = mode;
    if (switch_value_2 == bitmap_composite_mode.multiply) {
      return ((cb * cs) / 255.0);
    }
    else if (switch_value_2 == bitmap_composite_mode.screen) {
      return ((cb + cs) - ((cb * cs) / 255.0));
    }
    else if (switch_value_2 == bitmap_composite_mode.add) {
      return std::min(255.0, (cb + cs));
    }
    else if (switch_value_2 == bitmap_composite_mode.subtract) {
      return std::max(0.0, (cb - cs));
    }
    else if (switch_value_2 == bitmap_composite_mode.darken) {
      return std::min(cb, cs);
    }
    else if (switch_value_2 == bitmap_composite_mode.lighten) {
      return std::max(cb, cs);
    }
    else if (switch_value_2 == bitmap_composite_mode.difference) {
      return std::abs((cb - cs));
    }
    else if (switch_value_2 == bitmap_composite_mode.exclusion) {
      return ((cb + cs) - (((2.0 * cb) * cs) / 255.0));
    }
    else if (switch_value_2 == bitmap_composite_mode.overlay) {
      return ((cb < 128.0) ? (((2.0 * cb) * cs) / 255.0) : (255.0 - (((2.0 * (255.0 - cb)) * (255.0 - cs)) / 255.0)));
    }
    else if (switch_value_2 == bitmap_composite_mode.hard_light) {
      return ((cs < 128.0) ? (((2.0 * cb) * cs) / 255.0) : (255.0 - (((2.0 * (255.0 - cb)) * (255.0 - cs)) / 255.0)));
    }
    else if (switch_value_2 == bitmap_composite_mode.soft_light) {
      return soft_light_channel(cb, cs);
    }
    else if (switch_value_2 == bitmap_composite_mode.color_dodge) {
      return ((cs >= 255.0) ? 255.0 : std::min(255.0, ((cb * 255.0) / (255.0 - cs))));
    }
    else if (switch_value_2 == bitmap_composite_mode.color_burn) {
      return ((cs <= 0.0) ? 0.0 : (255.0 - std::min(255.0, (((255.0 - cb) * 255.0) / cs))));
    }
    else if (switch_value_2 == bitmap_composite_mode.invert) {
      return (255.0 - cb);
    }
    else {
      return cs;
    }
  }
  throw std::logic_error("Flight function completed without a value");
}

inline double soft_light_channel(double cb, double cs) {
  const double b = (cb / 255.0);
  const double s = (cs / 255.0);
  const double d = ((b <= 0.25) ? (((((16.0 * b) - 12.0) * b) + 4.0) * b) : std::sqrt(b));
  const double out = ((s <= 0.5) ? (b - (((1.0 - (2.0 * s)) * b) * (1.0 - b))) : (b + (((2.0 * s) - 1.0) * (d - b))));
  return (out * 255.0);
}

inline void composite_pixel_into(flight::Uint8ClampedArray dest, double di, double r, double g, double b, double a, bitmap_composite_mode mode) {
  const double src_a = (a / 255.0);
  const double dst_a = (dest.element((di + 3.0)) / 255.0);
  std::tuple<double, double> array_pattern_value = porter_duff_factors(mode, src_a, dst_a);
  const double fa = std::get<0>(array_pattern_value);
  const double fb = std::get<1>(array_pattern_value);
  const double out_a = ((fa * src_a) + (fb * dst_a));
  if ((out_a <= 0.0)) {
    dest.element(di) = 0.0;
    dest.element((di + 1.0)) = 0.0;
    dest.element((di + 2.0)) = 0.0;
    dest.element((di + 3.0)) = 0.0;
    return;
  }
  std::optional<auto> cb_r = dest.get(di);
  std::optional<auto> cb_g = dest.get((di + 1.0));
  std::optional<auto> cb_b = dest.get((di + 2.0));
  const double cs_r = (((1.0 - dst_a) * r) + (dst_a * blend_channel(mode, cb_r, r)));
  const double cs_g = (((1.0 - dst_a) * g) + (dst_a * blend_channel(mode, cb_g, g)));
  const double cs_b = (((1.0 - dst_a) * b) + (dst_a * blend_channel(mode, cb_b, b)));
  dest.element(di) = flight::round(((((fa * src_a) * cs_r) + ((fb * dst_a) * cb_r)) / out_a));
  dest.element((di + 1.0)) = flight::round(((((fa * src_a) * cs_g) + ((fb * dst_a) * cb_g)) / out_a));
  dest.element((di + 2.0)) = flight::round(((((fa * src_a) * cs_b) + ((fb * dst_a) * cb_b)) / out_a));
  dest.element((di + 3.0)) = flight::round((out_a * 255.0));
}

} // namespace flighthq_bitmap
