// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapMorphological.ts#BitmapMorphological. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_bitmap {

inline void dilate_bitmap(flight::Uint8ClampedArray out, BitmapRegion source, double radius) {
  apply_morphological(out, source, radius, true);
}

inline void erode_bitmap(flight::Uint8ClampedArray out, BitmapRegion source, double radius) {
  apply_morphological(out, source, radius, false);
}

inline void apply_morphological(flight::Uint8ClampedArray out, BitmapRegion source, double radius, bool dilate) {
  const double r = std::max(0.0, flight::round(radius));
  auto w = source.width;
  auto h = source.height;
  auto bitmap_width = source.bitmap.width;
  auto bitmap_height = source.bitmap.height;
  auto data = source.bitmap.data;
  auto identity = (dilate ? 0.0 : 255.0);
  {
    double py = 0.0;
    while ((py < h)) {
      {
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto v_r = identity;
              auto v_g = identity;
              auto v_b = identity;
              auto v_a = identity;
              {
                double ky = -r;
                while ((ky <= r)) {
                  {
                    const double sy = std::max(0.0, std::min((bitmap_height - 1.0), ((source.y + py) + ky)));
                    {
                      double kx = -r;
                      while ((kx <= r)) {
                        {
                          const double sx = std::max(0.0, std::min((bitmap_width - 1.0), ((source.x + px) + kx)));
                          const double si = (((sy * bitmap_width) + sx) * 4.0);
                          if (dilate) {
                            if ((data[static_cast<size_t>(si)] > v_r)) {
                              v_r = data[static_cast<size_t>(si)];
                            }
                            if ((data[static_cast<size_t>((si + 1.0))] > v_g)) {
                              v_g = data[static_cast<size_t>((si + 1.0))];
                            }
                            if ((data[static_cast<size_t>((si + 2.0))] > v_b)) {
                              v_b = data[static_cast<size_t>((si + 2.0))];
                            }
                            if ((data[static_cast<size_t>((si + 3.0))] > v_a)) {
                              v_a = data[static_cast<size_t>((si + 3.0))];
                            }
                          }
                          else {
                            if ((data[static_cast<size_t>(si)] < v_r)) {
                              v_r = data[static_cast<size_t>(si)];
                            }
                            if ((data[static_cast<size_t>((si + 1.0))] < v_g)) {
                              v_g = data[static_cast<size_t>((si + 1.0))];
                            }
                            if ((data[static_cast<size_t>((si + 2.0))] < v_b)) {
                              v_b = data[static_cast<size_t>((si + 2.0))];
                            }
                            if ((data[static_cast<size_t>((si + 3.0))] < v_a)) {
                              v_a = data[static_cast<size_t>((si + 3.0))];
                            }
                          }
                        }
                        kx += 1.0;
                      }
                    }
                  }
                  ky += 1.0;
                }
              }
              const double di = (((py * w) + px) * 4.0);
              out.element(di) = v_r;
              out.element((di + 1.0)) = v_g;
              out.element((di + 2.0)) = v_b;
              out.element((di + 3.0)) = v_a;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
}

} // namespace flighthq_bitmap
