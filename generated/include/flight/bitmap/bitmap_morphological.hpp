// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapMorphological.ts#BitmapMorphological at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { struct Bitmap; }
namespace flight::types { struct BitmapRegion; }

#include <flight/types/alpha_type.hpp>
#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/types/texture_source_kind.hpp>

namespace flight::bitmap {

inline void apply_morphological(flight::Uint8ClampedArray out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, double radius, bool dilate) {
  const double r = flight::maximum(0.0, flight::round(radius));
  const double w = flight::row_get<flight::RowKey<"width">>(source);
  const double h = flight::row_get<flight::RowKey<"height">>(source);
  const double bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(source)->width;
  const double bitmap_height = flight::row_get<flight::RowKey<"bitmap">>(source)->height;
  flight::Uint8ClampedArray data = flight::row_get<flight::RowKey<"bitmap">>(source)->data;
  const double identity = (dilate ? 0.0 : 255.0);
  {
    double py = 0.0;
    while ((py < h)) {
      {
        {
          double px = 0.0;
          while ((px < w)) {
            {
              double v_r = identity;
              double v_g = identity;
              double v_b = identity;
              double v_a = identity;
              {
                double ky = -r;
                while ((ky <= r)) {
                  {
                    const double sy = flight::maximum(0.0, flight::minimum((bitmap_height - 1.0), ((flight::row_get<flight::RowKey<"y">>(source) + py) + ky)));
                    {
                      double kx = -r;
                      while ((kx <= r)) {
                        {
                          const double sx = flight::maximum(0.0, flight::minimum((bitmap_width - 1.0), ((flight::row_get<flight::RowKey<"x">>(source) + px) + kx)));
                          const double si = (((sy * bitmap_width) + sx) * 4.0);
                          if (dilate) {
                            if ((static_cast<double>(data.element(si)) > v_r)) {
                              (v_r = static_cast<double>(data.element(si)));
                            }
                            if ((static_cast<double>(data.element((si + 1.0))) > v_g)) {
                              (v_g = static_cast<double>(data.element((si + 1.0))));
                            }
                            if ((static_cast<double>(data.element((si + 2.0))) > v_b)) {
                              (v_b = static_cast<double>(data.element((si + 2.0))));
                            }
                            if ((static_cast<double>(data.element((si + 3.0))) > v_a)) {
                              (v_a = static_cast<double>(data.element((si + 3.0))));
                            }
                          }
                          else {
                            if ((static_cast<double>(data.element(si)) < v_r)) {
                              (v_r = static_cast<double>(data.element(si)));
                            }
                            if ((static_cast<double>(data.element((si + 1.0))) < v_g)) {
                              (v_g = static_cast<double>(data.element((si + 1.0))));
                            }
                            if ((static_cast<double>(data.element((si + 2.0))) < v_b)) {
                              (v_b = static_cast<double>(data.element((si + 2.0))));
                            }
                            if ((static_cast<double>(data.element((si + 3.0))) < v_a)) {
                              (v_a = static_cast<double>(data.element((si + 3.0))));
                            }
                          }
                        }
                        (kx += 1.0);
                      }
                    }
                  }
                  (ky += 1.0);
                }
              }
              const double di = (((py * w) + px) * 4.0);
              (out.element(di) = v_r);
              (out.element((di + 1.0)) = v_g);
              (out.element((di + 2.0)) = v_b);
              (out.element((di + 3.0)) = v_a);
            }
            (px += 1.0);
          }
        }
      }
      (py += 1.0);
    }
  }
}

inline void dilate_bitmap(flight::Uint8ClampedArray out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, double radius) {
  apply_morphological(out, source, radius, true);
}

inline void erode_bitmap(flight::Uint8ClampedArray out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, double radius) {
  apply_morphological(out, source, radius, false);
}

} // namespace flight::bitmap
