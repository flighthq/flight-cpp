// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapRegion.ts#BitmapRegion. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_bitmap {

inline NonEntityCreateResult<BitmapRegion, flight::String> create_bitmap_region(Bitmap bitmap, std::optional<double> x = std::nullopt, std::optional<double> y = std::nullopt, std::optional<double> width = std::nullopt, std::optional<double> height = std::nullopt) {
  x = x.value_or(0.0);
  y = y.value_or(0.0);
  width = width.value_or(bitmap.width);
  height = height.value_or(bitmap.height);
  return {.bitmap = bitmap, .x = x.value(), .y = y.value(), .width = width.value(), .height = height.value()};
}

inline BitmapRegion set_bitmap_region(BitmapRegion out, Bitmap bitmap, std::optional<double> x = std::nullopt, std::optional<double> y = std::nullopt, std::optional<double> width = std::nullopt, std::optional<double> height = std::nullopt) {
  x = x.value_or(0.0);
  y = y.value_or(0.0);
  width = width.value_or(bitmap.width);
  height = height.value_or(bitmap.height);
  out.bitmap = bitmap;
  out.x = x.value();
  out.y = y.value();
  out.width = width.value();
  out.height = height.value();
  return out;
}

} // namespace flighthq_bitmap
