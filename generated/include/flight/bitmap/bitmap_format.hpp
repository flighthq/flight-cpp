// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapFormat.ts#BitmapFormat at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/pixel_order.hpp>

namespace flight::bitmap {

inline void premultiply_bitmap_pixels(flight::Uint8ClampedArray out, flight::Uint8ClampedArray source, double length) {
  {
    double i = 0.0;
    while ((i < length)) {
      {
        const double a = (source.element((i + 3.0)) / 255.0);
        out.element(i) = flight::round((source.element(i) * a));
        out.element((i + 1.0)) = flight::round((source.element((i + 1.0)) * a));
        out.element((i + 2.0)) = flight::round((source.element((i + 2.0)) * a));
        out.element((i + 3.0)) = source.element((i + 3.0));
      }
      i += 4.0;
    }
  }
}

inline void unpremultiply_bitmap_pixels(flight::Uint8ClampedArray out, flight::Uint8ClampedArray source, double length) {
  {
    double i = 0.0;
    while ((i < length)) {
      {
        auto a = source.element((i + 3.0));
        if ((a == 0.0)) {
          out.element(i) = 0.0;
          out.element((i + 1.0)) = 0.0;
          out.element((i + 2.0)) = 0.0;
          out.element((i + 3.0)) = 0.0;
        }
        else {
          const double inv = (255.0 / a);
          out.element(i) = flight::minimum(255.0, flight::round((source.element(i) * inv)));
          out.element((i + 1.0)) = flight::minimum(255.0, flight::round((source.element((i + 1.0)) * inv)));
          out.element((i + 2.0)) = flight::minimum(255.0, flight::round((source.element((i + 2.0)) * inv)));
          out.element((i + 3.0)) = a;
        }
      }
      i += 4.0;
    }
  }
}

inline flight::Array<double> channel_offsets(flight::types::PixelOrder order) {
  {
    auto switch_value = order;
    if (switch_value == flight::String("RGBA")) {
      return flight::Array<double>{0.0, 1.0, 2.0, 3.0};
    }
    else if (switch_value == flight::String("BGRA")) {
      return flight::Array<double>{2.0, 1.0, 0.0, 3.0};
    }
    else if (switch_value == flight::String("ARGB")) {
      return flight::Array<double>{1.0, 2.0, 3.0, 0.0};
    }
    else if (switch_value == flight::String("ABGR")) {
      return flight::Array<double>{3.0, 2.0, 1.0, 0.0};
    }
  }
  throw std::logic_error("Flight function completed without a value");
}

inline void convert_bitmap_pixel_order(flight::Uint8ClampedArray out, flight::Uint8ClampedArray source, double length, flight::types::PixelOrder from, flight::types::PixelOrder to) {
  if ((from == to)) {
    if ((out != source)) {
      out.set(source.subarray(0.0, length));
    }
    return;
  }
  flight::Array<double> array_pattern_value = channel_offsets(from);
  const double src_r = array_pattern_value.element(0.0);
  const double src_g = array_pattern_value.element(1.0);
  const double src_b = array_pattern_value.element(2.0);
  const double src_a = array_pattern_value.element(3.0);
  flight::Array<double> array_pattern_value_2 = channel_offsets(to);
  const double dst_r = array_pattern_value_2.element(0.0);
  const double dst_g = array_pattern_value_2.element(1.0);
  const double dst_b = array_pattern_value_2.element(2.0);
  const double dst_a = array_pattern_value_2.element(3.0);
  {
    double i = 0.0;
    while ((i < length)) {
      {
        auto r = source.element((i + src_r));
        auto g = source.element((i + src_g));
        auto b = source.element((i + src_b));
        auto a = source.element((i + src_a));
        out.element((i + dst_r)) = r;
        out.element((i + dst_g)) = g;
        out.element((i + dst_b)) = b;
        out.element((i + dst_a)) = a;
      }
      i += 4.0;
    }
  }
}

} // namespace flight::bitmap
