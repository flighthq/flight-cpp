// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapTone.ts#BitmapTone at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <variant>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/bitmap/bitmap.hpp>

namespace flight::bitmap {

inline void apply_bitmap_curve(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, std::optional<std::variant<flight::Uint8Array, flight::Uint8ClampedArray>> red_lut, std::optional<std::variant<flight::Uint8Array, flight::Uint8ClampedArray>> green_lut, std::optional<std::variant<flight::Uint8Array, flight::Uint8ClampedArray>> blue_lut, std::optional<std::optional<std::variant<flight::Uint8Array, flight::Uint8ClampedArray>>> alpha_lut = std::nullopt) {
  alpha_lut = alpha_lut.value_or(nullptr);
  auto w = flight::minimum(flight::row_get<flight::RowKey<"width">>(out), flight::row_get<flight::RowKey<"width">>(source));
  auto h = flight::minimum(flight::row_get<flight::RowKey<"height">>(out), flight::row_get<flight::RowKey<"height">>(source));
  flight::Uint8ClampedArray<flight::ArrayBuffer> od = flight::row_get<flight::RowKey<"bitmap">>(out)->data;
  flight::Uint8ClampedArray<flight::ArrayBuffer> sd = flight::row_get<flight::RowKey<"bitmap">>(source)->data;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (flight::row_get<flight::RowKey<"y">>(source) + py);
        auto oy = (flight::row_get<flight::RowKey<"y">>(out) + py);
        if (((((sy < 0.0) || (sy >= flight::row_get<flight::RowKey<"bitmap">>(source)->height)) || (oy < 0.0)) || (oy >= flight::row_get<flight::RowKey<"bitmap">>(out)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (flight::row_get<flight::RowKey<"x">>(source) + px);
              auto ox = (flight::row_get<flight::RowKey<"x">>(out) + px);
              if (((((sx < 0.0) || (sx >= flight::row_get<flight::RowKey<"bitmap">>(source)->width)) || (ox < 0.0)) || (ox >= flight::row_get<flight::RowKey<"bitmap">>(out)->width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * flight::row_get<flight::RowKey<"bitmap">>(source)->width) + sx) * 4.0);
              const double oi = (((oy * flight::row_get<flight::RowKey<"bitmap">>(out)->width) + ox) * 4.0);
              auto r = sd.element(si);
              auto g = sd.element((si + 1.0));
              auto b = sd.element((si + 2.0));
              auto a = sd.element((si + 3.0));
              od.element(oi) = (red_lut.has_value() ? red_lut[static_cast<size_t>(r)] : r);
              od.element((oi + 1.0)) = (green_lut.has_value() ? green_lut[static_cast<size_t>(g)] : g);
              od.element((oi + 2.0)) = (blue_lut.has_value() ? blue_lut[static_cast<size_t>(b)] : b);
              od.element((oi + 3.0)) = (alpha_lut.value().has_value() ? alpha_lut.value()[static_cast<size_t>(a)] : a);
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(out));
}

inline void apply_bitmap_levels(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, std::optional<double> black_point = std::nullopt, std::optional<double> white_point = std::nullopt, std::optional<double> gamma = std::nullopt) {
  black_point = black_point.value_or(0.0);
  white_point = white_point.value_or(255.0);
  gamma = gamma.value_or(1.0);
  auto bp = flight::maximum(0.0, flight::minimum(254.0, black_point.value()));
  auto wp = flight::maximum((bp + 1.0), flight::minimum(255.0, white_point.value()));
  const double span = (wp - bp);
  const double inv_gamma = ((gamma.value() > 0.0) ? (1.0 / gamma.value()) : 1.0);
  flight::Uint8ClampedArray lut = flight::Uint8ClampedArray(256.0);
  {
    double i = 0.0;
    while ((i < 256.0)) {
      {
        auto normalized = flight::maximum(0.0, flight::minimum(1.0, ((i - bp) / span)));
        lut.element(i) = flight::round((flight::power(normalized, inv_gamma) * 255.0));
      }
      i += 1.0;
    }
  }
  apply_bitmap_curve(out, source, lut, lut, lut, nullptr);
}

} // namespace flight::bitmap
