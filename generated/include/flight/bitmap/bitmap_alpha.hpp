// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapAlpha.ts#BitmapAlpha at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/bitmap/bitmap.hpp>

namespace flight::bitmap {

inline void copy_bitmap_alpha(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source) {
  auto w = flight::minimum(flight::row_get<flight::RowKey<"width">>(dest), flight::row_get<flight::RowKey<"width">>(source));
  auto h = flight::minimum(flight::row_get<flight::RowKey<"height">>(dest), flight::row_get<flight::RowKey<"height">>(source));
  flight::Uint8ClampedArray<flight::ArrayBuffer> sd = flight::row_get<flight::RowKey<"bitmap">>(source)->data;
  flight::Uint8ClampedArray<flight::ArrayBuffer> dd = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (flight::row_get<flight::RowKey<"y">>(source) + py);
        auto dy = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        if (((((sy < 0.0) || (sy >= flight::row_get<flight::RowKey<"bitmap">>(source)->height)) || (dy < 0.0)) || (dy >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (flight::row_get<flight::RowKey<"x">>(source) + px);
              auto dx = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((((sx < 0.0) || (sx >= flight::row_get<flight::RowKey<"bitmap">>(source)->width)) || (dx < 0.0)) || (dx >= flight::row_get<flight::RowKey<"bitmap">>(dest)->width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * flight::row_get<flight::RowKey<"bitmap">>(source)->width) + sx) * 4.0);
              const double di = (((dy * flight::row_get<flight::RowKey<"bitmap">>(dest)->width) + dx) * 4.0);
              auto alpha = sd.element((si + 3.0));
              dd.element((di + 3.0)) = alpha;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

inline void multiply_bitmap_alpha(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> out, double factor) {
  auto f = flight::maximum(0.0, flight::minimum(1.0, factor));
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(out)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(out)->width;
  {
    double py = 0.0;
    while ((py < flight::row_get<flight::RowKey<"height">>(out))) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(out) + py);
        if (((y < 0.0) || (y >= flight::row_get<flight::RowKey<"bitmap">>(out)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < flight::row_get<flight::RowKey<"width">>(out))) {
            {
              auto x = (flight::row_get<flight::RowKey<"x">>(out) + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double i = ((((y * bitmap_width) + x) * 4.0) + 3.0);
              data.element(i) = flight::round((data.element(i) * f));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(out));
}

inline void set_bitmap_alpha(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> out, double alpha) {
  auto a = flight::maximum(0.0, flight::minimum(255.0, flight::round(alpha)));
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(out)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(out)->width;
  {
    double py = 0.0;
    while ((py < flight::row_get<flight::RowKey<"height">>(out))) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(out) + py);
        if (((y < 0.0) || (y >= flight::row_get<flight::RowKey<"bitmap">>(out)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < flight::row_get<flight::RowKey<"width">>(out))) {
            {
              auto x = (flight::row_get<flight::RowKey<"x">>(out) + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              data.element(((((y * bitmap_width) + x) * 4.0) + 3.0)) = a;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(out));
}

} // namespace flight::bitmap
