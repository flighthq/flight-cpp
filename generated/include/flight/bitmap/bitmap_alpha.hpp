// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapAlpha.ts#BitmapAlpha. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline void copy_bitmap_alpha(BitmapRegion dest, BitmapRegion source) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              auto alpha = sd[static_cast<size_t>((si + 3.0))];
              dd[static_cast<size_t>((di + 3.0))] = alpha;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void multiply_bitmap_alpha(BitmapRegion out, double factor) {
  const double f = std::max(0.0, std::min(1.0, factor));
  auto data = out.bitmap.data;
  auto bitmap_width = out.bitmap.width;
  {
    double py = 0.0;
    while ((py < out.height)) {
      {
        auto y = (out.y + py);
        if (((y < 0.0) || (y >= out.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < out.width)) {
            {
              auto x = (out.x + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double i = ((((y * bitmap_width) + x) * 4.0) + 3.0);
              data[static_cast<size_t>(i)] = flight::round((data[static_cast<size_t>(i)] * f));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(out.bitmap);
}

inline void set_bitmap_alpha(BitmapRegion out, double alpha) {
  const double a = std::max(0.0, std::min(255.0, flight::round(alpha)));
  auto data = out.bitmap.data;
  auto bitmap_width = out.bitmap.width;
  {
    double py = 0.0;
    while ((py < out.height)) {
      {
        auto y = (out.y + py);
        if (((y < 0.0) || (y >= out.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < out.width)) {
            {
              auto x = (out.x + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              data[static_cast<size_t>(((((y * bitmap_width) + x) * 4.0) + 3.0))] = a;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(out.bitmap);
}

} // namespace flighthq_bitmap
