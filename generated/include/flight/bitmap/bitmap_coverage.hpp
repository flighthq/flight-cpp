// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapCoverage.ts#BitmapCoverage at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/alpha_type.hpp>
#include <flight/types/bitmap.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/types/texture_source_kind.hpp>

namespace flight::bitmap {

inline double get_bitmap_coverage(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Bitmap>>>> source, double background_color, std::optional<double> channel_tolerance = std::nullopt) {
  channel_tolerance = channel_tolerance.value_or(0.0);
  const double br = flight::bitwise_and(flight::unsigned_right_shift(background_color, 24.0), 255.0);
  const double bg = flight::bitwise_and(flight::signed_right_shift(background_color, 16.0), 255.0);
  const double bb = flight::bitwise_and(flight::signed_right_shift(background_color, 8.0), 255.0);
  const double ba = flight::bitwise_and(background_color, 255.0);
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"data">>(source);
  const double total_pixels = (flight::row_get<flight::RowKey<"width">>(source) * flight::row_get<flight::RowKey<"height">>(source));
  if ((total_pixels == 0.0)) {
    return 0.0;
  }
  double covered = 0.0;
  {
    double i = 0.0;
    while ((i < static_cast<double>(data.size()))) {
      {
        if (((((std::abs((data.element(i) - br)) > channel_tolerance.value()) || (std::abs((data.element((i + 1.0)) - bg)) > channel_tolerance.value())) || (std::abs((data.element((i + 2.0)) - bb)) > channel_tolerance.value())) || (std::abs((data.element((i + 3.0)) - ba)) > channel_tolerance.value()))) {
          covered++;
        }
      }
      i += 4.0;
    }
  }
  return (covered / total_pixels);
}

} // namespace flight::bitmap
