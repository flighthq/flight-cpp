// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapGradientFill.ts#BitmapGradientFill at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/gradient_spread.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/bitmap/bitmap.hpp>

namespace flight::bitmap {

inline double spread_index(double t, flight::types::GradientSpread spread) {
  double s;
  {
    auto switch_value = spread;
    if (switch_value == flight::String("repeat")) {
      {
        s = (t - std::floor(t));
      }
    }
    else if (switch_value == flight::String("reflect")) {
      {
        const double wrapped = (t - (std::floor((t / 2.0)) * 2.0));
        s = ((wrapped <= 1.0) ? wrapped : (2.0 - wrapped));
      }
    }
    else {
      {
        s = flight::maximum(0.0, flight::minimum(1.0, t));
      }
    }
  }
  return flight::minimum(255.0, flight::round((s * 255.0)));
}

inline void fill_bitmap_linear_gradient(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::Uint8ClampedArray ramp, double x0, double y0, double x1, double y1, std::optional<flight::types::GradientSpread> spread = std::nullopt) {
  spread = spread.value_or(flight::String("pad"));
  const double dw = flight::row_get<flight::RowKey<"width">>(dest);
  const double dh = flight::row_get<flight::RowKey<"height">>(dest);
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  auto bitmap_height = flight::row_get<flight::RowKey<"bitmap">>(dest)->height;
  const double axis_x = (x1 - x0);
  const double axis_y = (y1 - y0);
  const double len_sq = ((axis_x * axis_x) + (axis_y * axis_y));
  const double inv_len = ((len_sq > 0.0) ? (1.0 / len_sq) : 0.0);
  {
    double py = 0.0;
    while ((py < dh)) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        if (((y < 0.0) || (y >= bitmap_height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dw)) {
            {
              auto x = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double t = ((((px - x0) * axis_x) + ((py - y0) * axis_y)) * inv_len);
              const double idx = spread_index(t, spread.value());
              const double ri = (idx * 4.0);
              const double i = (((y * bitmap_width) + x) * 4.0);
              data.element(i) = ramp.element(ri);
              data.element((i + 1.0)) = ramp.element((ri + 1.0));
              data.element((i + 2.0)) = ramp.element((ri + 2.0));
              data.element((i + 3.0)) = ramp.element((ri + 3.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

inline void fill_bitmap_radial_gradient(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::Uint8ClampedArray ramp, double cx, double cy, double radius, std::optional<double> focal_x = std::nullopt, std::optional<double> focal_y = std::nullopt, std::optional<flight::types::GradientSpread> spread = std::nullopt) {
  focal_x = focal_x.value_or(cx);
  focal_y = focal_y.value_or(cy);
  spread = spread.value_or(flight::String("pad"));
  const double dw = flight::row_get<flight::RowKey<"width">>(dest);
  const double dh = flight::row_get<flight::RowKey<"height">>(dest);
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  auto bitmap_height = flight::row_get<flight::RowKey<"bitmap">>(dest)->height;
  const double inv_radius = ((radius > 0.0) ? (1.0 / radius) : 0.0);
  const double fdx = (focal_x.value() - cx);
  const double fdy = (focal_y.value() - cy);
  {
    double py = 0.0;
    while ((py < dh)) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        if (((y < 0.0) || (y >= bitmap_height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dw)) {
            {
              auto x = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double dx = (px - focal_x.value());
              const double dy = (py - focal_y.value());
              const double t = ((std::sqrt(((dx * dx) + (dy * dy))) * inv_radius) - ((((dx * fdx) + (dy * fdy)) * inv_radius) * inv_radius));
              const double idx = spread_index(t, spread.value());
              const double ri = (idx * 4.0);
              const double i = (((y * bitmap_width) + x) * 4.0);
              data.element(i) = ramp.element(ri);
              data.element((i + 1.0)) = ramp.element((ri + 1.0));
              data.element((i + 2.0)) = ramp.element((ri + 2.0));
              data.element((i + 3.0)) = ramp.element((ri + 3.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

} // namespace flight::bitmap
