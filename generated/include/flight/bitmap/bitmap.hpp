// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmap.ts#Bitmap at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/entity/entity.hpp>
#include <flight/types/alpha_type.hpp>
#include <flight/types/bitmap.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/types/texture_source_kind.hpp>

namespace flight::bitmap {

inline flight::Ref<flight::types::Bitmap> clone_bitmap(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Bitmap>>>> source) {
  flight::types::EntityConstruction<flight::Ref<flight::types::Bitmap>> out = flight::entity::allocate_entity<flight::Ref<flight::types::Bitmap>>();
  flight::row_set<flight::RowKey<"alphaType">>(out, flight::row_get<flight::RowKey<"alphaType">>(source));
  flight::row_set<flight::RowKey<"gamut">>(out, flight::row_get<flight::RowKey<"gamut">>(source));
  flight::row_set<flight::RowKey<"data">>(out, flight::Uint8ClampedArray(flight::row_get<flight::RowKey<"data">>(source)));
  flight::row_set<flight::RowKey<"format">>(out, flight::row_get<flight::RowKey<"format">>(source));
  flight::row_set<flight::RowKey<"height">>(out, flight::row_get<flight::RowKey<"height">>(source));
  flight::row_set<flight::RowKey<"kind">>(out, flight::row_get<flight::RowKey<"kind">>(source));
  flight::row_set<flight::RowKey<"version">>(out, 0.0);
  flight::row_set<flight::RowKey<"width">>(out, flight::row_get<flight::RowKey<"width">>(source));
  return flight::entity::finish_entity(out);
}

inline void initialize_bitmap(flight::types::EntityConstruction<flight::Ref<flight::types::Bitmap>> out, double width, double height, std::optional<double> color = std::nullopt) {
  color = color.value_or(0.0);
  flight::Uint8ClampedArray data = flight::Uint8ClampedArray(((width * height) * 4.0));
  if ((color.value() != 0.0)) {
    const double r = flight::bitwise_and(flight::unsigned_right_shift(color.value(), 24.0), 255.0);
    const double g = flight::bitwise_and(flight::signed_right_shift(color.value(), 16.0), 255.0);
    const double b = flight::bitwise_and(flight::signed_right_shift(color.value(), 8.0), 255.0);
    const double a = flight::bitwise_and(color.value(), 255.0);
    {
      double i = 0.0;
      while ((i < static_cast<double>(data.size()))) {
        {
          data.element(i) = r;
          data.element((i + 1.0)) = g;
          data.element((i + 2.0)) = b;
          data.element((i + 3.0)) = a;
        }
        i += 4.0;
      }
    }
  }
  flight::row_set<flight::RowKey<"alphaType">>(out, flight::String("straight"));
  flight::row_set<flight::RowKey<"gamut">>(out, flight::String("srgb"));
  flight::row_set<flight::RowKey<"data">>(out, data);
  flight::row_set<flight::RowKey<"format">>(out, flight::String("rgba8unorm"));
  flight::row_set<flight::RowKey<"height">>(out, height);
  flight::row_set<flight::RowKey<"kind">>(out, flight::types::bitmap_texture_source_kind);
  flight::row_set<flight::RowKey<"version">>(out, 0.0);
  flight::row_set<flight::RowKey<"width">>(out, width);
}

inline flight::Ref<flight::types::Bitmap> create_bitmap(double width, double height, std::optional<double> color = std::nullopt) {
  color = color.value_or(0.0);
  flight::types::EntityConstruction<flight::Ref<flight::types::Bitmap>> out = flight::entity::allocate_entity<flight::Ref<flight::types::Bitmap>>();
  initialize_bitmap(out, width, height, color.value());
  return flight::entity::finish_entity(out);
}

inline void invalidate_bitmap(flight::Ref<flight::types::Bitmap> bitmap) {
  bitmap->version = flight::unsigned_right_shift((bitmap->version + 1.0), 0.0);
}

inline void convert_bitmap_alpha_type(flight::Ref<flight::types::Bitmap> out, flight::types::AlphaType target) {
  if ((out->alpha_type == target)) {
    return;
  }
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = out->data;
  const double len = ((out->width * out->height) * 4.0);
  if ((target == flight::String("premultiplied"))) {
    {
      double i = 0.0;
      while ((i < len)) {
        {
          const double a = (data.element((i + 3.0)) / 255.0);
          data.element(i) = flight::round((data.element(i) * a));
          data.element((i + 1.0)) = flight::round((data.element((i + 1.0)) * a));
          data.element((i + 2.0)) = flight::round((data.element((i + 2.0)) * a));
        }
        i += 4.0;
      }
    }
  }
  else {
    {
      double i_2 = 0.0;
      while ((i_2 < len)) {
        {
          auto a_2 = data.element((i_2 + 3.0));
          if ((a_2 == 0.0)) {
            data.element(i_2) = 0.0;
            data.element((i_2 + 1.0)) = 0.0;
            data.element((i_2 + 2.0)) = 0.0;
          }
          else {
            const double inv = (255.0 / a_2);
            data.element(i_2) = flight::minimum(255.0, flight::round((data.element(i_2) * inv)));
            data.element((i_2 + 1.0)) = flight::minimum(255.0, flight::round((data.element((i_2 + 1.0)) * inv)));
            data.element((i_2 + 2.0)) = flight::minimum(255.0, flight::round((data.element((i_2 + 2.0)) * inv)));
          }
        }
        i_2 += 4.0;
      }
    }
  }
  out->alpha_type = target;
  invalidate_bitmap(out);
}

} // namespace flight::bitmap
