// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapFill.ts#BitmapFill. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline std::optional<flight::Uint8Array> flood_fill_visited = nullptr;

inline void fill_bitmap_rectangle(BitmapRegion dest, double color) {
  const double r = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(color)) >> static_cast<uint32_t>(24.0))) & static_cast<int32_t>(255.0));
  const double g = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color) >> static_cast<int32_t>(16.0))) & static_cast<int32_t>(255.0));
  const double b = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color) >> static_cast<int32_t>(8.0))) & static_cast<int32_t>(255.0));
  const double a = static_cast<double>(static_cast<int32_t>(color) & static_cast<int32_t>(255.0));
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        if (((y < 0.0) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              auto x = (dest.x + px);
              if (((x < 0.0) || (x >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double i = (((y * dest.bitmap.width) + x) * 4.0);
              dest.bitmap.data[static_cast<size_t>(i)] = r;
              dest.bitmap.data[static_cast<size_t>((i + 1.0))] = g;
              dest.bitmap.data[static_cast<size_t>((i + 2.0))] = b;
              dest.bitmap.data[static_cast<size_t>((i + 3.0))] = a;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void flood_fill_bitmap(Bitmap out, double x, double y, double color) {
  if (((((x < 0.0) || (x >= out.width)) || (y < 0.0)) || (y >= out.height))) {
    return;
  }
  const double fill_r = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(color)) >> static_cast<uint32_t>(24.0))) & static_cast<int32_t>(255.0));
  const double fill_g = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color) >> static_cast<int32_t>(16.0))) & static_cast<int32_t>(255.0));
  const double fill_b = static_cast<double>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(color) >> static_cast<int32_t>(8.0))) & static_cast<int32_t>(255.0));
  const double fill_a = static_cast<double>(static_cast<int32_t>(color) & static_cast<int32_t>(255.0));
  const double target_i = (((y * out.width) + x) * 4.0);
  auto target_r = out.data[static_cast<size_t>(target_i)];
  auto target_g = out.data[static_cast<size_t>((target_i + 1.0))];
  auto target_b = out.data[static_cast<size_t>((target_i + 2.0))];
  auto target_a = out.data[static_cast<size_t>((target_i + 3.0))];
  if (((((target_r == fill_r) && (target_g == fill_g)) && (target_b == fill_b)) && (target_a == fill_a))) {
    return;
  }
  const double needed = (out.width * out.height);
  if ((!flood_fill_visited.value().has_value() || (flood_fill_visited.value().length < needed))) {
    flood_fill_visited.value() = flight::Uint8Array(needed);
  }
  else {
    flood_fill_visited.value().fill(0.0, 0.0, needed);
  }
  std::optional<flight::Uint8Array> visited = flood_fill_visited.value();
  flight::Array<double> stack = flight::Array<double>{(x + (y * out.width))};
  while ((static_cast<double>(stack.size()) > 0.0)) {
    const double idx = stack.pop();
    if (visited.value()[static_cast<size_t>(idx)]) {
      continue;
    }
    visited.value()[static_cast<size_t>(idx)] = 1.0;
    const double px = std::fmod(idx, out.width);
    const double py = std::floor((idx / out.width));
    const double i = (idx * 4.0);
    if (((((out.data[static_cast<size_t>(i)] != target_r) || (out.data[static_cast<size_t>((i + 1.0))] != target_g)) || (out.data[static_cast<size_t>((i + 2.0))] != target_b)) || (out.data[static_cast<size_t>((i + 3.0))] != target_a))) {
      continue;
    }
    out.data[static_cast<size_t>(i)] = fill_r;
    out.data[static_cast<size_t>((i + 1.0))] = fill_g;
    out.data[static_cast<size_t>((i + 2.0))] = fill_b;
    out.data[static_cast<size_t>((i + 3.0))] = fill_a;
    if ((px > 0.0)) {
      stack.push((idx - 1.0));
    }
    if ((px < (out.width - 1.0))) {
      stack.push((idx + 1.0));
    }
    if ((py > 0.0)) {
      stack.push((idx - out.width));
    }
    if ((py < (out.height - 1.0))) {
      stack.push((idx + out.width));
    }
  }
  invalidate_bitmap(out);
}

} // namespace flighthq_bitmap
