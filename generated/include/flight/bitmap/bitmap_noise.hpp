// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapNoise.ts#BitmapNoise. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline void fill_bitmap_noise(BitmapRegion dest, double seed, std::optional<double> low = std::nullopt, std::optional<double> high = std::nullopt, std::optional<bool> gray_scale = std::nullopt) {
  low = low.value_or(0.0);
  high = high.value_or(255.0);
  gray_scale = gray_scale.value_or(false);
  double state = (static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)))) >> static_cast<uint32_t>(0.0)) || 1.0);
  const double lo = std::max(0.0, std::min(255.0, low.value()));
  const double span = (std::max(0.0, std::min(255.0, high.value())) - lo);
  auto data = dest.bitmap.data;
  auto bitmap_width = dest.bitmap.width;
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              state = next_random_state(state);
              const double r = (lo + ((state / 4294967296.0) * span));
              double g = r;
              double b = r;
              if (!gray_scale.value()) {
                state = next_random_state(state);
                g = (lo + ((state / 4294967296.0) * span));
                state = next_random_state(state);
                b = (lo + ((state / 4294967296.0) * span));
              }
              auto x = (dest.x + px);
              if (((((y < 0.0) || (y >= dest.bitmap.height)) || (x < 0.0)) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double i = (((y * bitmap_width) + x) * 4.0);
              data[static_cast<size_t>(i)] = flight::round(r);
              data[static_cast<size_t>((i + 1.0))] = flight::round(g);
              data[static_cast<size_t>((i + 2.0))] = flight::round(b);
              data[static_cast<size_t>((i + 3.0))] = 255.0;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void fill_bitmap_perlin_noise(BitmapRegion dest, double base_x, double base_y, double octaves, double seed, std::optional<bool> gray_scale = std::nullopt, std::optional<bool> stitch = std::nullopt, std::optional<double> channel_options = std::nullopt) {
  gray_scale = gray_scale.value_or(false);
  stitch = stitch.value_or(false);
  channel_options = channel_options.value_or(7.0);
  const double fx0 = ((base_x > 0.0) ? (1.0 / base_x) : 0.0);
  const double fy0 = ((base_y > 0.0) ? (1.0 / base_y) : 0.0);
  const double passes = std::max(1.0, flight::round(octaves));
  auto data = dest.bitmap.data;
  auto bitmap_width = dest.bitmap.width;
  auto w = dest.width;
  auto h = dest.height;
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        if (((y < 0.0) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              auto x = (dest.x + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double di = (((y * bitmap_width) + x) * 4.0);
              const double nx = (stitch.value() ? stitched_coord((px * fx0), (w * fx0)) : (px * fx0));
              const double ny = (stitch.value() ? stitched_coord((py * fy0), (h * fy0)) : (py * fy0));
              if (gray_scale.value()) {
                const double value = fractal_value_noise(nx, ny, passes, static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)));
                const double byte = flight::round((value * 255.0));
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_r))) {
                  data[static_cast<size_t>(di)] = byte;
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_g))) {
                  data[static_cast<size_t>((di + 1.0))] = byte;
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_b))) {
                  data[static_cast<size_t>((di + 2.0))] = byte;
                }
              }
              else {
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_r))) {
                  data[static_cast<size_t>(di)] = flight::round((fractal_value_noise(nx, ny, passes, static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0))) * 255.0));
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_g))) {
                  data[static_cast<size_t>((di + 1.0))] = flight::round((fractal_value_noise(nx, ny, passes, (static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)) + 2654435761.0)) * 255.0));
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_b))) {
                  data[static_cast<size_t>((di + 2.0))] = flight::round((fractal_value_noise(nx, ny, passes, (static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)) + 2654435762.0)) * 255.0));
                }
              }
              if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_a))) {
                data[static_cast<size_t>((di + 3.0))] = flight::round((fractal_value_noise(nx, ny, passes, (static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)) + 2654435763.0)) * 255.0));
              }
              else {
                data[static_cast<size_t>((di + 3.0))] = 255.0;
              }
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void fill_bitmap_turbulence(BitmapRegion dest, double base_x, double base_y, double octaves, double seed, std::optional<bool> gray_scale = std::nullopt, std::optional<bool> stitch = std::nullopt, std::optional<double> channel_options = std::nullopt) {
  gray_scale = gray_scale.value_or(false);
  stitch = stitch.value_or(false);
  channel_options = channel_options.value_or(7.0);
  const double fx0 = ((base_x > 0.0) ? (1.0 / base_x) : 0.0);
  const double fy0 = ((base_y > 0.0) ? (1.0 / base_y) : 0.0);
  const double passes = std::max(1.0, flight::round(octaves));
  auto data = dest.bitmap.data;
  auto bitmap_width = dest.bitmap.width;
  auto w = dest.width;
  auto h = dest.height;
  {
    double py = 0.0;
    while ((py < dest.height)) {
      {
        auto y = (dest.y + py);
        if (((y < 0.0) || (y >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < dest.width)) {
            {
              auto x = (dest.x + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double di = (((y * bitmap_width) + x) * 4.0);
              const double nx = (stitch.value() ? stitched_coord((px * fx0), (w * fx0)) : (px * fx0));
              const double ny = (stitch.value() ? stitched_coord((py * fy0), (h * fy0)) : (py * fy0));
              if (gray_scale.value()) {
                const double value = turbulence_noise(nx, ny, passes, static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)));
                const double byte = flight::round((value * 255.0));
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_r))) {
                  data[static_cast<size_t>(di)] = byte;
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_g))) {
                  data[static_cast<size_t>((di + 1.0))] = byte;
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_b))) {
                  data[static_cast<size_t>((di + 2.0))] = byte;
                }
              }
              else {
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_r))) {
                  data[static_cast<size_t>(di)] = flight::round((turbulence_noise(nx, ny, passes, static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0))) * 255.0));
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_g))) {
                  data[static_cast<size_t>((di + 1.0))] = flight::round((turbulence_noise(nx, ny, passes, (static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)) + 2654435761.0)) * 255.0));
                }
                if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_b))) {
                  data[static_cast<size_t>((di + 2.0))] = flight::round((turbulence_noise(nx, ny, passes, (static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)) + 2654435762.0)) * 255.0));
                }
              }
              if (static_cast<double>(static_cast<int32_t>(channel_options.value()) & static_cast<int32_t>(bitmap_noise_channel_a))) {
                data[static_cast<size_t>((di + 3.0))] = flight::round((turbulence_noise(nx, ny, passes, (static_cast<double>(static_cast<int32_t>(seed) | static_cast<int32_t>(0.0)) + 2654435763.0)) * 255.0));
              }
              else {
                data[static_cast<size_t>((di + 3.0))] = 255.0;
              }
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline const double bitmap_noise_channel_a = 8.0;

inline const double bitmap_noise_channel_b = 4.0;

inline const double bitmap_noise_channel_g = 2.0;

inline const double bitmap_noise_channel_r = 1.0;

inline double fractal_value_noise(double x, double y, double octaves, double seed) {
  double sum = 0.0;
  double amplitude = 1.0;
  double amplitude_sum = 0.0;
  double frequency = 1.0;
  {
    double o = 0.0;
    while ((o < octaves)) {
      {
        sum += (value_noise((x * frequency), (y * frequency), (seed + (o * 2246822507.0))) * amplitude);
        amplitude_sum += amplitude;
        amplitude *= 0.5;
        frequency *= 2.0;
      }
      o += 1.0;
    }
  }
  return ((amplitude_sum > 0.0) ? (sum / amplitude_sum) : 0.0);
}

inline double hash_lattice(double ix, double iy, double seed) {
  double h = static_cast<double>(static_cast<int32_t>(((cmath.imul(ix, 374761393.0) + cmath.imul(iy, 668265263.0)) + cmath.imul(seed, 2654435761.0))) | static_cast<int32_t>(0.0));
  h = cmath.imul(static_cast<double>(static_cast<int32_t>(h) ^ static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(h)) >> static_cast<uint32_t>(13.0)))), 1274126177.0);
  return (static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(h) ^ static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(h)) >> static_cast<uint32_t>(16.0)))))) >> static_cast<uint32_t>(0.0)) / 4294967296.0);
}

inline double next_random_state(double state) {
  double t = static_cast<double>(static_cast<int32_t>((state + 1831565813.0)) | static_cast<int32_t>(0.0));
  t = cmath.imul(static_cast<double>(static_cast<int32_t>(t) ^ static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(t)) >> static_cast<uint32_t>(15.0)))), static_cast<double>(static_cast<int32_t>(t) | static_cast<int32_t>(1.0)));
  t ^= (t + cmath.imul(static_cast<double>(static_cast<int32_t>(t) ^ static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(t)) >> static_cast<uint32_t>(7.0)))), static_cast<double>(static_cast<int32_t>(t) | static_cast<int32_t>(61.0))));
  return (static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(static_cast<double>(static_cast<int32_t>(t) ^ static_cast<int32_t>(static_cast<double>(static_cast<uint32_t>(static_cast<int32_t>(t)) >> static_cast<uint32_t>(14.0)))))) >> static_cast<uint32_t>(0.0)) || 1.0);
}

inline double smooth_step(double t) {
  return ((t * t) * (3.0 - (2.0 * t)));
}

inline double stitched_coord(double t, double period) {
  if ((period <= 0.0)) {
    return t;
  }
  return std::fmod((std::fmod(t, period) + period), period);
}

inline double turbulence_noise(double x, double y, double octaves, double seed) {
  double sum = 0.0;
  double amplitude = 1.0;
  double amplitude_sum = 0.0;
  double frequency = 1.0;
  {
    double o = 0.0;
    while ((o < octaves)) {
      {
        sum += (std::abs(((value_noise((x * frequency), (y * frequency), (seed + (o * 2246822507.0))) * 2.0) - 1.0)) * amplitude);
        amplitude_sum += amplitude;
        amplitude *= 0.5;
        frequency *= 2.0;
      }
      o += 1.0;
    }
  }
  return ((amplitude_sum > 0.0) ? (sum / amplitude_sum) : 0.0);
}

inline double value_noise(double x, double y, double seed) {
  const double ix = std::floor(x);
  const double iy = std::floor(y);
  const double fx = smooth_step((x - ix));
  const double fy = smooth_step((y - iy));
  const double v00 = hash_lattice(ix, iy, seed);
  const double v10 = hash_lattice((ix + 1.0), iy, seed);
  const double v01 = hash_lattice(ix, (iy + 1.0), seed);
  const double v11 = hash_lattice((ix + 1.0), (iy + 1.0), seed);
  const double top = (v00 + ((v10 - v00) * fx));
  const double bottom = (v01 + ((v11 - v01) * fx));
  return (top + ((bottom - top) * fy));
}

} // namespace flighthq_bitmap
