// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapNoise.ts#BitmapNoise at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/bitmap/bitmap.hpp>

namespace flight::bitmap {

inline const double bitmap_noise_channel_a = 8.0;

inline const double bitmap_noise_channel_b = 4.0;

inline const double bitmap_noise_channel_g = 2.0;

inline const double bitmap_noise_channel_r = 1.0;

inline double hash_lattice(double ix, double iy, double seed) {
  double h = flight::bitwise_or(((([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(ix, 374761393.0) + ([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(iy, 668265263.0)) + ([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(seed, 2654435761.0)), 0.0);
  h = ([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(flight::bitwise_xor(h, flight::unsigned_right_shift(h, 13.0)), 1274126177.0);
  return (flight::unsigned_right_shift(flight::bitwise_xor(h, flight::unsigned_right_shift(h, 16.0)), 0.0) / 4294967296.0);
}

inline double next_random_state(double state) {
  double t = flight::bitwise_or((state + 1831565813.0), 0.0);
  t = ([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(flight::bitwise_xor(t, flight::unsigned_right_shift(t, 15.0)), flight::bitwise_or(t, 1.0));
  ([&]() { auto&& assignment_target = t; assignment_target = flight::bitwise_xor(assignment_target, (t + ([](double left, double right) noexcept { const auto to_uint32 = [](double value) noexcept { if (!std::isfinite(value) || value == 0.0) return std::uint32_t{0}; constexpr double modulus = 4294967296.0; double remainder = std::fmod(std::trunc(value), modulus); if (remainder < 0.0) remainder += modulus; return static_cast<std::uint32_t>(remainder); }; const std::uint32_t product = to_uint32(left) * to_uint32(right); return product < 0x80000000U ? static_cast<double>(product) : static_cast<double>(static_cast<std::int64_t>(product) - 0x100000000LL); })(flight::bitwise_xor(t, flight::unsigned_right_shift(t, 7.0)), flight::bitwise_or(t, 61.0)))); return assignment_target; }());
  return (flight::unsigned_right_shift(flight::bitwise_xor(t, flight::unsigned_right_shift(t, 14.0)), 0.0) || 1.0);
}

inline void fill_bitmap_noise(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, double seed, std::optional<double> low = std::nullopt, std::optional<double> high = std::nullopt, std::optional<bool> gray_scale = std::nullopt) {
  low = low.value_or(0.0);
  high = high.value_or(255.0);
  gray_scale = gray_scale.value_or(false);
  double state = (flight::unsigned_right_shift(flight::bitwise_or(seed, 0.0), 0.0) || 1.0);
  auto lo = flight::maximum(0.0, flight::minimum(255.0, low.value()));
  const double span = (flight::maximum(0.0, flight::minimum(255.0, high.value())) - lo);
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  {
    double py = 0.0;
    while ((py < flight::row_get<flight::RowKey<"height">>(dest))) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        {
          double px = 0.0;
          while ((px < flight::row_get<flight::RowKey<"width">>(dest))) {
            {
              state = next_random_state(state);
              const double r = (lo + ((state / 4294967296.0) * span));
              double g = r;
              double b = r;
              if (!gray_scale.value()) {
                state = next_random_state(state);
                g = (lo + ((state / 4294967296.0) * span));
                state = next_random_state(state);
                b = (lo + ((state / 4294967296.0) * span));
              }
              auto x = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((((y < 0.0) || (y >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height)) || (x < 0.0)) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double i = (((y * bitmap_width) + x) * 4.0);
              data.element(i) = flight::round(r);
              data.element((i + 1.0)) = flight::round(g);
              data.element((i + 2.0)) = flight::round(b);
              data.element((i + 3.0)) = 255.0;
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

inline double smooth_step(double t) {
  return ((t * t) * (3.0 - (2.0 * t)));
}

inline double stitched_coord(double t, double period) {
  if ((period <= 0.0)) {
    return t;
  }
  return std::fmod((std::fmod(t, period) + period), period);
}

inline double value_noise(double x, double y, double seed) {
  auto ix = std::floor(x);
  auto iy = std::floor(y);
  const double fx = smooth_step((x - ix));
  const double fy = smooth_step((y - iy));
  const double v00 = hash_lattice(ix, iy, seed);
  const double v10 = hash_lattice((ix + 1.0), iy, seed);
  const double v01 = hash_lattice(ix, (iy + 1.0), seed);
  const double v11 = hash_lattice((ix + 1.0), (iy + 1.0), seed);
  const double top = (v00 + ((v10 - v00) * fx));
  const double bottom = (v01 + ((v11 - v01) * fx));
  return (top + ((bottom - top) * fy));
}

inline double fractal_value_noise(double x, double y, double octaves, double seed) {
  double sum = 0.0;
  double amplitude = 1.0;
  double amplitude_sum = 0.0;
  double frequency = 1.0;
  {
    double o = 0.0;
    while ((o < octaves)) {
      {
        sum += (value_noise((x * frequency), (y * frequency), (seed + (o * 2246822507.0))) * amplitude);
        amplitude_sum += amplitude;
        amplitude *= 0.5;
        frequency *= 2.0;
      }
      o += 1.0;
    }
  }
  return ((amplitude_sum > 0.0) ? (sum / amplitude_sum) : 0.0);
}

inline void fill_bitmap_perlin_noise(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, double base_x, double base_y, double octaves, double seed, std::optional<bool> gray_scale = std::nullopt, std::optional<bool> stitch = std::nullopt, std::optional<double> channel_options = std::nullopt) {
  gray_scale = gray_scale.value_or(false);
  stitch = stitch.value_or(false);
  channel_options = channel_options.value_or(7.0);
  const double fx0 = ((base_x > 0.0) ? (1.0 / base_x) : 0.0);
  const double fy0 = ((base_y > 0.0) ? (1.0 / base_y) : 0.0);
  auto passes = flight::maximum(1.0, flight::round(octaves));
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  const double w = flight::row_get<flight::RowKey<"width">>(dest);
  const double h = flight::row_get<flight::RowKey<"height">>(dest);
  {
    double py = 0.0;
    while ((py < flight::row_get<flight::RowKey<"height">>(dest))) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        if (((y < 0.0) || (y >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < flight::row_get<flight::RowKey<"width">>(dest))) {
            {
              auto x = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double di = (((y * bitmap_width) + x) * 4.0);
              const double nx = (stitch.value() ? stitched_coord((px * fx0), (w * fx0)) : (px * fx0));
              const double ny = (stitch.value() ? stitched_coord((py * fy0), (h * fy0)) : (py * fy0));
              if (gray_scale.value()) {
                const double value = fractal_value_noise(nx, ny, passes, flight::bitwise_or(seed, 0.0));
                auto byte = flight::round((value * 255.0));
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_r)) {
                  data.element(di) = byte;
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_g)) {
                  data.element((di + 1.0)) = byte;
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_b)) {
                  data.element((di + 2.0)) = byte;
                }
              }
              else {
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_r)) {
                  data.element(di) = flight::round((fractal_value_noise(nx, ny, passes, flight::bitwise_or(seed, 0.0)) * 255.0));
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_g)) {
                  data.element((di + 1.0)) = flight::round((fractal_value_noise(nx, ny, passes, (flight::bitwise_or(seed, 0.0) + 2654435761.0)) * 255.0));
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_b)) {
                  data.element((di + 2.0)) = flight::round((fractal_value_noise(nx, ny, passes, (flight::bitwise_or(seed, 0.0) + 2654435762.0)) * 255.0));
                }
              }
              if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_a)) {
                data.element((di + 3.0)) = flight::round((fractal_value_noise(nx, ny, passes, (flight::bitwise_or(seed, 0.0) + 2654435763.0)) * 255.0));
              }
              else {
                data.element((di + 3.0)) = 255.0;
              }
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

inline double turbulence_noise(double x, double y, double octaves, double seed) {
  double sum = 0.0;
  double amplitude = 1.0;
  double amplitude_sum = 0.0;
  double frequency = 1.0;
  {
    double o = 0.0;
    while ((o < octaves)) {
      {
        sum += (std::abs(((value_noise((x * frequency), (y * frequency), (seed + (o * 2246822507.0))) * 2.0) - 1.0)) * amplitude);
        amplitude_sum += amplitude;
        amplitude *= 0.5;
        frequency *= 2.0;
      }
      o += 1.0;
    }
  }
  return ((amplitude_sum > 0.0) ? (sum / amplitude_sum) : 0.0);
}

inline void fill_bitmap_turbulence(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, double base_x, double base_y, double octaves, double seed, std::optional<bool> gray_scale = std::nullopt, std::optional<bool> stitch = std::nullopt, std::optional<double> channel_options = std::nullopt) {
  gray_scale = gray_scale.value_or(false);
  stitch = stitch.value_or(false);
  channel_options = channel_options.value_or(7.0);
  const double fx0 = ((base_x > 0.0) ? (1.0 / base_x) : 0.0);
  const double fy0 = ((base_y > 0.0) ? (1.0 / base_y) : 0.0);
  auto passes = flight::maximum(1.0, flight::round(octaves));
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto bitmap_width = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  const double w = flight::row_get<flight::RowKey<"width">>(dest);
  const double h = flight::row_get<flight::RowKey<"height">>(dest);
  {
    double py = 0.0;
    while ((py < flight::row_get<flight::RowKey<"height">>(dest))) {
      {
        auto y = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        if (((y < 0.0) || (y >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < flight::row_get<flight::RowKey<"width">>(dest))) {
            {
              auto x = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((x < 0.0) || (x >= bitmap_width))) {
                px += 1.0;
                continue;
              }
              const double di = (((y * bitmap_width) + x) * 4.0);
              const double nx = (stitch.value() ? stitched_coord((px * fx0), (w * fx0)) : (px * fx0));
              const double ny = (stitch.value() ? stitched_coord((py * fy0), (h * fy0)) : (py * fy0));
              if (gray_scale.value()) {
                const double value = turbulence_noise(nx, ny, passes, flight::bitwise_or(seed, 0.0));
                auto byte = flight::round((value * 255.0));
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_r)) {
                  data.element(di) = byte;
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_g)) {
                  data.element((di + 1.0)) = byte;
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_b)) {
                  data.element((di + 2.0)) = byte;
                }
              }
              else {
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_r)) {
                  data.element(di) = flight::round((turbulence_noise(nx, ny, passes, flight::bitwise_or(seed, 0.0)) * 255.0));
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_g)) {
                  data.element((di + 1.0)) = flight::round((turbulence_noise(nx, ny, passes, (flight::bitwise_or(seed, 0.0) + 2654435761.0)) * 255.0));
                }
                if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_b)) {
                  data.element((di + 2.0)) = flight::round((turbulence_noise(nx, ny, passes, (flight::bitwise_or(seed, 0.0) + 2654435762.0)) * 255.0));
                }
              }
              if (flight::bitwise_and(channel_options.value(), bitmap_noise_channel_a)) {
                data.element((di + 3.0)) = flight::round((turbulence_noise(nx, ny, passes, (flight::bitwise_or(seed, 0.0) + 2654435763.0)) * 255.0));
              }
              else {
                data.element((di + 3.0)) = 255.0;
              }
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

} // namespace flight::bitmap
