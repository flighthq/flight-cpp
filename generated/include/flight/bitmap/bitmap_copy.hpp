// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapCopy.ts#BitmapCopy. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"
#include "bitmap_image_channel.hpp"

namespace flighthq_bitmap {

inline void copy_bitmap_channel(BitmapRegion dest, ImageChannel dest_channel, BitmapRegion source, ImageChannel source_channel) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              dest.bitmap.data[static_cast<size_t>((di + dest_channel))] = source.bitmap.data[static_cast<size_t>((si + source_channel))];
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

inline void copy_bitmap_pixels(BitmapRegion dest, BitmapRegion source, std::optional<bool> composite = std::nullopt) {
  composite = composite.value_or(false);
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + py);
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + px);
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= source.bitmap.width)) || (dx < 0.0)) || (dx >= dest.bitmap.width))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * source.bitmap.width) + sx) * 4.0);
              const double di = (((dy * dest.bitmap.width) + dx) * 4.0);
              if (composite.value()) {
                const double src_a = (sd[static_cast<size_t>((si + 3.0))] / 255.0);
                const double dst_a = (dd[static_cast<size_t>((di + 3.0))] / 255.0);
                const double out_a = (src_a + (dst_a * (1.0 - src_a)));
                if ((out_a > 0.0)) {
                  dd[static_cast<size_t>(di)] = flight::round((((sd[static_cast<size_t>(si)] * src_a) + ((dd[static_cast<size_t>(di)] * dst_a) * (1.0 - src_a))) / out_a));
                  dd[static_cast<size_t>((di + 1.0))] = flight::round((((sd[static_cast<size_t>((si + 1.0))] * src_a) + ((dd[static_cast<size_t>((di + 1.0))] * dst_a) * (1.0 - src_a))) / out_a));
                  dd[static_cast<size_t>((di + 2.0))] = flight::round((((sd[static_cast<size_t>((si + 2.0))] * src_a) + ((dd[static_cast<size_t>((di + 2.0))] * dst_a) * (1.0 - src_a))) / out_a));
                  dd[static_cast<size_t>((di + 3.0))] = flight::round((out_a * 255.0));
                }
              }
              else {
                dd[static_cast<size_t>(di)] = sd[static_cast<size_t>(si)];
                dd[static_cast<size_t>((di + 1.0))] = sd[static_cast<size_t>((si + 1.0))];
                dd[static_cast<size_t>((di + 2.0))] = sd[static_cast<size_t>((si + 2.0))];
                dd[static_cast<size_t>((di + 3.0))] = sd[static_cast<size_t>((si + 3.0))];
              }
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
  invalidate_bitmap(dest.bitmap);
}

} // namespace flighthq_bitmap
