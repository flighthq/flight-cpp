// Generated by @flighthq/tool-compiler from @flighthq/bitmap/packages/bitmap/src/bitmapFlip.ts#BitmapFlip at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/array_buffer.hpp>
#include <flight/structural_ref.hpp>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/bitmap.hpp>
#include <flight/types/bitmap_region.hpp>
#include <flight/types/pixel_format.hpp>
#include <flight/bitmap/bitmap.hpp>

namespace flight::bitmap {

inline void copy_mirrored(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source, double w, double h, bool mirror_x, bool mirror_y) {
  flight::Uint8ClampedArray<flight::ArrayBuffer> sd = flight::row_get<flight::RowKey<"bitmap">>(source)->data;
  flight::Uint8ClampedArray<flight::ArrayBuffer> dd = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto s_stride = flight::row_get<flight::RowKey<"bitmap">>(source)->width;
  auto d_stride = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (flight::row_get<flight::RowKey<"y">>(source) + (mirror_y ? ((h - 1.0) - py) : py));
        auto dy = (flight::row_get<flight::RowKey<"y">>(dest) + py);
        if (((((sy < 0.0) || (sy >= flight::row_get<flight::RowKey<"bitmap">>(source)->height)) || (dy < 0.0)) || (dy >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (flight::row_get<flight::RowKey<"x">>(source) + (mirror_x ? ((w - 1.0) - px) : px));
              auto dx = (flight::row_get<flight::RowKey<"x">>(dest) + px);
              if (((((sx < 0.0) || (sx >= s_stride)) || (dx < 0.0)) || (dx >= d_stride))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * s_stride) + sx) * 4.0);
              const double di = (((dy * d_stride) + dx) * 4.0);
              dd.element(di) = sd.element(si);
              dd.element((di + 1.0)) = sd.element((si + 1.0));
              dd.element((di + 2.0)) = sd.element((si + 2.0));
              dd.element((di + 3.0)) = sd.element((si + 3.0));
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
}

inline bool is_same_region(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> a, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> b) {
  return (((((flight::row_get<flight::RowKey<"bitmap">>(a) == flight::row_get<flight::RowKey<"bitmap">>(b)) && (flight::row_get<flight::RowKey<"x">>(a) == flight::row_get<flight::RowKey<"x">>(b))) && (flight::row_get<flight::RowKey<"y">>(a) == flight::row_get<flight::RowKey<"y">>(b))) && (flight::row_get<flight::RowKey<"width">>(a) == flight::row_get<flight::RowKey<"width">>(b))) && (flight::row_get<flight::RowKey<"height">>(a) == flight::row_get<flight::RowKey<"height">>(b)));
}

inline void swap_pixels(flight::Uint8ClampedArray data, double a, double b) {
  {
    double c = 0.0;
    while ((c < 4.0)) {
      {
        const double t = data.element((a + c));
        data.element((a + c)) = data.element((b + c));
        data.element((b + c)) = t;
      }
      c += 1.0;
    }
  }
}

inline void flip_bitmap_horizontal(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source) {
  auto w = flight::minimum(flight::row_get<flight::RowKey<"width">>(dest), flight::row_get<flight::RowKey<"width">>(source));
  auto h = flight::minimum(flight::row_get<flight::RowKey<"height">>(dest), flight::row_get<flight::RowKey<"height">>(source));
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto stride = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  if (is_same_region(dest, source)) {
    const double half = flight::signed_right_shift(w, 1.0);
    {
      double py = 0.0;
      while ((py < h)) {
        {
          auto y = (flight::row_get<flight::RowKey<"y">>(dest) + py);
          if (((y < 0.0) || (y >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height))) {
            py += 1.0;
            continue;
          }
          {
            double px = 0.0;
            while ((px < half)) {
              {
                auto xa = (flight::row_get<flight::RowKey<"x">>(dest) + px);
                auto xb = (flight::row_get<flight::RowKey<"x">>(dest) + ((w - 1.0) - px));
                if (((((xa < 0.0) || (xa >= stride)) || (xb < 0.0)) || (xb >= stride))) {
                  px += 1.0;
                  continue;
                }
                swap_pixels(data, (((y * stride) + xa) * 4.0), (((y * stride) + xb) * 4.0));
              }
              px += 1.0;
            }
          }
        }
        py += 1.0;
      }
    }
    return;
  }
  copy_mirrored(dest, source, w, h, true, false);
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

inline void flip_bitmap_vertical(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> dest, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::BitmapRegion>>>> source) {
  auto w = flight::minimum(flight::row_get<flight::RowKey<"width">>(dest), flight::row_get<flight::RowKey<"width">>(source));
  auto h = flight::minimum(flight::row_get<flight::RowKey<"height">>(dest), flight::row_get<flight::RowKey<"height">>(source));
  flight::Uint8ClampedArray<flight::ArrayBuffer> data = flight::row_get<flight::RowKey<"bitmap">>(dest)->data;
  auto stride = flight::row_get<flight::RowKey<"bitmap">>(dest)->width;
  if (is_same_region(dest, source)) {
    const double half = flight::signed_right_shift(h, 1.0);
    {
      double py = 0.0;
      while ((py < half)) {
        {
          auto y_top = (flight::row_get<flight::RowKey<"y">>(dest) + py);
          auto y_bottom = (flight::row_get<flight::RowKey<"y">>(dest) + ((h - 1.0) - py));
          if (((((y_top < 0.0) || (y_top >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height)) || (y_bottom < 0.0)) || (y_bottom >= flight::row_get<flight::RowKey<"bitmap">>(dest)->height))) {
            py += 1.0;
            continue;
          }
          {
            double px = 0.0;
            while ((px < w)) {
              {
                auto x = (flight::row_get<flight::RowKey<"x">>(dest) + px);
                if (((x < 0.0) || (x >= stride))) {
                  px += 1.0;
                  continue;
                }
                swap_pixels(data, (((y_top * stride) + x) * 4.0), (((y_bottom * stride) + x) * 4.0));
              }
              px += 1.0;
            }
          }
        }
        py += 1.0;
      }
    }
    return;
  }
  copy_mirrored(dest, source, w, h, false, true);
  flight::bitmap::invalidate_bitmap(flight::row_get<flight::RowKey<"bitmap">>(dest));
}

} // namespace flight::bitmap
