// Generated by @flighthq/tool-compiler from @flighthq/bitmap/bitmapFlip.ts#BitmapFlip. Do not edit.
#pragma once
#include <cstdint>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "bitmap.hpp"

namespace flighthq_bitmap {

inline void flip_bitmap_horizontal(BitmapRegion dest, BitmapRegion source) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto data = dest.bitmap.data;
  auto stride = dest.bitmap.width;
  if (is_same_region(dest, source)) {
    const double half = static_cast<double>(static_cast<int32_t>(w) >> static_cast<int32_t>(1.0));
    {
      double py = 0.0;
      while ((py < h)) {
        {
          auto y = (dest.y + py);
          if (((y < 0.0) || (y >= dest.bitmap.height))) {
            py += 1.0;
            continue;
          }
          {
            double px = 0.0;
            while ((px < half)) {
              {
                auto xa = (dest.x + px);
                auto xb = (dest.x + ((w - 1.0) - px));
                if (((((xa < 0.0) || (xa >= stride)) || (xb < 0.0)) || (xb >= stride))) {
                  px += 1.0;
                  continue;
                }
                swap_pixels(data, (((y * stride) + xa) * 4.0), (((y * stride) + xb) * 4.0));
              }
              px += 1.0;
            }
          }
        }
        py += 1.0;
      }
    }
    return;
  }
  copy_mirrored(dest, source, w, h, true, false);
  invalidate_bitmap(dest.bitmap);
}

inline void flip_bitmap_vertical(BitmapRegion dest, BitmapRegion source) {
  const double w = std::min(dest.width, source.width);
  const double h = std::min(dest.height, source.height);
  auto data = dest.bitmap.data;
  auto stride = dest.bitmap.width;
  if (is_same_region(dest, source)) {
    const double half = static_cast<double>(static_cast<int32_t>(h) >> static_cast<int32_t>(1.0));
    {
      double py = 0.0;
      while ((py < half)) {
        {
          auto y_top = (dest.y + py);
          auto y_bottom = (dest.y + ((h - 1.0) - py));
          if (((((y_top < 0.0) || (y_top >= dest.bitmap.height)) || (y_bottom < 0.0)) || (y_bottom >= dest.bitmap.height))) {
            py += 1.0;
            continue;
          }
          {
            double px = 0.0;
            while ((px < w)) {
              {
                auto x = (dest.x + px);
                if (((x < 0.0) || (x >= stride))) {
                  px += 1.0;
                  continue;
                }
                swap_pixels(data, (((y_top * stride) + x) * 4.0), (((y_bottom * stride) + x) * 4.0));
              }
              px += 1.0;
            }
          }
        }
        py += 1.0;
      }
    }
    return;
  }
  copy_mirrored(dest, source, w, h, false, true);
  invalidate_bitmap(dest.bitmap);
}

inline void copy_mirrored(BitmapRegion dest, BitmapRegion source, double w, double h, bool mirror_x, bool mirror_y) {
  auto sd = source.bitmap.data;
  auto dd = dest.bitmap.data;
  auto s_stride = source.bitmap.width;
  auto d_stride = dest.bitmap.width;
  {
    double py = 0.0;
    while ((py < h)) {
      {
        auto sy = (source.y + (mirror_y ? ((h - 1.0) - py) : py));
        auto dy = (dest.y + py);
        if (((((sy < 0.0) || (sy >= source.bitmap.height)) || (dy < 0.0)) || (dy >= dest.bitmap.height))) {
          py += 1.0;
          continue;
        }
        {
          double px = 0.0;
          while ((px < w)) {
            {
              auto sx = (source.x + (mirror_x ? ((w - 1.0) - px) : px));
              auto dx = (dest.x + px);
              if (((((sx < 0.0) || (sx >= s_stride)) || (dx < 0.0)) || (dx >= d_stride))) {
                px += 1.0;
                continue;
              }
              const double si = (((sy * s_stride) + sx) * 4.0);
              const double di = (((dy * d_stride) + dx) * 4.0);
              dd[static_cast<size_t>(di)] = sd[static_cast<size_t>(si)];
              dd[static_cast<size_t>((di + 1.0))] = sd[static_cast<size_t>((si + 1.0))];
              dd[static_cast<size_t>((di + 2.0))] = sd[static_cast<size_t>((si + 2.0))];
              dd[static_cast<size_t>((di + 3.0))] = sd[static_cast<size_t>((si + 3.0))];
            }
            px += 1.0;
          }
        }
      }
      py += 1.0;
    }
  }
}

inline bool is_same_region(BitmapRegion a, BitmapRegion b) {
  return (((((a.bitmap == b.bitmap) && (a.x == b.x)) && (a.y == b.y)) && (a.width == b.width)) && (a.height == b.height));
}

inline void swap_pixels(flight::Uint8ClampedArray data, double a, double b) {
  {
    double c = 0.0;
    while ((c < 4.0)) {
      {
        std::optional<auto> t = data.get((a + c));
        data.element((a + c)) = data.element((b + c));
        data.element((b + c)) = t;
      }
      c += 1.0;
    }
  }
}

} // namespace flighthq_bitmap
