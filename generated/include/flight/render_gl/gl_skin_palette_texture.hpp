// Generated by @flighthq/tool-compiler from @flighthq/render-gl/glSkinPaletteTexture.ts#GlSkinPaletteTexture. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_render_gl {

inline GlSkinPaletteTexture create_gl_skin_palette_texture(GlContext gl) {
  auto out = allocate_entity<GlSkinPaletteTexture>();
  initialize_gl_skin_palette_texture(out, gl);
  return finish_entity(out);
}

inline void destroy_gl_skin_palette_texture(GlContext gl, GlSkinPaletteTexture palette) {
  gl.delete_texture(palette.texture);
}

inline void initialize_gl_skin_palette_texture(EntityConstruction<GlSkinPaletteTexture> out, GlContext gl) {
  out.joint_capacity = 0.0;
  out.texture = gl.create_texture();
}

inline void upload_gl_skin_palette_texture(GlContext gl, GlSkinPaletteTexture palette, flight::Float32Array joint_matrices, double joint_count, std::optional<double> texels_per_joint = std::nullopt) {
  texels_per_joint = texels_per_joint.value_or(4.0);
  const double width = (joint_count * texels_per_joint.value());
  gl.bind_texture(gl.texture_2_d, palette.texture);
  if ((joint_count > palette.joint_capacity)) {
    gl.tex_image2_d(gl.texture_2_d, 0.0, gl.rgba32_f, width, 1.0, 0.0, gl.rgba, gl.float_, static_cast<flight::Float32Array>(joint_matrices));
    gl.tex_parameteri(gl.texture_2_d, gl.texture_min_filter, gl.nearest);
    gl.tex_parameteri(gl.texture_2_d, gl.texture_mag_filter, gl.nearest);
    gl.tex_parameteri(gl.texture_2_d, gl.texture_wrap_s, gl.clamp_to_edge);
    gl.tex_parameteri(gl.texture_2_d, gl.texture_wrap_t, gl.clamp_to_edge);
    palette.joint_capacity = joint_count;
  }
  else {
    gl.tex_sub_image2_d(gl.texture_2_d, 0.0, 0.0, 0.0, width, 1.0, gl.rgba, gl.float_, static_cast<flight::Float32Array>(joint_matrices));
  }
}

} // namespace flighthq_render_gl
