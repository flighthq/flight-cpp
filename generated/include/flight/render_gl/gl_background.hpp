// Generated by @flighthq/tool-compiler from @flighthq/render-gl/glBackground.ts#GlBackground. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_render_state.hpp"

namespace flighthq_render_gl {

inline void render_gl_background(GlRenderState state) {
  auto runtime = get_gl_render_state_runtime(state);
  auto gl = state.gl;
  auto viewport = runtime.render_target_viewport;
  gl.viewport(([&]() -> std::optional<auto> { auto optional_chain_receiver = viewport; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().x; }()).value_or(0.0), ([&]() -> std::optional<auto> { auto optional_chain_receiver = viewport; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().y; }()).value_or(0.0), ([&]() -> std::optional<auto> { auto optional_chain_receiver = viewport; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().width; }()).value_or(gl.drawing_buffer_width), ([&]() -> std::optional<auto> { auto optional_chain_receiver = viewport; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().height; }()).value_or(gl.drawing_buffer_height));
  auto rgba = state.background_color_rgba;
  if (((rgba.length >= 4.0) && (rgba[static_cast<size_t>(3.0)] > 0.0))) {
    const bool linear = (([&]() -> std::optional<auto> { auto optional_chain_receiver = runtime.current_render_target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().color_space; }()) == flight::String("linear"));
    gl.clear_color((linear ? srgb_channel_to_linear(rgba[static_cast<size_t>(0.0)]) : rgba[static_cast<size_t>(0.0)]), (linear ? srgb_channel_to_linear(rgba[static_cast<size_t>(1.0)]) : rgba[static_cast<size_t>(1.0)]), (linear ? srgb_channel_to_linear(rgba[static_cast<size_t>(2.0)]) : rgba[static_cast<size_t>(2.0)]), rgba[static_cast<size_t>(3.0)]);
  }
  else {
    gl.clear_color(0.0, 0.0, 0.0, 0.0);
  }
  gl.clear(gl.color_buffer_bit);
  runtime.context.current_blend_signature = nullptr;
}

} // namespace flighthq_render_gl
