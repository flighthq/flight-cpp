// Generated by @flighthq/tool-compiler from @flighthq/render-gl/glReadback.ts#GlReadback. Do not edit.
#pragma once
#include <optional>
#include <variant>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_render_state.hpp"

namespace flighthq_render_gl {

inline bool read_gl_render_target_pixels(GlRenderState state, GlRenderTarget target, double x, double y, double width, double height, std::variant<flight::Uint8Array, flight::Float32Array> out) {
  if (((target.width <= 0.0) || (target.height <= 0.0))) {
    return false;
  }
  auto runtime = get_gl_render_state_runtime(state);
  auto gl = state.gl;
  auto read_fbo = target.resolve_framebuffer.value_or(target.framebuffer);
  auto prev_fbo = runtime.current_framebuffer;
  gl.bind_framebuffer(gl.read_framebuffer, read_fbo);
  auto status = gl.check_framebuffer_status(gl.read_framebuffer);
  if ((status != gl.framebuffer_complete)) {
    gl.bind_framebuffer(gl.read_framebuffer, prev_fbo);
    return false;
  }
  auto format = gl.rgba;
  auto type = ((out instanceof flight::Float32Array) ? gl.float_ : gl.unsigned_byte);
  gl.read_pixels(x, y, width, height, format, type, out);
  gl.bind_framebuffer(gl.read_framebuffer, prev_fbo);
  return true;
}

} // namespace flighthq_render_gl
