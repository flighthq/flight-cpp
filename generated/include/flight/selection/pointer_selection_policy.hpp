// Generated by @flighthq/tool-compiler from @flighthq/selection/pointerSelectionPolicy.ts#PointerSelectionPolicy. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "selection_state.hpp"

namespace flighthq_selection {

template <typename NodeType>
inline void apply_pointer_selection_policy(SelectionState<NodeType> state, std::optional<NodeType> hit, SelectionModifierState modifiers) {
  if (modifiers.alt_key) {
    return;
  }
  if (!hit.has_value()) {
    if (!has_toggle_modifier(modifiers)) {
      clear_selection(state);
    }
    return;
  }
  if (has_toggle_modifier(modifiers)) {
    if ((modifiers.shift_key && !is_node_selected(state, hit.value()))) {
      add_node_to_selection(state, hit.value());
    }
    else {
      toggle_node_selection(state, hit.value());
    }
    return;
  }
  if (!is_node_selected(state, hit.value())) {
    select_node(state, hit.value());
  }
}

template <typename NodeType>
inline void apply_pointer_up_selection_policy(SelectionState<NodeType> state, std::optional<NodeType> hit, SelectionModifierState modifiers, bool did_drag) {
  if ((((did_drag || !hit.has_value()) || has_any_modifier(modifiers)) || !is_node_selected(state, hit.value()))) {
    return;
  }
  select_node(state, hit.value());
}

inline bool has_any_modifier(SelectionModifierState modifiers) {
  return (modifiers.alt_key || has_toggle_modifier(modifiers));
}

inline bool has_toggle_modifier(SelectionModifierState modifiers) {
  return ((modifiers.ctrl_key || modifiers.meta_key) || modifiers.shift_key);
}

} // namespace flighthq_selection
