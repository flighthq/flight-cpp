// Generated by @flighthq/tool-compiler from @flighthq/spatial/packages/spatial/src/spatialIndex3D.ts#SpatialIndex3D at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/number.hpp>
#include <flight/structural_ref.hpp>
#include <limits>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/entity/entity.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/spatial.hpp>
#include <flight/spatial/uniform_grid3_d.hpp>

namespace flight::spatial {

inline void clear_spatial_index3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index) {
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->clear_spatial_index();
}

inline bool insert_spatial_object3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, flight::types::SpatialObjectId id, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialAabb3D>>>> bounds) {
  return flight::row_get<flight::RowKey<"runtime">>(index)->backend->insert_spatial_object(id, bounds);
}

inline void query_spatial_pairs3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, flight::Array<flight::Ref<flight::types::SpatialPair>> out) {
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->query_spatial_pairs(out);
}

inline void query_spatial_point3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, double x, double y, double z, flight::Array<flight::types::SpatialObjectId> out) {
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->query_spatial_point(x, y, z, out);
}

inline void query_spatial_ray3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, double x, double y, double z, double dx, double dy, double dz, flight::Array<flight::types::SpatialObjectId> out) {
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->query_spatial_ray(x, y, z, dx, dy, dz, out);
}

inline void query_spatial_region3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialAabb3D>>>> region, flight::Array<flight::types::SpatialObjectId> out) {
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->query_spatial_region(region, out);
}

inline void remove_spatial_object3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, flight::types::SpatialObjectId id) {
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->remove_spatial_object(id);
}

inline bool update_spatial_object3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, flight::types::SpatialObjectId id, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialAabb3D>>>> bounds) {
  return flight::row_get<flight::RowKey<"runtime">>(index)->backend->update_spatial_object(id, bounds);
}

inline const double default_spatial_cell_size_3_d = 128.0;

inline void initialize_spatial_index3_d(flight::types::EntityConstruction<flight::Ref<flight::types::SpatialIndex3D>> out, std::optional<flight::Ref<flight::types::SpatialIndexBackend3D>> backend = std::nullopt) {
  flight::row_set<flight::RowKey<"runtime">>(out, flight::make_ref<flight::types::SpatialIndexRuntime3D>(flight::types::SpatialIndexRuntime3D{.backend = backend}));
}

inline flight::Ref<flight::types::SpatialIndex3D> create_spatial_index3_d(std::optional<flight::Ref<flight::types::SpatialIndexBackend3D>> backend = std::nullopt) {
  flight::types::EntityConstruction<flight::Ref<flight::types::SpatialIndex3D>> out = flight::entity::allocate_entity<flight::Ref<flight::types::SpatialIndex3D>>();
  initialize_spatial_index3_d(out, backend);
  return flight::entity::finish_entity(out);
}

inline const double default_frustum_slices = 8.0;

inline flight::Ref<flight::types::SpatialAabb3D> query_region_scratch = ([&]() { auto object_member_max_x = 0.0; auto object_member_max_y = 0.0; auto object_member_max_z = 0.0; auto object_member_min_x = 0.0; auto object_member_min_y = 0.0; auto object_member_min_z = 0.0; return flight::make_ref<flight::types::SpatialAabb3D>(flight::types::SpatialAabb3D{.min_x = object_member_min_x, .min_y = object_member_min_y, .min_z = object_member_min_z, .max_x = object_member_max_x, .max_y = object_member_max_y, .max_z = object_member_max_z}); }());

inline void query_spatial_sphere3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, double x, double y, double z, double radius, flight::Array<flight::types::SpatialObjectId> out) {
  ([&]() { auto&& assignment_receiver = out; const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
  if ((((!std::isfinite(x) || !std::isfinite(y)) || !std::isfinite(z)) || !(radius >= 0.0))) {
    return;
  }
  query_region_scratch->min_x = (x - radius);
  query_region_scratch->min_y = (y - radius);
  query_region_scratch->min_z = (z - radius);
  query_region_scratch->max_x = (x + radius);
  query_region_scratch->max_y = (y + radius);
  query_region_scratch->max_z = (z + radius);
  flight::row_get<flight::RowKey<"runtime">>(index)->backend->query_spatial_region(query_region_scratch, out);
}

inline flight::Set<flight::types::SpatialObjectId> frustum_seen = flight::Set<flight::types::SpatialObjectId>();

inline flight::Array<flight::types::SpatialObjectId> frustum_slice = flight::Array<flight::types::SpatialObjectId>{};

inline void query_spatial_frustum3_d(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialIndex3D>>>> index, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::SpatialFrustum3D>>>> frustum, flight::Array<flight::types::SpatialObjectId> out, std::optional<double> slices = std::nullopt) {
  slices = slices.value_or(default_frustum_slices);
  ([&]() { auto&& assignment_receiver = out; const auto assignment_value = 0.0; assignment_receiver.resize(assignment_value); return assignment_value; }());
  flight::Array<double> corners = flight::row_get<flight::RowKey<"corners">>(frustum);
  if (((static_cast<double>(corners.size()) < 24.0) || !(slices.value() >= 1.0))) {
    return;
  }
  auto steps = std::floor(slices.value());
  frustum_seen.clear();
  {
    double slice = 0.0;
    while ((slice < steps)) {
      {
        const double near_t = (slice / steps);
        const double far_t = ((slice + 1.0) / steps);
        auto min_x = std::numeric_limits<double>::infinity();
        auto min_y = std::numeric_limits<double>::infinity();
        auto min_z = std::numeric_limits<double>::infinity();
        double max_x = -std::numeric_limits<double>::infinity();
        double max_y = -std::numeric_limits<double>::infinity();
        double max_z = -std::numeric_limits<double>::infinity();
        {
          double corner = 0.0;
          while ((corner < 4.0)) {
            {
              const double near = (corner * 3.0);
              const double far = (12.0 + (corner * 3.0));
              for (auto t : flight::Array{near_t, far_t}) {
                auto x = (corners.element(near) + ((corners.element(far) - corners.element(near)) * t));
                auto y = (corners.element((near + 1.0)) + ((corners.element((far + 1.0)) - corners.element((near + 1.0))) * t));
                auto z = (corners.element((near + 2.0)) + ((corners.element((far + 2.0)) - corners.element((near + 2.0))) * t));
                if ((x < min_x)) {
                  min_x = x;
                }
                if ((x > max_x)) {
                  max_x = x;
                }
                if ((y < min_y)) {
                  min_y = y;
                }
                if ((y > max_y)) {
                  max_y = y;
                }
                if ((z < min_z)) {
                  min_z = z;
                }
                if ((z > max_z)) {
                  max_z = z;
                }
              }
            }
            corner += 1.0;
          }
        }
        if ((!std::isfinite(min_x) || !std::isfinite(max_z))) {
          return;
        }
        query_region_scratch->min_x = min_x;
        query_region_scratch->min_y = min_y;
        query_region_scratch->min_z = min_z;
        query_region_scratch->max_x = max_x;
        query_region_scratch->max_y = max_y;
        query_region_scratch->max_z = max_z;
        flight::row_get<flight::RowKey<"runtime">>(index)->backend->query_spatial_region(query_region_scratch, frustum_slice);
        for (auto id : frustum_slice) {
          if (frustum_seen.has(id)) {
            continue;
          }
          frustum_seen.add(id);
          out.push(id);
        }
      }
      slice += 1.0;
    }
  }
}

} // namespace flight::spatial
