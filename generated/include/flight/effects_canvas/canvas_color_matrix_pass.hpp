// Generated by @flighthq/tool-compiler from @flighthq/effects-canvas/canvasColorMatrixPass.ts#CanvasColorMatrixPass. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <stdexcept>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "canvas_effect_compositing.hpp"

namespace flighthq_effects_canvas {

inline void apply_color_matrix_pass_to_canvas(CanvasRenderTarget source, CanvasRenderTarget dest, flight::Array<double> matrix) {
  draw_canvas_image_data_pass(dest, source, [=](auto data, auto pixel_count) {
  apply_color_matrix_to_image_data_bytes(data, pixel_count, matrix);
  throw std::logic_error("Flight function completed without a value");
});
}

inline void apply_color_matrix_to_image_data_bytes(flight::Uint8ClampedArray data, double pixel_count, flight::Array<double> matrix) {
  const double m0 = matrix.get(0.0).value_or(0.0);
  const double m1 = matrix.get(1.0).value_or(0.0);
  const double m2 = matrix.get(2.0).value_or(0.0);
  const double m3 = matrix.get(3.0).value_or(0.0);
  const double m4 = matrix.get(4.0).value_or(0.0);
  const double m5 = matrix.get(5.0).value_or(0.0);
  const double m6 = matrix.get(6.0).value_or(0.0);
  const double m7 = matrix.get(7.0).value_or(0.0);
  const double m8 = matrix.get(8.0).value_or(0.0);
  const double m9 = matrix.get(9.0).value_or(0.0);
  const double m10 = matrix.get(10.0).value_or(0.0);
  const double m11 = matrix.get(11.0).value_or(0.0);
  const double m12 = matrix.get(12.0).value_or(0.0);
  const double m13 = matrix.get(13.0).value_or(0.0);
  const double m14 = matrix.get(14.0).value_or(0.0);
  const double m15 = matrix.get(15.0).value_or(0.0);
  const double m16 = matrix.get(16.0).value_or(0.0);
  const double m17 = matrix.get(17.0).value_or(0.0);
  const double m18 = matrix.get(18.0).value_or(0.0);
  const double m19 = matrix.get(19.0).value_or(0.0);
  {
    double i = 0.0;
    while ((i < pixel_count)) {
      {
        const double p = (i * 4.0);
        std::optional<auto> r = data.get(p);
        std::optional<auto> g = data.get((p + 1.0));
        std::optional<auto> b = data.get((p + 2.0));
        std::optional<auto> a = data.get((p + 3.0));
        data.element(p) = (((((m0 * r) + (m1 * g)) + (m2 * b)) + (m3 * a)) + (m4 * 255.0));
        data.element((p + 1.0)) = (((((m5 * r) + (m6 * g)) + (m7 * b)) + (m8 * a)) + (m9 * 255.0));
        data.element((p + 2.0)) = (((((m10 * r) + (m11 * g)) + (m12 * b)) + (m13 * a)) + (m14 * 255.0));
        data.element((p + 3.0)) = (((((m15 * r) + (m16 * g)) + (m17 * b)) + (m18 * a)) + (m19 * 255.0));
      }
      i += 1.0;
    }
  }
}

} // namespace flighthq_effects_canvas
