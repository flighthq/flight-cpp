// Generated by @flighthq/tool-compiler from @flighthq/effects-canvas/canvasColorLutPass.ts#CanvasColorLutPass. Do not edit.
#pragma once
#include <functional>
#include <stdexcept>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "canvas_effect_compositing.hpp"

namespace flighthq_effects_canvas {

inline void apply_color_lut_pass_to_canvas(CanvasRenderTarget source, CanvasRenderTarget dest, ColorLut lut) {
  std::tuple<double, double, double> rgb = std::make_tuple(0.0, 0.0, 0.0);
  draw_canvas_image_data_pass(dest, source, [=](auto data, auto pixel_count) {
  {
    double i = 0.0;
    while ((i < pixel_count)) {
      {
        const double p = (i * 4.0);
        sample_color_lut(lut, rgb, (data[static_cast<size_t>(p)] / 255.0), (data[static_cast<size_t>((p + 1.0))] / 255.0), (data[static_cast<size_t>((p + 2.0))] / 255.0));
        data[static_cast<size_t>(p)] = (std::get<0>(rgb) * 255.0);
        data[static_cast<size_t>((p + 1.0))] = (std::get<1>(rgb) * 255.0);
        data[static_cast<size_t>((p + 2.0))] = (std::get<2>(rgb) * 255.0);
      }
      i += 1.0;
    }
  }
  throw std::logic_error("Flight function completed without a value");
});
}

} // namespace flighthq_effects_canvas
