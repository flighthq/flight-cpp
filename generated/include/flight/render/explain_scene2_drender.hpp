// Generated by @flighthq/tool-compiler from @flighthq/render/explainScene2DRender.ts#ExplainScene2DRender. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "render_proxy.hpp"
#include "render_state.hpp"

namespace flighthq_render {

inline Scene2DRenderExplanation explain_scene2_drender(RenderState state, Renderable source) {
  auto kind = source.kind;
  const bool has_renderer = (([&]() -> std::optional<auto> { auto optional_chain_receiver = get_render_state_runtime(state).registries.renderers.entries.get(kind); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().state; }()) == registry_entry_state.bound);
  auto proxy = get_render_proxy2_d(state, source);
  const bool prepared = proxy.has_value();
  HasAppearance appearance = static_cast<HasAppearance>(static_cast<auto>(source));
  auto visible = (proxy.has_value() ? proxy.visible : appearance.visible);
  auto effective_alpha = (proxy.has_value() ? proxy.alpha : appearance.alpha);
  Scene2DRenderBlankReason reason;
  if (!has_renderer) {
    reason = flight::String("no-renderer");
  }
  else {
    if (!prepared) {
      reason = flight::String("not-prepared");
    }
    else {
      if (!visible) {
        reason = flight::String("not-visible");
      }
      else {
        if ((effective_alpha <= 0.0)) {
          reason = flight::String("zero-alpha");
        }
        else {
          reason = flight::String("ok");
        }
      }
    }
  }
  return {.kind = kind, .has_renderer = has_renderer, .prepared = prepared, .visible = visible, .effective_alpha = effective_alpha, .reason = reason};
}

} // namespace flighthq_render
