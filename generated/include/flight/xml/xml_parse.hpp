// Generated by @flighthq/tool-compiler from @flighthq/xml/packages/xml/src/xmlParse.ts#XmlParse at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <flight/number.hpp>
#include <flight/regexp.hpp>
#include <functional>
#include <optional>
#include <unordered_map>
#include <variant>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/types/xml_element.hpp>

namespace flight::xml {

struct ParseState;

struct ParseState : public flight::ReferenceEnabled {
  double depth;
  bool depth_exceeded;
  double pos;
};

inline const double max_xml_entity_passes = 8.0;

inline const double max_xml_entity_growth = 16.0;

inline const double max_xml_entity_budget = 65536.0;

inline const double max_xml_element_depth = 256.0;

inline std::unordered_map<flight::String, flight::String> xml_entities = {{flight::String("amp"), flight::String("&")}, {flight::String("apos"), flight::String("'")}, {flight::String("gt"), flight::String(">")}, {flight::String("lt"), flight::String("<")}, {flight::String("quot"), flight::String("\"")}};

inline flight::String expand_xml_entities(flight::String src, std::unordered_map<flight::String, flight::String> entities) {
  flight::String output = src;
  const double budget = ((static_cast<double>(src.length()) * max_xml_entity_growth) + max_xml_entity_budget);
  {
    double pass = 0.0;
    while ((pass < max_xml_entity_passes)) {
      {
        const auto expanded_capture = flight::make_binding_cell(bool{false});
        auto next = output.replace(flight::RegExp(flight::String("&([\\w:.-]+);"), flight::String("g")), [=](flight::String reference, flight::String name) {
  const flight::String replacement = entities[name];
  if (!replacement.has_value()) {
    return reference;
  }
  expanded_capture.rebind(true);
  return replacement;
});
        if ((!expanded_capture.read_binding() || (static_cast<double>(next.length()) > budget))) {
          return output;
        }
        output = next;
      }
      pass += 1.0;
    }
  }
  return output;
}

inline flight::String decode_xml_entities(flight::String s) {
  return s.replace(flight::RegExp(flight::String("&(?:#(\\d+)|#x([\\da-fA-F]+)|(\\w+));"), flight::String("g")), [=](flight::String reference, flight::String dec, flight::String hex, flight::String name) {
  const flight::String numeric = dec;
  if (numeric.has_value()) {
    auto codepoint = flight::parse_int(numeric, (dec.has_value() ? 10.0 : 16.0));
    if (((codepoint > 1114111.0) || ((codepoint >= 55296.0) && (codepoint <= 57343.0)))) {
      return reference;
    }
    return flight::String::from_code_point(codepoint);
  }
  return xml_entities[name];
});
}

inline std::unordered_map<flight::String, flight::String> parse_xml_attributes(flight::String attrs) {
  std::unordered_map<flight::String, flight::String> result = {};
  flight::RegExp re = flight::RegExp(flight::String("([\\w:.-]+)\\s*=\\s*(?:\"([^\"]*)\"|'([^']*)')"), flight::String("g"));
  std::optional<flight::RegExpExecArray> m;
  while (m = re.exec(attrs).has_value()) {
    const flight::String attr_name = m.value()[static_cast<size_t>(1.0)];
    const flight::String value = (m.value()[static_cast<size_t>(2.0)].has_value() ? m.value()[static_cast<size_t>(2.0)] : m.value()[static_cast<size_t>(3.0)].value_or(flight::String("")));
    result[attr_name] = decode_xml_entities(value);
  }
  return result;
}

inline void skip_whitespace(flight::String src, flight::Ref<ParseState> state) {
  while (((state->pos < static_cast<double>(src.length())) && flight::RegExp(flight::String("\\s"), flight::String("")).test(src[static_cast<size_t>(state->pos)]))) {
    state->pos++;
  }
}

inline std::optional<flight::Ref<flight::types::XmlElement>> parse_element(flight::String src, flight::Ref<ParseState> state) {
  if ((state->depth >= max_xml_element_depth)) {
    state->depth_exceeded = true;
    return std::nullopt;
  }
  skip_whitespace(src, state);
  if (((state->pos >= static_cast<double>(src.length())) || (src[static_cast<size_t>(state->pos)] != flight::String("<")))) {
    return std::nullopt;
  }
  state->pos++;
  if ((src[static_cast<size_t>(state->pos)] == flight::String("?"))) {
    auto end = src.index_of(flight::String("?>"), state->pos);
    state->pos = ((end >= 0.0) ? (end + 2.0) : static_cast<double>(src.length()));
    return parse_element(src, state);
  }
  const double name_start = state->pos;
  while (((state->pos < static_cast<double>(src.length())) && !flight::RegExp(flight::String("[\\s>/]"), flight::String("")).test(src[static_cast<size_t>(state->pos)]))) {
    state->pos++;
  }
  auto name = src.slice(name_start, state->pos);
  if (!name) {
    return std::nullopt;
  }
  skip_whitespace(src, state);
  flight::String attrs_str = flight::String("");
  flight::String quote = flight::String("");
  while ((state->pos < static_cast<double>(src.length()))) {
    const flight::String ch = src[static_cast<size_t>(state->pos)];
    if (quote) {
      if ((ch == quote)) {
        quote = flight::String("");
      }
    }
    else {
      if (((ch == flight::String("\"")) || (ch == flight::String("'")))) {
        quote = ch;
      }
      else {
        if (((ch == flight::String(">")) || ((ch == flight::String("/")) && (src[static_cast<size_t>((state->pos + 1.0))] == flight::String(">"))))) {
          break;
        }
      }
    }
    attrs_str += ch;
    state->pos++;
  }
  const bool self_closing = (src[static_cast<size_t>(state->pos)] == flight::String("/"));
  state->pos += (self_closing ? 2.0 : 1.0);
  std::unordered_map<flight::String, flight::String> attributes = parse_xml_attributes(attrs_str);
  flight::Array<flight::Ref<flight::types::XmlElement>> children = flight::Array<flight::Ref<flight::types::XmlElement>>{};
  flight::Array<std::variant<flight::Ref<flight::types::XmlElement>, flight::String>> content = flight::Array<std::variant<flight::Ref<flight::types::XmlElement>, flight::String>>{};
  flight::String text = flight::String("");
  if (!self_closing) {
    while ((state->pos < static_cast<double>(src.length()))) {
      if ((state->pos >= static_cast<double>(src.length()))) {
        break;
      }
      if ((src[static_cast<size_t>(state->pos)] != flight::String("<"))) {
        const double text_start = state->pos;
        while (((state->pos < static_cast<double>(src.length())) && (src[static_cast<size_t>(state->pos)] != flight::String("<")))) {
          state->pos++;
        }
        const flight::String decoded = decode_xml_entities(src.slice(text_start, state->pos));
        text += decoded.trim();
        if ((decoded != flight::String(""))) {
          content.push(decoded);
        }
        continue;
      }
      if ((src.slice(state->pos, (state->pos + 9.0)) == flight::String("<![CDATA["))) {
        const double cdata_start = (state->pos + 9.0);
        auto cdata_end = src.index_of(flight::String("]]>"), cdata_start);
        const double content_end = ((cdata_end >= 0.0) ? cdata_end : static_cast<double>(src.length()));
        auto cdata = src.slice(cdata_start, content_end);
        text += cdata.trim();
        if ((cdata != flight::String(""))) {
          content.push(cdata);
        }
        state->pos = ((cdata_end >= 0.0) ? (cdata_end + 3.0) : static_cast<double>(src.length()));
        continue;
      }
      if ((src[static_cast<size_t>((state->pos + 1.0))] == flight::String("/"))) {
        while (((state->pos < static_cast<double>(src.length())) && (src[static_cast<size_t>(state->pos)] != flight::String(">")))) {
          state->pos++;
        }
        state->pos++;
        break;
      }
      state->depth++;
      auto child = parse_element(src, state);
      state->depth--;
      if (state->depth_exceeded) {
        return std::nullopt;
      }
      if (child) {
        children.push(child.value());
        content.push(child.value());
      }
    }
  }
  return std::optional<flight::Ref<flight::types::XmlElement>>{flight::make_ref<flight::types::XmlElement>(flight::types::XmlElement{.attributes = attributes, .children = children, .content = content, .name = name, .text = text})};
}

inline flight::String strip_xml_comments(flight::String xml) {
  double copy_start = 0.0;
  flight::String output = flight::String("");
  double pos = 0.0;
  while ((pos < static_cast<double>(xml.length()))) {
    if ((xml.slice(pos, (pos + 9.0)) == flight::String("<![CDATA["))) {
      auto cdata_end = xml.index_of(flight::String("]]>"), (pos + 9.0));
      pos = ((cdata_end >= 0.0) ? (cdata_end + 3.0) : static_cast<double>(xml.length()));
      continue;
    }
    if ((xml.slice(pos, (pos + 4.0)) != flight::String("<!--"))) {
      pos++;
      continue;
    }
    output += xml.slice(copy_start, pos);
    auto comment_end = xml.index_of(flight::String("-->"), (pos + 4.0));
    pos = ((comment_end >= 0.0) ? (comment_end + 3.0) : static_cast<double>(xml.length()));
    copy_start = pos;
  }
  return output + xml.slice(copy_start);
}

inline void collect_xml_entity_declarations(flight::String doctype, std::unordered_map<flight::String, flight::String> out) {
  flight::RegExp declaration = flight::RegExp(flight::String("<!ENTITY\\s+([\\w:.-]+)\\s*(?:\"([^\"]*)\"|'([^']*)')\\s*>"), flight::String("g"));
  std::optional<flight::RegExpExecArray> match;
  while (match = declaration.exec(doctype).has_value()) {
    out[match.value()[static_cast<size_t>(1.0)]] = match.value()[static_cast<size_t>(2.0)].value_or(match.value()[static_cast<size_t>(3.0)]);
  }
}

inline flight::String strip_xml_doctypes(flight::String xml, std::unordered_map<flight::String, flight::String> out) {
  double copy_start = 0.0;
  flight::String output = flight::String("");
  double pos = 0.0;
  while ((pos < static_cast<double>(xml.length()))) {
    if (((xml[static_cast<size_t>(pos)] != flight::String("<")) || (xml.slice(pos, (pos + 9.0)).to_lower() != flight::String("<!doctype")))) {
      pos++;
      continue;
    }
    output += xml.slice(copy_start, pos);
    const double doctype_start = pos;
    pos += 9.0;
    double internal_subset_depth = 0.0;
    flight::String quote = flight::String("");
    while ((pos < static_cast<double>(xml.length()))) {
      const flight::String ch = xml[static_cast<size_t>(pos)];
      if (quote) {
        if ((ch == quote)) {
          quote = flight::String("");
        }
      }
      else {
        if (((ch == flight::String("\"")) || (ch == flight::String("'")))) {
          quote = ch;
        }
        else {
          if ((ch == flight::String("["))) {
            internal_subset_depth++;
          }
          else {
            if (((ch == flight::String("]")) && (internal_subset_depth > 0.0))) {
              internal_subset_depth--;
            }
            else {
              if (((ch == flight::String(">")) && (internal_subset_depth == 0.0))) {
                pos++;
                break;
              }
            }
          }
        }
      }
      pos++;
    }
    collect_xml_entity_declarations(xml.slice(doctype_start, pos), out);
    copy_start = pos;
  }
  return output + xml.slice(copy_start);
}

inline std::optional<flight::Ref<flight::types::XmlElement>> parse_xml_document(flight::String xml) {
  auto src = strip_xml_comments(xml).replace(flight::RegExp(flight::String("\\r\\n?"), flight::String("g")), flight::String("\n"));
  std::unordered_map<flight::String, flight::String> entities = {};
  src = strip_xml_doctypes(src.replace(flight::RegExp(flight::String("<\\?[\\s\\S]*?\\?>"), flight::String("g")), flight::String("")), entities).trim();
  return parse_element(expand_xml_entities(src, entities), flight::make_ref<ParseState>(ParseState{.depth = 0.0, .depth_exceeded = false, .pos = 0.0}));
}

} // namespace flight::xml
