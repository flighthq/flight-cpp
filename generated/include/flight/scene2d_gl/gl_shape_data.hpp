// Generated by @flighthq/tool-compiler from @flighthq/scene2d-gl/glShapeData.ts#GlShapeData. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_scene2d_gl {

inline std::optional<Raster2DSurface> acquire_gl_shape_raster_surface(Raster2DSurfaceProvider provider, GlShapeRendererData data) {
  auto existing = data.surface;
  if (existing.has_value()) {
    return existing;
  }
  auto surface = create_raster2_dsurface(provider, 1.0, 1.0);
  if (!surface.has_value()) {
    return nullptr;
  }
  data.surface = surface;
  return surface;
}

inline std::optional<RendererData> create_gl_shape_data(GlRenderState state, Renderable source) {
  auto out = allocate_entity<GlShapeRendererData>();
  initialize_gl_shape_data(out, state, source);
  return finish_entity(out);
}

inline void destroy_gl_shape_data(GlRenderState state, RendererData data) {
  auto runtime = get_gl_render_state_runtime(state);
  auto surface = get_gl_shape_data(data).surface;
  if (!surface.has_value()) {
    return;
  }
  auto entry = runtime.context.texture_source_premultiplied_texture_cache.get(surface.image);
  if (entry.has_value()) {
    state.gl.delete_texture(entry.texture);
    runtime.context.texture_source_premultiplied_texture_cache.delete_(surface.image);
  }
  destroy_raster2_dsurface(surface);
}

inline GlShapeRendererData get_gl_shape_data(RendererData data) {
  return static_cast<GlShapeRendererData>(data);
}

inline void initialize_gl_shape_data(EntityConstruction<GlShapeRendererData> out, GlRenderState state, Renderable source) {
  out.surface = nullptr;
  out.last_content_id = -1.0;
  out.last_pixel_ratio = 0.0;
  out.last_w = 0.0;
  out.last_h = 0.0;
  out.mesh_version = -1.0;
  out.meshes = nullptr;
}

inline RendererData to_gl_shape_renderer_data(GlShapeRendererData data) {
  return data;
}

} // namespace flighthq_scene2d_gl
