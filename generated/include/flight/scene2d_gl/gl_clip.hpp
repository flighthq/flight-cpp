// Generated by @flighthq/tool-compiler from @flighthq/scene2d-gl/glClip.ts#GlClip. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "gl_clip_contours.hpp"
#include "gl_clip_rectangle.hpp"

namespace flighthq_scene2d_gl {

inline void enable_gl_clip_support(GlRenderState state) {
  state.display_object_clip_hooks = webgl_clip_hooks;
}

inline void pop_one_gl_clip(GlRenderState state) {
  auto form = get_gl_render_state_runtime(state).clip_forms.pop();
  if ((form == flight::String("contour"))) {
    pop_gl_clip_contours(state);
  }
  else {
    pop_gl_clip_rectangle(state);
  }
}

inline Scene2DClipHooks webgl_clip_hooks = {.finalize = [=](GlRenderState state) {
  auto runtime = get_gl_render_state_runtime(state);
  while ((runtime.clip_forms.length > 0.0)) {
    pop_one_gl_clip(state);
  }
}, .pop_clip = [=](GlRenderState state, RenderProxy2D data, Node2D source) {
  auto runtime = get_gl_render_state_runtime(state);
  const double target = (data.clip_depth - ((source.clip != nullptr) ? 1.0 : 0.0));
  while ((runtime.clip_forms.length > target)) {
    pop_one_gl_clip(state);
  }
}, .push_clip = [=](GlRenderState state, RenderProxy2D data, Node2D source) {
  auto runtime = get_gl_render_state_runtime(state);
  auto clip = source.clip;
  if (!clip.has_value()) {
    return;
  }
  if ((clip.contours == nullptr)) {
    push_gl_clip_rectangle(state, clip.rect, data.transform2_d);
    runtime.clip_forms.push(flight::String("rect"));
  }
  else {
    push_gl_clip_contours(state, clip.contours, clip.winding, data.transform2_d);
    runtime.clip_forms.push(flight::String("contour"));
  }
}};

} // namespace flighthq_scene2d_gl
