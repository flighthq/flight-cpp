// Generated by @flighthq/tool-compiler from @flighthq/effects/packages/effects/src/boxBlurMath.ts#BoxBlurMath at 7e2fc7df5378313ceaf21ba8d6ef39cc18a6f4ad. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::effects {

inline double compute_box_blur_radius(double sigma, double passes) {
  if ((sigma <= 0.0)) {
    return 0.0;
  }
  return flight::maximum(0.0, flight::round(((-1.0 + std::sqrt((1.0 + (((12.0 * sigma) * sigma) / passes)))) / 2.0)));
}

inline double compute_gaussian_sigma_for_blur_radius(double radius, double passes) {
  if (((radius <= 0.0) || (passes <= 0.0))) {
    return 0.0;
  }
  const double width = ((2.0 * radius) + 1.0);
  return std::sqrt((((passes * width) * width) / 12.0));
}

inline double compute_box_blur_lower_width(double sigma, double passes) {
  double width = std::floor(std::sqrt(((((12.0 * sigma) * sigma) / passes) + 1.0)));
  if ((std::fmod(width, 2.0) == 0.0)) {
    (width -= 1.0);
  }
  return width;
}

inline double compute_box_blur_lower_pass_count(double sigma, double passes, double lower_width) {
  return flight::round(((((12.0 * sigma) * sigma) - (passes * (((lower_width * lower_width) + (4.0 * lower_width)) + 3.0))) / ((-4.0 * lower_width) - 4.0)));
}

inline double compute_box_blur_pass_radius(double sigma, double passes, double pass) {
  if ((sigma <= 0.0)) {
    return 0.0;
  }
  const double lower_width = compute_box_blur_lower_width(sigma, passes);
  const double lower_count = compute_box_blur_lower_pass_count(sigma, passes, lower_width);
  const double width = ((pass < lower_count) ? lower_width : (lower_width + 2.0));
  return flight::maximum(0.0, ((width - 1.0) / 2.0));
}

} // namespace flight::effects
