// Generated by @flighthq/tool-compiler from @flighthq/effects/kuwaharaMath.ts#KuwaharaMath. Do not edit.
#pragma once
#include <optional>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_effects {

inline double compute_kuwahara_gaussian_weights(double radius, flight::Float32Array out) {
  const double r = std::max(1.0, std::floor(radius));
  const double size = (r + 1.0);
  const double sigma = (r / 2.0);
  const double two_sigma_sq = ((2.0 * sigma) * sigma);
  double sum = 0.0;
  {
    double y = 0.0;
    while ((y < size)) {
      {
        {
          double x = 0.0;
          while ((x < size)) {
            {
              const double d = ((x * x) + (y * y));
              out.element(((y * size) + x)) = cmath.exp((-d / two_sigma_sq));
              sum += out.element(((y * size) + x));
            }
            x += 1.0;
          }
        }
      }
      y += 1.0;
    }
  }
  const double inv_sum = ((sum > 1e-10) ? (1.0 / sum) : 1.0);
  {
    double i = 0.0;
    while ((i < (size * size))) {
      {
        out.element(i) *= inv_sum;
      }
      i += 1.0;
    }
  }
  return (size * size);
}

inline void compute_kuwahara_sector_offsets(double radius, std::tuple<double, double, double, double, double, double, double, double> out) {
  const double r = std::max(1.0, std::floor(radius));
  const double half = r;
  const double v0 = -half;
  const double v1 = -half;
  const double v2 = 0.0;
  const double v3 = -half;
  const double v4 = -half;
  const double v5 = 0.0;
  const double v6 = 0.0;
  const double v7 = 0.0;
  std::get<0>(out) = v0;
  std::get<1>(out) = v1;
  std::get<2>(out) = v2;
  std::get<3>(out) = v3;
  std::get<4>(out) = v4;
  std::get<5>(out) = v5;
  std::get<6>(out) = v6;
  std::get<7>(out) = v7;
}

inline double compute_kuwahara_sector_pixel_count(KuwaharaEffect effect) {
  const double s = compute_kuwahara_sector_size(effect);
  return (s * s);
}

inline double compute_kuwahara_sector_size(KuwaharaEffect effect) {
  return (std::max(1.0, std::floor(effect.radius.value_or(3.0))) + 1.0);
}

} // namespace flighthq_effects
