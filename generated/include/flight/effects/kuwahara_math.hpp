// Generated by @flighthq/tool-compiler from @flighthq/effects/packages/effects/src/kuwaharaMath.ts#KuwaharaMath at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flight::types { struct KuwaharaEffect; }

#include <flight/types/entity.hpp>
#include <flight/types/kuwahara_effect.hpp>

namespace flight::effects {

inline double compute_kuwahara_gaussian_weights(double radius, flight::Float32Array out) {
  auto r = flight::maximum(1.0, std::floor(radius));
  const double size = (r + 1.0);
  const double sigma = (r / 2.0);
  const double two_sigma_sq = ((2.0 * sigma) * sigma);
  double sum = 0.0;
  {
    double y = 0.0;
    while ((y < size)) {
      {
        {
          double x = 0.0;
          while ((x < size)) {
            {
              const double d = ((x * x) + (y * y));
              (out.element(((y * size) + x)) = std::exp((-d / two_sigma_sq)));
              (sum += out.element(((y * size) + x)));
            }
            (x += 1.0);
          }
        }
      }
      (y += 1.0);
    }
  }
  const double inv_sum = ((sum > 1e-10) ? (1.0 / sum) : 1.0);
  {
    double i = 0.0;
    while ((i < (size * size))) {
      {
        (out.element(i) *= inv_sum);
      }
      (i += 1.0);
    }
  }
  return (size * size);
}

inline void compute_kuwahara_sector_offsets(double radius, flight::Array<double> out) {
  auto r = flight::maximum(1.0, std::floor(radius));
  auto half = r;
  const double v0 = -half;
  const double v1 = -half;
  const double v2 = 0.0;
  const double v3 = -half;
  const double v4 = -half;
  const double v5 = 0.0;
  const double v6 = 0.0;
  const double v7 = 0.0;
  (out.element(0.0) = v0);
  (out.element(1.0) = v1);
  (out.element(2.0) = v2);
  (out.element(3.0) = v3);
  (out.element(4.0) = v4);
  (out.element(5.0) = v5);
  (out.element(6.0) = v6);
  (out.element(7.0) = v7);
}

inline double compute_kuwahara_sector_size(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::KuwaharaEffect>>>> effect) {
  return (flight::maximum(1.0, std::floor(flight::row_get<flight::RowKey<"radius">>(effect).value_or(3.0))) + 1.0);
}

inline double compute_kuwahara_sector_pixel_count(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::KuwaharaEffect>>>> effect) {
  const double s = compute_kuwahara_sector_size(effect);
  return (s * s);
}

} // namespace flight::effects
