// Generated by @flighthq/tool-compiler from @flighthq/effects/colorTemperatureMath.ts#ColorTemperatureMath. Do not edit.
#pragma once
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_effects {

inline void compute_color_temperature_rgb(double kelvin, std::tuple<double, double, double> out) {
  const double temp = (std::max(1000.0, std::min(40000.0, kelvin)) / 100.0);
  double r;
  double g;
  double b;
  if ((temp <= 66.0)) {
    r = 1.0;
    g = (((99.4708025861 * cmath.log(temp)) - 161.1195681661) / 255.0);
    b = ((temp <= 19.0) ? 0.0 : (((138.5177312231 * cmath.log((temp - 10.0))) - 305.0447927307) / 255.0));
  }
  else {
    r = ((329.698727446 * std::pow((temp - 60.0), -0.1332047592)) / 255.0);
    g = ((288.1221695283 * std::pow((temp - 60.0), -0.0755148492)) / 255.0);
    b = 1.0;
  }
  std::get<0>(out) = std::max(0.0, std::min(1.0, r));
  std::get<1>(out) = std::max(0.0, std::min(1.0, g));
  std::get<2>(out) = std::max(0.0, std::min(1.0, b));
}

inline void compute_white_balance_multipliers(double temperature, double tint, std::tuple<double, double, double> out) {
  const double kelvin = (6500.0 - (temperature * 4500.0));
  compute_color_temperature_rgb(kelvin, out);
  const double green_shift = (-tint * 0.1);
  std::get<0>(out) = std::max(0.0, std::get<0>(out));
  std::get<1>(out) = std::max(0.0, (std::get<1>(out) + green_shift));
  std::get<2>(out) = std::max(0.0, std::get<2>(out));
}

} // namespace flighthq_effects
