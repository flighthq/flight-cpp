// Generated by @flighthq/tool-compiler from @flighthq/effects/godRaysMath.ts#GodRaysMath. Do not edit.
#pragma once
#include <optional>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_effects {

inline double compute_god_rays_accumulation_scale(GodRaysEffect effect) {
  const double samples = std::max(1.0, effect.samples.value_or(100.0));
  const double weight = std::max(0.000001, effect.weight.value_or(0.4));
  const double exposure = std::max(0.000001, effect.exposure.value_or(0.1));
  return (1.0 / ((samples * weight) * exposure));
}

inline void compute_god_rays_light_center(GodRaysEffect effect, std::tuple<double, double> out) {
  std::get<0>(out) = std::max(0.0, std::min(1.0, effect.center_x.value_or(0.5)));
  std::get<1>(out) = std::max(0.0, std::min(1.0, effect.center_y.value_or(0.5)));
}

inline double compute_god_rays_sample_weight(GodRaysEffect effect, double sample_index) {
  auto decay = effect.decay.value_or(0.96);
  auto weight = effect.weight.value_or(0.4);
  auto exposure = effect.exposure.value_or(0.1);
  return ((std::pow(decay, sample_index) * weight) * exposure);
}

inline void compute_god_rays_step_size(GodRaysEffect effect, double px, double py, std::tuple<double, double> out) {
  auto cx = effect.center_x.value_or(0.5);
  auto cy = effect.center_y.value_or(0.5);
  auto density = effect.density.value_or(0.96);
  const double samples = std::max(1.0, effect.samples.value_or(100.0));
  const double dx = (((cx - px) * density) / samples);
  const double dy = (((cy - py) * density) / samples);
  std::get<0>(out) = dx;
  std::get<1>(out) = dy;
}

} // namespace flighthq_effects
