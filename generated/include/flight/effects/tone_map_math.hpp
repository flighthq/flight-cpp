// Generated by @flighthq/tool-compiler from @flighthq/effects/toneMapMath.ts#ToneMapMath. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_effects {

inline double compute_aces_tone_map(double x) {
  const double a = 2.51;
  const double b = 0.03;
  const double c = 2.43;
  const double d = 0.59;
  const double e = 0.14;
  return std::max(0.0, std::min(1.0, ((x * ((a * x) + b)) / ((x * ((c * x) + d)) + e))));
}

inline double compute_agx_tone_map(double x, std::optional<AgxToneMapOptions> options = std::nullopt) {
  auto min_ev = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().min_ev; }()).value_or(-12.47393);
  auto max_ev = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().max_ev; }()).value_or(4.026069);
  const double val = std::max(1e-10, x);
  const double log = std::max(min_ev, std::min(max_ev, cmath.log2(val)));
  const double normalized = ((log - min_ev) / (max_ev - min_ev));
  return agx_default_contrast_approx(normalized);
}

inline double compute_exposure_scale(double exposure) {
  return std::pow(2.0, exposure);
}

inline double compute_filmic_tone_map(double x, std::optional<FilmicToneMapOptions> options = std::nullopt) {
  auto max_brightness = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().max_brightness; }()).value_or(1.0);
  auto contrast = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().contrast; }()).value_or(1.0);
  auto linear_start = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().linear_start; }()).value_or(0.22);
  auto linear_length = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().linear_length; }()).value_or(0.4);
  auto black_tighten = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().black_tighten; }()).value_or(1.33);
  auto pedestal = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().pedestal; }()).value_or(0.0);
  const double l0 = (((max_brightness - linear_start) * linear_length) / contrast);
  const double l0_2 = (linear_start - (linear_start / contrast));
  auto l1 = (linear_start + ((1.0 - linear_start) / contrast));
  auto s0 = (linear_start + l0);
  auto s1 = (linear_start + (contrast * l0));
  const double c2 = (contrast / (max_brightness - s1));
  const double cp = (-c2 / cmath.log(2.0));
  const double w0 = (1.0 - smoothstep01(linear_start, s0, x));
  auto t = ((linear_start * std::pow((x / linear_start), black_tighten)) + pedestal);
  auto l = (linear_start + (contrast * (x - linear_start)));
  const double s = (max_brightness - ((max_brightness - s1) * cmath.exp((cp * (x - s0)))));
  return std::max(0.0, ((((w0 * (1.0 - smoothstep01(l0_2, l1, x))) * t) + (smoothstep01(l0_2, l1, x) * l)) + ((1.0 - w0) * s)));
}

inline double compute_reinhard_extended_tone_map(double x, double white) {
  const double w2 = (white * white);
  return ((x * (1.0 + (x / w2))) / (1.0 + x));
}

inline double compute_reinhard_tone_map(double x) {
  return (x / (1.0 + x));
}

inline double compute_uncharted2_tone_map(double x) {
  const double a = 0.15;
  const double b = 0.5;
  const double c = 0.1;
  const double d = 0.2;
  const double e = 0.02;
  const double f = 0.3;
  return ((((x * ((a * x) + (c * b))) + (d * e)) / ((x * ((a * x) + b)) + (d * f))) - (e / f));
}

inline void get_aces_input_matrix(flight::Float32Array out) {
  out.element(0.0) = 0.59719;
  out.element(1.0) = 0.076;
  out.element(2.0) = 0.0284;
  out.element(3.0) = 0.35458;
  out.element(4.0) = 0.90834;
  out.element(5.0) = 0.13383;
  out.element(6.0) = 0.04823;
  out.element(7.0) = 0.01566;
  out.element(8.0) = 0.83777;
}

inline void get_aces_output_matrix(flight::Float32Array out) {
  out.element(0.0) = 1.60475;
  out.element(1.0) = -0.10208;
  out.element(2.0) = -0.00327;
  out.element(3.0) = -0.53108;
  out.element(4.0) = 1.10813;
  out.element(5.0) = -0.07276;
  out.element(6.0) = -0.07367;
  out.element(7.0) = -0.00605;
  out.element(8.0) = 1.07602;
}

inline double agx_default_contrast_approx(double x) {
  const double x2 = (x * x);
  const double x4 = (x2 * x2);
  return ((((((((15.5 * x4) * x2) - ((40.14 * x4) * x)) + (31.96 * x4)) - ((6.868 * x2) * x)) + (0.4298 * x2)) + (0.1191 * x)) - 0.00232);
}

inline double smoothstep01(double edge0, double edge1, double x) {
  const double t = std::max(0.0, std::min(1.0, ((x - edge0) / (edge1 - edge0))));
  return ((t * t) * (3.0 - (2.0 * t)));
}

} // namespace flighthq_effects
