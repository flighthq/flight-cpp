// Generated by @flighthq/tool-compiler from @flighthq/effects/renderEffectCaptureGeometry.ts#RenderEffectCaptureGeometry. Do not edit.
#pragma once
#include <variant>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "render_effect_padding.hpp"

namespace flighthq_effects {

struct height_width {
  double height;
  double width;
};

inline bool compute_render_effect_capture_geometry(RenderEffectCaptureGeometry out, RenderState state, NodeAny source, std::variant<RenderEffect, flight::Array<RenderEffect>> effects) {
  if ((get_node_runtime(source).traits != node2_dtraits_key)) {
    return false;
  }
  compute_node_root_local_bounds_rectangle(bounds, static_cast<Node2D>(source));
  if (is_empty_rectangle(bounds)) {
    return false;
  }
  compute_render_effect_padding(state, effects, padding);
  compute_render_target_size(target_size, bounds, padding);
  compute_scene2_drender_target_transform(capture_transform, static_cast<Node2D>(source), bounds, padding.left, padding.top);
  copy_rectangle(out.bounds, bounds);
  copy_padding(out.padding, padding);
  copy_matrix(out.capture_transform, capture_transform);
  out.target_height = target_size.height;
  out.target_width = target_size.width;
  return true;
}

inline void copy_padding(RenderEffectPadding out, RenderEffectPadding source) {
  out.bottom = source.bottom;
  out.left = source.left;
  out.right = source.right;
  out.top = source.top;
}

inline auto bounds = create_rectangle();

inline auto capture_transform = create_matrix();

inline RenderEffectPadding padding = {.bottom = 0.0, .left = 0.0, .right = 0.0, .top = 0.0};

inline height_width target_size = {.height = 0.0, .width = 0.0};

} // namespace flighthq_effects
