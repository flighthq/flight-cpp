// Generated by @flighthq/tool-compiler from @flighthq/effects/edgeDetectMath.ts#EdgeDetectMath. Do not edit.
#pragma once
#include <cstdint>
#include <optional>
#include <tuple>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_effects {

inline void compute_outline_edge_params(OutlineEffect effect, std::tuple<double, double, double, double, double, double> out) {
  const double threshold = std::max(0.0, effect.threshold.value_or(0.1));
  const double feather = (threshold * 0.5);
  auto color = effect.color.value_or(255.0);
  const double r = (static_cast<double>(static_cast<int32_t>((color >>> 24.0)) & static_cast<int32_t>(255.0)) / 255.0);
  const double g = (static_cast<double>(static_cast<int32_t>((color >>> 16.0)) & static_cast<int32_t>(255.0)) / 255.0);
  const double b = (static_cast<double>(static_cast<int32_t>((color >>> 8.0)) & static_cast<int32_t>(255.0)) / 255.0);
  const double a = (static_cast<double>(static_cast<int32_t>(color) & static_cast<int32_t>(255.0)) / 255.0);
  std::get<0>(out) = threshold;
  std::get<1>(out) = feather;
  std::get<2>(out) = r;
  std::get<3>(out) = g;
  std::get<4>(out) = b;
  std::get<5>(out) = a;
}

inline double compute_outline_thickness_px(OutlineEffect effect) {
  return std::max(0.0, flight::round(effect.thickness.value_or(1.0)));
}

inline void compute_sketch_edge_params(SketchEffect effect, std::tuple<double, double> out) {
  const double strength = std::max(0.0, std::min(1.0, effect.strength.value_or(1.0)));
  const double threshold = std::max(0.01, std::min(1.0, (1.0 - (strength * 0.95))));
  std::get<0>(out) = threshold;
  std::get<1>(out) = strength;
}

inline void get_sobel_kernel_coefficients(flight::Float32Array out) {
  out.element(0.0) = -1.0;
  out.element(1.0) = 0.0;
  out.element(2.0) = 1.0;
  out.element(3.0) = -2.0;
  out.element(4.0) = 0.0;
  out.element(5.0) = 2.0;
  out.element(6.0) = -1.0;
  out.element(7.0) = 0.0;
  out.element(8.0) = 1.0;
  out.element(9.0) = -1.0;
  out.element(10.0) = -2.0;
  out.element(11.0) = -1.0;
  out.element(12.0) = 0.0;
  out.element(13.0) = 0.0;
  out.element(14.0) = 0.0;
  out.element(15.0) = 1.0;
  out.element(16.0) = 2.0;
  out.element(17.0) = 1.0;
}

} // namespace flighthq_effects
