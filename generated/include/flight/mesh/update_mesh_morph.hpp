// Generated by @flighthq/tool-compiler from @flighthq/mesh/updateMeshMorph.ts#UpdateMeshMorph. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "mesh_geometry.hpp"
#include "morph_mesh_geometry.hpp"

namespace flighthq_mesh {

inline void update_mesh_morph(Mesh mesh) {
  auto morph = mesh.morph;
  if (!morph.has_value()) {
    return;
  }
  auto geometry = mesh.geometry;
  auto bind_pose = get_mesh_geometry_morph_bind_pose(geometry);
  if (!bind_pose.has_value()) {
    bind_pose = capture_mesh_morph_bind_pose(geometry);
    set_mesh_geometry_morph_bind_pose(geometry, bind_pose);
  }
  else {
    if (!has_morph_weights_changed(geometry, morph)) {
      return;
    }
  }
  blend_mesh_geometry_morph(geometry, morph, bind_pose);
  record_morph_blended_weights(geometry, morph);
}

inline bool has_morph_weights_changed(MeshGeometry geometry, MeshMorph morph) {
  std::optional<MeshGeometryRuntime> runtime = static_cast<std::optional<MeshGeometryRuntime>>(geometry[static_cast<size_t>(entity_runtime_key)]);
  auto blended = ([&]() -> std::optional<auto> { auto optional_chain_receiver = runtime.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().morph_blended_weights; }());
  if (!blended.has_value()) {
    return true;
  }
  auto weights = morph.weights;
  if ((blended.length != weights.length)) {
    return true;
  }
  {
    double i = 0.0;
    while ((i < weights.length)) {
      {
        if ((blended[static_cast<size_t>(i)] != weights[static_cast<size_t>(i)])) {
          return true;
        }
      }
      i += 1.0;
    }
  }
  return false;
}

inline void record_morph_blended_weights(MeshGeometry geometry, MeshMorph morph) {
  std::optional<MeshGeometryRuntime> runtime = static_cast<std::optional<MeshGeometryRuntime>>(geometry[static_cast<size_t>(entity_runtime_key)]);
  if (!runtime.value().has_value()) {
    return;
  }
  auto weights = morph.weights;
  auto blended = runtime.value().morph_blended_weights;
  if ((!blended.has_value() || (blended.length != weights.length))) {
    blended = flight::Float32Array(weights.length);
    runtime.value().morph_blended_weights = blended;
  }
  blended.set(weights);
}

} // namespace flighthq_mesh
