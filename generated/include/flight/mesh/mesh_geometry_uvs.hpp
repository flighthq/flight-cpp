// Generated by @flighthq/tool-compiler from @flighthq/mesh/meshGeometryUvs.ts#MeshGeometryUvs. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "mesh_geometry_attributes.hpp"

namespace flighthq_mesh {

inline void offset_mesh_geometry_uvs(MeshGeometry geometry, double du, double dv) {
  auto float_offset = get_vertex_attribute_float_offset(geometry.layout, flight::String("uv0"));
  if ((float_offset < 0.0)) {
    return;
  }
  const double floats_per_vertex = (geometry.layout.stride / 4.0);
  const double vertex_count = ((floats_per_vertex > 0.0) ? std::floor((geometry.vertices.length / floats_per_vertex)) : 0.0);
  auto verts = geometry.vertices;
  {
    double i = 0.0;
    while ((i < vertex_count)) {
      {
        auto base = ((i * floats_per_vertex) + float_offset);
        verts[static_cast<size_t>(base)] += du;
        verts[static_cast<size_t>((base + 1.0))] += dv;
      }
      i += 1.0;
    }
  }
  if ((vertex_count > 0.0)) {
    geometry.version++;
  }
}

inline void scale_mesh_geometry_uvs(MeshGeometry geometry, double su, double sv) {
  auto float_offset = get_vertex_attribute_float_offset(geometry.layout, flight::String("uv0"));
  if ((float_offset < 0.0)) {
    return;
  }
  const double floats_per_vertex = (geometry.layout.stride / 4.0);
  const double vertex_count = ((floats_per_vertex > 0.0) ? std::floor((geometry.vertices.length / floats_per_vertex)) : 0.0);
  auto verts = geometry.vertices;
  {
    double i = 0.0;
    while ((i < vertex_count)) {
      {
        auto base = ((i * floats_per_vertex) + float_offset);
        verts[static_cast<size_t>(base)] *= su;
        verts[static_cast<size_t>((base + 1.0))] *= sv;
      }
      i += 1.0;
    }
  }
  if ((vertex_count > 0.0)) {
    geometry.version++;
  }
}

} // namespace flighthq_mesh
