// Generated by @flighthq/tool-compiler from @flighthq/text/enableTextLabelGuards.ts#EnableTextLabelGuards. Do not edit.
#pragma once
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include "text_label_layout.hpp"

namespace flighthq_text {

inline void disable_text_label_guards() {
  set_text_label_guard(nullptr);
}

inline void enable_text_label_guards() {
  set_text_label_guard(warn_on_stale_text_label_content);
}

inline void warn_on_stale_text_label_content(flight::String live_string, flight::String rasterized_string) {
  const flight::String live_preview = ((static_cast<double>(live_string.length()) > 40.0) ? live_string.slice(0.0, 40.0) + flight::String("...") : live_string);
  const flight::String raster_preview = ((static_cast<double>(rasterized_string.length()) > 40.0) ? rasterized_string.slice(0.0, 40.0) + flight::String("...") : rasterized_string);
  log_once(flight::String("text:stale-text-label-content"), log_level.warn, {.message = flight::String("TextLabel data.text was mutated directly (\"") + flight::to_string(live_preview) + flight::String("\") without calling setTextLabelString, ") + flight::String("so the content revision was not bumped and renderers still show the previous text (\"") + flight::to_string(raster_preview) + flight::String("\"). ") + flight::String("Use setTextLabelString(label, text) to update text at runtime.")}, flight::String("text"));
}

} // namespace flighthq_text
