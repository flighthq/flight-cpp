// Generated by @flighthq/tool-compiler from @flighthq/text/textLabelLayout.ts#TextLabelLayout. Do not edit.
#pragma once
#include <functional>
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_text {

inline void ensure_text_layout(TextLabel source) {
  TextLabelRuntime runtime = static_cast<TextLabelRuntime>(get_node2_druntime(source));
  auto content_id = get_node_local_content_revision(source);
  if (((runtime.text_layout != nullptr) && (runtime.text_layout_using_content_id == content_id))) {
    if ((((text_label_guard != nullptr) && (runtime.text_layout_using_text != nullptr)) && (source.data.text != runtime.text_layout_using_text))) {
      text_label_guard(source.data.text, runtime.text_layout_using_text);
    }
    return;
  }
  auto measure = get_text_layout_measure_provider();
  if (!measure.has_value()) {
    return;
  }
  auto params = runtime.build_text_layout_params(source, measure);
  auto result = get_text_layout_result(runtime);
  compute_text_layout(result, params);
  runtime.text_layout_using_content_id = content_id;
  runtime.text_layout_using_text = source.data.text;
}

inline std::optional<TextLayoutResult> get_text_layout(TextLabel source) {
  ensure_text_layout(source);
  return static_cast<TextLabelRuntime>(get_node2_druntime(source)).text_layout;
}

inline void get_text_layout_metrics(TextMetrics out, TextLabel source) {
  std::optional<TextLayoutResult> layout = get_text_layout(source);
  if (!layout.value().has_value()) {
    out.height = 0.0;
    out.num_lines = 0.0;
    out.width = 0.0;
    return;
  }
  get_text_metrics(out, layout.value());
}

inline void set_text_label_guard(std::optional<std::function<void(flight::String, flight::String)>> guard) {
  text_label_guard = guard;
}

inline std::optional<std::function<void(flight::String, flight::String)>> text_label_guard = nullptr;

} // namespace flighthq_text
