// Generated by @flighthq/tool-compiler from @flighthq/camera-controls/cameraShake.ts#CameraShake. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_camera_controls {

inline void add_camera_shake_trauma(CameraShake shake, double amount) {
  shake.trauma = clamp((shake.trauma + amount), 0.0, 1.0);
}

inline CameraShake create_camera_shake(std::optional<CameraShakeOptions> options = std::nullopt) {
  auto out = allocate_entity<CameraShake>();
  initialize_camera_shake(out, options.value());
  return finish_entity(out);
}

inline CameraShakeOffset create_camera_shake_offset() {
  auto out = allocate_entity<CameraShakeOffset>();
  initialize_camera_shake_offset(out);
  return finish_entity(out);
}

inline void initialize_camera_shake(EntityConstruction<CameraShake> out, std::optional<CameraShakeOptions> options = std::nullopt) {
  out.decay = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().decay; }()).value_or(1.5);
  out.frequency = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().frequency; }()).value_or(15.0);
  out.rotation_amplitude = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().rotation_amplitude; }()).value_or(3.0);
  out.time = 0.0;
  out.translation_amplitude = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().translation_amplitude; }()).value_or(0.5);
  out.trauma = 0.0;
}

inline void initialize_camera_shake_offset(EntityConstruction<CameraShakeOffset> out) {
  out.rotation_x = 0.0;
  out.rotation_y = 0.0;
  out.rotation_z = 0.0;
  out.x = 0.0;
  out.y = 0.0;
  out.z = 0.0;
}

inline void reset_camera_shake(CameraShake shake) {
  shake.trauma = 0.0;
  shake.time = 0.0;
}

inline void update_camera_shake(CameraShake shake, double dt, CameraShakeOffset out) {
  shake.time += dt;
  shake.trauma = std::max(0.0, (shake.trauma - (shake.decay * dt)));
  if ((shake.trauma <= 0.0)) {
    out.x = 0.0;
    out.y = 0.0;
    out.z = 0.0;
    out.rotation_x = 0.0;
    out.rotation_y = 0.0;
    out.rotation_z = 0.0;
    return;
  }
  const double intensity = (shake.trauma * shake.trauma);
  const double t = (shake.time * shake.frequency);
  const double tx = sample_noise(t, seed_tx_a, seed_tx_b, seed_tx_c);
  const double ty = sample_noise(t, seed_ty_a, seed_ty_b, seed_ty_c);
  const double tz = sample_noise(t, seed_tz_a, seed_tz_b, seed_tz_c);
  const double rx = sample_noise(t, seed_rx_a, seed_rx_b, seed_rx_c);
  const double ry = sample_noise(t, seed_ry_a, seed_ry_b, seed_ry_c);
  const double rz = sample_noise(t, seed_rz_a, seed_rz_b, seed_rz_c);
  out.x = ((tx * intensity) * shake.translation_amplitude);
  out.y = ((ty * intensity) * shake.translation_amplitude);
  out.z = ((tz * intensity) * shake.translation_amplitude);
  out.rotation_x = ((rx * intensity) * shake.rotation_amplitude);
  out.rotation_y = ((ry * intensity) * shake.rotation_amplitude);
  out.rotation_z = ((rz * intensity) * shake.rotation_amplitude);
}

inline double sample_noise(double t, double seed_a, double seed_b, double seed_c) {
  return (((std::sin((t * seed_a)) + std::sin((t * seed_b))) + std::sin((t * seed_c))) / 3.0);
}

inline const double seed_tx_a = 1.0;

inline const double seed_tx_b = 2.31;

inline const double seed_tx_c = 3.53;

inline const double seed_ty_a = 1.17;

inline const double seed_ty_b = 2.63;

inline const double seed_ty_c = 3.89;

inline const double seed_tz_a = 1.37;

inline const double seed_tz_b = 2.41;

inline const double seed_tz_c = 3.71;

inline const double seed_rx_a = 1.53;

inline const double seed_rx_b = 2.79;

inline const double seed_rx_c = 3.47;

inline const double seed_ry_a = 1.71;

inline const double seed_ry_b = 2.17;

inline const double seed_ry_c = 3.91;

inline const double seed_rz_a = 1.89;

inline const double seed_rz_b = 2.53;

inline const double seed_rz_c = 3.29;

} // namespace flighthq_camera_controls
