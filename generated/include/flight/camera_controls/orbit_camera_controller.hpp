// Generated by @flighthq/tool-compiler from @flighthq/camera-controls/packages/camera-controls/src/orbitCameraController.ts#OrbitCameraController at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/number.hpp>
#include <flight/structural_ref.hpp>
#include <limits>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/camera/camera.hpp>
#include <flight/entity/entity.hpp>
#include <flight/geometry/vector3.hpp>
#include <flight/math/angle.hpp>
#include <flight/math/clamp.hpp>
#include <flight/math/interpolation_advanced.hpp>
#include <flight/types/camera3_d.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/orbit_camera_controller.hpp>
#include <flight/types/vector3.hpp>

namespace flight::camera_controls {

inline void copy_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::OrbitCameraController>>>> source) {
  out->azimuth = flight::row_get<flight::RowKey<"azimuth">>(source);
  out->distance = flight::row_get<flight::RowKey<"distance">>(source);
  out->goal_azimuth = flight::row_get<flight::RowKey<"goalAzimuth">>(source);
  out->goal_distance = flight::row_get<flight::RowKey<"goalDistance">>(source);
  out->goal_polar = flight::row_get<flight::RowKey<"goalPolar">>(source);
  out->max_distance = flight::row_get<flight::RowKey<"maxDistance">>(source);
  out->max_polar = flight::row_get<flight::RowKey<"maxPolar">>(source);
  out->min_distance = flight::row_get<flight::RowKey<"minDistance">>(source);
  out->min_polar = flight::row_get<flight::RowKey<"minPolar">>(source);
  out->polar = flight::row_get<flight::RowKey<"polar">>(source);
  out->smooth_time = flight::row_get<flight::RowKey<"smoothTime">>(source);
  out->target->x = flight::row_get<flight::RowKey<"target">>(source)->x;
  out->target->y = flight::row_get<flight::RowKey<"target">>(source)->y;
  out->target->z = flight::row_get<flight::RowKey<"target">>(source)->z;
}

inline void dolly_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> controller, double delta_distance) {
  controller->goal_distance = flight::math::clamp((controller->goal_distance + delta_distance), controller->min_distance, controller->max_distance);
}

inline void pan_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> controller, double delta_right, double delta_up) {
  auto cos_azimuth = std::cos(controller->goal_azimuth);
  auto sin_azimuth = std::sin(controller->goal_azimuth);
  flight::Ref<flight::types::Vector3> target = controller->target;
  target->x += (cos_azimuth * delta_right);
  target->y += delta_up;
  target->z += (-sin_azimuth * delta_right);
}

inline void pan_orbit_camera_controller_in_view_plane(flight::Ref<flight::types::OrbitCameraController> controller, double delta_right, double delta_up) {
  auto cos_azimuth = std::cos(controller->goal_azimuth);
  auto sin_azimuth = std::sin(controller->goal_azimuth);
  auto cos_polar = std::cos(controller->goal_polar);
  auto sin_polar = std::sin(controller->goal_polar);
  flight::Ref<flight::types::Vector3> target = controller->target;
  target->x += ((cos_azimuth * delta_right) - ((sin_azimuth * sin_polar) * delta_up));
  target->y += (cos_polar * delta_up);
  target->z += ((-sin_azimuth * delta_right) - ((cos_azimuth * sin_polar) * delta_up));
}

inline void rotate_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> controller, double delta_azimuth, double delta_polar) {
  controller->goal_azimuth += delta_azimuth;
  controller->goal_polar = flight::math::clamp((controller->goal_polar + delta_polar), controller->min_polar, controller->max_polar);
}

inline void snap_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> controller) {
  controller->azimuth = controller->goal_azimuth;
  controller->distance = flight::math::clamp(controller->goal_distance, controller->min_distance, controller->max_distance);
  controller->polar = flight::math::clamp(controller->goal_polar, controller->min_polar, controller->max_polar);
}

inline const double default_max_polar = ((flight::pi / 2.0) - 0.01);

inline const double default_min_distance = 0.01;

inline const double default_min_polar = ((-flight::pi / 2.0) + 0.01);

inline void initialize_orbit_camera_controller(flight::types::EntityConstruction<flight::Ref<flight::types::OrbitCameraController>> out, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::OrbitCameraControllerOptions>>>>> options = std::nullopt) {
  auto azimuth = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->azimuth; }()).value_or(0.0);
  auto polar = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->polar; }()).value_or(0.0);
  auto distance = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->distance; }()).value_or(10.0);
  std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> target = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->target; }());
  flight::row_set<flight::RowKey<"azimuth">>(out, azimuth);
  flight::row_set<flight::RowKey<"distance">>(out, distance);
  flight::row_set<flight::RowKey<"goalAzimuth">>(out, azimuth);
  flight::row_set<flight::RowKey<"goalDistance">>(out, distance);
  flight::row_set<flight::RowKey<"goalPolar">>(out, polar);
  flight::row_set<flight::RowKey<"maxDistance">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->max_distance; }()).value_or(std::numeric_limits<double>::infinity()));
  flight::row_set<flight::RowKey<"maxPolar">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->max_polar; }()).value_or(default_max_polar));
  flight::row_set<flight::RowKey<"minDistance">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->min_distance; }()).value_or(default_min_distance));
  flight::row_set<flight::RowKey<"minPolar">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->min_polar; }()).value_or(default_min_polar));
  flight::row_set<flight::RowKey<"polar">>(out, polar);
  flight::row_set<flight::RowKey<"smoothTime">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->smooth_time; }()).value_or(0.0));
  flight::row_set<flight::RowKey<"target">>(out, flight::geometry::create_vector3(([&]() -> std::optional<double> { auto optional_chain_receiver = target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->x; }()).value_or(0.0), ([&]() -> std::optional<double> { auto optional_chain_receiver = target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->y; }()).value_or(0.0), ([&]() -> std::optional<double> { auto optional_chain_receiver = target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->z; }()).value_or(0.0)));
}

inline flight::Ref<flight::types::OrbitCameraController> create_orbit_camera_controller(std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::OrbitCameraControllerOptions>>>>> options = std::nullopt) {
  flight::types::EntityConstruction<flight::Ref<flight::types::OrbitCameraController>> out = flight::entity::allocate_entity<flight::Ref<flight::types::OrbitCameraController>>();
  initialize_orbit_camera_controller(out, options.value());
  return flight::entity::finish_entity(out);
}

inline flight::Ref<flight::types::OrbitCameraController> clone_orbit_camera_controller(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::OrbitCameraController>>>> source) {
  flight::Ref<flight::types::OrbitCameraController> clone = create_orbit_camera_controller();
  copy_orbit_camera_controller(clone, source);
  return clone;
}

inline void reset_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> controller, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::OrbitCameraControllerOptions>>>>> options = std::nullopt) {
  auto azimuth = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->azimuth; }()).value_or(0.0);
  auto polar = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->polar; }()).value_or(0.0);
  auto distance = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->distance; }()).value_or(10.0);
  std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> target = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->target; }());
  controller->azimuth = azimuth;
  controller->distance = distance;
  controller->goal_azimuth = azimuth;
  controller->goal_distance = distance;
  controller->goal_polar = polar;
  controller->max_distance = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->max_distance; }()).value_or(std::numeric_limits<double>::infinity());
  controller->max_polar = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->max_polar; }()).value_or(default_max_polar);
  controller->min_distance = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->min_distance; }()).value_or(default_min_distance);
  controller->min_polar = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->min_polar; }()).value_or(default_min_polar);
  controller->polar = polar;
  controller->smooth_time = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->smooth_time; }()).value_or(0.0);
  controller->target->x = ([&]() -> std::optional<double> { auto optional_chain_receiver = target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->x; }()).value_or(0.0);
  controller->target->y = ([&]() -> std::optional<double> { auto optional_chain_receiver = target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->y; }()).value_or(0.0);
  controller->target->z = ([&]() -> std::optional<double> { auto optional_chain_receiver = target; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->z; }()).value_or(0.0);
}

inline flight::Ref<flight::types::Vector3> world_up = flight::geometry::create_vector3(0.0, 1.0, 0.0);

inline flight::Ref<flight::types::Vector3> scratch_eye = flight::geometry::create_vector3();

inline void update_orbit_camera_controller(flight::Ref<flight::types::OrbitCameraController> controller, flight::Ref<flight::types::Camera3D> camera, double delta_time) {
  const double goal_polar = flight::math::clamp(controller->goal_polar, controller->min_polar, controller->max_polar);
  const double goal_distance = flight::math::clamp(controller->goal_distance, controller->min_distance, controller->max_distance);
  if (((controller->smooth_time > 0.0) && (delta_time > 0.0))) {
    const double lambda = (1.0 / controller->smooth_time);
    const double nearest_goal_azimuth = (controller->azimuth + flight::math::delta_angle(controller->azimuth, controller->goal_azimuth));
    controller->azimuth = flight::math::damp(controller->azimuth, nearest_goal_azimuth, lambda, delta_time);
    controller->polar = flight::math::damp(controller->polar, goal_polar, lambda, delta_time);
    controller->distance = flight::math::damp(controller->distance, goal_distance, lambda, delta_time);
  }
  else {
    controller->azimuth = controller->goal_azimuth;
    controller->polar = goal_polar;
    controller->distance = goal_distance;
  }
  auto cos_polar = std::cos(controller->polar);
  auto sin_polar = std::sin(controller->polar);
  auto cos_azimuth = std::cos(controller->azimuth);
  auto sin_azimuth = std::sin(controller->azimuth);
  flight::Ref<flight::types::Vector3> target = controller->target;
  scratch_eye->x = (target->x + ((controller->distance * sin_azimuth) * cos_polar));
  scratch_eye->y = (target->y + (controller->distance * sin_polar));
  scratch_eye->z = (target->z + ((controller->distance * cos_azimuth) * cos_polar));
  flight::camera::set_camera3_dview_matrix4_from_look_at(camera, scratch_eye, target, world_up);
}

} // namespace flight::camera_controls
