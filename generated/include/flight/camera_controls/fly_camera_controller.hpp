// Generated by @flighthq/tool-compiler from @flighthq/camera-controls/packages/camera-controls/src/flyCameraController.ts#FlyCameraController at 1274ec5c923947dc64d5ffedcbd8169fc758cd9f. Do not edit.
#pragma once
#include <cmath>
#include <cstdint>
#include <flight/structural_ref.hpp>
#include <optional>
#include <random>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

#include <flight/camera/camera.hpp>
#include <flight/entity/entity.hpp>
#include <flight/geometry/vector3.hpp>
#include <flight/math/angle.hpp>
#include <flight/math/clamp.hpp>
#include <flight/math/interpolation_advanced.hpp>
#include <flight/types/camera3_d.hpp>
#include <flight/types/entity.hpp>
#include <flight/types/fly_camera_controller.hpp>
#include <flight/types/vector3.hpp>

namespace flight::camera_controls {

inline void copy_fly_camera_controller(flight::Ref<flight::types::FlyCameraController> out, flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::FlyCameraController>>>> source) {
  out->goal_pitch = flight::row_get<flight::RowKey<"goalPitch">>(source);
  out->goal_yaw = flight::row_get<flight::RowKey<"goalYaw">>(source);
  out->max_pitch = flight::row_get<flight::RowKey<"maxPitch">>(source);
  out->min_pitch = flight::row_get<flight::RowKey<"minPitch">>(source);
  out->pitch = flight::row_get<flight::RowKey<"pitch">>(source);
  out->position->x = flight::row_get<flight::RowKey<"position">>(source)->x;
  out->position->y = flight::row_get<flight::RowKey<"position">>(source)->y;
  out->position->z = flight::row_get<flight::RowKey<"position">>(source)->z;
  out->smooth_time = flight::row_get<flight::RowKey<"smoothTime">>(source);
  out->yaw = flight::row_get<flight::RowKey<"yaw">>(source);
}

inline void look_fly_camera_controller(flight::Ref<flight::types::FlyCameraController> controller, double delta_yaw, double delta_pitch) {
  controller->goal_yaw += delta_yaw;
  controller->goal_pitch = flight::math::clamp((controller->goal_pitch + delta_pitch), controller->min_pitch, controller->max_pitch);
}

inline void move_fly_camera_controller(flight::Ref<flight::types::FlyCameraController> controller, double forward, double right, double up) {
  auto sin_yaw = std::sin(controller->yaw);
  auto cos_yaw = std::cos(controller->yaw);
  flight::Ref<flight::types::Vector3> position = controller->position;
  position->x += ((sin_yaw * forward) + (cos_yaw * right));
  position->y += up;
  position->z += ((-cos_yaw * forward) + (sin_yaw * right));
}

inline void snap_fly_camera_controller(flight::Ref<flight::types::FlyCameraController> controller) {
  controller->pitch = flight::math::clamp(controller->goal_pitch, controller->min_pitch, controller->max_pitch);
  controller->yaw = controller->goal_yaw;
}

inline const double default_max_pitch = ((flight::pi / 2.0) - 0.01);

inline const double default_min_pitch = ((-flight::pi / 2.0) + 0.01);

inline void initialize_fly_camera_controller(flight::types::EntityConstruction<flight::Ref<flight::types::FlyCameraController>> out, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::FlyCameraControllerOptions>>>>> options = std::nullopt) {
  auto yaw = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->yaw; }()).value_or(0.0);
  auto pitch = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->pitch; }()).value_or(0.0);
  std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> position = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->position; }());
  flight::row_set<flight::RowKey<"goalPitch">>(out, pitch);
  flight::row_set<flight::RowKey<"goalYaw">>(out, yaw);
  flight::row_set<flight::RowKey<"maxPitch">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->max_pitch; }()).value_or(default_max_pitch));
  flight::row_set<flight::RowKey<"minPitch">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->min_pitch; }()).value_or(default_min_pitch));
  flight::row_set<flight::RowKey<"pitch">>(out, pitch);
  flight::row_set<flight::RowKey<"position">>(out, flight::geometry::create_vector3(([&]() -> std::optional<double> { auto optional_chain_receiver = position; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->x; }()).value_or(0.0), ([&]() -> std::optional<double> { auto optional_chain_receiver = position; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->y; }()).value_or(0.0), ([&]() -> std::optional<double> { auto optional_chain_receiver = position; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->z; }()).value_or(0.0)));
  flight::row_set<flight::RowKey<"smoothTime">>(out, ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->smooth_time; }()).value_or(0.0));
  flight::row_set<flight::RowKey<"yaw">>(out, yaw);
}

inline flight::Ref<flight::types::FlyCameraController> create_fly_camera_controller(std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::FlyCameraControllerOptions>>>>> options = std::nullopt) {
  flight::types::EntityConstruction<flight::Ref<flight::types::FlyCameraController>> out = flight::entity::allocate_entity<flight::Ref<flight::types::FlyCameraController>>();
  initialize_fly_camera_controller(out, options.value());
  return flight::entity::finish_entity(out);
}

inline flight::Ref<flight::types::FlyCameraController> clone_fly_camera_controller(flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::FlyCameraController>>>> source) {
  flight::Ref<flight::types::FlyCameraController> clone = create_fly_camera_controller();
  copy_fly_camera_controller(clone, source);
  return clone;
}

inline void reset_fly_camera_controller(flight::Ref<flight::types::FlyCameraController> controller, std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::FlyCameraControllerOptions>>>>> options = std::nullopt) {
  auto yaw = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->yaw; }()).value_or(0.0);
  auto pitch = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->pitch; }()).value_or(0.0);
  std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> position = ([&]() -> std::optional<flight::StructuralRef<flight::RowReadonly<flight::RowOf<flight::Ref<flight::types::Vector3Like>>>>> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->position; }());
  controller->goal_pitch = pitch;
  controller->goal_yaw = yaw;
  controller->max_pitch = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->max_pitch; }()).value_or(default_max_pitch);
  controller->min_pitch = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->min_pitch; }()).value_or(default_min_pitch);
  controller->pitch = pitch;
  controller->position->x = ([&]() -> std::optional<double> { auto optional_chain_receiver = position; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->x; }()).value_or(0.0);
  controller->position->y = ([&]() -> std::optional<double> { auto optional_chain_receiver = position; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->y; }()).value_or(0.0);
  controller->position->z = ([&]() -> std::optional<double> { auto optional_chain_receiver = position; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->z; }()).value_or(0.0);
  controller->smooth_time = ([&]() -> std::optional<double> { auto optional_chain_receiver = options; if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value()->smooth_time; }()).value_or(0.0);
  controller->yaw = yaw;
}

inline flight::Ref<flight::types::Vector3> world_up = flight::geometry::create_vector3(0.0, 1.0, 0.0);

inline flight::Ref<flight::types::Vector3> scratch_target = flight::geometry::create_vector3();

inline void update_fly_camera_controller(flight::Ref<flight::types::FlyCameraController> controller, flight::Ref<flight::types::Camera3D> camera, double delta_time) {
  const double goal_pitch = flight::math::clamp(controller->goal_pitch, controller->min_pitch, controller->max_pitch);
  if (((controller->smooth_time > 0.0) && (delta_time > 0.0))) {
    const double lambda = (1.0 / controller->smooth_time);
    const double nearest_goal_yaw = (controller->yaw + flight::math::delta_angle(controller->yaw, controller->goal_yaw));
    controller->yaw = flight::math::damp(controller->yaw, nearest_goal_yaw, lambda, delta_time);
    controller->pitch = flight::math::damp(controller->pitch, goal_pitch, lambda, delta_time);
  }
  else {
    controller->yaw = controller->goal_yaw;
    controller->pitch = goal_pitch;
  }
  auto cos_pitch = std::cos(controller->pitch);
  auto sin_pitch = std::sin(controller->pitch);
  auto cos_yaw = std::cos(controller->yaw);
  auto sin_yaw = std::sin(controller->yaw);
  flight::Ref<flight::types::Vector3> position = controller->position;
  scratch_target->x = (position->x + (sin_yaw * cos_pitch));
  scratch_target->y = (position->y + sin_pitch);
  scratch_target->z = (position->z - (cos_yaw * cos_pitch));
  flight::camera::set_camera3_dview_matrix4_from_look_at(camera, position, scratch_target, world_up);
}

} // namespace flight::camera_controls
