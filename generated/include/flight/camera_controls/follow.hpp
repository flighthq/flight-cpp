// Generated by @flighthq/tool-compiler from @flighthq/camera-controls/follow.ts#Follow. Do not edit.
#pragma once
#include <optional>
#include <flight/runtime.hpp>

static_assert(flight::runtime_contract.compiler_contract == "flight-runtime-contract/2", "Flight compiler/runtime contract mismatch");
static_assert(flight::runtime_contract.cpp_abi == 1, "Flight C++ runtime ABI mismatch");

namespace flighthq_camera_controls {

inline void update_camera2_dfollow(Camera2D camera, double target_x, double target_y, double delta_time, std::optional<Camera2DFollowOptions> options = std::nullopt) {
  auto cam_x = camera.x;
  auto cam_y = camera.y;
  auto dead_half_w = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().deadzone_half_width; }()).value_or(0.0);
  auto dead_half_h = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().deadzone_half_height; }()).value_or(0.0);
  auto smooth_time = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().smooth_time; }()).value_or(0.0);
  auto world_bounds = ([&]() -> std::optional<auto> { auto optional_chain_receiver = options.value(); if (!optional_chain_receiver.has_value()) return std::nullopt; return optional_chain_receiver.value().world_bounds; }());
  const double dx = (target_x - cam_x);
  auto goal_x = cam_x;
  if ((dx > dead_half_w)) {
    goal_x = (target_x - dead_half_w);
  }
  else {
    if ((dx < -dead_half_w)) {
      goal_x = (target_x + dead_half_w);
    }
  }
  const double dy = (target_y - cam_y);
  auto goal_y = cam_y;
  if ((dy > dead_half_h)) {
    goal_y = (target_y - dead_half_h);
  }
  else {
    if ((dy < -dead_half_h)) {
      goal_y = (target_y + dead_half_h);
    }
  }
  double next_x;
  double next_y;
  if (((smooth_time > 0.0) && (delta_time > 0.0))) {
    const double lambda = (1.0 / smooth_time);
    next_x = damp(cam_x, goal_x, lambda, delta_time);
    next_y = damp(cam_y, goal_y, lambda, delta_time);
  }
  else {
    next_x = goal_x;
    next_y = goal_y;
  }
  if (world_bounds) {
    get_camera2_dvisible_bounds(camera, scratch_bounds);
    const double half_vis_w = (scratch_bounds.width * 0.5);
    const double half_vis_h = (scratch_bounds.height * 0.5);
    if ((world_bounds.width <= scratch_bounds.width)) {
      next_x = (world_bounds.x + (world_bounds.width * 0.5));
    }
    else {
      next_x = clamp(next_x, (world_bounds.x + half_vis_w), ((world_bounds.x + world_bounds.width) - half_vis_w));
    }
    if ((world_bounds.height <= scratch_bounds.height)) {
      next_y = (world_bounds.y + (world_bounds.height * 0.5));
    }
    else {
      next_y = clamp(next_y, (world_bounds.y + half_vis_h), ((world_bounds.y + world_bounds.height) - half_vis_h));
    }
  }
  camera.x = next_x;
  camera.y = next_y;
}

inline auto scratch_bounds = create_rectangle();

} // namespace flighthq_camera_controls
